---
phase: 07-hooks
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/vitest/src/Dsl.ts
  - packages/vitest/src/describeFeature.ts
  - packages/vitest/test/describeFeature.test.ts
autonomous: true
requirements: [DSL-07]

must_haves:
  truths:
    - "A test author can register all six hooks from the callback describeFeature hands them"
    - "A hook body requiring a service the ambient Layer does not provide is a type error where the hook is written"
    - "A hook registrar is reachable from the Feature-level dsl and from nowhere else — not from a Scenario or Background callback"
    - "Two describeFeature calls in one file share no hook state"
    - "Hooks of one kind reach the collection in registration order (D-01)"
  artifacts:
    - path: "packages/vitest/src/Dsl.ts"
      provides: "HookRegistrar<ROut> and six FeatureDsl members"
      contains: "HookRegistrar"
    - path: "packages/vitest/src/describeFeature.ts"
      provides: "per-call hook registry, hookRegistrar, FeatureCollection.hooks"
      contains: "createHookRegistry"
  key_links:
    - from: "packages/vitest/src/describeFeature.ts"
      to: "packages/vitest/src/Hook.ts"
      via: "hookRegistrar normalises the body through registerHook before recording it"
      pattern: "registerHook"
    - from: "packages/vitest/src/describeFeature.ts"
      to: "packages/vitest/src/HookRegistry.ts"
      via: "one fresh hook registry per collect() invocation"
      pattern: "createHookRegistry"
---

<objective>
Give the six hooks a compile-time surface on `FeatureDsl` and a registration path through
`describeFeature`'s composition root, so a hook is collected exactly the way a step is.

Purpose: this is the DSL-07 type surface. `Dsl.ts` is the single place the project's core value is
spelled, and a hook registrar that lets `R` float free would constrain nothing — note (e)'s vacuous-
generic trap applies to hooks unchanged. This plan stops at COLLECTION: nothing is executed yet, so
`collectFeature` makes every registration claim assertable without a runner.
Output: `HookRegistrar<ROut>`, six `FeatureDsl` members, `FeatureCollection.hooks`, and the
`describeFeature.test.ts` assertions that discriminate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hooks/07-CONTEXT.md
@.planning/phases/07-hooks/07-PATTERNS.md
@packages/vitest/src/Dsl.ts
@packages/vitest/src/describeFeature.ts
@packages/vitest/test/describeFeature.test.ts
@.planning/phases/07-hooks/07-01-SUMMARY.md

<interfaces>
<!-- Produced by plan 07-01. Use directly; do not re-derive. -->
packages/vitest/src/HookRegistry.ts
  export type HookKind = "Before" | "After" | "BeforeStep" | "AfterStep" | "BeforeAllScenarios" | "AfterAllScenarios"
  export type HookDefinition<Fn> = { readonly kind: HookKind; readonly body: Fn }
  export const createHookRegistry: <Fn>() => { register(kind: HookKind, body: Fn): void; hooks(): ReadonlyArray<HookDefinition<Fn>> }

packages/vitest/src/Hook.ts
  export type HookBody = () => Effect.Effect<any, any, any>
  export type HookSet = { readonly [K in HookKind]: ReadonlyArray<HookBody> }
  export const registerHook: <A, E, R>(kind: HookKind, fn) => () => Effect.Effect<A, E, R>
  export const groupHooks: (definitions: ReadonlyArray<HookDefinition<HookBody>>) => HookSet

Existing, unchanged (packages/vitest/src/Dsl.ts):
  export interface StepRegistrar<ROut> {
    <Params extends ReadonlyArray<any>, A, E>(
      pattern: string,
      fn:
        | ((...p: Params) => Effect.gen.Return<A, E, ROut | Scope.Scope>)
        | ((...p: Params) => Effect.Effect<A, E, ROut | Scope.Scope>)
    ): void
  }
  export interface FeatureDsl<ROut> extends ScenarioDsl<ROut> { Background; Scenario }

Existing, unchanged (packages/vitest/src/describeFeature.ts):
  export type FeatureCollection = { feature; layer; definitions; plan }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HookRegistrar and the six FeatureDsl members</name>
  <files>packages/vitest/src/Dsl.ts</files>
  <read_first>
    - packages/vitest/src/Dsl.ts (read whole — notes (a), (b), (d), (e) all constrain this edit, and `FeatureDsl extends ScenarioDsl` at the bottom is the reason placement matters)
    - .planning/phases/07-hooks/07-PATTERNS.md section "packages/vitest/src/Dsl.ts"
    - .planning/phases/07-hooks/07-CONTEXT.md "Phase Boundary" — hooks are Feature-scoped only
    - packages/vitest/src/Step.ts (note (b) — the union-order constraint this file's note (a) is the other half of)
  </read_first>
  <action>
    Add `HookRegistrar<ROut>` to `packages/vitest/src/Dsl.ts` as a callable INTERFACE, not a type alias
    for a function — same reason as `StepRegistrar<ROut>` (note (e)): the call signature is generic per
    call site in `A` and `E` while `ROut` stays fixed by the enclosing `describeFeature`. Its one call
    signature takes a single `fn` parameter and returns `void`:

    - two union members, GENERATOR BRANCH FIRST: `() => Effect.gen.Return<A, E, ROut | Scope.Scope>`
      then `() => Effect.Effect<A, E, ROut | Scope.Scope>`.
    - `Scope.Scope` spelled out on BOTH members, never factored into an alias — note (b) measured the
      cost of the alias on the `TS2345` chain and it is real.
    - no `Params`, and therefore no `ReadonlyArray<any>`: a hook takes no arguments (ADR-EC-005's
      Negative consequence — `BeforeStep`/`AfterStep` do not receive the step). `HookRegistrar` must
      contain no `any` at all; note (d)'s "the one `any` below is the ONLY one permitted in this
      module" stays literally true.
    - `R` bound to `ROut` through the interface's type parameter, never a free type parameter of the
      call signature. 07-CONTEXT.md's canonical_refs says note (e)'s reasoning "applies to hooks too".

    Add six members to `FeatureDsl<ROut>` — `Before`, `After`, `BeforeStep`, `AfterStep`,
    `BeforeAllScenarios`, `AfterAllScenarios` — each `readonly` and each typed `HookRegistrar<ROut>`,
    each with its own one-line doc comment. Put them on `FeatureDsl` and NOWHERE else. `FeatureDsl`
    extends `ScenarioDsl`, so a hook member added to `ScenarioDsl` would silently leak into every
    `Scenario(...)` container callback and into `BackgroundDsl`'s siblings — a leak nothing in the type
    system objects to. Do not touch `ScenarioDsl` or `BackgroundDsl`.

    Add a note (f) to the module header in the file's existing numbered form, stating in bold that the
    six hook members live on `FeatureDsl` ONLY; naming the plausible tidy-up ("make the containers
    consistent" / "a Scenario should be able to have its own Before"); and naming the behavioral proof
    — plan 07-03's `test/tsgo-gate/src/hook-satisfied.ts`, which carries a `@ts-expect-error` on a
    `Scenario` callback reaching for `Before`, so a leak turns that directive into an unused-directive
    error and fails gate assertion 10. Also extend note (a) to say the generator-branch-first rule now
    has three copies (here, `Step.ts`, `Hook.ts`) and that the hook fixture pair is what proves it for
    this new surface.

    This module stays TYPES ONLY: no `const`, no function, no runtime value; both imports remain
    `import type`. `dist/Dsl.js` must still emit zero statements.
  </action>
  <acceptance_criteria>
    - `grep -c "HookRegistrar" packages/vitest/src/Dsl.ts` is at least `7` (the interface plus six members)
    - `grep -vE '^\s*(//|\*|/\*)' packages/vitest/src/Dsl.ts | grep -c "any"` outputs `1` — the single `ReadonlyArray<any>` in `StepRegistrar` remains the only one
    - `grep -n "readonly Before\|readonly After\|readonly BeforeStep\|readonly AfterStep\|readonly BeforeAllScenarios\|readonly AfterAllScenarios" packages/vitest/src/Dsl.ts` reports all six, and every reported line number is greater than the line number of `export interface FeatureDsl`
    - `grep -vE '^\s*(//|\*|/\*)' packages/vitest/src/Dsl.ts | grep -cE "^(export )?(const|function|let|var) "` outputs `0`
    - In `HookRegistrar`, the `Effect.gen.Return` member appears on an earlier line than the `Effect.Effect` member
    - `pnpm build` exits 0 and `node -e "const s=require('fs').readFileSync('packages/vitest/dist/Dsl.js','utf8')"` — after a build, `dist/Dsl.js` contains no statement beyond the `export {}` marker
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm build && pnpm lint && pnpm verify:tsgo-gate</automated>
  </verify>
  <done>`HookRegistrar<ROut>` exists with the generator branch first and `Scope.Scope` on both members; six hook members are on `FeatureDsl` and on nothing else; `Dsl.ts` is still types-only with exactly one `any`.</done>
</task>

<task type="auto">
  <name>Task 2: hook registration in the composition root, and FeatureCollection.hooks</name>
  <files>packages/vitest/src/describeFeature.ts, packages/vitest/test/describeFeature.test.ts</files>
  <read_first>
    - packages/vitest/src/describeFeature.ts (read whole — the `registrar` closure, the `dsl` object literal, the one-registry-per-invocation line, and notes (b), (c), (e))
    - packages/vitest/test/describeFeature.test.ts (read whole — the existing `FeatureCollection` assertion style and mutation header)
    - packages/vitest/src/Hook.ts and packages/vitest/src/HookRegistry.ts (from plan 07-01)
    - packages/vitest/src/Registry.ts note (a) — one registry per invocation, never module scope
  </read_first>
  <action>
    In `collect`, beside the existing `const registry = createRegistry<StepBody>(feature.name)` line,
    create ONE fresh hook registry per invocation: `const hookRegistry = createHookRegistry<HookBody>()`.
    Never hoisted to module scope, never memoised — `Registry.ts` note (a)'s Pitfall 14 argument
    applies identically, and two `describeFeature` calls in one file sharing a hook store would make
    the second Feature run the first Feature's `Before` hooks.

    Add a `hookRegistrar` closure mirroring the existing `registrar` closure exactly:
    `const hookRegistrar = (kind: HookKind): HookRegistrar<any> => (fn) => { hookRegistry.register(kind, registerHook(kind, fn)) }`.
    The body is normalised through `Hook.ts`'s `registerHook` at REGISTRATION time, exactly as a step
    body is normalised through `Step.ts`'s `register` — so nothing downstream ever re-wraps it, and a
    hook already wrapped by the author keeps its single span. No `captureCallSite()` call: plan 07-01
    deliberately gave `HookRegistry` no `definedAt`, ADR-EC-005's named span is the attribution channel
    for a hook failure, and a captured-but-unconsumed site would be the "say only what is true"
    violation AGENTS.md §4 names. Say that in a short comment where the absence would otherwise look
    like an omission.

    Add the six members to the `dsl` object literal as SIBLINGS of `Background` and `Scenario` — not
    spread into `scenarioDsl`, because `scenarioDsl` is the same object handed to every `Scenario(...)`
    callback and to `backgroundDsl`. Each is `hookRegistrar("Before")`, `hookRegistrar("After")`, and
    so on for all six kinds.

    Add a REQUIRED `hooks: HookSet` field to the exported `FeatureCollection` type and populate it in
    `collect`'s return object as `groupHooks(hookRegistry.hooks())`. Required, not optional: an
    optional field would let a later consumer forget hooks exist, which is the exact failure
    `ParsedFeature.parameterTypes` was made required to prevent (03-05's recorded decision). Grouping
    happens HERE, in the shared implementation, for the same reason planning does: `describeFeature`
    and `collectFeature` must not drift into two behaviours.

    Do NOT change `emitFeature`'s call in this plan. `FeatureCollection.hooks` is collected and
    nothing consumes it yet; plan 07-04 threads it into the runner. Note that in a short comment
    naming 07-04, following this repo's "name the owning plan" convention for a deferred wiring.

    Extend `packages/vitest/test/describeFeature.test.ts` with assertions against `collectFeature`:
    - all six kinds register and land under their own kind in `collection.hooks`.
    - two `Before` hooks come back in registration order (D-01) — asserted by reference identity on the
      two bodies in order, not by length.
    - a kind nobody registered is an empty array, not an absent key: assert the six keys are all
      present on a collection where no hook was registered at all.
    - two `collectFeature` calls in one test share no hook state: register a `Before` in the first and
      assert the second's `hooks.Before` has length 0. This is the load-bearing isolation assertion;
      reference inequality between the two collections proves nothing.
    - a hook registered as an already-`Effect.fn`-wrapped function reaches `collection.hooks` by
      reference identity, proving the registration path normalises once and does not re-wrap.
    - registering a hook from inside a `Scenario(...)` callback is not expressible — covered at the
      type level by task 1 and by plan 07-03's fixture, so assert here only that the object handed to
      a `Scenario` callback has no `Before` key at runtime.

    Add to the file's mutation header: (i) `hookRegistry` hoisted to module scope → the two-collections
    isolation test fails; (ii) the six members spread into `scenarioDsl` → the Scenario-callback key
    assertion fails.
  </action>
  <acceptance_criteria>
    - `grep -c "createHookRegistry" packages/vitest/src/describeFeature.ts` outputs `1`
    - `grep -vE '^\s*(//|\*|/\*)' packages/vitest/src/describeFeature.ts | grep -c "captureCallSite" ` outputs `1` (the step registrar's, unchanged — no second capture was added for hooks)
    - `grep -c "readonly hooks: HookSet" packages/vitest/src/describeFeature.ts` outputs `1`
    - `grep -c "groupHooks(hookRegistry.hooks())" packages/vitest/src/describeFeature.ts` outputs `1`
    - `git diff packages/vitest/src/describeFeature.ts | grep -c "emitFeature"` outputs `0` — the emission call is untouched by this plan
    - `pnpm vitest run packages/vitest/test/describeFeature.test.ts` passes and includes a test asserting `hooks.Before` is length 1 on the first collection and length 0 on a second collection made in the same test
    - `pnpm build`, `pnpm lint`, `pnpm typecheck:test` and `pnpm test` all exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/vitest/test/describeFeature.test.ts && pnpm build && pnpm lint && pnpm typecheck:test && pnpm test</automated>
  </verify>
  <done>All six hooks register through the Feature-level dsl into one fresh per-call registry, arrive on `FeatureCollection.hooks` grouped by kind in registration order, and two collections in one process share no hook state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test author's source → this library | In-process, fully trusted. This plan adds a registration path for developer-authored callbacks; no untrusted input, no network, no credentials, no serialization. |
| type surface → build gate | A compile-time guarantee that decays silently is this project's principal threat, and it is a correctness threat rather than an ASVS one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | `HookRegistrar`'s union member order in `Dsl.ts` | mitigate | Reordering the union still rejects the bad case, so no test goes red while `effect(missingEffectContext)` stops covering hooks. Mitigated by the acceptance criterion pinning generator-branch-first and, behaviorally, by plan 07-03's `hook-missing-service.ts` fixture and gate assertion 11. |
| T-07-02-02 | Elevation of privilege | a hook member leaking onto `ScenarioDsl` | mitigate | `FeatureDsl extends ScenarioDsl`, so a misplaced member silently widens the surface every `Scenario`/`Background` callback receives. Mitigated by the line-order acceptance criterion, the runtime key assertion in task 2, and plan 07-03's `@ts-expect-error` fixture line. |
| T-07-02-03 | Spoofing | shared hook state across two `describeFeature` calls in one file | mitigate | One fresh `createHookRegistry` per `collect` invocation; the two-collections isolation test is what catches a hoist, and reference inequality deliberately is not relied on. |
| T-07-02-SC | Tampering | npm/pip/cargo installs | n/a | This plan adds no dependency. `pnpm-lock.yaml` and both manifests are unchanged; no package-legitimacy checkpoint applies. |
</threat_model>

<verification>
- `pnpm build` compiles clean and `dist/Dsl.js` still emits no runtime statement.
- `pnpm verify:tsgo-gate` still exits 0 — the existing nine assertions must not regress from the
  `Dsl.ts` edit (plan 07-03 adds the two new hook assertions).
- `pnpm typecheck:test` compiles `packages/vitest/tsconfig.test.json` clean.
- `pnpm lint` and `pnpm test` exit 0.
</verification>

<success_criteria>
- `HookRegistrar<ROut>` exists in `Dsl.ts` with the generator branch first, `Scope.Scope` on both members, and no `any`.
- Exactly six hook members exist on `FeatureDsl<ROut>` and none on `ScenarioDsl` or `BackgroundDsl`.
- `collect` creates one hook registry per invocation and returns `hooks: HookSet` on `FeatureCollection`.
- Hook bodies are normalised once, at registration time, through `registerHook`.
- Emission is untouched: `describeFeature`'s `emitFeature` call is byte-identical to before this plan.
</success_criteria>

<output>
Create `.planning/phases/07-hooks/07-02-SUMMARY.md` when done.
</output>
