---
phase: 07-hooks
plan: 08
type: execute
wave: 6
depends_on: ["07-07"]
files_modified:
  - spec/behaviors/07-hook-ordering-and-guarantees.md
  - spec/behaviors/index.yaml
  - spec/behaviors/06-datatable-and-docstring-arguments.md
  - spec/behaviors/02-shared-layers-and-tags.md
  - spec/invariants.md
  - spec/decisions/005-effect-fn-for-step-and-hook-bodies.md
  - spec/traceability.md
  - spec/roadmap.md
  - README.md
  - packages/vitest/README.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DSL-07, RUN-02]

must_haves:
  truths:
    - "The spec's hook signatures describe the DSL members that exist, not free-standing module exports that do not"
    - "BEH-EC-006's Effect.ensuring requirement is corrected in place to name the guarantee and the combinator that delivers it"
    - "INV-EC-004 names a real source module and a real test instead of a planned mechanism"
    - "Every hook ordering and guarantee decision from this phase (D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09) is normative somewhere in spec/"
    - "DSL-07 and RUN-02 are Complete in REQUIREMENTS.md, each backed by a named automated assertion"
  artifacts:
    - path: "spec/behaviors/07-hook-ordering-and-guarantees.md"
      provides: "BEH-EC-017 — hook ordering, batch independence, and the three guarantees"
      contains: "BEH-EC-017"
    - path: "spec/traceability.md"
      provides: "§1, §2, §3 and §4 rows for this phase"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DSL-07 and RUN-02 marked Complete"
  key_links:
    - from: "spec/behaviors/07-hook-ordering-and-guarantees.md"
      to: "spec/behaviors/index.yaml"
      via: "the disk→index registration verify-traceability.sh check 1 enforces"
      pattern: "BEH-EC-017"
    - from: "spec/traceability.md"
      to: "packages/vitest/test/Hook.test.ts"
      via: "§4 is enumerated from disk"
      pattern: "Hook.test.ts"
---

<objective>
Reconcile the specification with what this phase actually built, and mark DSL-07 and RUN-02 Complete.

Purpose: two pieces of spec text did not survive contact with the installed build, and both are
load-bearing. BEH-EC-006 publishes hooks as free-standing module exports — an architecture DSL-04
forbids — and it names `Effect.ensuring`, whose finalizer error channel is `never` in
`effect@4.0.0-rc.112` and which merges no causes. Meanwhile every ordering and guarantee decision this
phase made (D-01 through D-09) is currently normative nowhere. Phase 3's and Phase 4's reconciliation
plans set the pattern: correct in place with a dated blockquote, never rewrite the superseded sentence.
Output: BEH-EC-017, two in-place corrections, traceability rows, three status documents, and the
requirement markings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-hooks/07-CONTEXT.md
@.planning/phases/07-hooks/07-PATTERNS.md
@spec/behaviors/02-shared-layers-and-tags.md
@spec/behaviors/index.yaml
@spec/invariants.md
@spec/traceability.md
@spec/decisions/005-effect-fn-for-step-and-hook-bodies.md
@AGENTS.md
@.planning/phases/07-hooks/07-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BEH-EC-017, the BEH-EC-006 correction, and the INV-EC-004/ADR-EC-005 notes</name>
  <files>spec/behaviors/07-hook-ordering-and-guarantees.md, spec/behaviors/index.yaml, spec/behaviors/06-datatable-and-docstring-arguments.md, spec/behaviors/02-shared-layers-and-tags.md, spec/invariants.md, spec/decisions/005-effect-fn-for-step-and-hook-bodies.md</files>
  <read_first>
    - spec/behaviors/02-shared-layers-and-tags.md (BEH-EC-006's current text — the four free-standing signatures and the `Effect.ensuring` REQUIREMENT block this task corrects in place)
    - spec/behaviors/05-step-matching-and-parameter-types.md and spec/behaviors/06-datatable-and-docstring-arguments.md (the `_Previous:_` / `_Next:_` footer form to copy; 06 currently has no `_Next:_`)
    - spec/behaviors/index.yaml (the entry shape: id, file, title, id_range)
    - spec/invariants.md INV-EC-004 (its "Source (planned)" line names `Effect.ensuring`)
    - spec/decisions/005-effect-fn-for-step-and-hook-bodies.md (read whole — ADR-EC-005's Decision and Consequences, including the Negative consequence that hooks receive no arguments)
    - spec/decisions/007-cucumber-expressions-for-step-matching.md (the dated correction-blockquote form Phase 3 established — correct in place, never rewrite the superseded sentence)
    - AGENTS.md §2 (specification code fences) and §6 (identifier scheme: ids are contiguous, never renumbered, never reused)
    - packages/vitest/src/Dsl.ts, packages/vitest/src/ScenarioEffect.ts, packages/vitest/src/Runner.ts (the real behavior BEH-EC-017 must describe)
  </read_first>
  <action>
    Create `spec/behaviors/07-hook-ordering-and-guarantees.md` carrying `BEH-EC-017` — the next free id,
    since BEH-EC-016 is allocated to the DataTable file. Register it in `spec/behaviors/index.yaml` in
    the SAME commit with `id: "BEH-EC-017"`, `file: "07-hook-ordering-and-guarantees.md"`, a title, and
    `id_range: "017"`; `verify-traceability.sh` check 1 fails in the disk→index direction otherwise.
    Add a `_Next:_` footer to `06-datatable-and-docstring-arguments.md` pointing at the new file, and
    both a `_Previous:_` (pointing at 06) and no `_Next:_` on the new file, copying file 05's footer
    form exactly.

    BEH-EC-017 must state, as normative REQUIREMENT text in this spec's existing block form, every
    decision this phase locked:
    - the ordering: `BeforeAllScenarios` → per Scenario (`Before` → per step (`BeforeStep` → step →
      `AfterStep`) → `After`) → `AfterAllScenarios`, with Background steps wrapped by the per-step pair
      identically to Scenario steps (ADR-EC-004).
    - a Feature MAY register more than one hook of a kind, and they run in registration order (D-01).
    - hooks in one batch are INDEPENDENT: a failing hook does not stop the rest of its batch, and every
      failure in the batch is combined into the one reported failure — never first-wins, never dropped
      (D-02, D-03). Say explicitly that this is a deliberate departure from INV-EC-001's fail-fast, and
      that it applies to the batch only.
    - a Scenario's steps run only if EVERY `Before` hook succeeded (D-04).
    - `After`, `AfterStep` and `AfterAllScenarios` run whether the thing they guard succeeded or failed;
      `AfterStep`'s guarantee spans the whole `BeforeStep`+step unit, so it runs even when the paired
      `BeforeStep` failed (D-05, D-07, D-09).
    - a guaranteed hook that fails does not mask or replace the failure it was guarding; both reach the
      reported failure (D-06).
    - `BeforeAllScenarios` runs once per Feature and its failure is reported by every Scenario in that
      Feature individually (D-08).
    - hooks are Feature-scoped: they are registered through the Feature-level dsl only, and there is no
      Rule-scoped hook narrowing in this milestone.
    - hook bodies receive NO arguments, `BeforeStep`/`AfterStep` included (ADR-EC-005's Negative
      consequence).
    Any `typescript`-tagged fence must import only from a package barrel and import everything it uses
    (AGENTS.md §2 and the planned doc-examples check); a `ts` fence is reference material but must still
    be syntactically valid TypeScript or `dprint` cannot format it and `pnpm lint` fails.

    Correct BEH-EC-006 IN PLACE in `spec/behaviors/02-shared-layers-and-tags.md` with a dated
    correction blockquote in the ADR-EC-007 / ADR-EC-014 form. Do not delete or rewrite the superseded
    lines. The blockquote must state two independent corrections:
    (i) the four published free-standing signatures are not the export shape. Hooks are `readonly`
    members of `FeatureDsl<ROut>` typed `HookRegistrar<ROut>`, reached from the object
    `describeFeature` hands `define` — DSL-04 forbids the module-level registry the published form
    implies, exactly as BEH-EC-003's signature was corrected for the same reason in Phase 5. The
    published `(stepText: string)` parameter on `BeforeStep`/`AfterStep` is also wrong: hook bodies
    take no arguments.
    (ii) the `Effect.ensuring` REQUIREMENT names the GUARANTEE, not the combinator. In
    `effect@4.0.0-rc.112` `Effect.ensuring`'s finalizer is typed `Effect<X, never, R1>`, so a hook that
    can fail is not assignable to it and it merges no causes; `Effect.onExit` is what the runner uses,
    because its documented behaviour — runs on success, failure and interruption, and merges the two
    causes when both fail — is what INV-EC-004 plus the do-not-mask requirement actually need. Point
    the reader at BEH-EC-017 for the full ordering and at `packages/vitest/src/ScenarioEffect.ts`.

    Update INV-EC-004 in `spec/invariants.md`: replace the "**Source (planned)**" line's
    `scenarioEffect.pipe(Effect.ensuring(afterHookEffect))` sketch with the real mechanism —
    `Effect.onExit` applied around the whole composed Scenario Effect in
    `packages/vitest/src/ScenarioEffect.ts`, with `Effect.provide` still the last pipe — and add one
    sentence recording that `Effect.ensuring` was the planned combinator and cannot express the
    requirement, citing the `never` error channel. Keep the invariant's own wording; only the source and
    implication lines change.

    Add a dated implementation note to `spec/decisions/005-effect-fn-for-step-and-hook-bodies.md`
    recording that the hook auto-wrap is delivered by `packages/vitest/src/Hook.ts`'s `registerHook`,
    which delegates to `Step.ts`'s `register` with the hook's own kind in the span-name position, so
    there is exactly one generator-vs-Effect discriminator in the package; and that the Negative
    consequence about hooks receiving no arguments is now enforced by `HookRegistrar`'s zero-parameter
    call signature.

    Run `pnpm format` after editing — `spec/**/*.md` is dprint-formatted, fenced blocks included, and
    hand-formatting risks failing `dprint check`.
  </action>
  <acceptance_criteria>
    - `pnpm verify:spec` exits 0
    - `grep -c "BEH-EC-017" spec/behaviors/index.yaml` outputs `1` and `grep -c "BEH-EC-017" spec/behaviors/07-hook-ordering-and-guarantees.md` is at least `1`
    - `grep -c "_Next:_" spec/behaviors/06-datatable-and-docstring-arguments.md` outputs `1` and `grep -c "_Previous:_" spec/behaviors/07-hook-ordering-and-guarantees.md` outputs `1`
    - `git diff --numstat spec/behaviors/02-shared-layers-and-tags.md` reports 0 deletions — the correction is additive and the superseded text is marked in place, never rewritten
    - `grep -c "Effect.onExit" spec/behaviors/02-shared-layers-and-tags.md` is at least `1` and `grep -c "Effect.onExit" spec/invariants.md` is at least `1`
    - `grep -c "Effect.ensuring" spec/invariants.md` is at least `1` — the superseded combinator is still named, as the record of what was planned
    - `grep -cE "BeforeStep: \(fn: \(stepText" spec/behaviors/02-shared-layers-and-tags.md` outputs `1` — the original block is intact under the correction
    - `pnpm lint` exits 0 (dprint check over `spec/**/*.md`)
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:spec && pnpm lint</automated>
  </verify>
  <done>BEH-EC-017 exists, is registered and footer-linked; BEH-EC-006 and INV-EC-004 carry additive in-place corrections naming `Effect.onExit` and the DSL member shape; ADR-EC-005 records where the hook auto-wrap lives.</done>
</task>

<task type="auto">
  <name>Task 2: traceability §1, §2, §3, §4 and spec/roadmap.md</name>
  <files>spec/traceability.md, spec/roadmap.md</files>
  <read_first>
    - spec/traceability.md (read whole — the §1 planned/real preamble, §2's "Not yet written" convention, §3's `—` convention, and §4's enumerated-from-disk rule with its `StepArgs.types.ts` caveat)
    - spec/roadmap.md lines around 25 and 78 (the two places hooks are described as an intended contract only)
    - spec/behaviors/07-hook-ordering-and-guarantees.md (as written in task 1)
    - the summaries of plans 07-01 through 07-07 (the real modules and test files to name)
  </read_first>
  <action>
    §1: the row for `02 — Background, hooks, shared Layers, and tags` currently names planned modules
    `packages/vitest/src/{ScenarioEffect,Background,Hooks,SharedLayer,Tags}.ts`. Replace the hook part
    with the modules that exist — `Hook.ts`, `HookRegistry.ts`, `Dsl.ts`, `ScenarioEffect.ts`,
    `Runner.ts`, `describeFeature.ts` — while leaving the still-planned `SharedLayer`/`Tags` entries
    for Phases 9 and 10 alone. There is no `Background.ts` and there never will be (Background is
    inlined, ADR-EC-004); drop it and say so in the row or the preamble if the preamble is where such
    notes live. Add a new §1 row for `07 — Hook ordering and guarantees` / `BEH-EC-017` naming the same
    real modules.

    §2: INV-EC-004's row moves from planned to enforced. **Enforced by** becomes `Effect.onExit` around
    the composed Scenario Effect in `ScenarioEffect.ts`'s `buildScenarioEffect`, and **Test** names the
    real assertions: `packages/vitest/test/ScenarioEffect.test.ts` (After on success, After on step
    failure, After after a Before failure, and the failing-After-does-not-mask case) and
    `packages/vitest/test/Runner.test.ts` (the full six-hook ordering). Update the paragraph above the
    table that currently says only INV-EC-001/002/003 are enforced today, so the prose and the table
    cannot disagree.

    §3: fill ADR-EC-005's **Source module** column — currently `—` — with
    `packages/vitest/src/{Hook,Step,Dsl}.ts`, and its **Affected invariants** with INV-EC-004 if the
    row does not already carry it. Update §3's preamble sentence that enumerates which phases have
    filled which rows, adding Phase 7 and ADR-EC-005.

    §4: this section is enumerated from disk, and this phase added two test files. Add one row each for
    `packages/vitest/test/Hook.test.ts` and `packages/vitest/test/HookRegistry.test.ts`, each naming
    BEH-EC-017 in **Covers** and describing what it asserts. Then re-run 03-06's reusable cross-check —
    a `node -e` script that reads `packages/gherkin/test/*.test.ts` and `packages/vitest/test/*.test.ts`
    from disk and fails naming any file absent from this section — and record its clean output in the
    summary. Update the **Covers** column of the existing `ScenarioEffect.test.ts`, `Runner.test.ts`
    and `emission.test.ts` rows to add BEH-EC-017, and extend their descriptions to mention the hook
    assertions they now carry.

    `spec/roadmap.md`: line ~25's "What is still an intended contract only" list must drop "hooks and
    `Effect.ensuring`-backed `After` (Phase 7)" and the line ~78 sentence listing hooks among the
    things "layered on top of the pipeline" must move hooks from planned to built. Leave the Phase 8,
    9 and 10 entries untouched. `spec/roadmap.md` is named in several files as the single authority on
    build status, so a stale line here is a defect under AGENTS.md §4.

    Run `pnpm format` after editing.
  </action>
  <acceptance_criteria>
    - `pnpm verify:spec` exits 0
    - `grep -c "BEH-EC-017" spec/traceability.md` is at least `4` (one §1 row plus the §4 Covers entries)
    - `grep -c "Not yet written" spec/traceability.md` is one fewer than before this plan, and INV-EC-004's row no longer contains it
    - `grep -c "Hook.test.ts" spec/traceability.md` is at least `1` and `grep -c "HookRegistry.test.ts" spec/traceability.md` is at least `1`
    - `grep -c "Background.ts" spec/traceability.md` outputs `0`
    - `grep -c "Effect.ensuring" spec/roadmap.md` outputs `0`
    - The from-disk cross-check `node -e` script reports every `packages/*/test/*.test.ts` file as present in §4, with output recorded in the summary
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:spec && pnpm lint && node -e "const fs=require('fs');const doc=fs.readFileSync('spec/traceability.md','utf8');const files=['packages/gherkin/test','packages/vitest/test'].flatMap((d)=>fs.readdirSync(d).filter((f)=>f.endsWith('.test.ts')).map((f)=>d+'/'+f));const missing=files.filter((f)=>!doc.includes(f));if(missing.length>0){throw new Error('missing from traceability §4: '+missing.join(', '))}console.log('§4 covers all '+files.length+' test files')"</automated>
  </verify>
  <done>Traceability §1-§4 name the real hook modules and test files, INV-EC-004 is recorded as enforced with named tests, ADR-EC-005 has a source module, and `spec/roadmap.md` no longer lists hooks as unbuilt.</done>
</task>

<task type="auto">
  <name>Task 3: the two READMEs and the requirement markings</name>
  <files>README.md, packages/vitest/README.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - packages/vitest/README.md (read whole — the `## Status` section, especially the "What is not built yet" paragraph naming hooks and `Effect.ensuring`)
    - README.md (the "Still specified rather than built" sentence and the package table row describing the vitest package)
    - .planning/REQUIREMENTS.md (DSL-07 and RUN-02's checkboxes, the traceability table, and the footer's per-requirement-evidence convention set by the Phase 6 entry)
    - the summaries of plans 07-01 through 07-07 (the named automated assertions each requirement rests on)
    - AGENTS.md §4
  </read_first>
  <action>
    `packages/vitest/README.md`: add a `**Hooks run, and the guarantees are real.**` paragraph to
    `## Status`, in the same voice as the existing paragraphs, stating: all six hooks are registered
    through the same dsl object as `Given`/`When`/`Then` and accept a bare generator function
    auto-wrapped with the hook's own name as its span; more than one hook of a kind is allowed and they
    run in registration order; a batch is independent and every failure in it reaches the one reported
    failure; a Scenario's steps run only if every `Before` succeeded; `After`, `AfterStep` and
    `AfterAllScenarios` run whether the thing they guard passed or failed and never mask its error;
    `BeforeAllScenarios` runs once per Feature and its failure is reported by every Scenario.
    Then delete the hooks clause from the "What is not built yet" paragraph, including its
    `Effect.ensuring` mention, leaving the Phase 8/9/10 clauses intact.

    `README.md`: drop hooks from the "Still specified rather than built" sentence, and update the
    package-table row for `@effect-cucumber/vitest` so it no longer says "hooks are specified, not yet
    built".

    `.planning/REQUIREMENTS.md`: tick DSL-07 and RUN-02 in the Registration DSL and Execution semantics
    lists, set both rows in the Traceability table to `Complete`, and replace the footer's
    `*Last updated: …*` line with a new entry in the Phase 6 entry's form. That entry must name, per
    requirement, the automated assertion that fails if the requirement stops being true:
    - DSL-07 by `packages/vitest/test/Hook.test.ts` (the span-name and reference-identity assertions),
      `scripts/verify-tsgo-gate.sh` assertions 10 and 11 (a hook requiring an unprovided service is
      rejected by name), and `packages/vitest/test/Runner.test.ts`'s full six-hook ordering test;
    - RUN-02 by `packages/vitest/test/ScenarioEffect.test.ts`'s After-on-step-failure and
      failing-After-does-not-mask assertions, and `packages/vitest/test/emission.test.ts`'s real-run
      hook proof.
    State that no requirement outside DSL-07 and RUN-02 changed status, and cite
    `.planning/phases/07-hooks/07-08-SUMMARY.md` for the per-requirement evidence — following the
    convention that the plan which makes a requirement true end to end is the one that marks it.

    Run `pnpm format` after editing so `dprint check` stays clean.
  </action>
  <acceptance_criteria>
    - `grep -c "Effect.ensuring" packages/vitest/README.md` outputs `0` and `grep -c "Effect.ensuring" README.md` outputs `0`
    - `grep -c "hooks are specified, not yet built" README.md` outputs `0`
    - `grep -cE "^- \[x\] \*\*DSL-07\*\*" .planning/REQUIREMENTS.md` outputs `1` and `grep -cE "^- \[x\] \*\*RUN-02\*\*" .planning/REQUIREMENTS.md` outputs `1`
    - `grep -cE "^\| DSL-07 \| Phase 7 \| Complete \|" .planning/REQUIREMENTS.md` outputs `1` and the same for `RUN-02`
    - `grep -c "Pending" .planning/REQUIREMENTS.md` is exactly two fewer than before this plan
    - The footer's last-updated entry names at least four distinct test-file or script paths as evidence
    - `pnpm lint` and `pnpm verify:spec` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm verify:spec && pnpm test && pnpm build && pnpm verify:tsgo-gate && pnpm verify:pack</automated>
  </verify>
  <done>Both READMEs describe hooks as built with their guarantees stated, and DSL-07 and RUN-02 are Complete with a per-requirement evidence trail naming real automated assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| specification → future implementer | The only boundary this plan touches. Stale or wrong spec text is the failure mode; there is no code, no input parsing, no network and no credential handling anywhere in it. |
| README → npm page / public reader | Published prose describing capabilities the package may or may not have. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Repudiation | a normative document silently contradicting the shipped code | mitigate | BEH-EC-006 and INV-EC-004 are corrected in place with dated blockquotes and zero deletions, so a future reader sees both what was planned and what was built; the zero-deletions acceptance criterion enforces the form. |
| T-07-08-02 | Repudiation | a new behavior file unreachable because it was not indexed | mitigate | `spec/behaviors/index.yaml` is edited in the same commit and `pnpm verify:spec` check 1 fails in the disk→index direction if it is not. |
| T-07-08-03 | Repudiation | a requirement marked Complete with no assertion behind it | mitigate | The footer entry must name a specific failing-if-untrue assertion per requirement, following the Phase 6 precedent, and an acceptance criterion counts the named paths. |
| T-07-08-04 | Information disclosure | published README describing a capability that does not exist | mitigate | AGENTS.md §4; the `Effect.ensuring` and "specified, not yet built" greps must both return zero, and `verify:pack` still runs so the shipped README is the edited one. |
| T-07-08-SC | Tampering | npm/pip/cargo installs | n/a | This plan adds no dependency and touches no manifest. No package-legitimacy checkpoint applies. |
</threat_model>

<verification>
- `pnpm verify:spec` exits 0 — the behavior file is indexed in both directions and the traceability
  checks pass.
- The from-disk §4 cross-check reports every `packages/*/test/*.test.ts` file as covered.
- `pnpm lint` exits 0 (dprint over `spec/**/*.md`, both READMEs, and `.planning/REQUIREMENTS.md`).
- `pnpm build`, `pnpm test`, `pnpm verify:tsgo-gate` and `pnpm verify:pack` all still exit 0 — this is
  a documentation plan and none of them may move.
</verification>

<success_criteria>
- `spec/behaviors/07-hook-ordering-and-guarantees.md` carries BEH-EC-017 stating D-01 through D-09 as normative requirements, and is registered and footer-linked.
- BEH-EC-006 carries an additive, dated, two-part correction — the DSL member shape and `Effect.onExit` — with zero deletions.
- INV-EC-004 names `Effect.onExit` in `ScenarioEffect.ts` and real tests, and traceability §2 no longer says "Not yet written" for it.
- Traceability §1/§3/§4 name the real hook modules and both new test files; `spec/roadmap.md` and both READMEs describe hooks as built.
- DSL-07 and RUN-02 are Complete in `.planning/REQUIREMENTS.md`, each with a named automated assertion in the footer entry.
</success_criteria>

<output>
Create `.planning/phases/07-hooks/07-08-SUMMARY.md` when done.
</output>
