---
phase: 09-tags
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/vitest/src/Plan.ts
  - packages/vitest/test/Plan.test.ts
  - packages/vitest/test/Runner.test.ts
autonomous: true
requirements: [RUN-05]

must_haves:
  truths:
    - "A planned Scenario carries every tag its Feature, Rule, Scenario and Examples block gave it, in that order"
    - "The tags on a planned Scenario are assertable without parsing a document or running a test"
    - "A Scenario with no tags plans with an empty array, never a missing field"
  artifacts:
    - path: "packages/vitest/src/Plan.ts"
      provides: "ScenarioPlan.tags — the per-Scenario tag set the emission walk reads"
      contains: "readonly tags: ReadonlyArray<string>"
  key_links:
    - from: "packages/vitest/src/Plan.ts"
      to: "packages/gherkin/src/Model.ts ParsedScenario.tags"
      via: "planFeature's allScenarios map"
      pattern: "tags: scenario\\.tags"
---

<objective>
Carry each Scenario's already-flattened tag set onto `ScenarioPlan`, so the emission walk in plan
09-04 reads one object per Scenario instead of reaching back into the parsed document, and so the
value is assertable from `Plan.test.ts` rather than only observable at emission.

Purpose: RESEARCH Finding 11 Option A. `ScenarioPlan` already copies three `ParsedScenario` fields
(`name`, `astName`, `ruleId`); `tags` is the fourth of the same kind.
Output: one required field, one mapping line, assertions, and the mechanical fixture update that
keeps `pnpm typecheck:test` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tags/09-PATTERNS.md
@AGENTS.md

<interfaces>
<!-- Contract created here. Plan 09-04's emission walk reads `scenarioPlan.tags` and nothing else. -->

packages/vitest/src/Plan.ts — ScenarioPlan gains one REQUIRED field:

  export type ScenarioPlan = {
    readonly scenarioId: string
    readonly name: string
    readonly astName: string
    readonly ruleId: Option.Option<string>
    readonly tags: ReadonlyArray<string>
    readonly steps: ReadonlyArray<PlannedStep>
  }

Source of the value, packages/gherkin/src/Model.ts:133-136 —

  /**
   * `Pickle.tags` names, already flattened by `compile()` in
   * feature then rule then scenario then examples-block order. Do not recompute inheritance.
   */
  readonly tags: ReadonlyArray<string>

Verified ordering contract, packages/gherkin/test/Correlate.test.ts:173 —

  expect(onlyScenario().tags).toEqual(["@featuretag", "@ruletag", "@scenariotag", "@exampletag"])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the required tags field to ScenarioPlan and populate it in planFeature</name>
  <read_first>
    - packages/vitest/src/Plan.ts (lines 215-244 for the `ScenarioPlan` type and `FeaturePlan`'s doc comment; lines 596-615 for the `feature.allScenarios.map((scenario): ScenarioPlan => …)` mapping site)
    - packages/gherkin/src/Model.ts (lines 117-140 — `ParsedScenario.tags`, its flattening contract and its "Do not recompute inheritance" instruction)
    - .planning/phases/09-tags/09-PATTERNS.md (section `packages/vitest/src/Plan.ts` — the documented tension the added field's own comment must answer)
    - .planning/phases/09-tags/09-RESEARCH.md (Finding 11)
  </read_first>
  <action>
Add `readonly tags: ReadonlyArray<string>` to the `ScenarioPlan` type, positioned after `ruleId` and
before `steps`, and populate it in `planFeature`'s `feature.allScenarios.map` with `tags: scenario.tags`
— one added line at each site.

REQUIRED, never optional. `describeFeature.ts`'s `FeatureCollection.hooks` field comment states the
general reason ("an optional field would let a later consumer forget hooks exist") and STATE.md's
03-05 entry states the specific one for `ParsedFeature.parameterTypes`. A Scenario with no tags gets
`[]`, which is what `ParsedScenario.tags` already is for that case — there is no "absent" state to
model.

`ReadonlyArray<string>`, not `string[]`. The framework's own options type wants a mutable array, and
the single `[...tags]` widening for that lives in `describeFeature.ts`'s adapter (plan 09-05). Do not
widen the field here to "fix" the assignment error that will appear downstream.

The added field's doc comment must answer the tension `FeaturePlan`'s own doc comment raises — that
"a plan that copied a subset would have to grow a field every time the runner learned to read one
more". The answer, per RESEARCH Finding 11: that sentence is about FEATURE-level fields, and
`ScenarioPlan` already copies three Scenario-level ones (`name`, `astName`, `ruleId`); `tags` is the
fourth of exactly that kind. Write the answer down rather than leaving the reader to reconstruct it
— this repo's norm is that the non-obvious lives in the module comment.

The comment must also state, citing `Model.ts`, that the value arrives ALREADY FLATTENED in feature →
rule → scenario → examples order with literal `@` prefixes retained, and that nothing in this package
recomputes inheritance or normalises the prefix. The prefix is load-bearing: it is what a
`--tagsFilter '@slow'` invocation matches against.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'readonly tags: ReadonlyArray<string>' packages/vitest/src/Plan.ts` is 1.
    - `grep -c 'tags: scenario.tags' packages/vitest/src/Plan.ts` is 1.
    - `grep -c 'string\[\]' packages/vitest/src/Plan.ts` is 0.
    - `pnpm build` exits 0.
    - The field's doc comment names `Model.ts`'s flattening order and states why copying a fourth Scenario-level field does not contradict `FeaturePlan`'s "copied a subset" sentence.
  </acceptance_criteria>
  <done>`ScenarioPlan` carries a required, read-only, already-flattened tag array populated from the parsed Scenario.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the tag flow in Plan.test.ts and update Runner.test.ts's hand-built fixtures</name>
  <read_first>
    - packages/vitest/test/Plan.test.ts (WHOLE FILE — its fixture-building style and its existing `ScenarioPlan` value assertions)
    - packages/vitest/test/Runner.test.ts (lines 130-240 for the recording fake and hand-built plan fixtures; then `grep -n 'scenarioId:' packages/vitest/test/Runner.test.ts` to find every literal `ScenarioPlan` that now needs the field)
    - packages/gherkin/test/Correlate.test.ts (line ~173 — the verified four-level inheritance ordering this plan's assertion should mirror)
    - packages/vitest/src/Plan.ts (as amended in Task 1)
  </read_first>
  <action>
In `packages/vitest/test/Plan.test.ts`, add assertions proving the tag flow, built from a real parsed
Feature source rather than a synthetic `ScenarioPlan`:
- A Feature-level tag, a Rule-level tag, a Scenario-level tag and an Examples-block tag all appear on
  the planned Scenario, in feature → rule → scenario → examples order, matching
  `Correlate.test.ts:173`'s verified expectation. Use the eight tag names plan 09-01 declared
  (`@featuretag`, `@ruletag`, `@scenariotag`, `@exampletag`) so the source is reusable elsewhere in
  the repo without tripping the config's tag validation.
- A Scenario with no tags at all plans with `tags` equal to `[]` — a present empty array, not a
  missing field.
- Every row of a Scenario Outline carries the same tag set, since the Examples-block tags are part of
  the flattened set on each Pickle.

In `packages/vitest/test/Runner.test.ts`, add `tags: []` (or a meaningful set where a test is about
to want one) to every hand-built `ScenarioPlan` literal. This is the mechanical cost RESEARCH
Finding 11 Option A names, and it must land in this plan rather than 09-04's so that
`pnpm typecheck:test` stays green at every commit — STATE.md's 03-05 entry records the same lesson
("a plan that adds a required field to an already-consumed contract cannot be scoped to one file").
Do not add any tag ASSERTIONS to `Runner.test.ts` here; plan 09-04 owns those.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/vitest && pnpm typecheck:test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run packages/vitest` exits 0 and the repo's total test count increased by at least 3.
    - `pnpm typecheck:test` exits 0 (proves every hand-built `ScenarioPlan` literal was updated).
    - A `Plan.test.ts` assertion compares a planned Scenario's `tags` to a four-element array in feature → rule → scenario → examples order.
    - A `Plan.test.ts` assertion compares an untagged Scenario's `tags` to `[]`.
    - Mutation proof recorded in the summary: deleting the `tags: scenario.tags` line makes `pnpm build` fail (the field is required), and changing it to `tags: []` makes the ordering assertion fail.
    - `grep -c 'toEqual' packages/vitest/test/Runner.test.ts` is unchanged from before this task (no new assertions added there).
  </acceptance_criteria>
  <done>The four-level tag inheritance reaches `ScenarioPlan` and is asserted; the repo type-checks with the new required field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.feature` author → `ScenarioPlan` | Tag strings cross from user-authored source into a library data structure. No rendering happens here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | `ScenarioPlan.tags` optionality | mitigate | The field is required and `pnpm build` fails without the mapping line; an optional field would let plan 09-04's walk silently see `undefined` and emit nothing. |
| T-09-03-02 | Information disclosure | Tag strings rendered here | accept | Nothing in `Plan.ts` renders a tag into a message or a node title in this plan; every rendering path is plan 09-02's factories and plan 09-05's terminal channel, both of which `JSON.stringify`. |
| T-09-03-03 | Tampering | Silent re-derivation of inheritance | mitigate | `Model.ts` states the value is already flattened; the field's own doc comment repeats "do not recompute", and the four-level ordering assertion would fail if anything re-derived it. |
| T-09-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; RESEARCH's Package Legitimacy Audit records zero new dependencies for this phase. |
</threat_model>

<verification>
- `pnpm build` — the required field compiles across the solution build.
- `pnpm typecheck:test` — every hand-built fixture carries the field.
- `pnpm exec vitest run packages/vitest` — new Plan assertions green, no existing assertion changed.
- `pnpm lint` — formatting clean.
</verification>

<success_criteria>
`ScenarioPlan.tags` is a required, already-flattened `ReadonlyArray<string>` populated by
`planFeature`, asserted at all four inheritance levels and for the untagged case, with the whole repo
type-checking.
</success_criteria>

<output>
Create `.planning/phases/09-tags/09-03-SUMMARY.md` when done. Record the mutation proofs and the
final `ScenarioPlan` shape verbatim — plan 09-04's walk depends on it.
</output>
