---
phase: 09-tags
plan: 06
type: execute
wave: 4
depends_on: ["09-01", "09-05"]
files_modified:
  - packages/vitest/test/emission.test.ts
autonomous: true
requirements: [RUN-05]

must_haves:
  truths:
    - "A Feature whose Scenarios carry declared tags collects and runs through the real describeFeature, not a fake"
    - "A @skip Scenario's Before, After, BeforeStep and AfterStep hooks never run"
    - "A @skip Scenario's step bodies never run"
    - "A @skip Scenario containing a step no definition matches does not fail the run"
    - "A Scenario carrying a tag the config does not declare still runs, and one warning naming the file, the Scenario and the tag is printed"
    - "A run whose excludeTags removed Scenarios prints one summary line saying how many and why"
  artifacts:
    - path: "packages/vitest/test/emission.test.ts"
      provides: "Runtime acceptance for criteria 1 and 2 and for D-08 and D-10, through the real describeFeature"
      min_lines: 60
  key_links:
    - from: "packages/vitest/test/emission.test.ts"
      to: "vitest.config.ts"
      via: "every emitted tag is declared there except the deliberate D-08 probe"
      pattern: "@undeclared-on-purpose"
---

<objective>
Prove at runtime, through the real `describeFeature`, what plan 09-04 proved against a recording
fake: tags reach the actual test framework, `@skip` prevents the Scenario body and every hook from
running at all, an undeclared tag degrades instead of destroying the file, and a registration
exclusion announces itself.

Purpose: `packages/vitest/test/emission.test.ts` is the ONLY file in the repo that calls
`describeFeature` for real. Everything a recording fake cannot see lands here.
Output: new blocks in one existing test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tags/09-CONTEXT.md
@.planning/phases/09-tags/09-RESEARCH.md
@.planning/phases/09-tags/09-01-SUMMARY.md
@.planning/phases/09-tags/09-05-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- The tag universe this file may emit. Everything except the last entry is declared in
     vitest.config.ts by plan 09-01. Emitting anything else fails this whole file to zero tests. -->

Declared:    "@skip", "@only", "@slow", "@wip", "@featuretag", "@ruletag", "@scenariotag", "@exampletag"
NOT declared, deliberately: "@undeclared-on-purpose"   (the D-08 probe — see plan 09-01 comment (d))

Public entry point as of plan 09-05:
  describeFeature(feature, layer, define, options?: DescribeFeatureOptions)
  DescribeFeatureOptions = { readonly includeTags?: ReadonlyArray<string>; readonly excludeTags?: ReadonlyArray<string> }

Structural guarantees this plan asserts (RESEARCH Finding 5): a skipped test's handler is never
invoked, so `buildScenarioEffect` is never called and no hook Effect is ever constructed. There is no
vitest `beforeEach`/`afterEach` anywhere in `packages/vitest/src` — every hook is woven INSIDE the
Scenario's Effect by `ScenarioEffect.ts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert a tagged Feature collects and runs for real</name>
  <read_first>
    - packages/vitest/test/emission.test.ts (WHOLE FILE — its header rules at lines 1-60, especially "this is the ONLY file in this repo that calls describeFeature for real" and "the Feature is deliberately, entirely happy-path"; its inline `parseFeature` source style; and the existing module-scope-stub-then-assert-inside-an-`it` idiom used by the terminal-channel block)
    - vitest.config.ts (the declared tag list from plan 09-01)
    - packages/gherkin/test/Correlate.test.ts (line ~173 — the four-level inheritance ordering)
  </read_first>
  <action>
Add a block whose inline `.feature` source carries a Feature-level tag, a Rule-level tag, a
Scenario-level tag and an Examples-block tag, drawn from the declared list
(`@featuretag`, `@ruletag`, `@scenariotag`, `@exampletag`), plus one Scenario tagged `@slow` and one
untagged Scenario. Register happy-path step definitions for all of them and let them run.

The assertion this block carries is largely STRUCTURAL and must be stated in the block's comment,
because it is not obvious: with `strictTags` at its default `true`, emitting a tag the config does
not declare throws at collection. So a tagged Feature that COLLECTS AT ALL — non-zero tests, all
green — is itself proof that the library handed those exact tag strings to the framework's validator
and that the validator accepted them. Task 3's undeclared-tag probe is the other half of the pair: it
proves the same path is genuinely live rather than the library silently dropping tags, because a
library that dropped them would produce no warning there.

Add explicit assertions for what IS in-process observable: each Scenario's steps ran (module-scope
counters), and the `@slow` and untagged Scenarios both ran. Do not attempt to read the framework's
task object from inside a Scenario body — the library's `TestApi.effect` self thunk takes no
arguments by design (`TestApi.ts` note (d)), so no test context is reachable there. The definitive
"the tag landed on the real task" assertion is plan 09-08's CLI gate; say so in the comment rather
than leaving a reader to wonder why it is absent here.

Also add one Scenario tagged `@only` with an ordinary passing body, and a comment recording that its
presence in a suite run under `allowOnly: false` IS roadmap criterion 3 — the repo's own suite is the
assertion, per RESEARCH Finding 15. Do not use the framework's `.only` anywhere.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/vitest/test/emission.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run packages/vitest/test/emission.test.ts` exits 0 and reports a non-zero test count (a zero-test collection is the exact failure mode this block detects).
    - `pnpm exec vitest run packages/vitest/test/emission.test.ts --allowOnly=false` exits 0 — criterion 3 verified deterministically and independently of the environment.
    - The block's source contains all four of `@featuretag`, `@ruletag`, `@scenariotag`, `@exampletag`, plus `@slow` and `@only`.
    - `grep -c '\.only(' packages/vitest/test/emission.test.ts` is 0.
    - Mutation proof recorded in the summary: emitting a tag absent from `vitest.config.ts` from this block (before Task 3's catch-and-degrade block exists in the file) collapses the file to zero tests — confirming the framework validation is live and this block's collection is not vacuous. Reverted.
  </acceptance_criteria>
  <done>A four-level-tagged Feature runs green through the real entry point under a CI-mode `allowOnly` setting.</done>
</task>

<task type="auto">
  <name>Task 2: Assert @skip runs nothing — no body, no hooks, and no failure from an unmatched step</name>
  <read_first>
    - packages/vitest/test/emission.test.ts (as amended in Task 1; reuse its module-scope counter idiom)
    - packages/vitest/src/ScenarioEffect.ts (confirm every hook is woven inside the returned Effect — `runHookBatch(args.hooks.Before)` and the `Effect.onExit` for `After` — so a never-invoked thunk constructs no hook at all)
    - packages/vitest/src/Plan.ts (confirm `planFeature` stores an unresolved step rather than throwing, and that its `StepMatchError` is only reached at `yield*` time)
    - .planning/phases/09-tags/09-RESEARCH.md (Finding 5; and Pitfall 15's skip-ordering rule)
  </read_first>
  <action>
Add a block whose Feature registers all four per-Scenario hooks — `Before`, `After`, `BeforeStep`,
`AfterStep` — each incrementing its own module-scope counter, and whose Scenarios are:

- One `@skip`-tagged Scenario whose step bodies increment a counter that must stay at zero.
- One `@skip`-tagged Scenario containing a step text NO registered definition matches. This is
  Pitfall 15: it must report skipped, not undefined and not failed. State in the block's comment why
  this is safe despite the file's "deliberately, entirely happy-path" rule — the body is never
  invoked, so `buildScenarioEffect` is never called and the unresolved step's error is never reached.
  The safety is non-obvious and is exactly the kind of thing this file's header asks to be written
  down.
- One untagged Scenario in the SAME Feature with a passing body, which is what makes the hook
  counters non-vacuous: they must be greater than zero from that Scenario and no larger than that
  Scenario's own contribution.

Assert in a sibling `it` (module-scope counters read inside a test, matching the file's existing
idiom) that every hook counter equals exactly the count the one runnable Scenario produces, and that
the skipped Scenarios' body counters are zero.

Add a second, separate Feature in which EVERY Scenario is `@skip`-tagged and an `AfterAllScenarios`
hook increments a counter. Assert that counter is zero: plan 09-04 suppresses the
`⚙ AfterAllScenarios` node when nothing runnable was emitted, and this is the runtime half of that
claim. State the reason in the comment — `BeforeAllScenarios` is a once-cell reached only from inside
a Scenario thunk and structurally cannot run in that state, so an unconditional teardown would tear
down resources that were never set up.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/vitest/test/emission.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run packages/vitest/test/emission.test.ts` exits 0.
    - The run reports at least two SKIPPED tests (a `@skip` Scenario that PASSES instead of skipping is the failure this detects); the skipped count is recorded in the summary.
    - An assertion proves every one of `Before`, `After`, `BeforeStep`, `AfterStep` ran exactly the number of times the single runnable Scenario accounts for, and no more.
    - An assertion proves both `@skip` Scenarios' step-body counters are zero.
    - An assertion proves the `AfterAllScenarios` counter is zero for the all-skipped Feature.
    - Mutation proof 1 recorded in the summary: making `isSkipped` always return `false` turns the unmatched-step `@skip` Scenario RED and makes the hook-counter assertion fail — proving the skip is what carries Pitfall 15 here, not the absence of the step.
    - Mutation proof 2 recorded in the summary: dropping the runnable-count conjunct from `Runner.ts`'s `AfterAllScenarios` condition makes the all-skipped Feature's counter non-zero.
  </acceptance_criteria>
  <done>A skipped Scenario provably runs no step and no hook, an unmatched step inside one is harmless, and a fully-skipped Feature runs no teardown.</done>
</task>

<task type="auto">
  <name>Task 3: Assert the D-08 undeclared-tag degradation and the D-10 exclusion notice</name>
  <read_first>
    - packages/vitest/test/emission.test.ts (as amended; reuse the existing module-scope console stub used by the terminal-channel block, and its comment explaining why the stub is installed at module scope and asserted inside an `it`)
    - packages/vitest/src/describeFeature.ts (the adapter factory and the notice print from plan 09-05)
    - packages/vitest/src/Errors.ts (`makeUndeclaredTagWarning`, `makeExcludedScenariosNotice` and their exact message shapes from plan 09-02)
    - vitest.config.ts (confirm `@undeclared-on-purpose` is absent from `test.tags` and that its comment forbids adding it)
    - .planning/phases/09-tags/09-CONTEXT.md (D-08, D-10)
  </read_first>
  <action>
Add two blocks, each installing a module-scope `console.warn` capture in the file's existing idiom and
asserting the captured lines inside an `it`.

**D-08 degradation.** A Feature with one Scenario tagged `@undeclared-on-purpose` and an ordinary
passing body. Assert: the Scenario RAN (its step counter incremented), the file collected normally
(other Scenarios in the same block still ran), and exactly one captured warning line names the
Feature's uri, the Scenario title, and the tag in its quoted form. Assert the quoted form specifically
— a raw tag string in the message would be the security regression plan 09-02's factory exists to
prevent. Write into the block's comment that this test is ALSO the positive control for Task 1: a
library that silently dropped tags would print nothing here and Task 1's block would still be green,
so this assertion is what makes the tag path provably live. Add a comment naming
`@undeclared-on-purpose` as reserved and stating that declaring it in `vitest.config.ts` deletes this
test's meaning while leaving it green.

**D-10 exclusion notice.** A Feature with three Scenarios, two of them tagged `@wip`, called through
`describeFeature(feature, layer, define, { excludeTags: ["@wip"] })`. Assert: exactly one captured
line reports the exclusion, it names the count `2`, it names `excludeTags`, and it quotes the tag; and
that neither excluded Scenario appears as a test at all — assert absence by title against the run's
own results or by a module-scope counter that stays at zero, NOT by a total count, since a count
cannot distinguish "absent" from "skipped" and that distinction is D-03's whole point.

Add a third, small case: `describeFeature(feature, layer, define, { excludeTags: [] })` prints NO
notice and emits every Scenario. This is the empty-array rule at the public boundary, and it is the
assertion that catches a regression where a computed-empty array silences a suite.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/vitest/test/emission.test.ts && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run packages/vitest/test/emission.test.ts` exits 0 with a non-zero test count.
    - `pnpm test` exits 0 for the whole repo.
    - `grep -v '^\s*//' vitest.config.ts | grep -c 'undeclared-on-purpose'` is 0 — the probe tag is still undeclared.
    - An assertion matches the D-08 warning against the tag in its `JSON.stringify`'d form, and would fail against the raw form.
    - An assertion proves the `@undeclared-on-purpose` Scenario's step body ran.
    - An assertion proves exactly one D-10 notice line was printed, naming the count 2 and the word `excludeTags`.
    - An assertion proves both `@wip` Scenarios produced no test node and no step execution.
    - An assertion proves `excludeTags: []` prints no notice and emits every Scenario.
    - Mutation proof recorded in the summary: removing the catch-and-degrade from `describeFeature.ts`'s adapter collapses `emission.test.ts` to zero collected tests, which is the failure mode D-08 exists to prevent. Reverted.
  </acceptance_criteria>
  <done>An undeclared tag warns and keeps running; a registration exclusion announces itself once; an empty filter array announces nothing and excludes nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.feature` author → terminal | This plan is the only place the rendered warning text is asserted end to end. |
| library → test framework | The undeclared-tag probe deliberately crosses the framework's validation boundary and back. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Spoofing | A tag rendered raw into the D-08 warning | mitigate | The assertion matches the `JSON.stringify`'d form specifically and fails against the raw form. |
| T-09-06-02 | Tampering (silent green) | An excluded Scenario disappearing with no signal | mitigate | Exactly one D-10 notice line is asserted, naming the count and the option; absence of the Scenario is asserted by title/counter, never by a total count. |
| T-09-06-03 | Denial of signal | The whole file collecting zero tests | mitigate | Every task's acceptance criteria assert a non-zero test count, and Task 1's recorded mutation shows a zero-test collection is what an undeclared tag produces without the degradation. |
| T-09-06-04 | Elevation of privilege | A committed only-modifier | mitigate | `grep -c '\.only('` is 0, and the file is run with `--allowOnly=false` as an acceptance command. |
| T-09-06-05 | Tampering | Declaring the probe tag, silently voiding the D-08 test | mitigate | An acceptance grep asserts `@undeclared-on-purpose` stays out of `vitest.config.ts`'s declared list, and both files carry a comment saying so. |
| T-09-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; RESEARCH's Package Legitimacy Audit records ONE new direct dependency for the phase overall (tinyglobby@0.2.17, added by plan 09-07 under CONTEXT.md's D-09 addendum), not zero. |
</threat_model>

<verification>
- `pnpm exec vitest run packages/vitest/test/emission.test.ts` — non-zero tests, all green, at least two skipped.
- `pnpm exec vitest run packages/vitest/test/emission.test.ts --allowOnly=false` — exits 0 (criterion 3).
- `pnpm test` — whole repo green.
- `pnpm typecheck:test`, `pnpm lint` — exit 0.
</verification>

<success_criteria>
Through the real `describeFeature`: a four-level-tagged Feature collects and runs, a `@skip` Scenario
runs no step and no hook and is harmless even with an unmatched step, a fully-skipped Feature runs no
teardown, an undeclared tag warns with quoted text and keeps running, and an exclusion prints exactly
one notice while an empty filter array prints none.
</success_criteria>

<output>
Create `.planning/phases/09-tags/09-06-SUMMARY.md` when done. Record: the file's skipped-test count,
the exact titles of the `@only`-tagged and `@skip`-tagged Scenarios (plan 09-08's CLI gate greps for
them), and all four mutation proofs.
</output>
