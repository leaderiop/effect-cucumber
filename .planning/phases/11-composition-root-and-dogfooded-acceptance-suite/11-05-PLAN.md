---
phase: 11-composition-root-and-dogfooded-acceptance-suite
plan: 05
type: execute
wave: 5
depends_on: ["11-04"]
files_modified:
  - packages/vitest/test/acceptance/hooks.feature
  - packages/vitest/test/acceptance/hooks.steps.test.ts
  - scripts/verify-acceptance-ref-state.sh
  - scripts/verify-acceptance-no-any.sh
  - package.json
  - .github/workflows/check.yml
  - spec/traceability.md
autonomous: true
requirements: [RUN-06]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All six hook kinds are registered from a real acceptance Feature's `define` callback as bare generator functions, and their full ordering across a two-Scenario Feature is asserted from inside a running step."
    - "`pnpm verify:acceptance-ref-state` fails when an acceptance step module declares a mutable binding, and passes otherwise — with a positive control that fails when the scanned directory is empty or renamed."
    - "`pnpm verify:acceptance-no-any` fails when an acceptance step module contains the TypeScript escape-hatch type as a standalone token, and passes otherwise — with a positive control asserting the same regex still matches a known control file."
    - "Both new gates run in CI on every push, as root `package.json` scripts that also run locally."
  artifacts:
    - packages/vitest/test/acceptance/hooks.feature
    - packages/vitest/test/acceptance/hooks.steps.test.ts
    - scripts/verify-acceptance-ref-state.sh
    - scripts/verify-acceptance-no-any.sh
    - "root package.json scripts verify:acceptance-ref-state and verify:acceptance-no-any"
    - ".github/workflows/check.yml steps for both gates"
    - "spec/traceability.md section 5 row for REQ-EC-016"
  key_links:
    - "`scripts/verify-acceptance-ref-state.sh` -> `packages/vitest/test/acceptance/*.steps.test.ts`: INV-EC-006's first automated enforcement anywhere in the repository."
    - "`scripts/verify-acceptance-no-any.sh` -> INV-EC-003's boundary condition: the invariant holds for step bodies free of the escape-hatch type, and this gate is where that boundary becomes enforced rather than stated."
    - "root `package.json` script -> `.github/workflows/check.yml` step: a gate script with no CI step is back to being a convention (STATE.md, Phase 01-06)."
  prohibitions:
    - id: PROH-11-01
      statement: "A `@REQ-EC-NNN` tag must never be placed on a Scenario that would still pass with the behavior it claims to cover removed from the library."
      source: planner-prohibition-probe
    - id: PROH-11-02
      statement: "The TypeScript escape-hatch type must never be introduced into an acceptance step body, a World service type, or an acceptance Layer in order to make something compile."
      source: planner-prohibition-probe
    - id: PROH-11-03
      statement: "Cross-step Scenario state must never be carried by a module-scope mutable array, object, or counter that a step writes to in order to satisfy the letter of the structural gate while defeating INV-EC-006's intent."
      source: planner-prohibition-probe
---

<!-- planner-discipline-allow: any -->
<!-- planner-discipline-allow: let -->
<!-- planner-discipline-allow: var -->

<objective>
Close the sixth green acceptance requirement — `@REQ-EC-016` (DSL-07, all six hooks) — and build the
two structural gates this phase owes: `scripts/verify-acceptance-ref-state.sh` (RUN-06 / INV-EC-006,
roadmap success criterion 2) and `scripts/verify-acceptance-no-any.sh` (D-04b, INV-EC-003's boundary
condition), both wired into `package.json` and `.github/workflows/check.yml`.

Purpose: INV-EC-006 is the last invariant in `spec/traceability.md` section 2 whose **Test** column
reads "None yet — candidate lint rule". This plan is what makes it read like the other five. And
INV-EC-003's boundary condition — that the guarantee holds for step bodies free of the escape-hatch
type — is currently stated in `spec/invariants.md` and enforced only over the tsgo-gate fixtures; D-04
extends it to the acceptance suite, which is the suite whose whole job is to prove INV-EC-003.

Output: the fifth acceptance pair, two new gate scripts, their CI steps, and one more section 5 row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-composition-root-and-dogfooded-acceptance-suite/11-CONTEXT.md
@.planning/phases/11-composition-root-and-dogfooded-acceptance-suite/11-PATTERNS.md
@.planning/phases/11-composition-root-and-dogfooded-acceptance-suite/11-04-SUMMARY.md
@spec/behaviors/07-hook-ordering-and-guarantees.md
@spec/invariants.md
</context>

## Artifacts this phase produces

The full list is in `11-01-PLAN.md` under the same heading. This plan creates:

- `packages/vitest/test/acceptance/hooks.feature` and `packages/vitest/test/acceptance/hooks.steps.test.ts`
- `scripts/verify-acceptance-ref-state.sh` and `scripts/verify-acceptance-no-any.sh`
- Root `package.json` scripts `verify:acceptance-ref-state` and `verify:acceptance-no-any`
- Two new steps in `.github/workflows/check.yml`'s `package` job
- Module-local TypeScript symbols in the hooks step module, none exported: a `HookLog`
  `Context.Service` class with a `static readonly layer`, and the module-scope `feature` constant
- Permanent Gherkin tag identifier `@REQ-EC-016`

## Assumptions (flagged, from the spec-less edge probe against RUN-06)

Carried forward from `11-01-PLAN.md`. This is the plan where ASSUMPTION-11-B is exercised.

- **ASSUMPTION-11-B (empty / single-element).** A structural gate that scans a directory and finds
  ZERO matching files passes vacuously, so a renamed or moved acceptance directory silently disables
  both new gates. This plan mitigates it with positive controls, but the mitigation is itself an
  assumption: it assumes a control file elsewhere in the repository will keep containing the pattern
  the gate searches for. **Unverified as a standing guarantee** — if that control file is ever
  cleaned up, the control fails loudly, which is the intended failure direction but is still a
  maintenance coupling this plan is choosing knowingly.
- **ASSUMPTION-11-A (adjacency).** A fifth service class in a fifth acceptance module. Unverified that
  same-named `Context.Service` tag ids across files do not collide.
- **ASSUMPTION-11-C (ordering).** The six-hook ordering assertion depends on the two Scenarios of the
  hooks Feature running in declaration order. Unverified as a stated guarantee.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The hooks pair — all six hook kinds, ordered, from a real Feature</name>
  <files>packages/vitest/test/acceptance/hooks.feature, packages/vitest/test/acceptance/hooks.steps.test.ts</files>
  <reversibility rating="reversible">One new acceptance pair.</reversibility>
  <read_first>
    - `spec/behaviors/07-hook-ordering-and-guarantees.md` BEH-EC-017 in full — the six-hook ordering, the independence-and-combine batch semantics, the `Before` gate, and the `After`/`AfterStep` guarantee.
    - `packages/vitest/test/Runner.test.ts`'s full six-hook ordering test — the existing in-process proof and the exact expected sequence across a two-Scenario Feature.
    - `packages/vitest/test/emission.test.ts`'s second `describeFeature` call, which registers all six hook kinds for real and proves the trailing `AfterAllScenarios` node executed.
    - `packages/vitest/src/Dsl.ts`'s `FeatureDsl` — the six hook members and `HookRegistrar<ROut>`'s zero-parameter call signature. A hook body receives NO arguments, including `BeforeStep`/`AfterStep` (BEH-EC-006 correction (i)).
    - `packages/vitest/test/acceptance/parsing-and-matching.steps.test.ts` — the established file shape.
  </read_first>
  <behavior>
    - Test 1 (`@REQ-EC-016`, DSL-07): the first Scenario's final step asserts the recorded hook log so far equals the exact expected prefix — `BeforeAllScenarios`, then `Before`, then `BeforeStep`/step/`AfterStep` per step.
    - Test 2: the second Scenario's final step asserts the log now additionally contains the first Scenario's `After` and its own `Before`, and that `BeforeAllScenarios` appears exactly ONCE across both Scenarios.
    - Both Scenarios pass, and every hook body is written as a bare generator function that the library wrapped.
  </behavior>
  <action>
Create `packages/vitest/test/acceptance/hooks.feature` with two Scenarios, each with two steps, so the
per-step `BeforeStep`/`AfterStep` bracketing is observable more than once. Tag the FIRST Scenario
`@REQ-EC-016`; leave the second untagged, because D-01 puts each tag on exactly one Scenario and the
second Scenario is evidence for the same claim rather than a claim of its own.

Create `packages/vitest/test/acceptance/hooks.steps.test.ts` following the established shape. Declare
a `HookLog` `Context.Service` whose shape is `{ readonly entries: Ref.Ref<ReadonlyArray<string>> }`,
backed by a `Layer.effect`. Because the per-Scenario Layer is rebuilt for every Scenario, a
per-Scenario `Ref` cannot carry the cross-SCENARIO half of the ordering claim — so provide `HookLog`
through the `shared` tier of the object Layer form, with `perScenario: Layer.empty` beside it, and say
so in the module doc comment. This is the one acceptance module where the shared tier is chosen for a
reason other than dogfooding the shared tier itself, and the reason must be written down.

Register all six hook kinds from the `define` callback — `BeforeAllScenarios`, `Before`, `BeforeStep`,
`AfterStep`, `After`, `AfterAllScenarios` — each as a bare generator function taking no arguments,
each appending its own label to the `HookLog` `Ref`. Register the four step definitions the two
Scenarios need, each also appending a label.

The final step of each Scenario reads the log and asserts against an exact expected array. Assert the
FULL sequence, not a suffix or a membership check: an ordering claim asserted by membership passes
against a runner that ran the hooks in the wrong order. Additionally assert that the
`BeforeAllScenarios` label occurs exactly once across both Scenarios, which is the claim that
separates "runs once per Feature" from "runs once per Scenario".

Record in the module doc comment which parts of BEH-EC-017 this acceptance pair does NOT state, and
which artifact does: the `After`-on-FAILURE guarantee (plan 11-06's starved fixture and
`packages/vitest/test/ScenarioEffect.test.ts`), the failing-`After`-does-not-mask claim (same), and the
independent-batch combined-cause semantics (`packages/vitest/test/Hook.test.ts`).

No mutable bindings, no module-scope mutable holder for the log (PROH-11-03 — the log lives in the
`Ref`), and no standalone occurrence of the escape-hatch type (PROH-11-02).
  </action>
  <verify>
    <automated>pnpm test 2>&amp;1 | tee /tmp/11-05-t1.log; grep -q "hooks.steps.test.ts" /tmp/11-05-t1.log &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck:test &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 and reports exactly 2 passing tests from `hooks.steps.test.ts`.
    - `packages/vitest/test/acceptance/hooks.feature` contains exactly one `@REQ-EC-` tag, `@REQ-EC-016`.
    - The step module registers all six hook kinds; each of the six member names appears at least once as a call.
    - At least one assertion compares the hook log to a full expected array using a deep-equality assertion, not a membership or suffix check.
    - One assertion states that the `BeforeAllScenarios` label occurs exactly once across both Scenarios.
    - The module doc comment names `ScenarioEffect.test.ts` and `Hook.test.ts` as carrying the parts this pair does not state.
    - `pnpm lint`, `pnpm typecheck:test` and `pnpm build` each exit 0.
  </acceptance_criteria>
  <done>All six hook kinds run in their specified order from a real Feature file, asserted as a full sequence from inside running steps, with the parts this pair cannot state named rather than implied.</done>
</task>

<task type="auto">
  <name>Task 2: The two structural gates, and their CI steps</name>
  <files>scripts/verify-acceptance-ref-state.sh, scripts/verify-acceptance-no-any.sh, package.json, .github/workflows/check.yml</files>
  <reversibility rating="reversible">Two new scripts, two script entries and two CI steps; removing any of them is a local change.</reversibility>
  <read_first>
    - `scripts/verify-no-runner-dep.sh` in full — the canonical structural-grep gate. Its shebang, its METHOD NOTE, its comment-stripping rule, its positive control (assertion 1), its `fail()` helper naming the likely cause and the file to look at, its spelled-out path constants, and its closing ENFORCED line.
    - `scripts/verify-tsgo-gate.sh` lines 28-58 — the `set -euo pipefail`, `ROOT_DIR` and path-constant idiom.
    - `packages/vitest/src/Dsl.ts` line 160 — a real, standing occurrence of the escape-hatch type in this repository, which is the natural positive control for the second gate. Confirm it is still there before wiring it as the control; if it has moved, pick another and name it in the script.
    - `packages/vitest/src/Runner.ts`, `packages/vitest/src/CallSite.ts` or `packages/vitest/src/GherkinTags.ts` — each contains a real mutable binding, and one of them is the natural positive control for the first gate. Confirm before wiring.
    - `package.json`'s `scripts` block — the `verify:*` naming convention and the `bash scripts/...` form.
    - `.github/workflows/check.yml`'s `package` job — the other three structural gates already run there, each with a comment saying what a green `pnpm test` does NOT prove.
    - `.planning/STATE.md`'s repeated lesson (03-04, 10-01, 10-02): a grep-based criterion that forbids a literal also forbids documenting it, so comment lines must be stripped before counting.
  </read_first>
  <action>
Write `scripts/verify-acceptance-ref-state.sh`. It asserts roadmap success criterion 2 and INV-EC-006
structurally: no acceptance step module declares a mutable binding that a step could close over.

- Open with the repo's standard shebang, then a METHOD NOTE saying what a green `pnpm test` does not
  prove: a Scenario that closes over a mutable binding passes today and leaks between retries and
  re-runs, so observation cannot distinguish "has no mutable cross-step state" from "has it and did
  not trip on it today". Only a structural scan can. State that comment lines are stripped before any
  occurrence is counted, and why: counting raw text would make the gate self-invalidating, because
  documenting the rule would violate it.
- `set -euo pipefail`, `ROOT_DIR` resolved from `BASH_SOURCE`, `cd "$ROOT_DIR"`, and path constants
  spelled out in full rather than composed, so they stay greppable.
- Assertion 1, the positive control on POPULATION: the acceptance directory must contain at least five
  files matching `*.steps.test.ts`. Fail by name if it contains fewer, with a message saying the
  directory was probably renamed or moved and that assertions 2 and 3 would otherwise pass by scanning
  nothing. This is ASSUMPTION-11-B's mitigation.
- Assertion 2, the positive control on the REGEX: run the same declaration-matching regex against a
  named source file that really does contain a mutable binding, and fail if it matches zero times,
  with a message saying the regex has stopped working or the control file was cleaned up.
- Assertion 3, the claim: after stripping comment lines from every `*.steps.test.ts` under the
  acceptance directory, the declaration-matching regex must match zero times. On failure, print every
  offending file and line, and a message pointing at ADR-EC-009, INV-EC-006 and
  `packages/vitest/test/acceptance/README.md`'s state rule.
- Close with the ENFORCED line in the repo's format.

Write `scripts/verify-acceptance-no-any.sh` with the same structure, for D-04b:

- METHOD NOTE: one occurrence of the escape-hatch type in a step body is assignable to everything, so
  the step compiles against every ambient Layer and INV-EC-003 is erased for it — inside the suite
  whose whole job is to prove INV-EC-003. `pnpm build` and `pnpm test` both stay green in that state,
  which is why this needs a structural scan and not an observation. Same comment-stripping rule and
  same reason.
- Assertion 1, the population control: at least five `*.steps.test.ts` files, same as above.
- Assertion 2, the regex control: the token-matching regex must match at least once in the named
  control source file identified in `read_first`. Use a standalone-token regex with non-identifier
  boundaries on both sides, so ordinary English words containing the same three letters are not
  counted.
- Assertion 3, the claim: zero matches across the comment-stripped acceptance step modules and the
  acceptance `.feature` files. Include the `.feature` files deliberately, because a step's Gherkin
  text becomes a step pattern and this gate counts a standalone token wherever it appears in this
  directory; `packages/vitest/test/acceptance/README.md` already tells authors to avoid the bare
  English word here for exactly this reason.
- Close with the ENFORCED line.

Add `"verify:acceptance-ref-state": "bash scripts/verify-acceptance-ref-state.sh"` and
`"verify:acceptance-no-any": "bash scripts/verify-acceptance-no-any.sh"` to the root `package.json`
`scripts` block, alongside the existing `verify:*` entries.

Add both as steps in `.github/workflows/check.yml`'s `package` job, after `verify:testapi-seam` and
before `verify:spec`, each preceded by a comment in the established house style saying what a green
`pnpm test` does not prove and naming the script. STATE.md's Phase 01-06 rule applies: adding a gate
script means adding a CI step for it, and every CI step must stay a root `package.json` script that
also runs locally.
  </action>
  <verify>
    <automated>pnpm verify:acceptance-ref-state &amp;&amp; pnpm verify:acceptance-no-any &amp;&amp; node -e "const s=require('./package.json').scripts;process.exit(s['verify:acceptance-ref-state']&amp;&amp;s['verify:acceptance-no-any']?0:1)" &amp;&amp; grep -c 'verify:acceptance-ref-state' .github/workflows/check.yml &amp;&amp; grep -c 'verify:acceptance-no-any' .github/workflows/check.yml</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-acceptance-ref-state.sh` exits 0 and prints an ENFORCED line.
    - `bash scripts/verify-acceptance-no-any.sh` exits 0 and prints an ENFORCED line.
    - Each script contains the string `METHOD NOTE`, a `set -euo pipefail` line, a `fail()` helper, and at least two assertions labelled as positive controls.
    - Each script's population control names a minimum file count of at least 5 and fails when the count is lower.
    - Each script's regex control names a specific control file path and fails when that file stops matching.
    - `node -e` confirms both `verify:acceptance-ref-state` and `verify:acceptance-no-any` exist in the root `package.json` `scripts` block and invoke `bash scripts/...`.
    - `.github/workflows/check.yml` contains one step running each of the two new scripts, each with a preceding comment naming the script file.
    - Every command referenced in the two new CI steps is a root `package.json` script; no inline command is added to the workflow.
  </acceptance_criteria>
  <done>INV-EC-006 and INV-EC-003's boundary condition are both enforced by a structural scan that runs locally and in CI, each with a population control and a regex control so it cannot pass by scanning nothing.</done>
</task>

<task type="auto">
  <name>Task 3: Mutation-prove both gates, and add the REQ-EC-016 traceability row</name>
  <files>spec/traceability.md, scripts/verify-acceptance-ref-state.sh, scripts/verify-acceptance-no-any.sh, packages/vitest/test/acceptance/hooks.steps.test.ts</files>
  <reversibility rating="reversible">Documentation rows plus recorded mutation notes inside the two scripts and the step module.</reversibility>
  <read_first>
    - `scripts/verify-tags-filter.sh` lines 32-42 — the recorded mutation format for a gate script: the mutation, what went red, and the sharper form that must not be simplified back into the blunt one.
    - `scripts/verify-no-runner-dep.sh`'s preamble reference to STATE.md 01-02 — a grep-based gate in this repo that passed and was then proven vacuous by mutation testing.
    - `spec/traceability.md` section 5 as extended by plan 11-04, and its not-yet-carried list.
    - The two gate scripts as written by Task 2.
  </read_first>
  <action>
Perform, record and REVERT each mutation below. Record the results in the METHOD NOTE of the script
each one attacks, and the hooks-pair ones in `hooks.steps.test.ts`'s module doc comment.

Against `scripts/verify-acceptance-ref-state.sh`:

- **Mutation A.** Introduce one mutable binding into an acceptance step module, at module scope
  outside any DSL callback. Expected: the gate FAILS naming that file and line, while `pnpm test`,
  `pnpm build`, `pnpm lint` and `pnpm typecheck:test` all stay GREEN. Record all five results — the
  four that stayed green are the reason this gate exists.
- **Mutation B.** Point the gate's acceptance-directory path constant at a directory that does not
  exist. Expected: assertion 1 FAILS on the population control rather than assertion 3 passing
  vacuously.
- **Mutation C.** Delete assertion 2, the regex control, and then break the regex so it can never
  match. Expected: with the control deleted the gate PASSES against a broken regex. Record that this
  is what the control exists to prevent, and do not simplify the control away.

Against `scripts/verify-acceptance-no-any.sh`:

- **Mutation D.** Introduce the escape-hatch type into one acceptance step body's parameter
  annotation. Expected: the gate FAILS naming that file and line while `pnpm build` and
  `pnpm typecheck:test` stay GREEN — which is precisely INV-EC-003's boundary condition made
  observable.
- **Mutation E.** Add a comment line to an acceptance step module containing the forbidden token as
  prose. Expected: the gate STILL PASSES, because comment lines are stripped before counting. Record
  it — this is STATE.md's 03-04 lesson, and a gate that fails here is one that forbids explaining
  itself.

Then add one row to `spec/traceability.md` section 5 for `REQ-EC-016`, whose **Verified by** column
names the acceptance Scenario plus `packages/vitest/test/Hook.test.ts` (the batch semantics),
`packages/vitest/test/Runner.test.ts` (the in-process ordering proof) and
`scripts/verify-tsgo-gate.sh` assertions 10 and 11 (a hook requiring an unprovided service rejected by
name, a claim about what does not compile). Update the section 5 preamble's not-yet-carried list, which
after this plan names exactly `REQ-EC-003`, `REQ-EC-007`, `REQ-EC-008`, `REQ-EC-009` and `REQ-EC-018`.
  </action>
  <verify>
    <automated>pnpm test &amp;&amp; pnpm verify:spec &amp;&amp; pnpm verify:acceptance-ref-state &amp;&amp; pnpm verify:acceptance-no-any &amp;&amp; pnpm lint &amp;&amp; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - Each gate script's METHOD NOTE records its own mutations by letter, each naming what went red and what stayed green.
    - Mutation A's record explicitly lists the four commands that stayed green.
    - Mutation C's record explicitly states that the gate passed with a broken regex once the control was removed, and instructs against removing the control.
    - Mutation E's record explicitly states the gate stayed green with the forbidden token in a comment.
    - `spec/traceability.md` section 5 contains a `REQ-EC-016` row naming `Hook.test.ts`, `Runner.test.ts` and `verify-tsgo-gate.sh`, giving 17 rows in total after this plan.
    - The section 5 preamble's not-yet-carried list names exactly REQ-EC-003, 007, 008, 009 and 018.
    - `git status --porcelain` shows no mutation left in the working tree.
    - `pnpm test`, `pnpm verify:spec`, both new gates and `pnpm lint` each exit 0.
  </acceptance_criteria>
  <done>Both gates are proven non-vacuous against a real violation, a renamed directory and a broken regex, and are proven not to forbid their own documentation. DSL-07 is traced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| acceptance step module source -> structural gate regex | The gate's only input is text; a wrong regex or a wrong path is indistinguishable from a clean repository. |
| shared `HookLog` tier -> both Scenarios of the hooks Feature | The cross-Scenario half of the ordering claim depends on one log surviving both Scenarios. |
| root `package.json` script -> CI step | A script with no CI step is a convention, not a gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05-01 | Tampering | a structural gate scanning nothing | high | mitigate | Assertion 1 in each script is a population control requiring at least five matched files; mutation B proves it fires when the path is wrong. |
| T-11-05-02 | Tampering | a structural gate with a broken regex | high | mitigate | Assertion 2 in each script is a regex control against a named file known to contain the pattern; mutation C proves the gate passes without it. |
| T-11-05-03 | Elevation of privilege | the escape-hatch type in an acceptance step body | high | mitigate | `scripts/verify-acceptance-no-any.sh` assertion 3, with mutation D proving `pnpm build` and `pnpm typecheck:test` stay green in the violating state. |
| T-11-05-04 | Repudiation | a gate that forbids its own documentation | medium | mitigate | Comment lines are stripped before counting in both scripts; mutation E records that the gate stays green with the token in prose. |
| T-11-05-05 | Denial of service | a gate that exists but never runs | medium | mitigate | Both scripts are added to `.github/workflows/check.yml`'s `package` job as root `package.json` scripts, per STATE.md's Phase 01-06 rule; asserted by grepping the workflow. |
</threat_model>

<verification>
- `pnpm test` exits 0 with exactly 2 passing tests from `hooks.steps.test.ts` and no reduction elsewhere.
- `pnpm verify:acceptance-ref-state` and `pnpm verify:acceptance-no-any` each exit 0 and print an ENFORCED line.
- `pnpm verify:spec` exits 0 with 17 section 5 rows.
- `pnpm lint`, `pnpm typecheck:test`, `pnpm build` and every pre-existing `verify:*` script still exit 0.
- Five mutations recorded and reverted; the working tree is clean.
</verification>

<success_criteria>
- DSL-07 is traced to a running acceptance Feature that registers all six hook kinds and asserts their full ordering.
- INV-EC-006 has its first automated enforcement anywhere in the repository, and INV-EC-003's boundary condition is enforced over the acceptance suite rather than only stated.
- Both gates run in CI and are proven non-vacuous by recorded mutations.
</success_criteria>

<output>
Create `.planning/phases/11-composition-root-and-dogfooded-acceptance-suite/11-05-SUMMARY.md` when done.
</output>
