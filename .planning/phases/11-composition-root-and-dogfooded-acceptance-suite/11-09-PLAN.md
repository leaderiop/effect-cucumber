---
phase: 11-composition-root-and-dogfooded-acceptance-suite
plan: 09
type: execute
wave: 9
depends_on: ["11-08"]
files_modified:
  - packages/vitest/README.md
  - spec/overview.md
  - spec/invariants.md
  - spec/traceability.md
  - spec/roadmap.md
  - spec/README.md
  - spec/process/definitions-of-done.md
  - README.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RUN-06]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A consumer reading `packages/vitest/README.md` finds a recommended lint configuration for their own step modules, with the reason stated: the compile-time guarantee holds only for step bodies free of the escape-hatch type, and no DSL signature can prevent an erasure that happens inside the author's own body (D-04a)."
    - "`spec/invariants.md` INV-EC-006's Source line names a real enforcement mechanism instead of a planned one, and `spec/traceability.md` section 2's INV-EC-006 row names the gate and the acceptance suite instead of reading None yet."
    - "`spec/roadmap.md`'s Current state, both package READMEs' Status sections, `spec/README.md`'s glossary row and `spec/process/definitions-of-done.md`'s acceptance row all describe the acceptance suite as built, with no remaining sentence claiming no acceptance suite exists."
    - "`.planning/REQUIREMENTS.md` marks RUN-06 Complete with per-requirement evidence naming the assertion that fails if it stops being true, and its coverage block reads 22 of 22."
  artifacts:
    - "packages/vitest/README.md recommended consumer lint configuration section"
    - "spec/invariants.md INV-EC-003 lint-recommendation pointer and INV-EC-006 Source update"
    - "spec/traceability.md section 2 INV-EC-006 row"
    - ".planning/REQUIREMENTS.md with RUN-06 Complete"
  key_links:
    - "`spec/invariants.md` INV-EC-003's boundary condition -> `packages/vitest/README.md`'s recommended lint configuration -> `scripts/verify-acceptance-no-any.sh`: the advice is dogfooded by a gate over this repository's own acceptance suite (D-04)."
    - "`spec/invariants.md` INV-EC-006 -> `scripts/verify-acceptance-ref-state.sh`: the last invariant whose enforcement was planned rather than built."
    - "`.planning/REQUIREMENTS.md` RUN-06 -> the acceptance suite plus the ref-state gate: the requirement's own text says the convention is demonstrated in every worked example, which is now literally checkable."
  prohibitions:
    - id: PROH-11-01
      statement: "A status document must never claim a capability the repository does not have. Every sentence flipped in this plan must be backed by a named artifact that fails if the claim stops being true (AGENTS.md section 4)."
      source: planner-prohibition-probe
    - id: PROH-11-06
      statement: "A requirement must never be marked Complete on the strength of documentation. RUN-06's evidence must name an executable assertion, and the same discipline the previous nine phases applied when they declined to mark a requirement early applies here to marking one late."
      source: planner-prohibition-probe
---

<!-- planner-discipline-allow: any -->
<!-- planner-discipline-allow: let -->

<objective>
Close the phase: write D-04a's recommended consumer lint configuration, flip every status sentence the
acceptance suite now falsifies, give INV-EC-006 its first real Source line, and mark RUN-06 Complete
with evidence that names the assertion carrying it.

Purpose: this is the reconciliation plan every prior phase in this project has ended with, and it has
one thing they did not — INV-EC-006 has been the single row in `spec/traceability.md` section 2 whose
Test column reads "None yet — candidate lint rule" since the matrix was written. Plan 11-05 built the
gate; this plan is where the documents stop saying otherwise.

Output: the lint-recommendation docs, four status documents reconciled, INV-EC-006 traced, RUN-06
marked Complete, and a full-gate sweep proving nothing regressed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-composition-root-and-dogfooded-acceptance-suite/11-CONTEXT.md
@.planning/phases/11-composition-root-and-dogfooded-acceptance-suite/11-08-SUMMARY.md
@AGENTS.md
@spec/invariants.md
</context>

## Artifacts this phase produces

The full list is in `11-01-PLAN.md` under the same heading. This plan adds no new file; it modifies
documents. The one new named surface it introduces is a section in `packages/vitest/README.md`
recommending a consumer lint configuration, cross-referenced from `spec/overview.md` and
`spec/invariants.md`.

## Assumptions (flagged, from the spec-less edge probe against RUN-06)

- **ASSUMPTION-11-A (adjacency).** Unresolved and now permanent for this milestone: the acceptance step
  modules each declare a same-named `Context.Service` id and are assumed not to collide because vitest
  isolates modules per file. Task 3 must record the assumption in the phase summary so it is not lost
  when this plan closes the phase.
- **ASSUMPTION-11-B (empty / single-element).** Partly mitigated by the population and parse controls
  in plans 11-05 and 11-08, and still an assumption in the sense that both controls depend on external
  files continuing to contain what they contain. Record it in the summary.
- **ASSUMPTION-11-C (ordering).** Unresolved: several acceptance assertions depend on vitest running a
  file's tests in declaration order, which is observed behavior rather than a documented contract, and
  which shuffled sequencing would break. Note that checklist item P-21 runs the Outline case under
  shuffled sequencing but no acceptance file is run shuffled as a whole. Record it in the summary as an
  open follow-up rather than closing it.

<tasks>

<task type="auto">
  <name>Task 1: The recommended consumer lint configuration (D-04a)</name>
  <files>packages/vitest/README.md, spec/overview.md, spec/invariants.md</files>
  <reversibility rating="reversible">A new documentation section and two cross-references.</reversibility>
  <read_first>
    - `spec/invariants.md` INV-EC-003 lines 130-160 in full — the boundary-condition paragraph, its statement that no DSL signature can prevent the erasure because it happens in the author's own body, and its closing sentence that the compile-gate fixtures are asserted to contain none. That last sentence is what plan 11-05 extended to the acceptance suite and this section now tells consumers to extend to theirs.
    - `scripts/verify-tsgo-gate.sh` around line 169 — the standing warning against adding the escape-hatch type to a fixture to make it pass. That is the argument this section restates for consumers, and `11-PATTERNS.md` names it as the nearest tone reference because no "recommended consumer lint config" prose exists anywhere in the repository.
    - `packages/vitest/README.md` in full — its heading structure, its Status section, and where a consumer-facing section belongs relative to it.
    - `spec/overview.md` — its structure, and whether it already has a place for consumer guidance or needs a new heading.
    - `.oxlintrc.json` — the rule names and configuration shape this repository actually uses, so the recommendation names rules that exist rather than rules from a different linter's vocabulary.
    - `tsconfig.base.json` — the compiler options already on in this workspace, so the recommendation does not tell a consumer to turn on something the library's own build already requires and they would inherit anyway.
    - `.planning/research/PITFALLS.md` Pitfall 6 around line 200 — the finding, and its "Phase to address" line naming the docs half as this phase's job.
  </read_first>
  <action>
Add a section to `packages/vitest/README.md` titled for what it gives a consumer — a recommended lint
and compiler configuration for their own step modules — placed after the Status section so a reader
meets it once they know what the library does. It says, in this order:

- The claim the library makes: a step whose Effect requires a service the ambient Layer does not
  provide is a compile error where the step is written. That is INV-EC-003, and it is the package's
  whole reason to exist.
- The boundary on that claim, stated plainly rather than as a caveat: it holds for step bodies free of
  the escape-hatch type. A bare occurrence of that type, and an Effect whose three type parameters are
  all that type, are assignable to everything, so a step body containing either compiles against ANY
  ambient Layer — the requirement it would otherwise declare is erased before the check has anything to
  check. No DSL signature can prevent it, because the erasure happens inside the author's own body and
  not at the boundary the invariant guards.
- The concrete recommendation: enable the compiler option that rejects an implicit occurrence of that
  type, and enable the linter's unsafe-value rule family so an explicit one in a step body is flagged.
  Name the actual rule identifiers as they appear in `.oxlintrc.json`'s vocabulary, and give the exact
  configuration keys rather than describing them.
- The reason it is a recommendation and not an enforcement: the library cannot see a consumer's build,
  and there is no signature it could ship that would close the hole.
- What this project does about it in its own repository, so the advice is not merely advice: this
  library's own acceptance suite is scanned by `scripts/verify-acceptance-no-any.sh`, which runs on
  every push and fails naming the file and line, and its compile-gate fixtures carry the same
  prohibition. Link both.

Add a short cross-reference to `spec/overview.md` pointing at that section, in whatever place the
document's existing structure makes natural. Do not duplicate the argument in both places — one canonical
statement, one pointer.

Add one sentence to `spec/invariants.md` INV-EC-003's boundary-condition paragraph, immediately after
its existing closing sentence about the compile-gate fixtures, extending the same fact to the
acceptance suite and pointing at the README section for the consumer-facing recommendation. Do not
reword the existing paragraph — the wording was landed by an earlier phase and 11-CONTEXT.md says to
verify it is still accurate rather than re-word it unless a real gap is found. If a real gap IS found,
record it explicitly in the plan summary rather than silently editing.
  </action>
  <verify>
    <automated>grep -q "verify-acceptance-no-any" packages/vitest/README.md &amp;&amp; grep -q "INV-EC-003" packages/vitest/README.md &amp;&amp; grep -c "verify-acceptance-no-any\|acceptance" spec/invariants.md &amp;&amp; pnpm lint &amp;&amp; pnpm verify:spec</automated>
  </verify>
  <acceptance_criteria>
    - `packages/vitest/README.md` contains a section recommending a consumer lint and compiler configuration, naming at least one compiler option key and at least one linter rule identifier verbatim as they appear in this repository's own configuration files.
    - That section links both `scripts/verify-acceptance-no-any.sh` and the compile-gate fixtures directory, and states that the library cannot enforce this in a consumer's build.
    - `spec/overview.md` contains exactly one pointer to that section and does not restate its argument.
    - `spec/invariants.md` INV-EC-003's boundary-condition paragraph gains exactly one sentence extending the fixture prohibition to the acceptance suite and pointing at the README section; the pre-existing sentences are byte-identical.
    - `git diff spec/invariants.md` shows additions only within INV-EC-003's paragraph and INV-EC-006's Source block, with no deletion of pre-existing INV-EC-003 prose.
    - `pnpm lint` (which includes the formatter check over markdown) and `pnpm verify:spec` each exit 0.
  </acceptance_criteria>
  <done>The escape-hatch boundary is stated once for consumers with a concrete configuration, and the advice is backed by a gate this repository runs on itself rather than offered as a suggestion.</done>
</task>

<task type="auto">
  <name>Task 2: INV-EC-006's real Source, and every status sentence the acceptance suite falsifies</name>
  <files>spec/invariants.md, spec/traceability.md, spec/roadmap.md, spec/README.md, spec/process/definitions-of-done.md, packages/vitest/README.md, README.md</files>
  <reversibility rating="reversible">Documentation updates; each is a local change.</reversibility>
  <read_first>
    - `spec/invariants.md` lines 1-15 and INV-EC-006's entry at lines 265-283 — the header paragraph counting which invariants are enforced today, and INV-EC-006's `Source (planned)` label, both of which this task changes.
    - `spec/traceability.md` section 2's preamble (lines 81-92) and its INV-EC-006 row (line 101) — the preamble's sentence naming INV-EC-006 as the one remaining row whose enforcement is planned, and the row's `None yet — candidate lint rule` Test column.
    - `spec/roadmap.md` Current state (lines 3-60) and its Planned section (lines 154-180) — in particular the bullet about a lint rule enforcing ADR-EC-009, which is now partly built.
    - `spec/README.md` line 65 — the glossary row saying no `REQ-EC-NNN` tag exists yet.
    - `spec/process/definitions-of-done.md` lines 29 and 40 — the acceptance-suite definition of done and the acceptance test-level row.
    - `packages/vitest/README.md`'s Status section and the root `README.md` — every sentence claiming the acceptance suite does not exist.
    - `packages/gherkin/test/fixtures/README.md` lines 23-26 — the rule forbidding `@REQ-EC-NNN` on a parser fixture. That rule is still TRUE and must NOT be edited; the acceptance directory's README states the inverse for its own directory.
    - `AGENTS.md` section 4 — say only what is true, and `spec/roadmap.md`'s Current state table is the single place that says what is built.
  </read_first>
  <action>
Update `spec/invariants.md`:

- INV-EC-006's `Source (planned)` label becomes a real `Source` naming
  `scripts/verify-acceptance-ref-state.sh` as the structural enforcement over
  `packages/vitest/test/acceptance/*.steps.test.ts`, plus the acceptance suite itself as the
  demonstration. State precisely what the gate does and does NOT cover: it forbids a mutable binding
  anywhere in an acceptance step module, which is a superset of the invariant's own wording, and it
  covers THIS repository's acceptance suite only — a consumer's step modules are still governed by
  convention, and LINT-01 is what would close that. Name LINT-01 and say it is deferred to a later
  milestone.
- The header paragraph at lines 6-13 counts which invariants are enforced today and says two are not.
  Update the count and the wording so it names INV-EC-006 as enforced within this repository, with the
  consumer-side gap stated rather than glossed.

Update `spec/traceability.md`:

- Section 2's preamble sentence naming INV-EC-006 as the one remaining planned row is now false;
  replace it with an accurate statement of the new position, including the repository-versus-consumer
  boundary.
- Section 2's INV-EC-006 row: **Enforced by** names the gate script and the acceptance suite's
  Ref-through-World convention; **Test** names `scripts/verify-acceptance-ref-state.sh` and the
  acceptance suite, replacing `None yet — candidate lint rule`.
- Section 3: fill ADR-EC-009's **Source module** column, which currently reads a dash, with the
  acceptance suite and the gate — following the precedent that a phase's reconciliation plan fills the
  decisions that phase implemented.

Update `spec/roadmap.md`'s Current state with one paragraph in the established style, added after the
Tags paragraph: the library now runs its own spec, with the three worked examples from
`spec/behaviors/01` through `03` executing as real `.feature` plus `.steps.test.ts` pairs under
`packages/vitest/test/acceptance/`; all 22 v1 requirements carry a `@REQ-EC-NNN` acceptance tag and
`pnpm verify:spec` reports the count; the cross-step-state convention is enforced structurally for the
first time; and the "Looks Done But Isn't" checklist is a normative document at
`spec/process/looks-done-but-isnt-checklist.md` executed by a named artifact per item. Update the
Planned section's lint-rule bullet to say what is now built and what LINT-01 would still add.

Update `spec/README.md`'s glossary row for `REQ-EC-NNN` — it currently says none exist — to say where
they live and how many.

Update `spec/process/definitions-of-done.md` rows 4 and the acceptance test-level row, so the
acceptance suite's definition of done points at the real directory and the real gate.

Update `packages/vitest/README.md`'s Status section and the root `README.md` for every sentence the
acceptance suite falsifies. Two sentences need care rather than deletion: the statement that
ADR-EC-024's wrapped `loadFeature` is still not exported is STILL TRUE and must stay, and the
acceptance suite's own deviation from the worked examples exists because of it — say so, and link
`packages/vitest/test/acceptance/README.md`.

Do not edit `packages/gherkin/test/fixtures/README.md`. Its rule that no parser fixture may carry a
`@REQ-EC-NNN` tag remains true, and `spec/scripts/verify-traceability.sh` check 4 plus check 5 are what
enforce both halves.
  </action>
  <verify>
    <automated>pnpm verify:spec &amp;&amp; pnpm lint &amp;&amp; grep -c "None yet" spec/traceability.md; grep -c "Source (planned)" spec/invariants.md; git diff --exit-code packages/gherkin/test/fixtures/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'None yet' spec/traceability.md` is 0.
    - `grep -c 'Source (planned)' spec/invariants.md` is 0.
    - `spec/invariants.md` INV-EC-006's Source names `verify-acceptance-ref-state.sh` and names LINT-01 as the deferred consumer-side half.
    - `spec/traceability.md` section 2's INV-EC-006 row's Test column names `scripts/verify-acceptance-ref-state.sh`, and section 3's ADR-EC-009 row's Source module column is no longer a dash.
    - `spec/roadmap.md` Current state contains a paragraph naming `packages/vitest/test/acceptance/` and `spec/process/looks-done-but-isnt-checklist.md`.
    - `spec/README.md`'s `REQ-EC-NNN` glossary row no longer says none exist.
    - No file in the repository outside `.planning/` still contains a sentence asserting that no acceptance suite exists; a repo-wide grep for that claim's distinctive phrasing returns only historical planning artifacts.
    - `git diff --exit-code packages/gherkin/test/fixtures/README.md` exits 0.
    - `packages/vitest/README.md` still states that ADR-EC-024's wrapped `loadFeature` is not exported, and now links the acceptance README as where the resulting deviation is recorded.
    - `pnpm verify:spec` and `pnpm lint` each exit 0.
  </acceptance_criteria>
  <done>INV-EC-006 has a real enforcement mechanism recorded with its boundary stated, and no status document in the repository still claims the acceptance suite does not exist.</done>
</task>

<task type="auto">
  <name>Task 3: Mark RUN-06 Complete with evidence, and run the full gate sweep</name>
  <files>.planning/REQUIREMENTS.md</files>
  <reversibility rating="reversible">A status flip in a planning document, revertible in one edit.</reversibility>
  <read_first>
    - `.planning/REQUIREMENTS.md` in full — RUN-06's text at line 42, the traceability table's RUN-06 row at line 105, the coverage block at lines 107-112, and the per-requirement evidence paragraphs at lines 119-193, whose style and level of specificity this task must match.
    - `.planning/STATE.md`'s repeated pattern across Phases 3, 6, 7, 8 and 10: a requirement is marked by the plan that makes it true END TO END, and earlier plans decline the marking on "say only what is true" grounds. The mirror rule applies here — the evidence must name an assertion that FAILS if the requirement stops being true.
    - `spec/process/looks-done-but-isnt-checklist.md` and both new gate scripts — the artifacts the evidence paragraph names.
    - `.planning/ROADMAP.md` Phase 11's four success criteria — each must be addressable by a named artifact in the evidence.
    - `.github/workflows/check.yml` as extended by plans 11-05 and 11-08 — the full list of gates the sweep must run.
  </read_first>
  <action>
Mark RUN-06 Complete in `.planning/REQUIREMENTS.md`: flip its checkbox at line 42, flip its traceability
table row from Pending to Complete, and update the coverage block so it reads 22 of 22 mapped and
complete rather than 21 complete with one pending.

Add a dated evidence paragraph in the established style of the paragraphs already in that file, naming
for each of Phase 11's four roadmap success criteria the artifact that fails if it stops being true:

- Criterion 1, the worked examples run green: the three `worked-example-*` acceptance pairs, named by
  file, and the mutations recorded in each that turn them red when the Gherkin value or the cross-step
  `Ref` stops carrying the claim.
- Criterion 2, cross-step state flows through a `Ref` from World with no closed-over mutable binding:
  `scripts/verify-acceptance-ref-state.sh`, with its population control and its regex control named,
  and its recorded mutation showing that `pnpm test`, `pnpm build`, `pnpm lint` and `pnpm typecheck:test`
  all stay green against a violation.
- Criterion 3, 22 of 22 covered: `spec/scripts/verify-traceability.sh` check 5, which asserts the ids
  are contiguous, occur exactly once each, and appear in section 5, and prints the count.
- Criterion 4, the checklist runs in full and the escape-hatch wording is addressed:
  `spec/process/looks-done-but-isnt-checklist.md` with its 24 ids, the three artifacts executing them,
  `scripts/verify-pitfalls-checklist.sh`'s coverage cross-check, and `scripts/verify-acceptance-no-any.sh`
  plus the README section for D-04's two halves.

State explicitly which parts of RUN-06 are NOT enforced, so the marking claims only what is true
(PROH-11-06): the gate covers this repository's own acceptance step modules, not a consumer's, and
LINT-01 — deferred to a later milestone — is what would close that. Also record the three flagged
assumptions from the spec-less edge probe (11-A adjacency, 11-B empty, 11-C ordering) as open items
carried out of this phase, with 11-C's specific shape named: several acceptance assertions depend on
declaration order, which shuffled sequencing would break, and only the Outline case is run shuffled
today.

Then run the full gate sweep and record each result in the plan summary: `pnpm build`,
`pnpm typecheck:test`, `pnpm lint`, `pnpm test`, `pnpm circular`, `pnpm verify:pack`,
`pnpm verify:spec`, `pnpm verify:tsgo-gate`, `pnpm verify:oxlint-plugin`, `pnpm verify:no-runner-dep`,
`pnpm verify:testapi-seam`, `pnpm verify:tags-filter`, `pnpm verify:shared-layer-once`,
`pnpm verify:acceptance-ref-state`, `pnpm verify:acceptance-no-any`, `pnpm verify:watch-rerun` and
`pnpm verify:pitfalls`. Record the repo's final test file count and test count, and the delta from the
count recorded at the end of Phase 10, so a later reader can tell what this phase added.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm typecheck:test &amp;&amp; pnpm lint &amp;&amp; pnpm test &amp;&amp; pnpm circular &amp;&amp; pnpm verify:pack &amp;&amp; pnpm verify:spec &amp;&amp; pnpm verify:tsgo-gate &amp;&amp; pnpm verify:oxlint-plugin &amp;&amp; pnpm verify:no-runner-dep &amp;&amp; pnpm verify:testapi-seam &amp;&amp; pnpm verify:tags-filter &amp;&amp; pnpm verify:shared-layer-once &amp;&amp; pnpm verify:acceptance-ref-state &amp;&amp; pnpm verify:acceptance-no-any &amp;&amp; pnpm verify:watch-rerun &amp;&amp; pnpm verify:pitfalls</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` shows RUN-06 with a checked box, its traceability row reads Complete, and its coverage block states 22 mapped and 22 complete with 0 pending.
    - `grep -c 'Pending' .planning/REQUIREMENTS.md` returns 0 occurrences within the v1 traceability table.
    - The new evidence paragraph names, for each of the four roadmap success criteria, at least one artifact by path, and every path it names exists on disk.
    - The evidence paragraph explicitly states the consumer-side gap and names LINT-01.
    - The evidence paragraph records ASSUMPTION-11-A, 11-B and 11-C as open items carried out of the phase.
    - All seventeen sweep commands exit 0, and each result is recorded in the plan summary with the final test file count, test count and the delta from the end of Phase 10.
  </acceptance_criteria>
  <done>RUN-06 is Complete with evidence that names an executable assertion per success criterion, the milestone's requirement coverage reads 22 of 22, and every gate in the repository passes together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository state -> status documents | A status document is a claim about the repository that nothing type-checks; drift here is invisible until someone acts on it. |
| a requirement's Complete marking -> a future reader's trust | A requirement marked Complete on documentation rather than an assertion misleads every later phase that builds on it. |
| this repository's gate -> a consumer's build | The escape-hatch recommendation is advice a consumer may not take; claiming it as enforcement would be false. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-09-01 | Spoofing | a status document claiming more than the repository has | high | mitigate | PROH-11-01, plus the criterion that every path named in the evidence paragraph exists on disk and every flipped claim names a failing artifact. AGENTS.md section 4 is the standing rule. |
| T-11-09-02 | Spoofing | RUN-06 marked Complete on documentation | high | mitigate | PROH-11-06, plus the criterion that the evidence names one artifact per roadmap success criterion and that the full seventeen-gate sweep passes. |
| T-11-09-03 | Repudiation | the consumer-side gap disappearing from the record | medium | mitigate | The evidence paragraph and INV-EC-006's Source both state that the gate covers this repository only, and both name LINT-01 as the deferred half. |
| T-11-09-04 | Repudiation | the three flagged assumptions being lost when the phase closes | medium | mitigate | Task 3 records all three in `.planning/REQUIREMENTS.md`'s evidence paragraph and in the plan summary, with 11-C's specific shape named. |
| T-11-09-05 | Tampering | editing a rule that is still true in order to close the phase | medium | mitigate | `packages/gherkin/test/fixtures/README.md` is asserted byte-identical, and `spec/invariants.md`'s INV-EC-003 paragraph is asserted to gain a sentence rather than lose one. |
</threat_model>

<verification>
- All seventeen gates pass in one sweep, recorded individually.
- `grep -c 'None yet' spec/traceability.md` and `grep -c 'Source (planned)' spec/invariants.md` are both 0.
- `.planning/REQUIREMENTS.md` reads 22 of 22 complete.
- `git diff --exit-code packages/gherkin/test/fixtures/README.md` exits 0.
</verification>

<success_criteria>
- The escape-hatch boundary has a consumer-facing recommendation backed by a gate this repository runs on itself.
- INV-EC-006 is the sixth enforced invariant, with its repository-versus-consumer boundary stated rather than glossed.
- No status document in the repository claims the acceptance suite does not exist, and none claims more than it has.
- RUN-06 is Complete with evidence naming an executable assertion per roadmap success criterion, and the three unresolved edge assumptions are carried forward in writing rather than dropped.
</success_criteria>

<output>
Create `.planning/phases/11-composition-root-and-dogfooded-acceptance-suite/11-09-SUMMARY.md` when done.
</output>
