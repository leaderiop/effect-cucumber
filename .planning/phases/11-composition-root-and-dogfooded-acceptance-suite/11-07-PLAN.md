---
phase: 11-composition-root-and-dogfooded-acceptance-suite
plan: 07
type: execute
wave: 7
depends_on: ["11-06"]
files_modified:
  - spec/process/looks-done-but-isnt-checklist.md
  - packages/vitest/test/acceptance/pitfalls-checklist.test.ts
  - spec/traceability.md
autonomous: true
requirements: [RUN-06]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The 24-item \"Looks Done But Isn't\" checklist exists as a normative in-repo document, each item carrying a stable `P-NN` id and an **Executed by** column naming the artifact that runs it."
    - "Every checklist item whose original wording is superseded or unbuildable in this milestone carries a written note saying so, naming what it is executed as instead — never silently reworded and never silently dropped."
    - "Thirteen checklist items run as fresh, dedicated, in-process tests in `packages/vitest/test/acceptance/pitfalls-checklist.test.ts`, each titled with its `P-NN` id, independent of whatever equivalent assertion already exists elsewhere."
  artifacts:
    - spec/process/looks-done-but-isnt-checklist.md
    - packages/vitest/test/acceptance/pitfalls-checklist.test.ts
    - "spec/traceability.md section 4 rows for the new test files"
  key_links:
    - "`spec/process/looks-done-but-isnt-checklist.md` -> `scripts/verify-pitfalls-checklist.sh`'s coverage cross-check (plan 11-08): the document is the machine-readable source of the 24 ids, not a prose summary."
    - "each `P-NN` test title -> the coverage cross-check's grep: a renamed or deleted test is detected as a coverage gap rather than as a smaller pass count."
  prohibitions:
    - id: PROH-11-04
      statement: "A checklist item must never be marked covered by citing an existing test elsewhere in the repository. D-03's whole point is that the checklist becomes a literally re-runnable suite rather than a citation list, so every item gets its own executing artifact even where equivalent coverage already exists."
      source: planner-prohibition-probe
    - id: PROH-11-01
      statement: "A checklist item must never be reworded to match what was convenient to assert. Where the original wording is superseded or unbuildable, the item keeps its wording and gains a written note naming what it is executed as instead."
      source: planner-prohibition-probe
---

<!-- planner-discipline-allow: any -->

<objective>
Turn `.planning/research/PITFALLS.md`'s 24-item "Looks Done But Isn't" checklist into a normative
in-repo document with stable ids, and execute the thirteen items that have an honest in-process form
as fresh dedicated tests (D-03).

Purpose: roadmap success criterion 4 says the checklist "runs in full and passes". Today it is a
research artifact under `.planning/`, unversioned as a contract and unreferenced by any gate. D-03
chose the most feature-rich reading deliberately: a fresh test per item, even where equivalent
coverage exists, so the checklist is re-runnable rather than a citation list. Moving it into
`spec/process/` is what lets a gate cross-check it (plan 11-08) without a `scripts/` gate reaching into
GSD-internal planning files.

Output: the checklist document, the in-process half of its execution, and its traceability rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-composition-root-and-dogfooded-acceptance-suite/11-CONTEXT.md
@.planning/phases/11-composition-root-and-dogfooded-acceptance-suite/11-PATTERNS.md
@.planning/phases/11-composition-root-and-dogfooded-acceptance-suite/11-06-SUMMARY.md
@.planning/research/PITFALLS.md
</context>

## Artifacts this phase produces

The full list is in `11-01-PLAN.md` under the same heading. This plan creates:

- `spec/process/looks-done-but-isnt-checklist.md`
- `packages/vitest/test/acceptance/pitfalls-checklist.test.ts`
- Permanent checklist item identifiers `P-01` through `P-24`

## Assumptions (flagged, from the spec-less edge probe against RUN-06)

- **ASSUMPTION-11-A (adjacency).** Two checklist items can describe the same underlying property from
  different angles (P-02 and P-11 both count emitted things; P-06 and P-23 both concern unmatched
  steps). This plan ASSUMES each keeps its own test rather than being merged, per PROH-11-04.
  **Unverified** that keeping both adds information rather than duplication — the developer may
  prefer a merge, which would then need a written note under both ids.
- **ASSUMPTION-11-B (empty).** A checklist test suite whose file fails to collect reports zero
  failures, which is indistinguishable from success by exit code alone. This plan ASSUMES plan
  11-08's coverage cross-check is where that is caught, by grepping for each `P-NN` id in the file
  rather than trusting the run. **Unverified until 11-08 lands.**
- **ASSUMPTION-11-C (ordering).** The checklist's item order is `.planning/research/PITFALLS.md`'s own
  order, and the `P-NN` ids are assigned from it. This plan ASSUMES that order is stable enough to
  become permanent ids. **Unverified**, and it is a one-way commitment once the ids are cited by a
  gate.

<tasks>

<task type="auto">
  <name>Task 1: The normative checklist document with stable ids and an Executed by column</name>
  <files>spec/process/looks-done-but-isnt-checklist.md</files>
  <reversibility rating="costly">The `P-NN` ids become citations in two gate scripts and in test titles. Renumbering afterwards touches all three.</reversibility>
  <read_first>
    - `.planning/research/PITFALLS.md` around line 1009 — the "Looks Done But Isn't Checklist" section in full, all 24 items, in order. This is the source text.
    - `.planning/research/PITFALLS.md`'s "Pitfall-to-Phase Mapping" table — each item's originating Pitfall number and the verification it prescribes.
    - `.planning/research/PITFALLS.md` Pitfall 6 around line 200 — the escape-hatch-type finding whose docs half is D-04 and lands in plan 11-09.
    - `spec/process/definitions-of-done.md` and `spec/process/requirement-id-scheme.md` — the two existing documents in this directory, for tone, heading style and how an id family is introduced.
    - `.planning/REQUIREMENTS.md`'s v2 section — RETRY-01 and LINT-01 are deferred, which is what makes two checklist items unbuildable as written in this milestone.
    - `spec/decisions/026-registration-time-tag-filtering-and-declared-tag-universe.md` — supersedes ADR-EC-020, and with it the premise of the `@only` checklist item.
    - `spec/README.md` and `spec/behaviors/index.yaml` — check whether a new `spec/process/` document needs registering anywhere; `verify-traceability.sh`'s check 1 covers `behaviors/`, not `process/`, but confirm rather than assume.
  </read_first>
  <action>
Create `spec/process/looks-done-but-isnt-checklist.md`. Open with a short preamble stating: this is the
normative in-repo copy of the checklist first written in `.planning/research/PITFALLS.md`; the ids
`P-01` through `P-24` are permanent and follow AGENTS.md section 6's rules (allocated contiguously,
never renumbered, never reused, a withdrawn item keeps its number and is marked Withdrawn); each item
is EXECUTED by a named artifact rather than cited to an existing one (D-03); and
`scripts/verify-pitfalls-checklist.sh` (plan 11-08) cross-checks this document against those artifacts,
so an item silently losing its executor fails `pnpm verify:pitfalls`.

Then a table with the columns `Id | Item | Pitfall | Executed by | Note`, carrying all 24 items in
`.planning/research/PITFALLS.md`'s own order, with the item text transcribed rather than paraphrased.
Assign the ids and executors as follows:

In-process, executed by `packages/vitest/test/acceptance/pitfalls-checklist.test.ts` — P-01 (every AST
Scenario node has a correlated Pickle), P-02 (a three-row Outline yields three distinct names), P-03 (a
zero-step Scenario does not emit a passing test), P-04 (each step carries its origin), P-05 (two
Features in one process have no colliding node ids), P-06 (an unmatched step fails and two matching
patterns fail), P-07 (two `describeFeature` calls in one module see the same custom parameter type),
P-10 (a rebuilt per-Scenario Layer), P-11 (emitted test count equals compiled Pickle count), P-12
(Scenario 2 sees a clean simulated clock after Scenario 1 advanced it), P-19 (`@cucumber/messages`
resolves from inside `packages/gherkin`), P-20 (a step with both a DocString and a data table gets both
in source order), P-23 (a skipped Scenario containing an unmatched step reports skipped, not
undefined).

CLI, executed by `scripts/verify-pitfalls-checklist.sh` (plan 11-08) — P-08 (the negative type-test
file compiles clean and fails if a directive becomes unused), P-09 (a step using `Effect.acquireRelease`
compiles), P-13 (the `@only` case), P-15 (a scratch consumer's dependency resolution), P-16 (the packed
tarball's peer range), P-17 (the README install line), P-18 (an rc-bump checklist exists and names the
acceptance suite), P-21 (a three-row Outline under shuffled sequencing), P-22 (a tag filter selects
exactly the tagged Scenarios), P-24 (a failing step names the Gherkin step and its file and line in the
panel a reader sees first).

CLI, executed by `scripts/verify-watch-rerun.sh` (plan 11-08) — P-14 (editing a `.feature` file
triggers a rerun that picks up a newly added Scenario).

Three items need a **Note** because their original wording no longer matches what this milestone
builds. Write each note in full rather than rewording the item (PROH-11-01):

- **P-10.** The item says "a RETRIED Scenario rebuilds its per-Scenario Layer". Scenario-level retry is
  RETRY-01, deferred to v2 and not built. The executable form here is the property a retry would rely
  on: the per-Scenario Layer builder runs afresh on every EXECUTION of the same Scenario Effect. Say
  that the item is executed in that reduced form, name RETRY-01 as what would complete it, and say the
  item is NOT marked withdrawn because the reduced form is a real subset rather than a substitute.
- **P-13.** The item says an `@only` Scenario under CI produces the library's error naming the
  `.feature` file. ADR-EC-026 superseded ADR-EC-020, and `@only` is now emitted as a plain tag and
  never routed to only-mode, so there is no error to produce. The executable form is the stronger
  claim the current design makes: a Feature containing an `@only` Scenario passes a run with
  only-marking forbidden, and no only-marking is emitted anywhere. Cite ADR-EC-026.
- **P-15.** The item asks for a scratch consumer on a different rc, installed under both pnpm and npm.
  The executable form on every push is the structural precondition read out of the PACKED tarballs
  rather than a live install: neither `effect` nor `vitest` appears under `dependencies` in either
  tarball, both appear under `peerDependencies` as ranges, and the workspace dependency is expanded.
  Say plainly that the live two-package-manager install is a RELEASE-time step, name
  `spec/process/rc-bump-checklist.md` (plan 11-08) as where it is written down, and say why it is not a
  per-push gate: it needs the network and a real registry install, which would make every CI run depend
  on the availability of a third party.

Close the document with a short section stating the rule that keeps it honest: an item may be marked
Withdrawn, or gain a Note, but may never be deleted, reworded to match what was easy, or marked covered
by citing a test elsewhere (PROH-11-04).
  </action>
  <verify>
    <automated>test -f spec/process/looks-done-but-isnt-checklist.md &amp;&amp; grep -oE '^\| P-[0-9]{2}' spec/process/looks-done-but-isnt-checklist.md | sort -u | wc -l | tr -d ' ' | grep -qx 24 &amp;&amp; pnpm lint &amp;&amp; pnpm verify:spec</automated>
  </verify>
  <acceptance_criteria>
    - `spec/process/looks-done-but-isnt-checklist.md` contains exactly 24 table rows whose first column is an id from `P-01` through `P-24`, contiguous with no gaps.
    - Each row's **Executed by** column names exactly one of `packages/vitest/test/acceptance/pitfalls-checklist.test.ts`, `scripts/verify-pitfalls-checklist.sh` or `scripts/verify-watch-rerun.sh`.
    - Exactly 13 rows name the in-process test file, exactly 10 name `verify-pitfalls-checklist.sh`, and exactly 1 names `verify-watch-rerun.sh`.
    - The P-10, P-13 and P-15 rows each carry a non-empty Note; the P-13 note contains `ADR-EC-026`, the P-10 note contains `RETRY-01`, and the P-15 note contains `rc-bump-checklist.md`.
    - Each item's text is transcribed from `.planning/research/PITFALLS.md`, not paraphrased; a diff of the item column against the source list differs only in formatting.
    - The document contains a closing rule forbidding deletion, convenience-rewording, and coverage-by-citation.
    - `pnpm lint` and `pnpm verify:spec` each exit 0.
  </acceptance_criteria>
  <done>The checklist is a normative repository document with permanent ids and a named executor per item, and every place its original wording no longer holds says so in writing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The thirteen in-process checklist items, each as its own fresh test</name>
  <files>packages/vitest/test/acceptance/pitfalls-checklist.test.ts</files>
  <reversibility rating="reversible">One new test file.</reversibility>
  <read_first>
    - `spec/process/looks-done-but-isnt-checklist.md` as written by Task 1 — the thirteen items this file owns and their exact wording.
    - `packages/vitest/test/emission.test.ts` lines 90-180 — the blocked, header-documented, mutation-recorded structure this file mirrors, and its import block.
    - `packages/gherkin/test/fixtures/` — the existing fixture corpus. Several items need a fixture with a specific defect (an empty `Examples:`, a zero-step Scenario, a Background under an Outline). Reuse those files by path rather than adding near-duplicates to the acceptance directory, and say in the header why an item's fixture lives in the other package's corpus.
    - `packages/gherkin/src/Model.ts` — `ParsedStep.origin`, `ParsedScenario.id`/`astId`, and `ParsedFeature.pickles`, which P-04, P-05 and P-11 read.
    - `packages/gherkin/src/StepArguments.ts` and `packages/gherkin/test/StepArguments.test.ts` — the source-order rule P-20 asserts.
    - `packages/vitest/src/ScenarioEffect.ts` — where the per-Scenario Layer is provided, which is what P-10's reduced form observes.
    - `packages/vitest/test/acceptance/negative-requirements.test.ts` as written by plan 11-06 — how a starved fixture is driven without emitting a test, which several of these items also need.
  </read_first>
  <behavior>
    - Thirteen tests, each titled with its `P-NN` id followed by the item's own wording, each asserting the item's claim directly.
    - Every one of the thirteen is written fresh against the current API rather than delegating to an existing test (PROH-11-04); the header records, per item, which pre-existing assertion it deliberately duplicates and why the duplication is intended.
    - `P-11`'s test compares the number of emitted tests to the number of compiled Pickles for one fixture Feature, derived from the same `ParsedFeature` — not from two hand-written numbers.
    - `P-19`'s test resolves `@cucumber/messages` from a module inside `packages/gherkin` and fails if the resolution throws.
  </behavior>
  <action>
Create `packages/vitest/test/acceptance/pitfalls-checklist.test.ts`. Give it a module doc comment
stating: what the file is (the in-process half of `spec/process/looks-done-but-isnt-checklist.md`);
that every test here is written FRESH rather than delegating, because D-03 chose a re-runnable suite
over a citation list, and that the duplication with existing suites is deliberate and enumerated below;
that each test title begins with its `P-NN` id because `scripts/verify-pitfalls-checklist.sh` (plan
11-08) greps for those ids to prove the checklist runs in full; and a per-item line naming the
pre-existing assertion each one duplicates, so a future reader does not "de-duplicate" the suite out of
existence.

Write one test per item, titled `P-NN — <the item's own wording>`:

- **P-01** — load a fixture whose `Examples:` block is empty and assert the named `LoadFeatureError`
  rather than a silently-missing scenario.
- **P-02** — parse a three-row Outline and assert exactly three entries whose emitted titles are
  pairwise distinct, computed from the parsed values rather than hard-coded.
- **P-03** — load a zero-step Scenario fixture and assert the named error; separately assert that no
  passing test is produced, by asserting the collection stage rather than by observing a run.
- **P-04** — parse a fixture with both a Feature Background and a Rule Background and assert each
  step's `origin` value, one assertion per distinct origin.
- **P-05** — parse two different Features in one process and assert their node id sets are disjoint.
- **P-06** — drive one fixture whose step matches zero patterns and one whose step matches two, and
  assert each fails with its own named reason. Write it fresh; do not import plan 11-06's wrapper.
- **P-07** — register a custom parameter type once as data and drive TWO `collectFeature` calls in this
  one module, asserting both resolve it with no duplicate-registration failure.
- **P-10** — build one Scenario Effect and execute it twice, asserting the per-Scenario Layer builder
  ran twice. Add an inline comment repeating Task 1's P-10 note: this is the reduced form, RETRY-01 is
  what would complete it.
- **P-11** — for one fixture Feature, assert the number of emitted test nodes equals
  `feature.pickles.length`, with both sides derived from the same parsed value.
- **P-12** — on a shared-Layer Feature, assert a second Scenario's simulated clock still starts at 0
  after the first advanced it.
- **P-19** — import `IdGenerator` from `@cucumber/messages` through a module inside `packages/gherkin`
  and assert the import resolved and the symbol is callable.
- **P-20** — parse a step carrying both a DocString and a data table and assert both reach the step's
  arguments in source order, asserting the ORDER and not only the presence of both.
- **P-23** — emit a Feature whose skipped Scenario contains an unmatched step and assert it is reported
  skipped rather than failed or undefined.

Use `assert` from `@effect/vitest` inside Effect-bodied assertions and `expect` only in plain
synchronous `it` bodies. No standalone occurrence of the escape-hatch type (PROH-11-02). Where an item
needs a fixture that already exists under `packages/gherkin/test/fixtures/`, reference it by path
rather than copying it, and say so in the header.

If an item cannot be asserted in-process against the current API — for example if emitted test nodes
are not observable from inside a test — do NOT weaken the assertion. Move that item's **Executed by**
to `scripts/verify-pitfalls-checklist.sh` in `spec/process/looks-done-but-isnt-checklist.md`, adjust
Task 1's per-executor counts in this plan's record, and note the move in both files. Plan 11-08 then
owns it.
  </action>
  <verify>
    <automated>pnpm test 2>&amp;1 | tee /tmp/11-07-t2.log; grep -q "pitfalls-checklist.test.ts" /tmp/11-07-t2.log &amp;&amp; grep -oE 'P-[0-9]{2}' packages/vitest/test/acceptance/pitfalls-checklist.test.ts | sort -u | wc -l | tr -d ' ' | grep -qx 13 &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck:test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 and reports exactly 13 passing tests from `pitfalls-checklist.test.ts`, unless an item was moved to the CLI script, in which case the count matches the document's in-process count and both files record the move.
    - Every test title begins with a `P-NN` id, and the set of ids in the file exactly matches the set of in-process ids in `spec/process/looks-done-but-isnt-checklist.md`.
    - The module doc comment names, per item, the pre-existing assertion it deliberately duplicates.
    - The `P-11` test computes both sides of its equality from the same `ParsedFeature`; no test in the file compares two hard-coded integers.
    - The `P-10` test executes one Scenario Effect twice and asserts a builder count of 2, with an inline note naming RETRY-01.
    - The `P-20` test asserts the ORDER of the two step arguments, not only that both are present.
    - `grep -c "from \"../src/index" packages/vitest/test/acceptance/pitfalls-checklist.test.ts` is 0, and no import reaches plan 11-06's wrapper.
    - `pnpm lint` and `pnpm typecheck:test` each exit 0.
  </acceptance_criteria>
  <done>Thirteen checklist items run as fresh, independently-written, id-titled tests, and the deliberate duplication with existing suites is documented so nobody removes it as redundancy.</done>
</task>

<task type="auto">
  <name>Task 3: Traceability rows for the new test files, and the mutation record</name>
  <files>spec/traceability.md, packages/vitest/test/acceptance/pitfalls-checklist.test.ts</files>
  <reversibility rating="reversible">Documentation rows plus a doc-comment record.</reversibility>
  <read_first>
    - `spec/traceability.md` section 4 (lines 146-211) — the enumerated-from-disk table and its preamble's rule that every deliberate non-obvious entry is called out. Section 4's row set is enumerated from `packages/gherkin/test/*.test.ts` and `packages/vitest/test/*.test.ts`, and the acceptance directory is a level deeper, which is itself a deliberate non-obvious fact the preamble must now state.
    - `.planning/phases/03-parameter-types-and-step-matching/03-06-SUMMARY.md`'s `node -e` cross-check that reads the test files from disk and fails naming any file absent from section 4 — the reusable guard for keeping section 4 honest.
    - `packages/vitest/test/acceptance/README.md`'s standing mutation-record sentence.
  </read_first>
  <action>
Extend `spec/traceability.md` section 4 with rows for every test file this phase has added under
`packages/vitest/test/acceptance/`: the five `*.steps.test.ts` acceptance modules, `negative-requirements.test.ts`
and `pitfalls-checklist.test.ts`. Update section 4's preamble to state that its rows are now enumerated
from `packages/gherkin/test/*.test.ts`, `packages/vitest/test/*.test.ts` AND
`packages/vitest/test/acceptance/**/*.test.ts`, and that the acceptance directory's `.feature` files and
its `negative/` fixtures are deliberately NOT rows here — they are traced by section 5 instead, exactly
as `packages/vitest/test/tsgo-gate/` is traced by `scripts/verify-tsgo-gate.sh` rather than by this
table. Reuse the 03-06 `node -e` cross-check idea to confirm no file is missing, and record the result.

Then perform, record and REVERT these mutations, writing the results into
`pitfalls-checklist.test.ts`'s module doc comment:

- **Mutation A.** Delete one `P-NN` id from a test title, leaving the test itself intact. Expected:
  `pnpm test` stays GREEN with the same pass count. Record it — this is what plan 11-08's coverage
  cross-check exists to catch, and it is the reason the ids are in the titles rather than only in the
  document.
- **Mutation B.** Change `P-11`'s assertion to compare two hard-coded integers instead of two derived
  values. Expected: the test stays GREEN. Record it as the reason the derived form is required.
- **Mutation C.** Break the library so a Background step's `origin` is not set, or simulate it by
  reading a different field. Expected: `P-04` FAILS. Record which other tests in this file stayed
  green, because that is the measure of how much the checklist actually separates.
- **Mutation D.** Provide the per-Scenario Layer OUTSIDE the retried Effect for `P-10`. Expected: the
  builder count assertion FAILS with a count of 1. If it cannot be arranged without changing library
  source, record that fact instead of skipping the mutation, and say what would be needed.
  </action>
  <verify>
    <automated>pnpm test &amp;&amp; pnpm verify:spec &amp;&amp; pnpm lint &amp;&amp; grep -qi "mutation" packages/vitest/test/acceptance/pitfalls-checklist.test.ts &amp;&amp; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `spec/traceability.md` section 4 contains one row per test file under `packages/vitest/test/acceptance/`, and its preamble names the three enumeration globs and states why the `.feature` files and the `negative/` fixtures are not rows.
    - A `node -e` cross-check reading the three globs from disk reports zero files absent from section 4, and its result is recorded in the plan summary.
    - The module doc comment of `pitfalls-checklist.test.ts` records four mutations, A through D, each naming what went red and what stayed green.
    - Mutation A's entry explicitly states that the suite stayed green with an id removed, and names plan 11-08's cross-check as the mechanism that catches it.
    - Mutation D's entry either records the failing count or records why the mutation could not be arranged and what would be needed.
    - `git status --porcelain` shows no mutation left in the working tree.
    - `pnpm test`, `pnpm verify:spec` and `pnpm lint` each exit 0.
  </acceptance_criteria>
  <done>Every new acceptance test file is traced in section 4, the enumeration rule is updated to reach a directory deeper, and the checklist suite's own weak spots are recorded rather than assumed away.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/research/PITFALLS.md` -> `spec/process/looks-done-but-isnt-checklist.md` | A transcription boundary; a paraphrase here silently changes what the project claims to have checked. |
| checklist item text -> the assertion that executes it | An assertion weaker than its item makes the checklist a claim rather than a check. |
| `P-NN` ids in test titles -> the coverage cross-check | The ids are the only link between the document and the running suite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07-01 | Spoofing | an item marked covered by citing an existing test | high | mitigate | PROH-11-04 plus the acceptance criterion that the id set in the test file exactly matches the document's in-process id set, and the header naming the deliberate duplication per item. |
| T-11-07-02 | Tampering | an item reworded to match what was easy to assert | high | mitigate | Items are transcribed and only ever gain a Note; the criterion is a diff of the item column against the source list differing only in formatting. |
| T-11-07-03 | Repudiation | a test losing its `P-NN` id and going uncounted | medium | mitigate | Mutation A records that the suite stays green; plan 11-08's cross-check is the mechanism, and this plan's record names it. |
| T-11-07-04 | Spoofing | an assertion comparing two hard-coded values | medium | mitigate | Mutation B records that the hard-coded form stays green; the criterion requires both sides of `P-11` to be derived from one parsed value. |
</threat_model>

<verification>
- `pnpm test` exits 0 with the checklist suite's pass count matching the document's in-process item count.
- `spec/process/looks-done-but-isnt-checklist.md` has exactly 24 contiguous ids, each with exactly one named executor.
- `pnpm verify:spec`, `pnpm lint`, `pnpm typecheck:test`, `pnpm verify:acceptance-ref-state` and `pnpm verify:acceptance-no-any` all exit 0.
- Four mutations recorded and reverted; the working tree is clean.
</verification>

<success_criteria>
- The 24-item checklist is a normative repository document with permanent ids, a named executor per item, and a written note wherever its original wording no longer holds.
- Thirteen items run as fresh in-process tests, and the deliberate duplication with existing suites is documented rather than left to be discovered and removed.
- Section 4 is extended to cover the new acceptance test files with its enumeration rule stated.
</success_criteria>

<output>
Create `.planning/phases/11-composition-root-and-dogfooded-acceptance-suite/11-07-SUMMARY.md` when done.
</output>
