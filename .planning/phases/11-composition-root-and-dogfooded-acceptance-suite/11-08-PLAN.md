---
phase: 11-composition-root-and-dogfooded-acceptance-suite
plan: 08
type: execute
wave: 8
depends_on: ["11-07"]
files_modified:
  - scripts/verify-watch-rerun.sh
  - scripts/verify-pitfalls-checklist.sh
  - spec/process/rc-bump-checklist.md
  - spec/process/looks-done-but-isnt-checklist.md
  - package.json
  - .github/workflows/check.yml
autonomous: true
requirements: [RUN-06]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Editing a `.feature` file under a watching runner triggers a rerun that picks up a newly added Scenario, proved by a script that drives the real runner and reads its structured report, never its terminal glyphs."
    - "The eleven checklist items with no honest in-process form are executed by `scripts/verify-pitfalls-checklist.sh`, each assertion echoing its `P-NN` id."
    - "A coverage cross-check proves the checklist runs in FULL: every id from `P-01` to `P-24` appears exactly once in `spec/process/looks-done-but-isnt-checklist.md` and is present in the artifact that document names as its executor."
    - "An rc-bump checklist exists in the repository and names the acceptance suite as the gate, which is checklist item P-18 itself."
    - "Both new gates run in CI as root `package.json` scripts that also run locally, and neither mutates a committed file."
  artifacts:
    - scripts/verify-watch-rerun.sh
    - scripts/verify-pitfalls-checklist.sh
    - spec/process/rc-bump-checklist.md
    - "root package.json scripts verify:watch-rerun and verify:pitfalls"
    - ".github/workflows/check.yml steps for both gates"
  key_links:
    - "`spec/process/looks-done-but-isnt-checklist.md` -> `scripts/verify-pitfalls-checklist.sh`'s cross-check -> `pitfalls-checklist.test.ts` and the two scripts: a checklist item that loses its executor fails `pnpm verify:pitfalls`."
    - "`scripts/verify-watch-rerun.sh` -> a fixture copied into a temp directory: the one gate in this phase that mutates a file must never mutate a committed one."
    - "`scripts/verify-pitfalls-checklist.sh` -> `pnpm verify:pack`'s unpacked tarballs: P-15 and P-16 read the PACKED manifest, never the source one."
  prohibitions:
    - id: PROH-11-04
      statement: "A checklist item must never be marked covered by citing an existing test elsewhere in the repository. Every item gets its own executing artifact even where equivalent coverage already exists."
      source: planner-prohibition-probe
    - id: PROH-11-05
      statement: "A gate script must never leave the working tree dirty. The watch gate is the one gate in this phase that edits a file, and it edits a copy inside a temporary directory removed by a trap, never a committed fixture."
      source: planner-prohibition-probe
---

<!-- planner-discipline-allow: any -->

<objective>
Build the CLI half of the checklist's execution: `scripts/verify-watch-rerun.sh` for P-14, and
`scripts/verify-pitfalls-checklist.sh` for the remaining ten CLI items plus the coverage cross-check
that makes "runs in full" mechanical. Write `spec/process/rc-bump-checklist.md`, which is item P-18's
own subject. Wire both scripts into `package.json` and `.github/workflows/check.yml`.

Purpose: roadmap success criterion 4 says the checklist "runs in full and passes", and D-03 chose the
most feature-rich reading of that — a dedicated executing artifact per item, including the watch-mode
item D-03 explicitly asks to automate rather than leave manual. Without a cross-check, "in full" is a
reader's assertion — a test can lose its id, a script assertion can be
deleted, and both leave a green run. This plan makes the completeness claim a counted one.

Output: two gate scripts, the rc-bump checklist document, and their CI steps.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-composition-root-and-dogfooded-acceptance-suite/11-CONTEXT.md
@.planning/phases/11-composition-root-and-dogfooded-acceptance-suite/11-PATTERNS.md
@.planning/phases/11-composition-root-and-dogfooded-acceptance-suite/11-07-SUMMARY.md
@spec/process/looks-done-but-isnt-checklist.md
</context>

## Artifacts this phase produces

The full list is in `11-01-PLAN.md` under the same heading. This plan creates:

- `scripts/verify-watch-rerun.sh` and `scripts/verify-pitfalls-checklist.sh`
- `spec/process/rc-bump-checklist.md`
- Root `package.json` scripts `verify:watch-rerun` and `verify:pitfalls`
- Two new steps in `.github/workflows/check.yml`

## Assumptions (flagged, from the spec-less edge probe against RUN-06)

- **ASSUMPTION-11-B (empty / single-element).** The cross-check derives its expected id set from
  `spec/process/looks-done-but-isnt-checklist.md`. If that document fails to parse — a changed table
  format, a moved file — the derived set is EMPTY and every downstream assertion is vacuously true.
  This plan mitigates it with an explicit expected-count constant and a parse control, but the
  mitigation ASSUMES the document's table format stays machine-readable. **Unverified as a standing
  guarantee.**
- **ASSUMPTION-11-C (ordering).** The watch gate depends on the runner emitting a rerun report AFTER
  the edit rather than racing it. This plan ASSUMES a bounded poll on the report file is sufficient
  and does not assume any particular latency. **Unverified**; Task 1 must record the observed timing
  and the timeout it settled on.
- **ASSUMPTION-11-A (adjacency).** Two checklist items can be executed by assertions that overlap
  (P-21's shuffled Outline run and P-22's tag-filtered run both drive the runner over the acceptance
  suite). This plan ASSUMES each keeps its own invocation and its own echoed id rather than sharing
  one run. **Unverified** that the separation earns its runtime cost.

<tasks>

<task type="auto">
  <name>Task 1: The watch-mode gate (P-14)</name>
  <files>scripts/verify-watch-rerun.sh, package.json</files>
  <precondition>`node_modules/.bin/vitest` exists, because the gate drives the repo-local runner rather than a global one.</precondition>
  <reversibility rating="reversible">One new script and one script entry.</reversibility>
  <read_first>
    - `scripts/verify-tags-filter.sh` in full — the only gate that drives the real runner from outside and parses its report. Its `VITEST` repo-local binary constant, its `run_vitest` helper, its `report_query` `node -e` reader that treats the report as structured data rather than grepping glyphs, its vacuity control that fails when the unfiltered run reported zero results, its exact-title precondition anchored at end of line, and its `mktemp -d` plus `trap` temp-directory handling.
    - `scripts/verify-tsgo-gate.sh` lines 139-201 — the rationale for a committed pair over a script that edits a file, which is exactly why this gate must edit a COPY.
    - `packages/vitest/test/acceptance/worked-example-01-apples.feature` and its `.steps.test.ts` — the smallest acceptance pair, and the natural subject for the watch probe.
    - `vitest.config.ts` — note (c)'s prohibition on touching the include/exclude globs, which constrains how a temp-directory copy can be made discoverable.
    - `spec/process/looks-done-but-isnt-checklist.md`'s P-14 row.
  </read_first>
  <action>
Write `scripts/verify-watch-rerun.sh`, executing checklist item P-14: editing a `.feature` file
triggers a rerun and picks up a newly added Scenario.

Open with the repo's standard preamble and a METHOD NOTE saying what a green `pnpm test` does not
prove: a one-shot run cannot observe whether the runner watches `.feature` files at all, and a
consumer's dev loop is the first place a non-watched fixture is discovered. Also state the constraint
that shapes the whole script: this is the ONLY gate in this repository that mutates a file, so it
mutates a COPY inside a temporary directory removed by a trap, never a committed fixture. Cite
`verify-tsgo-gate.sh`'s "no mutable working tree, no cleanup path that can leave the repo dirty"
rationale as the rule it is honouring rather than breaking.

Mechanics, following `verify-tags-filter.sh`'s idiom throughout:

- `set -euo pipefail`, `ROOT_DIR` from `BASH_SOURCE`, path constants spelled out in full, a `fail()`
  helper naming the likely cause and the file to look at, and a closing ENFORCED line.
- `TMP_DIR="$(mktemp -d)"` with `trap 'rm -rf "$TMP_DIR"' EXIT` registered before anything is written.
- Copy the smallest acceptance pair — its `.feature` file and its `.steps.test.ts` — into a working
  location the runner will discover, and drive the repo-local `node_modules/.bin/vitest` in watch mode
  against that copy alone, with `--reporter=json` and `--outputFile` pointing inside `$TMP_DIR`.
  Decide and document how the copy is made discoverable without touching `vitest.config.ts`'s globs:
  either place the copy inside the repository under a path the default glob already reaches and delete
  it in the trap, or pass the copy's path as a positional filter. Whichever is chosen, the script must
  leave `git status --porcelain` clean when it exits, on both the success and the failure path.
- Assertion 1, the vacuity control: the FIRST run must report a non-zero total. A zero-total first run
  means the copy did not collect, against which every later assertion is trivially true. Fail naming
  the log.
- Assertion 2, the precondition: the newly-added Scenario's title must be ABSENT from the first run's
  results, anchored on an exact title match rather than a containment grep, following
  `verify-tags-filter.sh`'s exact-title pattern.
- Then append one new Scenario to the COPY of the `.feature` file, and append its step definition to
  the copy of the `.steps.test.ts` if the new Scenario needs one that is not already registered.
  Prefer a new Scenario reusing an already-registered step pattern, so only the `.feature` file is
  edited — that is the sharper form of the item, because it isolates "the runner watches `.feature`
  files" from "the runner watches TypeScript files", which it certainly does.
- Assertion 3, the claim: within a bounded poll, a SECOND report appears in which the new Scenario's
  title is present and passing. Poll the report file rather than sleeping a fixed interval, cap the
  wait, and on timeout fail with a message saying the runner did not rerun and naming the log.
- Assertion 4, the anti-vacuity partner: the second run's total must be strictly greater than the
  first's. A rerun that reported the same set proves nothing about picking up a new Scenario.
- Stop the watching runner cleanly on every exit path, including the timeout path, so the script never
  leaves a background process behind.

Record in the METHOD NOTE the observed rerun latency and the timeout the script settled on
(ASSUMPTION-11-C), and say that the timeout is a bound rather than a measurement of correctness.

Add `"verify:watch-rerun": "bash scripts/verify-watch-rerun.sh"` to the root `package.json` `scripts`
block.
  </action>
  <verify>
    <automated>pnpm verify:watch-rerun &amp;&amp; git status --porcelain | tee /tmp/11-08-t1-status.log &amp;&amp; test ! -s /tmp/11-08-t1-status.log &amp;&amp; node -e "process.exit(require('./package.json').scripts['verify:watch-rerun']?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-watch-rerun.sh` exits 0 and prints an ENFORCED line.
    - `git status --porcelain` is EMPTY immediately after a successful run and immediately after a deliberately failed run.
    - The script contains `mktemp -d` and a `trap` removing it, registered before any file is written.
    - The script contains a vacuity control failing when the first run reports zero total results, and an assertion that the second run's total is strictly greater than the first's.
    - The script reads the runner's JSON report through a `node -e` reader and contains no grep over reporter glyphs.
    - The script drives `node_modules/.bin/vitest`, never a bare `vitest`.
    - The METHOD NOTE records the observed rerun latency and the chosen timeout.
    - `node -e` confirms `verify:watch-rerun` exists in the root `package.json` `scripts` block.
  </acceptance_criteria>
  <done>The watch-mode item runs as a real gate that drives the real runner, edits only a copy, restores the tree on every path, and cannot pass by observing nothing.</done>
</task>

<task type="auto">
  <name>Task 2: The CLI checklist gate, the coverage cross-check, and the rc-bump checklist</name>
  <files>scripts/verify-pitfalls-checklist.sh, spec/process/rc-bump-checklist.md, spec/process/looks-done-but-isnt-checklist.md, package.json</files>
  <reversibility rating="costly">The cross-check makes the 24 `P-NN` ids a hard contract across three artifacts; a later phase adding or withdrawing an item must update the document and the executor in the same commit.</reversibility>
  <read_first>
    - `spec/process/looks-done-but-isnt-checklist.md` as written by plan 11-07 — the ten CLI item ids this script owns, their wording, and the P-13 and P-15 notes that define what those two are executed as.
    - `scripts/verify-pack.sh` — how the packed tarballs are produced and unpacked, and what it already asserts about `peerDependencies`. P-15 and P-16 read the PACKED manifest, and this script must not re-implement the packing.
    - `scripts/verify-tsgo-gate.sh` — the isolated-tsconfig-per-case invocation used by P-08 and P-09, and its two-check rule.
    - `scripts/verify-tags-filter.sh` — the `--tagsFilter` invocation and report reader P-22 needs, and the shuffled-sequence flag form P-21 needs.
    - `README.md` and `packages/vitest/README.md` install lines — P-17's subject, including the rule that `packages/gherkin/README.md` deliberately names neither dependency.
    - `.planning/research/PITFALLS.md` Pitfall 18 and Pitfall 31 — the rc-bump and failure-output items' full findings.
    - `package.json`'s `scripts` block and `.github/workflows/check.yml`'s `package` and `test` jobs.
  </read_first>
  <action>
Write `spec/process/rc-bump-checklist.md` first, because item P-18's assertion is about its existence
and content. It is a short, ordered procedure for bumping the pinned `effect` and `@effect/vitest` rc
in `pnpm-workspace.yaml`. It must state, each as its own step: that the acceptance suite under
`packages/vitest/test/acceptance/` is the GATE for the bump, not `tsc` — Pitfall 18's finding is that an
rc changelog's patch heading does not narrow what broke; that the two catalogs are separate and only
`pnpm-workspace.yaml` is edited; that `pnpm verify:pack` must be re-run because a catalog reference
expands at pack time; that the peer ranges mirror `@effect/vitest`'s own published peers and nothing
enforces that they stay in sync, so they are checked by hand; and that the live scratch-consumer
install under BOTH pnpm and npm — checklist item P-15's full form — is performed here, at release time,
naming the exact commands and the expected single-version result. Cross-reference
`spec/process/looks-done-but-isnt-checklist.md`'s P-15 and P-18 rows so the two documents point at each
other.

Then write `scripts/verify-pitfalls-checklist.sh`, executing the ten CLI checklist items plus the
coverage cross-check. Follow the repo's gate idiom throughout: standard preamble, METHOD NOTE,
`set -euo pipefail`, `ROOT_DIR`, spelled-out path constants, a `fail()` helper naming the likely cause
and the file to look at, a closing ENFORCED line, and every assertion echoing its `P-NN` id so the
script's own output is the evidence the item ran.

The METHOD NOTE states what a green `pnpm test` does not prove for this script's items — a packed
tarball's manifest, a README's install line, a compile-gate's directive-unused behavior, a shuffled
run, and a failure panel's contents are all invisible to an in-process suite — and states the
completeness argument: without the cross-check, "runs in full" is a claim, because a deleted assertion
and a renamed test both leave a green run.

The ten item assertions:

- **P-08.** Invoke the type-checker against the negative type-test file's isolated config and assert it
  exits 0; then, in a temp copy, remove the condition that makes one `@ts-expect-error` directive
  necessary and assert the check now FAILS on an unused directive. Two checks, not one.
- **P-09.** Invoke the type-checker against the fixture whose step uses `Effect.acquireRelease` and
  assert it exits 0, with a positive control that the config really compiled that file rather than an
  empty set.
- **P-13.** Drive the runner over the acceptance Feature containing an `@only`-tagged Scenario with
  only-marking forbidden, and assert the run passes AND that no result is marked as only. Cite
  ADR-EC-026 in the assertion's comment, per the P-13 note.
- **P-15.** Read the PACKED tarballs produced by the existing packing gate and assert neither `effect`
  nor `vitest` appears under `dependencies` in either manifest, that both appear under
  `peerDependencies`, and that the workspace dependency is expanded rather than left as a workspace
  protocol. Print a line naming `spec/process/rc-bump-checklist.md` as where the live two-package-manager
  install is performed, per the P-15 note.
- **P-16.** Assert the packed `peerDependencies.effect` value is a range and not an exact pin and not an
  unexpanded catalog reference. If the existing packing gate already asserts exactly this, still write
  the assertion here rather than delegating (PROH-11-04), and say in a comment that the duplication is
  deliberate.
- **P-17.** Assert the install lines in the root README and in the vitest package README each carry the
  release tag on both dependencies, and assert the gherkin package README names neither — that last
  assertion is what stops a well-meaning edit from adding a dependency line to a package that declares
  no such dependency.
- **P-18.** Assert `spec/process/rc-bump-checklist.md` exists and contains a sentence naming the
  acceptance suite as the gate.
- **P-21.** Drive the runner over the acceptance Feature containing the three-row Outline with
  shuffled sequencing enabled, and assert all three row tests pass. This is Pitfall 34's regression
  class under the ordering that would expose shared mutable state.
- **P-22.** Drive the runner with a tag filter selecting the tag the acceptance suite carries on exactly
  one Scenario, and assert the selected set is exactly that Scenario — both that it ran and that the
  others did not.
- **P-24.** Drive a deliberately failing step and assert the failure output names the Gherkin step text
  and the `.feature` file with a line number. Produce the failure from a temp copy, never from a
  committed acceptance fixture, so the suite stays green.

Then the coverage cross-check, as the script's final assertions:

- Parse `spec/process/looks-done-but-isnt-checklist.md`'s table and collect the id and executor of every
  row.
- Assert the parse produced a non-zero number of rows, and that the number equals a named constant
  written once in this script. This is ASSUMPTION-11-B's mitigation: a format change that breaks the
  parse must fail loudly rather than produce an empty set.
- Assert the ids are exactly `P-01` through `P-24`, contiguous, each occurring once.
- For every row whose executor is the in-process test file, assert that id appears in that file. For
  every row whose executor is a script, assert that id appears in that script. Fail naming any id whose
  executor does not mention it.
- Report the completeness result in the form roadmap success criterion 4 asks for, so the script's
  output is the evidence.

Add `"verify:pitfalls": "bash scripts/verify-pitfalls-checklist.sh"` to the root `package.json`
`scripts` block. If any item's honest executor turned out to differ from plan 11-07's assignment,
update `spec/process/looks-done-but-isnt-checklist.md`'s **Executed by** column in this same commit —
the cross-check is what makes that non-optional.
  </action>
  <verify>
    <automated>pnpm verify:pitfalls 2>&amp;1 | tee /tmp/11-08-t2.log; grep -c 'P-[0-9][0-9]' /tmp/11-08-t2.log &amp;&amp; test -f spec/process/rc-bump-checklist.md &amp;&amp; node -e "process.exit(require('./package.json').scripts['verify:pitfalls']?0:1)" &amp;&amp; git status --porcelain | tee /tmp/11-08-t2-status.log &amp;&amp; test ! -s /tmp/11-08-t2-status.log</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-pitfalls-checklist.sh` exits 0, prints an ENFORCED line, and its output contains all 24 ids `P-01` through `P-24`.
    - The script contains a named constant holding the expected checklist row count, written exactly once, and an assertion that fails when the document's table fails to parse.
    - The cross-check fails when an id is present in the document but absent from the artifact the document names as its executor.
    - `spec/process/rc-bump-checklist.md` exists, names `packages/vitest/test/acceptance/` as the bump gate, names `pnpm-workspace.yaml` as the only file edited, and describes the live scratch-consumer install under both package managers.
    - `spec/process/looks-done-but-isnt-checklist.md`'s P-15 and P-18 rows cross-reference `rc-bump-checklist.md`, and that document cross-references them back.
    - Every one of the ten CLI item assertions echoes its own `P-NN` id.
    - `git status --porcelain` is empty after a successful run and after a deliberately failed run; P-24's failing step is produced from a temp copy.
    - `node -e` confirms `verify:pitfalls` exists in the root `package.json` `scripts` block.
  </acceptance_criteria>
  <done>The eleven CLI checklist items execute for real, the rc-bump checklist exists and is itself checked, and the completeness of the 24-item checklist is a counted assertion rather than a reader's claim.</done>
</task>

<task type="auto">
  <name>Task 3: CI wiring, and mutation proofs for both gates</name>
  <files>.github/workflows/check.yml, scripts/verify-watch-rerun.sh, scripts/verify-pitfalls-checklist.sh</files>
  <reversibility rating="reversible">Two CI steps plus recorded mutation notes inside the scripts.</reversibility>
  <read_first>
    - `.github/workflows/check.yml` in full — the four jobs, the house comment style on each gate step (what a green `pnpm test` does not prove, plus the script name), the `if: matrix.node-version == 24` guard on the two runner-driving gates and its stated cost reasoning, and the ordering rule that a narrow gate runs AFTER `pnpm test`.
    - `.planning/STATE.md`'s Phase 01-06 constraints: every CI step must be a root `package.json` script that also runs locally, no inline command in a workflow, and adding a gate script means adding a CI step for it.
    - `scripts/verify-tags-filter.sh` lines 32-42 — the recorded mutation format for a gate script.
    - Both scripts as written by Tasks 1 and 2.
  </read_first>
  <action>
Add both gates to `.github/workflows/check.yml`. Place `verify:watch-rerun` and `verify:pitfalls` in
the `test` job, AFTER `pnpm test` and after the two existing runner-driving gates, each guarded with
`if: matrix.node-version == 24` and each preceded by a comment in the established house style that
says what a green `pnpm test` does not prove, names the script, and states the Node-24-only reasoning
in the same terms the existing two use (a structural claim that does not vary by Node version, against
a gate that invokes the runner more than once). Both must be root `package.json` scripts; add no inline
command.

Then perform, record and REVERT these mutations, writing the results into each script's METHOD NOTE.

Against `scripts/verify-watch-rerun.sh`:

- **Mutation A.** Make the appended Scenario reuse a step pattern that is NOT registered, so it would
  fail if it ran. Expected: the gate FAILS on assertion 3's pass requirement rather than passing on the
  mere presence of a new result. Record whether the assertion checked presence or status, because
  presence alone is the weaker form.
- **Mutation B.** Skip the file edit entirely and let the poll run to its timeout. Expected: the gate
  FAILS on timeout with the message naming the log, not a hang and not a silent pass.
- **Mutation C.** Remove the `trap` and force a failure mid-script. Expected: the temp directory
  survives. Record it as the reason the trap is registered before the first write, per PROH-11-05.

Against `scripts/verify-pitfalls-checklist.sh`:

- **Mutation D.** Delete one `P-NN` id from the in-process test file's titles, leaving the test intact.
  Expected: `pnpm test` stays GREEN and `pnpm verify:pitfalls` FAILS naming that id. This is the
  mutation plan 11-07 Task 3 recorded as uncatchable at that point, now caught.
- **Mutation E.** Change one row's **Executed by** column in
  `spec/process/looks-done-but-isnt-checklist.md` to name the other script. Expected: the cross-check
  FAILS naming that id, because the newly-named script does not mention it.
- **Mutation F.** Break the document's table format so the parse yields zero rows. Expected: the
  row-count control FAILS rather than the id assertions passing vacuously. This is ASSUMPTION-11-B's
  mitigation, proven.
- **Mutation G.** Weaken P-22's assertion to check only that the filtered run passed, dropping the
  claim that the OTHER Scenarios did not run. Expected: it still PASSES with a filter that selects
  everything. Record it — the sharp form must not be simplified back into the blunt one.
  </action>
  <verify>
    <automated>pnpm verify:watch-rerun &amp;&amp; pnpm verify:pitfalls &amp;&amp; grep -c 'verify:watch-rerun' .github/workflows/check.yml &amp;&amp; grep -c 'verify:pitfalls' .github/workflows/check.yml &amp;&amp; pnpm test &amp;&amp; git status --porcelain | tee /tmp/11-08-t3.log &amp;&amp; test ! -s /tmp/11-08-t3.log</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/check.yml` contains one step running each of the two new scripts, both in the `test` job, both after `pnpm test`, both guarded on Node 24, each with a preceding comment naming the script.
    - Every command in the two new steps is a root `package.json` script; a cross-check confirms no inline command exists in any workflow step.
    - Each script's METHOD NOTE records its own mutations by letter, each naming what went red and what stayed green.
    - Mutation D's record explicitly states that `pnpm test` stayed green while `pnpm verify:pitfalls` failed.
    - Mutation F's record explicitly states that the id assertions would have passed vacuously without the row-count control.
    - Mutation G's record explicitly states that the weakened form stayed green, and instructs against simplifying it back.
    - `git status --porcelain` is empty; no mutation and no temp artifact remains.
    - `pnpm verify:watch-rerun`, `pnpm verify:pitfalls` and `pnpm test` each exit 0.
  </acceptance_criteria>
  <done>Both gates run on every push, and each is proven non-vacuous against a weakened assertion, a missing edit, a broken parse and a lost id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the working tree -> the watch gate's file edit | The one gate in this phase that mutates a file; a mutation escaping into the committed tree is a real repository-integrity failure. |
| `spec/process/looks-done-but-isnt-checklist.md` -> the cross-check's parse | The completeness claim is derived entirely from parsing one document; a parse failure yields an empty set. |
| a spawned watching runner process -> the CI job | A background process left behind hangs or pollutes the job. |
| the packed tarballs -> P-15 and P-16 | The manifest read must be the PACKED one; reading the source manifest is vacuous, as STATE.md's Phase 01-04 lesson records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-08-01 | Tampering | the watch gate editing a committed fixture | high | mitigate | PROH-11-05: the edit is applied to a copy inside a `mktemp -d` directory with a `trap` registered before the first write; asserted by a clean `git status --porcelain` on both the success and the failure path, and mutation C records what happens without the trap. |
| T-11-08-02 | Denial of service | a watching runner left running | medium | mitigate | The script stops the runner on every exit path including the timeout path; mutation B exercises the timeout path. |
| T-11-08-03 | Spoofing | a vacuous completeness claim from an empty parse | high | mitigate | A named expected-row-count constant plus a parse control; mutation F proves the id assertions pass vacuously without it. |
| T-11-08-04 | Spoofing | reading the source manifest instead of the packed one | high | mitigate | P-15 and P-16 read the unpacked tarballs produced by the existing packing gate; a comment cites STATE.md's Phase 01-04 finding that a source-manifest assertion reads identically in the passing and failing cases. |
| T-11-08-05 | Repudiation | a checklist item losing its executor silently | high | mitigate | The cross-check asserts each id appears in the artifact its row names; mutations D and E prove both loss modes are caught while `pnpm test` stays green. |
| T-11-08-06 | Denial of service | a gate that exists but never runs | medium | mitigate | Both scripts are wired into `.github/workflows/check.yml` as root `package.json` scripts, per STATE.md's Phase 01-06 rule. |
</threat_model>

<verification>
- `pnpm verify:watch-rerun` and `pnpm verify:pitfalls` each exit 0 and print an ENFORCED line, and `verify:pitfalls`' output names all 24 ids.
- `git status --porcelain` is empty after each gate, on both the success and the failure path.
- `pnpm test`, `pnpm verify:spec`, `pnpm lint`, `pnpm typecheck:test`, `pnpm build` and every pre-existing `verify:*` script all exit 0.
- Seven mutations recorded and reverted; the working tree is clean.
</verification>

<success_criteria>
- The 24-item checklist runs in full: thirteen in-process tests, ten CLI assertions and one watch gate, with a cross-check proving no item lost its executor.
- An rc-bump checklist exists in the repository and names the acceptance suite as the gate, which is one of the items being checked.
- Both gates run on every push and neither can leave the working tree dirty.
</success_criteria>

<output>
Create `.planning/phases/11-composition-root-and-dogfooded-acceptance-suite/11-08-SUMMARY.md` when done.
</output>
