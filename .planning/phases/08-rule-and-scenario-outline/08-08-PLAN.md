---
phase: 08-rule-and-scenario-outline
plan: 08
type: execute
wave: 5
depends_on: ["08-07"]
files_modified:
  - spec/behaviors/03-rules-outlines-and-testclock.md
  - spec/invariants.md
  - spec/decisions/010-rule-and-scenario-scoped-extra-layers.md
  - spec/traceability.md
  - spec/roadmap.md
  - README.md
  - packages/vitest/README.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DSL-05, DSL-06]

must_haves:
  truths:
    - "BEH-EC-018 states, as normative REQUIREMENT text, every Phase 8 decision (D-01 through D-04) and the D-03 titling format"
    - "INV-EC-005 names a real source module and real tests instead of a planned mechanism"
    - "Every roadmap Phase 8 success criterion (1-4) is traceable to a named, real automated assertion"
    - "DSL-05 and DSL-06 are Complete in REQUIREMENTS.md, each backed by a named automated assertion"
  artifacts:
    - path: "spec/behaviors/03-rules-outlines-and-testclock.md"
      provides: "BEH-EC-018 — Rule/Scenario registration, hook ordering, Rule Background, and Outline row titling"
      contains: "BEH-EC-018"
    - path: "spec/traceability.md"
      provides: "§1, §2, §3 and §4 rows for Phase 8"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DSL-05 and DSL-06 marked Complete"
  key_links:
    - from: "spec/behaviors/03-rules-outlines-and-testclock.md"
      to: "spec/behaviors/index.yaml"
      via: "the disk→index registration verify-traceability.sh check 1 enforces"
      pattern: "BEH-EC-018"
    - from: "spec/traceability.md"
      to: "packages/vitest/test/OutlineTitle.test.ts"
      via: "§4 is enumerated from disk"
      pattern: "OutlineTitle.test.ts"
---

<objective>
Reconcile the specification with what Phase 8 actually built, and mark DSL-05 and DSL-06 Complete.

Purpose: `spec/behaviors/03-rules-outlines-and-testclock.md`'s BEH-EC-009/BEH-EC-010 already state the
Rule-extends-Layer and Outline-typed-for-free REQUIREMENTs correctly (verified against this phase's
actual implementation — no correction needed there), but nothing in `spec/` yet states the REGISTRATION
mechanics as built: both extra-Layer forms (D-01), Feature-then-Rule/Rule-then-Feature hook ordering
(D-02), Outline row titling (D-03), or Rule-level Background (D-04) — the same gap Phase 7's BEH-EC-017
closed for hook ordering after ADR-EC-005 alone had documented only the mechanism decision.
`spec/invariants.md`'s INV-EC-005 is still marked "Source (planned)". Following Phase 3/4/7's
established pattern: correct in place with a dated blockquote where something published turns out
wrong, add new normative text where something was simply unwritten, never rewrite a superseded
sentence.
Output: BEH-EC-018, INV-EC-005's real source, traceability rows, three status documents, and the
requirement markings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-rule-and-scenario-outline/08-CONTEXT.md
@.planning/phases/08-rule-and-scenario-outline/08-PATTERNS.md
@spec/behaviors/03-rules-outlines-and-testclock.md
@spec/behaviors/index.yaml
@spec/invariants.md
@spec/traceability.md
@spec/decisions/010-rule-and-scenario-scoped-extra-layers.md
@AGENTS.md
@.planning/phases/08-rule-and-scenario-outline/08-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BEH-EC-018 and the INV-EC-005 / ADR-EC-010 corrections</name>
  <files>spec/behaviors/03-rules-outlines-and-testclock.md, spec/invariants.md, spec/decisions/010-rule-and-scenario-scoped-extra-layers.md</files>
  <read_first>
    - spec/behaviors/03-rules-outlines-and-testclock.md (read whole — BEH-EC-009/010/011/012's exact section shape, and the file's own footer)
    - spec/behaviors/index.yaml (the entry shape: id, file, title, id_range — this file's `id_range` must widen to include 018, it does NOT need a new file entry)
    - spec/invariants.md INV-EC-005 (its "Source (planned)" line)
    - spec/decisions/010-rule-and-scenario-scoped-extra-layers.md (read whole — the Decision and Consequences this phase implemented)
    - spec/behaviors/07-hook-ordering-and-guarantees.md (the dated-correction-blockquote and REQUIREMENT-block form to copy)
    - AGENTS.md §2 (specification code fences) and §6 (identifier scheme: ids are contiguous, never renumbered, never reused)
    - packages/vitest/src/Dsl.ts, packages/vitest/src/describeFeature.ts, packages/vitest/src/Hook.ts, packages/vitest/src/OutlineTitle.ts (the real behavior BEH-EC-018 must describe)
  </read_first>
  <action>
    Add a `BEH-EC-018` section to the EXISTING `spec/behaviors/03-rules-outlines-and-testclock.md` —
    this phase's content is topically this file's own subject, so no new file is created; widen this
    file's `id_range` entry in `spec/behaviors/index.yaml` to cover `018` (`BEH-EC-018` is the next free
    id — `BEH-EC-017` is the highest allocated, per `Hook.test.ts`'s Phase 7 entry). `verify-traceability
    .sh` check 1 fails in the disk→index direction otherwise.

    BEH-EC-018 must state, as normative REQUIREMENT text in this file's existing block form, every
    decision this phase locked:
    - `Rule(name, extraLayer, define)` and `Scenario(name, extraLayer, define)` (D-01) both merge
      `extraLayer` onto whatever Layer was ambient at that call site via `Layer.provideMerge`, and both
      remain per-Scenario-fresh — there is no third "shared within a Rule" scope (ADR-EC-006).
    - Before-shaped hooks (`Before`/`BeforeStep`) declared inside a Rule run AFTER the Feature's own of
      the same kind; After-shaped hooks (`After`/`AfterStep`) run in the REVERSE order — the Rule's own
      first, then the Feature's (D-02). State explicitly this mirrors `describe(feature) → describe(rule)`
      nesting, outer setup before inner, inner guarantee before outer.
    - `RuleDsl` exposes `Given`/`When`/`Then`/`And`/`But`, `Background` (`Given`/`And` only, D-04),
      `Scenario`, and exactly `Before`/`After`/`BeforeStep`/`AfterStep` — never
      `BeforeAllScenarios`/`AfterAllScenarios`, which remain Feature-only.
    - a Scenario Outline row's emitted test title shows every Examples column and that row's value for
      it, appended to the row's own interpolated name, REGARDLESS of whether the Outline's title text
      already references a placeholder (D-03) — state the two concrete forms:
      `"Applying a valid discount code (code=SAVE10, percent=10, expected=31.50)"` (no placeholder in
      the title) and `"adding 1 (count=1)"` (placeholder already referenced, suffix still appended).
    - two rows of one Outline share no mutable state — each observes only its own Examples values,
      proven by a real running test per row (Pitfall 34's regression class).
    Any `typescript`-tagged fence must import only from a package barrel and import everything it uses;
    a plain `ts` fence is reference material but must still be syntactically valid TypeScript.

    Update `spec/invariants.md`'s INV-EC-005: replace "**Source (planned)**" with the real mechanism —
    `packages/vitest/src/Registry.ts`'s `RegistryScope.ruleId` and `packages/vitest/src/Plan.ts`'s
    ruleId-equality `isVisibleTo` cases (08-01) enforce the MATCHING half; `packages/vitest/src/
    describeFeature.ts`'s `resolveRuleId` and `Layer.provideMerge` (08-05a) enforce the REGISTRATION
    half; name the asserting tests (`packages/vitest/test/Plan.test.ts`'s cross-rule isolation test,
    `packages/vitest/test/describeFeature.test.ts`'s per-Rule Layer resolution test, and
    `scripts/verify-tsgo-gate.sh` assertions 12/13 for the compile-time half). Keep the invariant's own
    wording; only the Source and Implication lines change.

    Add a dated implementation note to `spec/decisions/010-rule-and-scenario-scoped-extra-layers.md`
    recording where each half of the Decision landed: `Registry.ts`/`Plan.ts` for the matching
    mechanism, `describeFeature.ts` for the registration and Layer-merge mechanism, `Hook.ts`'s
    `mergeHookSets` for the D-02 ordering, and `Runner.ts` for threading the merged result into
    emission.

    Run `pnpm format` after editing.
  </action>
  <acceptance_criteria>
    - `pnpm verify:spec` exits 0
    - `grep -c "BEH-EC-018" spec/behaviors/03-rules-outlines-and-testclock.md` is at least `1` and `grep -c "018" spec/behaviors/index.yaml` is at least `1`
    - `grep -c "Source (planned)" spec/invariants.md` outputs zero occurrences under `INV-EC-005` specifically (confirm by inspection in the summary if the section boundary makes a bare grep ambiguous)
    - `grep -c "resolveRuleId" spec/invariants.md` is at least `1`
    - `git diff --numstat spec/decisions/010-rule-and-scenario-scoped-extra-layers.md` reports 0 deletions
    - `pnpm lint` exits 0 (dprint check over `spec/**/*.md`)
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:spec && pnpm lint</automated>
  </verify>
  <done>BEH-EC-018 exists and is indexed; INV-EC-005 names real modules and tests; ADR-EC-010 carries an additive implementation note.</done>
</task>

<task type="auto">
  <name>Task 2: traceability §1-§4 and spec/roadmap.md</name>
  <files>spec/traceability.md, spec/roadmap.md</files>
  <read_first>
    - spec/traceability.md (read whole — §1's planned/real preamble, §2's "Not yet written"/"Source (planned)" convention, §3's `—` convention, §4's enumerated-from-disk rule)
    - spec/roadmap.md (the Phase 8 status lines — "still an intended contract only" and similar)
    - spec/behaviors/03-rules-outlines-and-testclock.md as edited by Task 1
    - the summaries of plans 08-01 through 08-07 (the real modules and test files to name)
  </read_first>
  <action>
    §1: add or extend the row covering `03 — Rules, Scenario Outlines, and TestClock` / `BEH-EC-018` to
    name the real modules: `packages/vitest/src/{Registry,Plan,Dsl,HookRegistry,Hook,describeFeature,
    Runner,OutlineTitle}.ts`.

    §2: INV-EC-005's row moves from planned to enforced, mirroring the exact text Task 1 wrote into
    `spec/invariants.md`. Update the paragraph above the table that enumerates which invariants are
    enforced today, adding INV-EC-005.

    §3: fill ADR-EC-010's **Source module** column (currently likely `—`) with
    `packages/vitest/src/{Registry,Plan,Hook,HookRegistry,describeFeature,Runner}.ts`, and its
    **Affected invariants** with INV-EC-005.

    §4: add rows for `packages/vitest/test/OutlineTitle.test.ts`, naming BEH-EC-018 in **Covers**, and
    update the **Covers** column of `Registry.test.ts`, `Plan.test.ts`, `Hook.test.ts`,
    `HookRegistry.test.ts`, `describeFeature.test.ts`, `Runner.test.ts` and `emission.test.ts` to add
    BEH-EC-018/INV-EC-005 where this phase extended them. Re-run the reusable from-disk cross-check (the
    `node -e` script Phase 3/7 established) and record its clean output in the summary.

    `spec/roadmap.md`: move Phase 8's Rule/Outline registration content from "still an intended contract
    only" (or the equivalent phrasing) to built, leaving Phase 9/10 entries untouched.

    Run `pnpm format` after editing.
  </action>
  <acceptance_criteria>
    - `pnpm verify:spec` exits 0
    - `grep -c "BEH-EC-018" spec/traceability.md` is at least `2`
    - `grep -c "OutlineTitle.test.ts" spec/traceability.md` is at least `1`
    - `grep -c "INV-EC-005" spec/traceability.md` is at least `1` and its §2 row no longer says "Not yet written" / "Source (planned)"
    - The from-disk §4 cross-check reports every `packages/*/test/*.test.ts` file present, with output recorded in the summary
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify:spec && pnpm lint && node -e "const fs=require('fs');const doc=fs.readFileSync('spec/traceability.md','utf8');const files=['packages/gherkin/test','packages/vitest/test'].flatMap((d)=>fs.readdirSync(d).filter((f)=>f.endsWith('.test.ts')).map((f)=>d+'/'+f));const missing=files.filter((f)=>!doc.includes(f));if(missing.length>0){throw new Error('missing from traceability §4: '+missing.join(', '))}console.log('§4 covers all '+files.length+' test files')"</automated>
  </verify>
  <done>Traceability §1-§4 name the real Phase 8 modules and test files, INV-EC-005 is recorded as enforced, and `spec/roadmap.md` no longer lists Rule/Outline registration as unbuilt.</done>
</task>

<task type="auto">
  <name>Task 3: the two READMEs and the requirement markings</name>
  <files>README.md, packages/vitest/README.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - packages/vitest/README.md (read whole — the `## Status` section's "What is NOT built yet" paragraph naming Phase 8/DSL-05/DSL-06)
    - README.md (any package-table row or "still specified" sentence naming Rule/Outline registration)
    - .planning/REQUIREMENTS.md (DSL-05/DSL-06's checkboxes, the traceability table, the footer's per-requirement-evidence convention set by the Phase 6/7 entries)
    - the summaries of plans 08-01 through 08-07 (the named automated assertions each requirement rests on)
    - AGENTS.md §4
  </read_first>
  <action>
    `packages/vitest/README.md`: add a paragraph to `## Status` stating Rules can extend the ambient
    Layer per-Rule (Before/After/BeforeStep/AfterStep scoped the same way, Background scoped the same
    way), a Scenario can do the identical thing individually, and Outline rows are typed for free with
    a distinctly-filterable title showing every column's value. Delete the Rule/Outline clause from
    "What is not built yet", leaving the Phase 9/10 clauses intact.

    `README.md`: drop Rule/Outline registration from any "still specified, not yet built" sentence and
    update the `@effect-cucumber/vitest` package-table row if it names this gap.

    `.planning/REQUIREMENTS.md`: tick DSL-05 and DSL-06, set both Traceability rows to `Complete`, and
    add a footer entry in the Phase 6/7 form naming, per requirement, the automated assertion that fails
    if the requirement stops being true:
    - DSL-05 by `packages/vitest/test/Plan.test.ts`'s cross-rule isolation and three-level precedence
      tests, `packages/vitest/test/describeFeature.test.ts`'s per-Rule Layer resolution and Rule/
      Scenario composition tests, `scripts/verify-tsgo-gate.sh` assertions 12/13 (the compile-time
      boundary, by name), and `packages/vitest/test/emission.test.ts`'s real end-to-end Rule run;
    - DSL-06 by `packages/vitest/test/OutlineTitle.test.ts`'s exact-title-format tests and
      `packages/vitest/test/emission.test.ts`'s real three-row independence proof (Pitfall 34).
    State that no requirement outside DSL-05 and DSL-06 changed status, and cite
    `.planning/phases/08-rule-and-scenario-outline/08-08-SUMMARY.md` for the per-requirement evidence.

    Run `pnpm format` after editing.
  </action>
  <acceptance_criteria>
    - `grep -cE "^- \[x\] \*\*DSL-05\*\*" .planning/REQUIREMENTS.md` outputs `1` and `grep -cE "^- \[x\] \*\*DSL-06\*\*" .planning/REQUIREMENTS.md` outputs `1`
    - `grep -cE "^\| DSL-05 \| Phase 8 \| Complete \|" .planning/REQUIREMENTS.md` outputs `1` and the same for `DSL-06`
    - `grep -c "Pending" .planning/REQUIREMENTS.md` is exactly two fewer than before this plan
    - The footer's new entry names at least four distinct test-file or script paths as evidence
    - `pnpm lint` and `pnpm verify:spec` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm verify:spec && pnpm test && pnpm build && pnpm verify:tsgo-gate && pnpm verify:pack</automated>
  </verify>
  <done>Both READMEs describe Rule/Outline registration as built, and DSL-05/DSL-06 are Complete with a per-requirement evidence trail naming real automated assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| specification → future implementer | The only boundary this plan touches. Stale or wrong spec text is the failure mode; there is no code, no input parsing, no network and no credential handling anywhere in it. |
| README → npm page / public reader | Published prose describing capabilities the package may or may not have. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Repudiation | a normative document silently contradicting the shipped code | mitigate | BEH-EC-018 is new normative text and INV-EC-005's correction is additive with zero deletions; the acceptance criteria enforce both. |
| T-08-08-02 | Repudiation | a new behavior section unreachable because its id range was not indexed | mitigate | `spec/behaviors/index.yaml`'s `id_range` for this file is widened in the same commit and `pnpm verify:spec` check 1 fails in the disk→index direction if it is not. |
| T-08-08-03 | Repudiation | a requirement marked Complete with no assertion behind it | mitigate | The footer entry must name a specific failing-if-untrue assertion per requirement, following the Phase 6/7 precedent, and an acceptance criterion counts the named paths. |
| T-08-08-04 | Information disclosure | published README describing a capability that does not exist | mitigate | AGENTS.md §4; the README updates are cross-checked against the real per-requirement evidence in the same task. |
| T-08-08-SC | Tampering | npm/pip/cargo installs | n/a | This plan adds no dependency and touches no manifest. No package-legitimacy checkpoint applies. |
</threat_model>

<verification>
- `pnpm verify:spec` exits 0 — BEH-EC-018 is indexed in both directions and the traceability checks pass.
- The from-disk §4 cross-check reports every `packages/*/test/*.test.ts` file as covered.
- `pnpm lint` exits 0 (dprint over `spec/**/*.md`, both READMEs, and `.planning/REQUIREMENTS.md`).
- `pnpm build`, `pnpm test`, `pnpm verify:tsgo-gate` and `pnpm verify:pack` all still exit 0 — this is a documentation plan and none of them may move.
</verification>

<success_criteria>
- `spec/behaviors/03-rules-outlines-and-testclock.md` carries BEH-EC-018 stating D-01 through D-04 and the D-03 titling format as normative, and is indexed.
- INV-EC-005 names real modules and real tests; ADR-EC-010 carries an additive implementation note.
- Traceability §1/§3/§4 name the real Phase 8 modules and test files; `spec/roadmap.md` and both READMEs describe Rule/Outline registration as built.
- DSL-05 and DSL-06 are Complete in `.planning/REQUIREMENTS.md`, each with a named automated assertion in the footer entry.
</success_criteria>

<output>
Create `.planning/phases/08-rule-and-scenario-outline/08-08-SUMMARY.md` when done.
</output>
