---
phase: 08-rule-and-scenario-outline
plan: 07
type: execute
wave: 4
depends_on: ["08-05b", "08-04", "08-02"]
files_modified:
  - packages/vitest/src/Runner.ts
  - packages/vitest/src/describeFeature.ts
  - packages/vitest/test/Runner.test.ts
  - packages/vitest/test/emission.test.ts
autonomous: true
requirements: [DSL-05]

major_component: "Runner.ts emission wiring: per-Rule merged hooks/Layer, per-Scenario Layer override"

must_haves:
  truths:
    - "A Scenario inside a Rule runs with the Feature's ambient Layer AND that Rule's extra Layer both reachable"
    - "A Scenario inside a Rule runs its Feature Before hooks, then that Rule's own Before hooks, in that order"
    - "The same Scenario's After sequence runs the Rule's own After hooks, then the Feature's, in that order"
    - "A Scenario (or Outline row) with its own extra Layer runs with that fully-merged effective Layer, never re-merged a second time"
    - "A Feature with no Rules and no Scenario-level extra Layers behaves byte-for-byte as it did before this phase"
  artifacts:
    - path: "packages/vitest/src/Runner.ts"
      provides: "emitFeature threading ruleHooks/ruleLayers/scenarioLayers into buildScenarioEffect"
      contains: "mergeHookSets"
  key_links:
    - from: "packages/vitest/src/Runner.ts"
      to: "packages/vitest/src/Hook.ts"
      via: "emitFeature merges the Feature's HookSet with the enclosing Rule's (or emptyHookSet) once per Rule"
      pattern: "mergeHookSets"
    - from: "packages/vitest/src/describeFeature.ts"
      to: "packages/vitest/src/Runner.ts"
      via: "the final emitFeature(...) call site passes collection.ruleHooks/.ruleLayers/.scenarioLayers"
      pattern: "ruleHooks: collection.ruleHooks"
---

<objective>
Thread everything the earlier Phase 8 plans built — `Registry.ts`/`Plan.ts`'s rule-aware matching
(08-01), `Hook.ts`'s `mergeHookSets`/`emptyHookSet` (08-02), and `describeFeature.ts`'s
`ruleLayers`/`ruleHooks`/`scenarioLayers` (08-05a/08-05b) — into `emitFeature`, so a Scenario inside a
Rule actually RUNS with the right merged Layer and the right merged, correctly-ordered hooks. This is
the plan that makes DSL-05's success criteria observable at runtime, not just resolvable in a
`FeatureCollection` object a test inspects directly.

Purpose: `Runner.ts`'s own header already states its `describe(rule.name, ...)` nesting loop is
unchanged since Phase 6 — this plan's whole job is computing RICHER `layer`/`hooks` arguments to hand
`buildScenarioEffect` inside that loop, once per Rule, and a per-Scenario override lookup inside BOTH
the Feature-level and Rule-nested emission loops. `ScenarioEffect.ts` itself needs no change at all:
`mergeHookSets`'s array concatenation already gives `runHookBatch` everything it needs.
Output: `Runner.ts`'s per-Rule merge computation and per-Scenario Layer override, the final
`describeFeature.ts` → `emitFeature` call-site wiring, and an end-to-end proof using a real `.feature`
file with a Rule, a Rule-scoped Layer, Rule-scoped hooks, and a Scenario-scoped Layer, all composed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-rule-and-scenario-outline/08-CONTEXT.md
@.planning/phases/08-rule-and-scenario-outline/08-PATTERNS.md
@packages/vitest/src/Hook.ts
@packages/vitest/src/ScenarioEffect.ts
@.planning/phases/08-rule-and-scenario-outline/08-05a-SUMMARY.md
@.planning/phases/08-rule-and-scenario-outline/08-05b-SUMMARY.md

<interfaces>
<!-- The exact shape emitFeature grows, and the exact call site describeFeature.ts must update to
     match. No exploration needed for either edit. -->

packages/vitest/src/Runner.ts — emitFeature's args grow three fields:
  export const emitFeature = (args: {
    readonly api: TestApi
    readonly plan: FeaturePlan
    readonly layer: Layer.Layer<any, any, never>
    readonly hooks: HookSet
    readonly ruleHooks: ReadonlyMap<string, HookSet>          // NEW — from FeatureCollection.ruleHooks
    readonly ruleLayers: ReadonlyMap<string, Layer.Layer<any, any, never>>   // NEW
    readonly scenarioLayers: ReadonlyMap<string, Layer.Layer<any, any, never>>   // NEW
  }): void => { ... }

  // Per Rule, computed ONCE per Rule iteration (not per Scenario):
  //   const ruleHookSet = mergeHookSets(hooks, args.ruleHooks.get(rule.id) ?? emptyHookSet)
  //   const ruleEffectiveLayer = args.ruleLayers.get(rule.id) ?? layer
  // Per Scenario (both the Feature-level loop, using `hooks`/`layer` directly, and the Rule-nested
  // loop, using `ruleHookSet`/`ruleEffectiveLayer`):
  //   const effectiveLayer = args.scenarioLayers.get(scenarioKey) ?? <the loop's own layer variable>
  // `scenarioKey` is built the SAME way describeFeature.ts's own `scenarioKey(ruleId, name)` builds
  // it — Runner.ts computes it locally against `scenarioPlan.astName` and
  // `Option.getOrNull(scenarioPlan.ruleId)`, since it cannot import describeFeature.ts (the dependency
  // runs the other way — Plan.ts's own closing paragraph states the identical rule for planFeature).

packages/vitest/src/describeFeature.ts — the closing emitFeature(...) call site becomes:
  emitFeature({
    api: vitestTestApi,
    plan: collection.plan,
    layer: collection.layer,
    hooks: collection.hooks,
    ruleHooks: collection.ruleHooks,
    ruleLayers: collection.ruleLayers,
    scenarioLayers: collection.scenarioLayers
  })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Runner.ts — per-Rule merge and per-Scenario Layer override</name>
  <files>packages/vitest/src/Runner.ts, packages/vitest/test/Runner.test.ts</files>
  <read_first>
    - packages/vitest/src/Runner.ts (read whole — `emitFeature`'s two emission loops, the `planById`/`planFor` Map-building precedent, the exact two `api.effect(scenarioPlan.name, ...)`/`buildScenarioEffect({...})` call sites this task edits — note 08-04 already changed `scenarioPlan.name` to a `titles.get(...)` lookup at both; this task must not disturb that)
    - packages/vitest/src/Hook.ts (as edited by 08-02 — `mergeHookSets`, `emptyHookSet`)
    - packages/vitest/src/Plan.ts (`ScenarioPlan.astName`, `.ruleId: Option.Option<string>`)
    - packages/vitest/test/Runner.test.ts (read whole — `makeRecordingApi`, `emptyHooks`, the `shop`/`outline` fixtures, and the existing Rule-nesting test this task's new tests extend)
    - .planning/phases/08-rule-and-scenario-outline/08-CONTEXT.md D-01, D-02
  </read_first>
  <action>
    Extend `emitFeature`'s `args` type with `ruleHooks`, `ruleLayers`, `scenarioLayers` exactly as
    specified in this plan's `<interfaces>` block. Add a local `scenarioKeyFor = (scenarioPlan:
    ScenarioPlan): string => \`${Option.getOrElse(scenarioPlan.ruleId, () => "<feature>")} ${scenarioPlan
    .astName}\`` — the SAME composite-key shape `describeFeature.ts`'s `scenarioKey` builds, computed
    independently here because `Runner.ts` cannot import `describeFeature.ts` (the dependency runs the
    other way; `emitFeature`'s own closing paragraph already states the identical prohibition for
    `Plan.ts`).

    In the Feature-level scenarios loop: no `ruleHooks`/`ruleLayers` lookup needed (there is no
    enclosing Rule). Before calling `buildScenarioEffect`, compute `const effectiveLayer =
    args.scenarioLayers.get(scenarioKeyFor(scenarioPlan)) ?? layer` and pass `effectiveLayer` in place
    of the bare `layer` argument.

    In the Rule-nested scenarios loop, computed ONCE per Rule (outside the inner `for (const scenario of
    rule.scenarios)`, alongside where `describe(rule.name, ...)` opens its block): `const ruleHookSet =
    mergeHookSets(hooks, args.ruleHooks.get(rule.id) ?? emptyHookSet)` and `const ruleLayer =
    args.ruleLayers.get(rule.id) ?? layer`. Inside the per-scenario loop, compute `const effectiveLayer =
    args.scenarioLayers.get(scenarioKeyFor(scenarioPlan)) ?? ruleLayer` and pass `ruleHookSet` and
    `effectiveLayer` to `buildScenarioEffect` in place of the Feature's flat `hooks`/`layer`.

    `ScenarioEffect.ts` is NOT touched by this task — `mergeHookSets`'s array concatenation already
    produces exactly the `HookSet` shape `buildScenarioEffect` already accepts; there is no new
    finalizer nesting to add.

    Extend `packages/vitest/test/Runner.test.ts` with a recording-fake-api test (mirroring the existing
    `shop` fixture's Rule-nesting style) proving:
    - a `Before` hook merged from `hooks`/`ruleHooks` for a Rule-nested Scenario runs in
      Feature-then-Rule ORDER (assert an append-only log's exact contents and order, not merely that
      both ran).
    - the corresponding `After` sequence runs Rule-then-Feature.
    - a Feature with no Rules at all (the existing `checkout`/`shop`-without-a-Rule-block fixtures)
      produces the IDENTICAL emission shape as before this task — pass `ruleHooks`/`ruleLayers`/
      `scenarioLayers` as empty `Map`s in every pre-existing test this task's signature change touches,
      and assert nothing about their recorded shape changes.
  </action>
  <acceptance_criteria>
    - `grep -c "ruleHooks" packages/vitest/src/Runner.ts` is at least `2` and `grep -c "ruleLayers" packages/vitest/src/Runner.ts` is at least `2` and `grep -c "scenarioLayers" packages/vitest/src/Runner.ts` is at least `2`
    - `grep -c "mergeHookSets" packages/vitest/src/Runner.ts` is at least `1`
    - `grep -c "scenarioKeyFor" packages/vitest/src/Runner.ts` is at least `2`
    - `git diff --stat packages/vitest/src/ScenarioEffect.ts` reports no change
    - `pnpm vitest run packages/vitest/test/Runner.test.ts` passes, including the Feature-then-Rule Before-order test and the Rule-then-Feature After-order test, and every pre-existing test in this file still passes with the new empty-Map arguments
    - `pnpm build`, `pnpm lint` and `pnpm test` all exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/vitest/test/Runner.test.ts && pnpm build && pnpm lint && pnpm test</automated>
  </verify>
  <done>`emitFeature` computes a merged `HookSet` and an effective Layer per Rule and per Scenario, `ScenarioEffect.ts` is untouched, and Runner.test.ts proves both the Before/After ordering and full backward compatibility for Feature's with no Rules.</done>
</task>

<task type="auto">
  <name>Task 2: describeFeature.ts's final call site and the end-to-end proof</name>
  <files>packages/vitest/src/describeFeature.ts, packages/vitest/test/emission.test.ts</files>
  <read_first>
    - packages/vitest/src/describeFeature.ts (as edited by 08-05a/08-05b — `collection.ruleHooks`/`.ruleLayers`/`.scenarioLayers`, and the current `emitFeature({ api, plan, layer, hooks })` call at the bottom of the exported `describeFeature` function)
    - packages/vitest/test/emission.test.ts (read whole — the real `describeFeature` call pattern, and the discount-codes worked example in spec/behaviors/03-rules-outlines-and-testclock.md as the shape to draw the new fixture from — a smaller version is fine)
    - spec/behaviors/03-rules-outlines-and-testclock.md (the discount-codes worked example — Rule-scoped Layer, a Scenario Outline inside the Rule, `TestClock` in a plain Scenario inside the same Rule)
  </read_first>
  <action>
    Update the `emitFeature({ api: vitestTestApi, plan: collection.plan, layer: collection.layer,
    hooks: collection.hooks })` call at the bottom of `describeFeature` to also pass `ruleHooks:
    collection.ruleHooks, ruleLayers: collection.ruleLayers, scenarioLayers: collection.scenarioLayers`.

    In `packages/vitest/test/emission.test.ts`, add a real, running end-to-end test (this file is the
    one place in the repo that calls `describeFeature` for real) with a `.feature` source carrying a
    `Rule:` block. Cover, in ONE real `describeFeature` call run through vitest's own `it.effect`:
    - a Rule-scoped extra Layer whose service a step INSIDE that Rule reads and asserts on — the
      runtime half of roadmap SC#1.
    - a `Before` hook registered at Feature level AND a `Before` hook registered inside the Rule, both
      appending to one shared `Ref` obtained from a service in the ambient Layer; assert the recorded
      order is Feature-then-Rule for `Before` and (via matching `After` hooks) Rule-then-Feature for
      `After` — the runtime half of D-02, observed through an ACTUAL running Scenario rather than a
      recording-fake.
    - a Scenario-level extra Layer (D-01's Scenario form) on a Scenario inside that same Rule, proving
      all three tiers (Feature, Rule, Scenario) are reachable from inside one real running step.
  </action>
  <acceptance_criteria>
    - `grep -c "ruleHooks: collection.ruleHooks" packages/vitest/src/describeFeature.ts` outputs `1`
    - `grep -c "ruleLayers: collection.ruleLayers" packages/vitest/src/describeFeature.ts` outputs `1`
    - `grep -c "scenarioLayers: collection.scenarioLayers" packages/vitest/src/describeFeature.ts` outputs `1`
    - `pnpm vitest run packages/vitest/test/emission.test.ts` passes, including the new Rule end-to-end test asserting both the Feature-then-Rule `Before` order and the Rule-then-Feature `After` order via a real recorded `Ref` log
    - `pnpm build`, `pnpm lint`, `pnpm test`, `pnpm verify:tsgo-gate` and `pnpm verify:pack` all exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/vitest/test/emission.test.ts && pnpm build && pnpm lint && pnpm test && pnpm verify:tsgo-gate</automated>
  </verify>
  <done>`describeFeature` passes the three new maps to `emitFeature`, and a real, running `.feature` file with a Rule, Rule-scoped Layer, Rule-scoped hooks, and a Scenario-scoped Layer all compose correctly at runtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test author's Rule/Scenario Layers and hooks → the emitted `it.effect` body | In-process, fully trusted code the developer wrote. No network, no serialization, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Tampering | a Scenario's own extra Layer being re-merged against the Rule's or Feature's Layer a SECOND time at emission | mitigate | `scenarioLayers`' invariant (stated in 08-05b) is that its entries are ALREADY fully merged; this task's `?? ruleLayer`/`?? layer` fallback pattern only substitutes, never re-wraps, and the three-tier end-to-end test in Task 2 would observe a doubled or dropped service if re-merging occurred. |
| T-08-07-02 | Denial of service | a Feature with no Rules regressing because the new Map parameters are mishandled | mitigate | Task 1's explicit backward-compatibility test passes empty `Map`s through every pre-existing `Runner.test.ts` fixture and asserts identical emission shape. |
| T-08-07-03 | Elevation of privilege | a Rule's hooks or Layer leaking into the FEATURE-level scenarios loop (which must never consult `ruleHooks`/`ruleLayers` at all) | mitigate | The Feature-level loop's code path only ever reads `hooks`/`layer` plus `scenarioLayers`, never `ruleHooks`/`ruleLayers` — an acceptance grep on the diff (recorded in the summary) confirms the Feature-level loop body contains neither identifier. |
| T-08-07-SC | Tampering | npm/pip/cargo installs | n/a | This plan adds no dependency. `pnpm-lock.yaml` and both manifests are unchanged; no package-legitimacy checkpoint applies. |
</threat_model>

<verification>
- `pnpm build` compiles both packages clean.
- `pnpm lint` exits 0.
- `pnpm test` passes, including the extended `Runner.test.ts` and `emission.test.ts`.
- `pnpm verify:tsgo-gate` still exits 0 (thirteen assertions from 08-06, unaffected by this runtime-only plan).
- `pnpm verify:pack` exits 0 — no manifest changed.
</verification>

<success_criteria>
- `emitFeature` computes a correctly-ordered merged `HookSet` and an effective Layer per Rule and per Scenario.
- `ScenarioEffect.ts` is untouched.
- `describeFeature.ts`'s final `emitFeature` call site passes all three new maps.
- A real, running end-to-end test proves DSL-05's Layer composition and D-02's hook ordering together, through the actual emitted `it.effect`.
- Every pre-existing Feature-with-no-Rules test still passes unchanged.
</success_criteria>

<output>
Create `.planning/phases/08-rule-and-scenario-outline/08-07-SUMMARY.md` when done.
</output>
