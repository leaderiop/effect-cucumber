---
phase: 08-rule-and-scenario-outline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/vitest/src/Dsl.ts
autonomous: true
requirements: [DSL-05]

must_haves:
  truths:
    - "FeatureDsl exposes a Rule member accepting a name, an extra Layer, and a define callback receiving RuleDsl"
    - "RuleDsl exposes the five step registrars, a Background container, a Scenario container, and exactly the four Rule-scopeable hooks (Before/After/BeforeStep/AfterStep)"
    - "RuleDsl does NOT expose BeforeAllScenarios or AfterAllScenarios"
    - "Scenario (on both FeatureDsl and RuleDsl) accepts an optional extra Layer via a second call signature, without breaking the existing two-argument call form"
  artifacts:
    - path: "packages/vitest/src/Dsl.ts"
      provides: "RuleDsl<ROut>, FeatureDsl<ROut>.Rule, ScenarioRegistrar<ROut>"
      contains: "export interface RuleDsl"
  key_links:
    - from: "packages/vitest/src/Dsl.ts"
      to: "packages/vitest/src/describeFeature.ts"
      via: "FeatureDsl<ROut> is the type parameter describeFeature's define callback receives"
      pattern: "RuleDsl"
---

<objective>
Extend the compile-time DSL surface with `RuleDsl<ROut>` and `FeatureDsl<ROut>.Rule`, and widen
`Scenario` (on both containers) to optionally accept an extra Layer — the type-only half of D-01 and
D-04, ahead of any runtime implementation.

Purpose: this file is types only — no `const`, no function, no runtime value — and its own header
states that both members of every step/hook function union are load-bearing in an order the type
checker's own error-reporting rules dictate. Landing the type surface first, verified by the tsgo-gate
fixture pair in 08-06 and consumed by the composition-root wiring in 08-05a, follows this package's own
Interface-First convention (`Registry.ts`/`HookRegistry.ts` before `describeFeature.ts` in Phase 7).
Output: `RuleDsl<ROut>`, `FeatureDsl<ROut>.Rule`, and a `ScenarioRegistrar<ROut>` callable interface
used by both containers' `Scenario` member.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-rule-and-scenario-outline/08-CONTEXT.md
@.planning/phases/08-rule-and-scenario-outline/08-PATTERNS.md
@spec/behaviors/03-rules-outlines-and-testclock.md
@spec/decisions/010-rule-and-scenario-scoped-extra-layers.md

<interfaces>
<!-- The contract 08-05a (describeFeature.ts wiring) and 08-06 (tsgo-gate fixtures) are written
     against. Both downstream plans read this shape directly — no exploration needed. -->

packages/vitest/src/Dsl.ts additions:

  export interface ScenarioRegistrar<ROut> {
    (name: string, define: (dsl: ScenarioDsl<ROut>) => void): void
    <R2, E2>(
      name: string,
      extraLayer: Layer.Layer<R2, E2, any>,
      define: (dsl: ScenarioDsl<ROut | R2>) => void
    ): void
  }

  export interface RuleDsl<ROut> extends ScenarioDsl<ROut> {
    readonly Background: (define: (dsl: BackgroundDsl<ROut>) => void) => void
    readonly Scenario: ScenarioRegistrar<ROut>
    readonly Before: HookRegistrar<ROut>
    readonly After: HookRegistrar<ROut>
    readonly BeforeStep: HookRegistrar<ROut>
    readonly AfterStep: HookRegistrar<ROut>
    // Deliberately NO BeforeAllScenarios / AfterAllScenarios — 08-CONTEXT.md Claude's Discretion default.
  }

  // FeatureDsl<ROut> changes:
  //   - `Scenario: (name, define) => void` becomes `Scenario: ScenarioRegistrar<ROut>`
  //   - new member: `Rule: <R2, E2>(name: string, extraLayer: Layer.Layer<R2, E2, any>, define: (dsl: RuleDsl<ROut | R2>) => void) => void`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RuleDsl and FeatureDsl.Rule</name>
  <files>packages/vitest/src/Dsl.ts</files>
  <read_first>
    - packages/vitest/src/Dsl.ts (read whole — the module doc comment's notes (a)-(f) in full, `BackgroundDsl`, `FeatureDsl`, `HookRegistrar`; note (f) is the one this task directly contradicts and must update)
    - spec/behaviors/03-rules-outlines-and-testclock.md BEH-EC-009 (the literal `Rule` signature this task implements: `Rule: <R2, E2>(name: string, extraLayer: Layer.Layer<R2, E2, any>, define: (dsl: RuleDsl<R | R2>) => void) => void`)
    - spec/decisions/010-rule-and-scenario-scoped-extra-layers.md (read whole — the Decision and both Consequences)
    - .planning/phases/08-rule-and-scenario-outline/08-CONTEXT.md decisions D-02, D-04 and the "Claude's Discretion" entries on RuleDsl's extension base and on omitting BeforeAllScenarios/AfterAllScenarios
    - .planning/phases/08-rule-and-scenario-outline/08-PATTERNS.md section "packages/vitest/src/Dsl.ts"
  </read_first>
  <action>
    Add `import type * as Layer from "effect/Layer"` (type-only, alongside the existing `Effect`/`Scope`
    type-only imports — this file stays zero runtime statements, per its own closing paragraph).

    Declare `RuleDsl<ROut> extends ScenarioDsl<ROut>` (per Claude's Discretion, mirroring
    `FeatureDsl<ROut> extends ScenarioDsl<ROut>` — the established precedent, no concrete reason argues
    otherwise here). It adds:
    - `Background: (define: (dsl: BackgroundDsl<ROut>) => void) => void` — IDENTICAL shape to
      `FeatureDsl.Background`, reusing `BackgroundDsl<ROut>` as-is (D-04: no new interface needed, same
      `Given`/`And`-only omission reasoning note (c) already states).
    - `Scenario: ScenarioRegistrar<ROut>` (Task 2 declares this type; this task may declare `RuleDsl`
      referencing it forward in the same file).
    - `Before`, `After`, `BeforeStep`, `AfterStep`, each `HookRegistrar<ROut>` — the four hooks
      ADR-EC-010 names as Rule-scopeable (D-02). Deliberately OMIT `BeforeAllScenarios` and
      `AfterAllScenarios` — per 08-CONTEXT.md's Claude's Discretion default, ADR-EC-010's hook list for
      Rule scoping is explicitly the four above, and the "AllScenarios" semantic does not cleanly narrow
      to "once per Rule" without its own design pass. Do not add them "for symmetry with FeatureDsl".

    Add `Rule` to `FeatureDsl<ROut>`, positioned as a sibling of `Background`/`Scenario` (not spread
    into `ScenarioDsl`, for the identical leak reason note (f) already states for the six hook members):
    `readonly Rule: <R2, E2>(name: string, extraLayer: Layer.Layer<R2, E2, any>, define: (dsl:
    RuleDsl<ROut | R2>) => void) => void`. The generic union `ROut | R2` is what lets a step inside this
    Rule's `define` callback use BOTH the Feature's ambient services and the ones `extraLayer`
    contributes — this is the type-level half of ADR-EC-010's compile-time boundary; the runtime merge
    (`Layer.provideMerge`) is 08-05a's job, not this file's.

    Update note (f) IN PLACE. It currently states "The six hook members... live on `FeatureDsl` ONLY,
    and nowhere else" — this is now false for four of the six. Rewrite it to state precisely which
    hooks are Feature-only (`BeforeAllScenarios`, `AfterAllScenarios` — still true, still guarded by the
    SAME leak argument: a member added to `ScenarioDsl` would leak into every `Scenario(...)` callback)
    versus which four are now ALSO on `RuleDsl` by design (`Before`/`After`/`BeforeStep`/`AfterStep`),
    and that this is new capability, not a leak the type system failed to prevent — the leak argument
    still fully applies to keep hooks OFF `ScenarioDsl` itself. Reference that plan 08-06's tsgo-gate
    fixture pair is this note's behavioral proof, mirroring how 07-03's fixture pair proved note (f) for
    the original six-on-`FeatureDsl`-only claim.
  </action>
  <acceptance_criteria>
    - `grep -c "export interface RuleDsl" packages/vitest/src/Dsl.ts` outputs `1`
    - `grep -A10 "export interface RuleDsl" packages/vitest/src/Dsl.ts | grep -c "BeforeAllScenarios\|AfterAllScenarios"` outputs `0`
    - `grep -A10 "export interface RuleDsl" packages/vitest/src/Dsl.ts | grep -cE "Before:|After:|BeforeStep:|AfterStep:"` is at least `4`
    - `grep -c "readonly Rule:" packages/vitest/src/Dsl.ts` outputs `1`
    - `grep -c "^import type \* as Layer" packages/vitest/src/Dsl.ts` outputs `1`
    - `pnpm build` exits 0 (this file is types-only; `pnpm build` is the only automated check available until 08-05a/08-06 exercise it)
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>`RuleDsl<ROut>` exists with the five step registrars, a Background container, a Scenario container, and exactly four hook registrars; `FeatureDsl<ROut>` gains `Rule`; note (f) states the real hook-placement rule.</done>
</task>

<task type="auto">
  <name>Task 2: ScenarioRegistrar — the Scenario extra-layer overload</name>
  <files>packages/vitest/src/Dsl.ts</files>
  <read_first>
    - packages/vitest/src/Dsl.ts (as edited by Task 1 — `RuleDsl.Scenario`'s forward reference to this type)
    - packages/vitest/src/StepRegistrar (the `StepRegistrar<ROut>` callable-interface pattern immediately above `HookRegistrar` — the direct template for declaring a MULTI-signature callable interface as a `readonly` property type rather than a plain function-type union, which does not give real overload resolution for a variable argument count)
    - spec/decisions/010-rule-and-scenario-scoped-extra-layers.md (`Scenario(name, extraLayer, () => {...})` — same per-Scenario scope as the Rule form, "built fresh every Scenario, same lifecycle as the Feature's default Layer")
    - .planning/phases/08-rule-and-scenario-outline/08-CONTEXT.md D-01 and its "Claude's Discretion" entry on whether the extra-Layer parameter is genuinely optional (two overloads) versus always-required-but-possibly-`Layer.empty`
  </read_first>
  <action>
    Declare `ScenarioRegistrar<ROut>` as a callable interface with TWO call signatures (not a union of
    two function types on a `readonly` property — a union does not give proper overload resolution for
    a variable argument count; the object-with-multiple-call-signatures form is what `StepRegistrar`
    already demonstrates for exactly this reason):
    - `(name: string, define: (dsl: ScenarioDsl<ROut>) => void): void` — the existing two-argument form,
      UNCHANGED, so every `Scenario("...", (dsl) => {...})` call site in the repo (and in every existing
      test) keeps compiling with no edit.
    - `<R2, E2>(name: string, extraLayer: Layer.Layer<R2, E2, any>, define: (dsl: ScenarioDsl<ROut |
      R2>) => void): void` — the new three-argument form (D-01's genuinely-optional choice, per Claude's
      Discretion: two overloads rather than an always-required `Layer.empty` parameter, because the
      two-argument form is the overwhelmingly common case and forcing every caller to write
      `Layer.empty` would be pure ceremony with no compile-time benefit — unlike `describeFeature`'s
      `{ shared, perScenario }` object form, `Scenario` has no D-03-style "collision precedence between
      two named slots" to make explicit, so there is nothing an always-required parameter would be
      documenting).

    Change `FeatureDsl<ROut>.Scenario`'s type from `(name: string, define: (dsl: ScenarioDsl<ROut>) =>
    void) => void` to `ScenarioRegistrar<ROut>`. `RuleDsl.Scenario` (Task 1) already references this
    type.

    Update note (e) if needed: it states `R` (here, `ROut`) is bound to the enclosing `describeFeature`'s
    output and is "not a free type parameter of the registrar's call signature" — confirm this still
    reads correctly given `ScenarioRegistrar`'s SECOND signature introduces its own per-call `R2`/`E2`
    that UNION with `ROut` rather than replacing it, and add a clause extending note (e)'s reasoning to
    this case explicitly: `R2` is per-call because it genuinely varies per `Scenario(...)` invocation
    (like `Params`/`A`/`E` already do for `StepRegistrar`), while `ROut` stays bound to the enclosing
    scope, unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c "export interface ScenarioRegistrar" packages/vitest/src/Dsl.ts` outputs `1`
    - `grep -A6 "export interface ScenarioRegistrar" packages/vitest/src/Dsl.ts | grep -c "define: (dsl: ScenarioDsl<ROut>) => void): void"` is at least `1` (the two-argument form is unchanged)
    - `grep -A6 "export interface ScenarioRegistrar" packages/vitest/src/Dsl.ts | grep -c "extraLayer: Layer.Layer<R2, E2, any>"` is at least `1`
    - `grep -c "Scenario: ScenarioRegistrar<ROut>" packages/vitest/src/Dsl.ts` is at least `2` (once on `FeatureDsl`, once on `RuleDsl`)
    - `pnpm build` exits 0
    - `grep -rn "\.Scenario(" packages/vitest/test/*.test.ts | wc -l` compared before/after this task shows every EXISTING two-argument `Scenario(...)` call site still present verbatim (no call site needed to change) — record the count in the summary
  </acceptance_criteria>
  <verify>
    <automated>pnpm build && pnpm test</automated>
  </verify>
  <done>`ScenarioRegistrar<ROut>` has both call signatures, both `FeatureDsl` and `RuleDsl` use it for `Scenario`, and every existing two-argument `Scenario(...)` call site in the repo still compiles unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test author's `describeFeature` call → compile-time type check | The whole boundary this file exists to police. No runtime code, no network, no untrusted input — the risk is a type surface that compiles but enforces nothing (INV-EC-003's failure mode), not an attacker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Elevation of privilege | a hook member (`BeforeAllScenarios`/`AfterAllScenarios`) leaking onto `RuleDsl` | mitigate | The acceptance grep asserts zero matches inside `RuleDsl`'s declaration; 08-06's tsgo-gate fixture pair adds the executable `@ts-expect-error` proof, mirroring 07-03's precedent for the original `ScenarioDsl` leak guard. |
| T-08-03-02 | Tampering | `ScenarioRegistrar`'s two-argument form silently broken by the new overload | mitigate | Task 2's acceptance criteria requires every existing `Scenario(...)` two-argument call site in the repo to keep compiling unchanged; `pnpm build`/`pnpm test` both gate on it. |
| T-08-03-03 | Tampering | `Rule`'s generic union (`ROut \| R2`) silently narrowed back to `ROut` alone, quietly disabling the compile-time boundary the Rule's own Layer is supposed to open | mitigate | 08-06's tsgo-gate fixture pair is the behavioral proof this note (a)-style union constraint holds; this plan's own acceptance criteria only checks the declaration exists, not its enforcement — 08-06 owns that. |
| T-08-03-SC | Tampering | npm/pip/cargo installs | n/a | This plan adds no dependency. `pnpm-lock.yaml` and both manifests are unchanged; no package-legitimacy checkpoint applies. |
</threat_model>

<verification>
- `pnpm build` compiles both packages clean (this file is types-only; declaration emit is the primary signal).
- `pnpm lint` exits 0.
- `pnpm test` passes unchanged — no runtime file is touched by this plan.
</verification>

<success_criteria>
- `RuleDsl<ROut>` exists, extending `ScenarioDsl<ROut>`, with `Background`, `Scenario`, and exactly `Before`/`After`/`BeforeStep`/`AfterStep`.
- `FeatureDsl<ROut>.Rule` exists with the exact BEH-EC-009 generic signature.
- `ScenarioRegistrar<ROut>` has both call signatures and is used by both `FeatureDsl.Scenario` and `RuleDsl.Scenario`.
- Every existing two-argument `Scenario(...)` call site in the repo still compiles with no edit.
- Note (f) is corrected in place to state which hooks are Feature-only versus Rule-scopeable.
</success_criteria>

<output>
Create `.planning/phases/08-rule-and-scenario-outline/08-03-SUMMARY.md` when done.
</output>
