---
phase: 10-layer-scopes-per-scenario-default-shared
plan: 08
type: execute
wave: 2
depends_on: ["10-07"]
files_modified:
  - spec/invariants.md
  - spec/behaviors/02-shared-layers-and-tags.md
  - spec/traceability.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [RUN-03, RUN-04]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No normative document in the repo describes the `shared` Layer's build discipline without the zero-runnable-Scenario case that plan 10-07 made true, and none describes `scripts/verify-shared-layer-once.sh` as asserting something it does not assert"
    - "BEH-EC-007's fenced REQUIREMENT block is byte-identical to what it was before this plan — the new finding is an appended dated correction, in ADR-EC-007's established format, never a rewrite that narrows the requirement to fit the implementation"
    - "RUN-03 and RUN-04 read Complete in `.planning/REQUIREMENTS.md` in both the checklist and the traceability table, and the marking happened only after all thirteen repository gates exited 0 in this plan's own run"
    - "`pnpm verify:spec` passes with every relative link resolving — the new cross-references point at anchors that exist"
  artifacts:
    - "spec/invariants.md — INV-EC-002's Mechanism and Assertions paragraphs cover the zero-runnable case and describe the CLI gate truthfully"
    - "spec/behaviors/02-shared-layers-and-tags.md — BEH-EC-007's second dated correction"
    - "spec/traceability.md — §2's INV-EC-002 Test cell names the new assertion and drops the same misdescription"
    - ".planning/REQUIREMENTS.md — RUN-03 and RUN-04 Complete again, honestly"
  key_links:
    - "`10-07`'s `emission.test.ts` regression block → INV-EC-002's Assertions paragraph → §2's INV-EC-002 Test cell"
    - "ADR-EC-026's registration-time exclusion contract → BEH-EC-007's new correction → the build-discipline clause it qualifies"
    - "the thirteen-gate sweep → the RUN-03/RUN-04 markings it gates"
---

<objective>
Write down what plan 10-07 made true, and re-mark RUN-03 and RUN-04 Complete behind a green
gate run.

The verifier flipped both requirements back (`.planning/REQUIREMENTS.md` lines 39-40 are `[ ]`
and its traceability table reads `Gaps Found` for both) when it found the shared Layer still
being built for a Feature whose Scenarios were all excluded. Plan 10-07 closes that. This plan
is the record: the invariant and the behavior gain the clause the fix made true, and the
requirement rows go back to Complete only after every gate is re-run green in this plan's own
session — not on the strength of 10-07's summary.

This plan also carries one truthfulness correction in a passage it is already opening. Plan
10-06 wrote INV-EC-002's Assertions paragraph describing `scripts/verify-shared-layer-once.sh`
as running vitest twice and comparing a build count. The script runs it three times and its
whole-vs-filtered EQUALITY assertion compares the clock-isolation Scenario's STATUS, not a build
count — the build-once claim is carried separately by two independent "passed" assertions. The
same misdescription is repeated in `spec/traceability.md` §2's INV-EC-002 Test cell. Under
AGENTS.md §4 a normative document describing a gate's assertions incorrectly is a defect, and
editing that exact paragraph while leaving a known-false sentence inside it is not defensible.

Purpose: keep D-05's rule from Phase 10 intact — no document in the repo describes this
library's Layer scopes as doing something they do not do, in either direction.

Output: two spec files corrected, one traceability cell corrected, two requirement rows marked.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-layer-scopes-per-scenario-default-shared/10-VERIFICATION.md
@.planning/phases/10-layer-scopes-per-scenario-default-shared/10-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The invariant and the behavior gain the clause the fix made true</name>

  <files>spec/invariants.md, spec/behaviors/02-shared-layers-and-tags.md</files>

  <read_first>
  - `.planning/phases/10-layer-scopes-per-scenario-default-shared/10-07-SUMMARY.md` in full — the
    names of the new assertions, the new field's final name, and the four recorded mutation
    results. Every sentence this task writes must be checkable against something in that summary
    or against the source itself; nothing here may be inferred from this plan's prose alone.
  - `spec/invariants.md`, INV-EC-002's whole entry including its preamble, `Source`, `Mechanism`
    and `Assertions` paragraphs.
  - `spec/behaviors/02-shared-layers-and-tags.md` lines 87-144 — BEH-EC-007's fenced REQUIREMENT
    block and the existing dated correction beneath it, whose FORMAT this task copies exactly.
  - `spec/decisions/007-cucumber-expressions-for-step-matching.md`'s correction blockquotes — the
    house format for a dated correction, and the precedent that a superseded sentence is marked
    in place and never rewritten.
  - `spec/decisions/026-registration-time-tag-filtering-and-declared-tag-universe.md` — the
    registration-time exclusion contract the new clause is read together with. Cite its real
    wording, not a paraphrase invented here.
  </read_first>

  <action>
  **(1) `spec/invariants.md`, INV-EC-002's Mechanism paragraph.**

  It currently says the shared tier is provided by the framework's `layer(...)` at the BLOCK
  level, around every test node the Feature emits. That sentence is now too broad in one exact
  place, and the narrowing is the finding: it is around every node whose body needs it. The
  library's own always-passing unused-step-definition nodes are deliberately not on that route,
  because the framework's shared-block test constructor builds the memoised Layer before running
  ANY body — including one that does nothing at all.

  State the consequence as the property a caller can rely on: a Feature whose Scenarios are all
  removed by a registration-time tag filter never builds its shared tier, so the tier stays as
  deferred as asking for it implies. Name the mechanism (a routing field on the library's own
  emission options, read by the composition root) without naming a framework specifier, matching
  the file's existing register.

  **(2) `spec/invariants.md`, INV-EC-002's Assertions paragraph.**

  Two edits, in one pass:

  - ADD the new assertion — plan 10-07's `emission.test.ts` block: one Feature with both tiers
    declared, an `excludeTags` filter removing every Scenario, and one unused step definition,
    asserting the shared build counter stays at 0 while the unused definition is still reported.
    Name the second half explicitly; it is the non-vacuity control and a reader who does not know
    it exists will read the first half as a claim that could be satisfied by suppressing
    warnings.
  - CORRECT the existing description of `scripts/verify-shared-layer-once.sh`. It says the script
    runs the CLI twice and asserts an identical build count in both. Read the script and describe
    what it actually asserts: three runs (whole, narrowed to the clock-isolation Scenario,
    narrowed to the build-count Scenario), the build-once claim carried as two independent
    "passed" assertions in the whole run and the narrowed one, and the whole-vs-filtered EQUALITY
    claim comparing the clock-isolation Scenario's reported STATUS between two runs — the one
    claim no in-process test can make. Verify each of those against the file before writing it;
    the point of this edit is to stop a normative document from describing a gate it did not
    read.

  **(3) `spec/behaviors/02-shared-layers-and-tags.md`, BEH-EC-007.**

  Append a SECOND dated correction beneath the existing one, in the same blockquote format, and
  leave the fenced REQUIREMENT block byte-identical. The existing correction records the RELEASE
  half shipping weaker than the wording claims; this one records that the BUILD half now has a
  boundary the requirement's wording does not mention, and that the boundary is a strengthening
  rather than a divergence.

  Content, in this order:

  - What the requirement says: built exactly once for the whole Feature.
  - What that leaves unstated: how many times a Feature with NO runnable Scenario builds it. Read
    literally, "exactly once" would say once; the answer that matches the rest of the system is
    zero, because ADR-EC-026's registration-time filter contract is that an excluded Scenario is
    absent from the report rather than skipped in it — quote its actual wording — and a tier
    built for a Feature nobody asked to run is a cost with no observer.
  - What shipped, and when: it built once until plan 10-07, because the library's own
    always-passing unused-step-definition nodes travelled the same emission route as the
    Scenarios and that route builds the memoised tier before running any body. The
    `⚙ AfterAllScenarios` teardown node was already suppressed in this situation; the warning
    node that forced the very build teardown would have needed was not.
  - What holds now, and where it is asserted: name the `emission.test.ts` block and, from the
    10-07 summary, the two mutations that proved the assertion measures the defect.
  - The consequence for a caller, stated the way the existing correction states its own: a
    `shared` tier holding a testcontainer or a database connection is no longer started for a
    Feature the caller explicitly filtered out on the strength of one stray unused pattern.

  Do not touch the existing correction's text. Do not renumber, reorder or retitle any behavior.
  If a new cross-reference is added, it must point at an anchor that exists — `pnpm verify:spec`
  resolves every relative link and will say so.
  </action>

  <verify>
    <automated>pnpm verify:spec &amp;&amp; pnpm lint &amp;&amp; git diff --numstat spec/behaviors/02-shared-layers-and-tags.md</automated>
  </verify>

  <done>
  - `pnpm verify:spec` exits 0 with every relative link resolving, and the PASS/FAIL/SKIP counts
    recorded.
  - `pnpm lint` exits 0 — `spec/**/*.md` is dprint-formatted and a hand-edit that breaks it fails
    the lint job.
  - `git diff --numstat spec/behaviors/02-shared-layers-and-tags.md` shows 0 deletions. The
    correction is appended; the REQUIREMENT block and the first correction are untouched.
  - `grep -c 'verify-shared-layer-once' spec/invariants.md` is at least 1, and the sentence
    around it names three runs and names the clock-isolation Scenario's status as the subject of
    the equality claim — verified by reading the paragraph back, not by the grep alone.
  - `grep -c 'ADR-EC-026\|026-registration-time' spec/behaviors/02-shared-layers-and-tags.md` is
    at least 1 — the new correction cites the contract it is read together with.
  </done>
</task>

<task type="auto">
  <name>Task 2: Traceability, the two requirement rows, and the gate sweep that gates them</name>

  <files>spec/traceability.md, .planning/REQUIREMENTS.md</files>

  <read_first>
  - `spec/traceability.md` §2's INV-EC-002 row (~line 97) — its Mechanism and Test cells, both of
    which currently repeat the same two claims Task 1 corrected in `spec/invariants.md`.
  - `spec/traceability.md` §4's preamble and its `packages/vitest/test/*.ts` rows — to confirm
    whether this gap closure added any test FILE (it did not; plan 10-07 appended to two existing
    files), so §4 needs no new row. Run the disk cross-check in BOTH directions anyway and record
    its output; that is the half of §4's maintenance that proves the section is currently correct
    rather than merely unedited.
  - `.planning/REQUIREMENTS.md` lines 39-41 and its traceability table rows 102-104.
  - `packages/vitest/README.md` and `packages/vitest/src/index.ts`'s module doc comment — read
    only, to confirm neither now says anything the fix changed. D-05 put a worked example in the
    README describing the build-once behaviour; a build-once claim stays true, but read it rather
    than assuming, and if a sentence is now incomplete, fix it in place and record it as a
    deviation.
  </read_first>

  <action>
  **(1) `spec/traceability.md` §2's INV-EC-002 row.**

  Its Mechanism cell repeats the "provides the shared tier at the BLOCK level via `layer(...)`"
  framing Task 1 narrowed, and its Test cell repeats the CLI gate misdescription Task 1
  corrected. Bring both cells into agreement with what `spec/invariants.md` now says. These are
  the same two claims in a second location, and leaving them divergent recreates the exact drift
  §4's own cross-check discipline exists to prevent. Keep the cells terse — this is an index, and
  the argument lives in `spec/invariants.md`.

  §3's ADR-EC-006 and ADR-EC-018 rows name source modules; confirm each named module still
  exists and still carries the responsibility claimed. `packages/vitest/src/TestApi.ts` now
  carries the routing field as well, which does not change either row's claim — verify rather
  than assume, and record the reading.

  §4 gains no row: plan 10-07 created no file. Run the disk cross-check in both directions and
  record its output verbatim, following 10-06's own recorded form (files on disk, distinct files
  named, on disk but no row, named but not on disk).

  **(2) `.planning/REQUIREMENTS.md`.**

  Run the full thirteen-gate sweep FIRST, in this session, and record every exit code. Only then
  edit:

  - lines 39-40: RUN-03 and RUN-04 back to `[x]`
  - the traceability table rows: RUN-03 and RUN-04 from `Gaps Found` to `Complete`, against Phase
    10

  Mark them TOGETHER and mark them LAST, after every other edit in this plan is on disk and every
  gate is green. They are one code path — the build-once route and the `excludeTestServices` fix
  — and every preceding phase in this project set the precedent that the plan making a
  requirement true end to end is the one that marks it, gated behind a real run rather than a
  read of somebody else's summary.

  If any gate does not exit 0, do NOT mark. Stop and report — a Complete marking behind a red
  gate is the one defect this whole plan exists to avoid producing.

  Leave RUN-05 alone; it is Phase 9's and is already Complete.

  **(3) The package's two consumer-facing documents.**

  Read `packages/vitest/README.md` and `packages/vitest/src/index.ts`'s doc comment. D-05's
  worked example describes a counter-based expensive-resource Layer under the `{ shared,
  perScenario }` call form, mirroring `emission.test.ts`'s own fixture, and the build-once claim
  it makes is still true. Confirm that by reading. If either document turns out to state
  something the fix changed, correct it in place and record it as a deviation with the sentence
  quoted — AGENTS.md §4 makes a stale claim in a published document a defect, not a nit. If
  neither does, say so explicitly in the summary; "checked and found clean" and "not checked" are
  different facts and only one of them is a finding.
  </action>

  <verify>
    <automated>pnpm build &amp;&amp; pnpm typecheck:test &amp;&amp; pnpm lint &amp;&amp; pnpm test &amp;&amp; pnpm circular &amp;&amp; pnpm verify:pack &amp;&amp; pnpm verify:spec &amp;&amp; pnpm verify:tsgo-gate &amp;&amp; pnpm verify:oxlint-plugin &amp;&amp; pnpm verify:no-runner-dep &amp;&amp; pnpm verify:testapi-seam &amp;&amp; pnpm verify:tags-filter &amp;&amp; pnpm verify:shared-layer-once</automated>
  </verify>

  <done>
  - All thirteen gates exited 0, each recorded, BEFORE either requirement row was edited.
  - `grep -c 'RUN-03.*Complete' .planning/REQUIREMENTS.md` is 1 and
    `grep -c 'RUN-03.*Gaps Found'` is 0; the same pair holds for RUN-04; the checklist lines 39
    and 40 both read `[x]`.
  - `grep -c 'RUN-05' .planning/REQUIREMENTS.md` is unchanged and RUN-05 still reads Complete
    against Phase 9 — this plan touched no other requirement.
  - §4's disk cross-check run in both directions, output recorded verbatim, CLEAN both ways, and
    no row added — plan 10-07 created no file.
  - §2's INV-EC-002 Mechanism and Test cells agree with `spec/invariants.md`'s corrected
    paragraphs, verified by reading both back side by side.
  - `packages/vitest/README.md` and `packages/vitest/src/index.ts` read, and the summary states
    which of the two outcomes obtained: clean, or corrected with the sentence quoted.
  - `pnpm verify:spec` exits 0 with its PASS/FAIL/SKIP counts and link count recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executed code → normative documents | `spec/` is normative; a sentence there is a claim the repo is accountable for, and nothing in CI checks its truth |
| Plan artifacts → `.planning/REQUIREMENTS.md` | A requirement row is the project's own record of what shipped; a wrong row misleads every later phase that reads it |
| npm dependency tree → build | Installed packages reach the build and the gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08-01 | Repudiation | `.planning/REQUIREMENTS.md` RUN-03 / RUN-04 rows | high | mitigate | Marking a requirement Complete on the strength of a prior plan's summary rather than a run makes the project's own record unable to say the implementation is incomplete — which is exactly what let this gap reach verification the first time. Mitigated by running all thirteen gates in this plan's own session, recording every exit code, and marking last; an explicit instruction forbids marking behind a red gate. |
| T-10-08-02 | Tampering | BEH-EC-007's fenced REQUIREMENT block | high | mitigate | Rewriting the requirement to match the implementation destroys the spec's ability to say the implementation is wrong — the precise reasoning 10-06 recorded when it left the RELEASE clause standing. Mitigated by the append-only dated-correction format and a `git diff --numstat` acceptance criterion of 0 deletions on that file. |
| T-10-08-03 | Information Disclosure | `spec/invariants.md` and `spec/traceability.md` descriptions of the CLI gate | medium | mitigate | A normative document describing a gate's assertions incorrectly trains a reader to trust a claim the gate does not make, and the same misdescription is currently duplicated in two files. Mitigated by correcting both in the same commit and by requiring the script be READ before either sentence is written. |
| T-10-08-04 | Spoofing | new relative cross-references in `spec/` | low | mitigate | A link to an anchor that does not exist reads as a citation and cites nothing. `pnpm verify:spec` resolves every relative link and is an acceptance criterion on both tasks. |
| T-10-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install task in this plan and no manifest edit — this plan changes documentation and one planning file only. `pnpm install --frozen-lockfile` is the sole permitted command if the worktree lacks `node_modules`, and it restores the already-declared set from the committed lockfile. The package-legitimacy gate is not triggered; `pnpm verify:pack` runs in Task 2 and would fail on manifest drift. |
</threat_model>

<verification>
- Every sentence written here is checkable against `10-07-SUMMARY.md` or against the source and
  script files it describes, and the plan requires reading each before writing about it.
- The two duplicated misdescriptions of `scripts/verify-shared-layer-once.sh` are corrected in
  the same pass, in both locations.
- BEH-EC-007's requirement text survives byte-identical; the finding is appended.
- The requirement markings are gated behind a thirteen-gate sweep run in this session.
</verification>

<success_criteria>
- All thirteen repository gates exit 0.
- RUN-03 and RUN-04 read Complete in both places in `.planning/REQUIREMENTS.md`.
- `git diff --numstat spec/behaviors/02-shared-layers-and-tags.md` shows 0 deletions.
- No document in the repo describes the `shared` Layer's build discipline, or the CLI gate's
  assertions, in terms the code does not support (D-05's standing rule for this phase).
- CR-01 remains untouched and is named in the summary as the follow-up phase candidate it is.
</success_criteria>

<output>
Create `.planning/phases/10-layer-scopes-per-scenario-default-shared/10-08-SUMMARY.md` when done
</output>