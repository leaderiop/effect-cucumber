---
phase: 06-plan-scenario-effect-runner-emission-and-drift-detection
plan: 08
type: execute
wave: 6
depends_on: ["06-07"]
files_modified:
  - spec/traceability.md
  - spec/invariants.md
  - spec/roadmap.md
  - packages/vitest/README.md
  - README.md
autonomous: true
requirements: [RUN-01, MATCH-03, MATCH-04, MATCH-05]

must_haves:
  truths:
    - "Every test file added by this phase has a row in the traceability matrix, enumerated from disk"
    - "ADR-EC-019 traces to real source modules instead of an em dash"
    - "INV-EC-001's Source label names the real scenario-Effect builder instead of a planned one"
    - "No document in the repo still claims the runner does not exist"
  artifacts:
    - path: "spec/traceability.md"
      provides: "§1/§3/§4 rows covering this phase's modules and test files"
      contains: "Runner.test.ts"
    - path: "packages/vitest/README.md"
      provides: "A Status section describing the shipped runner"
  key_links:
    - from: "spec/traceability.md"
      to: "packages/vitest/test"
      via: "§4 enumerated from disk, cross-checked by a node -e script"
      pattern: "packages/vitest/test/"
---

<objective>
Reconcile `spec/` and the three READMEs with what plan 06-07 shipped. Every document that says the
runner does not exist is now wrong, and AGENTS.md §1 makes a code change unreflected in `spec/` in
the same change "incomplete, not merely undocumented".

Purpose: this is the same shape of work plan 03-06 did for Phase 3 — a spec-reconciliation plan,
kept separate so it is cheap and so no implementation plan has to fight for the same files. It is
also the plan that pays the debts this phase incurred: five new test files owe §4 rows, ADR-EC-019
owes a source, INV-EC-001 owes a real Source label, and both READMEs plus `spec/roadmap.md` carry
"no runner" claims that are now false.

Output: no code. Documentation that says only what is true.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-plan-scenario-effect-runner-emission-and-drift-detection/06-CONTEXT.md
@.planning/phases/06-plan-scenario-effect-runner-emission-and-drift-detection/06-07-SUMMARY.md
@AGENTS.md
@CONTRIBUTING.md

<verified_facts>
1. `spec/scripts/verify-traceability.sh` runs eight checks and NONE of them covers §4's test file map — it checks index.yaml↔disk both ways, invariants→traceability, decisions→traceability, `.feature` REQ tags (currently SKIP), and relative link integrity. §4's disk↔document agreement is enforced by CONVENTION plus plan 03-06's reusable `node -e` cross-check, not by the script. This plan must run that cross-check explicitly; `pnpm verify:spec` exiting 0 does not prove §4 is complete.
2. `pnpm verify:spec` DOES check relative link integrity across `spec/`, and counts a gitignored target as broken. Any new relative link added here must resolve.
3. `spec/behaviors/index.yaml` needs no change: this phase adds no behavior file. BEH-EC-013 already exists in `spec/behaviors/01-steps-and-world.md` and already covers MATCH-03/04/05.
4. `spec/traceability.md` §1's row for `01 — Steps and World` ALREADY names `packages/vitest/src/Plan.ts` — it was written forward-looking. The other new modules from this phase are not named there yet.
5. `spec/traceability.md` §3's row for ADR-EC-019 currently has `—` in its source column.
6. `spec/invariants.md` line 19 carries `**Source (planned)**` for INV-EC-001, and its preamble (line 8) states the convention that a planned mechanism must say so in its `Source` label — so removing the word `planned` is the correct edit, not deleting the label.
7. `dprint` formats `spec/**/*.md` including fenced code blocks. Hand-formatting risks failing `pnpm lint`; run `pnpm format` after editing and before committing.
8. `spec/roadmap.md`'s "Current state" prose and its Gate table BOTH contain "No runner" claims — the paragraph at lines 5-27 and the "Packages exist" row at line 31. Both must move together.
</verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the traceability matrix and invariants</name>
  <files>spec/traceability.md, spec/invariants.md</files>
  <read_first>
    - spec/traceability.md (the whole file — §1's real/planned preamble convention, §3's ADR rows, and §4's from-disk enumeration rule with its `StepArgs.types.ts` exception paragraph)
    - spec/invariants.md (the preamble's `Source (planned)` convention at line 8, and INV-EC-001 at lines 14-25)
    - spec/scripts/verify-traceability.sh (which checks exist, so this task does not assume §4 is covered)
    - .planning/phases/06-plan-scenario-effect-runner-emission-and-drift-detection/06-07-SUMMARY.md (the definitive list of files this phase added)
  </read_first>
  <action>
**§1 — Behavior to source.** Update the `01 — Steps and World` row's Source module column to name
every module BEH-EC-013 is now implemented by, in addition to what is already listed:
`packages/vitest/src/{describeFeature,Dsl,Step,Registry,CallSite,Plan,ScenarioEffect,TestApi,Runner,Errors}.ts`
and `packages/gherkin/src/Snippet.ts`. Keep the existing gherkin entry. The column's header says
"real and planned — see the preamble", so no marker change is needed for entries that are now real,
but check the preamble's wording still reads correctly for this row.

**§3 — Decision traceability.** Replace the `—` in ADR-EC-019's source column with the modules that
implement it: `packages/vitest/src/{Plan,ScenarioEffect,Runner,Errors}.ts`. While in that section,
check ADR-EC-004's and ADR-EC-017's rows: ADR-EC-004 (one `it.effect` per Scenario) is now
implemented by `packages/vitest/src/{ScenarioEffect,Runner}.ts`, and ADR-EC-017's literal-text
matching half is now implemented by `packages/vitest/src/Plan.ts`. Update whichever of those still
say `—` or name only a planned module. Do not invent rows for ADRs this phase did not touch.

**§4 — Test file map.** Add one row per test file this phase created, in the same format as the
existing `packages/vitest/test/*` rows: file path, the behavior ids it covers, and a one-sentence
description of what it actually asserts. The files are `packages/vitest/test/CallSite.test.ts`,
`Errors.test.ts`, `Plan.test.ts`, `ScenarioEffect.test.ts`, `Runner.test.ts`, `emission.test.ts`,
and `packages/gherkin/test/Snippet.test.ts`. Confirm the exact set against the 06-07 summary and
against `ls` rather than against this list — if a plan deviated, disk wins.

Behavior id mapping for the new rows: `Plan.test.ts` and `Errors.test.ts` cover BEH-EC-013;
`ScenarioEffect.test.ts` covers BEH-EC-013 and BEH-EC-003; `Runner.test.ts` and `emission.test.ts`
cover BEH-EC-013, BEH-EC-002 and BEH-EC-004; `CallSite.test.ts` covers BEH-EC-013;
`Snippet.test.ts` covers BEH-EC-013 and BEH-EC-015.

Then RUN plan 03-06's reusable cross-check rather than eyeballing it: a `node -e` script that reads
`packages/gherkin/test/*.test.ts` and `packages/vitest/test/*.test.ts` off disk and fails by name
if any file is absent from §4's table. Recover the exact script from the 03-06 summary and run it;
its output is the acceptance evidence. Remember the two documented non-rows —
`packages/gherkin/test/StepArgs.types.ts` (present in the table on purpose) and
`packages/vitest/test/tsgo-gate/` (absent on purpose) — and leave both paragraphs intact.

**Invariants.** In `spec/invariants.md`, change INV-EC-001's `**Source (planned)**` label to
`**Source**` and update its text to name the real module:
`packages/vitest/src/ScenarioEffect.ts` composes Background and Scenario steps as sequential
`yield*`s inside one `Effect.gen`, and `packages/vitest/test/ScenarioEffect.test.ts` asserts a
failing step prevents every later one by recorded execution order. Leave the preamble's convention
text at line 8 alone — it is still correct for the invariants that remain planned. Do NOT touch
INV-EC-002's or any other invariant's label; Phase 10 owns those.

Run `pnpm format` after editing, then `pnpm verify:spec`.
  </action>
  <verify>
    <automated>pnpm verify:spec && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - The 03-06 `node -e` disk↔§4 cross-check runs and reports zero missing files; paste its output into the summary.
    - `grep -c 'packages/vitest/test/Runner.test.ts' spec/traceability.md` returns `1`, and the same for `Plan.test.ts`, `ScenarioEffect.test.ts`, `CallSite.test.ts`, `Errors.test.ts`, `emission.test.ts` and `packages/gherkin/test/Snippet.test.ts`.
    - ADR-EC-019's row in §3 no longer contains `—` and names at least `packages/vitest/src/Plan.ts`.
    - `grep -c 'Source (planned)' spec/invariants.md` is strictly less than its value before this task, and INV-EC-001's block no longer contains the word `planned`.
    - `grep -c 'StepArgs.types.ts' spec/traceability.md` is unchanged, and the `tsgo-gate` exception paragraph is still present.
    - `pnpm verify:spec` exits 0 with 7 PASS and 1 SKIP, matching the pre-task baseline.
    - `pnpm lint` exits 0 (`dprint check` passes on the edited markdown).
  </acceptance_criteria>
  <done>The traceability chain reaches from BEH-EC-013 and ADR-EC-019 to real modules and real test files, cross-checked against disk.</done>
</task>

<task type="auto">
  <name>Task 2: Retire every "there is no runner" claim</name>
  <files>spec/roadmap.md, packages/vitest/README.md, README.md</files>
  <read_first>
    - spec/roadmap.md (the "Current state" paragraph at lines 3-27 and the Gate table's "Packages exist" row at line 31 — both carry the claim)
    - packages/vitest/README.md (the whole "## Status" section, lines 9-31, whose last paragraph is entirely about there being no runner; also line 7's forward reference)
    - README.md (the "## Status" section at lines 13-21, which says "no library code has shipped" — already stale before this phase and now badly so)
    - .planning/ROADMAP.md (the authority on per-phase status, which `spec/roadmap.md` cites; phases 7-11 remain unbuilt)
    - .planning/phases/06-plan-scenario-effect-runner-emission-and-drift-detection/06-07-SUMMARY.md (what actually shipped, so no claim overshoots)
  </read_first>
  <action>
Rewrite three Status sections so they describe what exists. AGENTS.md §4 governs every sentence:
say only what is true, and where a capability is planned, say it is planned.

**`spec/roadmap.md`.** Rewrite the "Current state" opening paragraph. The runner now exists:
`describeFeature` emits one `it.effect` per Scenario inside a `describe` named after the Feature
(nested inside a `describe` per Rule where the Feature has Rules), Background steps run first as
leading `yield*`s inside the same `Effect.gen`, and the first failure short-circuits the rest.
An unmatched step and an ambiguous step each fail their own Scenario with a located
`StepMatchError` — the ambiguous one naming every matching pattern with its definition site,
ordered deterministically — and an unused pattern is a non-fatal warning on three channels. Then
state precisely what remains only specified, citing `.planning/ROADMAP.md` as the per-phase
authority: hooks (Phase 7), Rule-scoped extra Layers and typed Scenario Outline examples
(Phase 8), tag routing and `@skip`/`@only` (Phase 9), and the shared-Layer build-once plus
`TestClock` isolation optimisation (Phase 10). Note that `describe(rule.name)` NESTING itself does
ship in this phase while the Rule-scoped extra-Layer semantics do not — that distinction is exactly
the kind of thing this document exists to get right.

Update the Gate table's "Packages exist" row: its cell ends with "**No runner:** `describeFeature`
collects step definitions and emits no `it.effect` at all yet". Replace that clause with what is
now true, and keep the rest of the cell's inventory intact, adding the new modules
(`Plan`, `ScenarioEffect`, `TestApi`, `Runner`, `CallSite`, `Errors` in the vitest package;
`Snippet` in gherkin).

**`packages/vitest/README.md`.** Rewrite "## Status". The opening line "Nothing is published to npm
yet. The registration surface has shipped, the runner has not." becomes an accurate statement:
nothing is published yet, and the runner now ships. Keep the two paragraphs that are still true —
the `describeFeature(feature, layer, define)` description and the "**The core value is enforced,
not aspirational**" paragraph about `pnpm verify:tsgo-gate` — verbatim unless something in them is
now wrong. REPLACE the final "**There is no runner yet.**" paragraph with a description of what
running a Feature does today, and an explicit list of what is still absent (hooks, tags, `Rule`
extra Layers, typed Outline examples, shared-Layer build-once), each attributed to its phase, with
the pointer to `spec/roadmap.md` kept.

Also check line 7's forward reference to ADR-EC-024's `loadFeature` wrapper — that is still not
shipped, so that sentence stays as-is. Do not let the rewrite quietly promote it.

**`README.md`.** Rewrite "## Status". "no library code has shipped" and "scaffolding with no
implementation behind them" are both false. Say what is real: both packages have working
implementations, a `.feature` file runs as vitest tests today, and nothing is published to npm yet.
Keep the pointer to `spec/roadmap.md` as the single source of truth for built-versus-specified, and
keep the sentence warning that the install instructions describe the intended shape and will not
work until the first release — that one is still true.

Do not touch the `## Install`, `## Requirements` or `@rc`-tag guidance in either README: the
`effect@rc` / `@effect/vitest@rc` advice is still correct and STATE.md records that all three
READMEs should be revisited only when `effect@4.0.0` ships stable.

Run `pnpm format` after editing, then `pnpm lint` and `pnpm verify:spec`.
  </action>
  <verify>
    <automated>pnpm format && pnpm lint && pnpm verify:spec && pnpm verify:pack</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "no runner\|No runner\|the runner has not\|There is no runner\|no library code has shipped\|scaffolding with no implementation" README.md packages/vitest/README.md packages/gherkin/README.md spec/roadmap.md` produces no output.
    - `grep -c "emits no \`it.effect\`" spec/roadmap.md` returns `0`.
    - `packages/vitest/README.md` still contains the paragraph beginning "**The core value is enforced, not aspirational.**" and still contains `pnpm verify:tsgo-gate`.
    - `packages/vitest/README.md` still contains its ADR-EC-024 `loadFeature`-wrapper sentence unchanged.
    - Each of `spec/roadmap.md`, `packages/vitest/README.md` and `README.md` names at least three of Phases 7-10's outstanding capabilities and attributes each to a phase.
    - `git diff README.md packages/vitest/README.md` shows no change inside the `## Install` or `## Requirements` sections.
    - `pnpm lint` exits 0 (`dprint check` passes), `pnpm verify:spec` exits 0, `pnpm verify:pack` exits 0 (both READMEs still ship in their tarballs).
  </acceptance_criteria>
  <done>No document in the repo claims a runner that now exists does not, and none claims a capability that does not exist yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary is crossed by this plan. It modifies five Markdown documents and no executable
code, no configuration consumed at runtime, no dependency manifest, and no published entry point.
The only inputs are files already in the repository, edited by hand.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | Repudiation | documentation overstating what is enforced | mitigate | AGENTS.md §4 makes an overstated capability a defect. Each rewrite is constrained to what plan 06-07's summary records as shipped, and acceptance criteria require each document to name at least three still-outstanding capabilities with their owning phase — so the edit cannot pass by deleting the caveats. |
| T-06-08-02 | Tampering | a broken or gitignored relative link in `spec/` | mitigate | `pnpm verify:spec`'s link-integrity check resolves every relative markdown link and counts a gitignored target as broken. It is in the required verification set and must exit 0. |
| T-06-08-03 | Information Disclosure | README content shipped in both npm tarballs | accept | `pnpm verify:pack` asserts each README is present in its tarball. The rewritten content describes the library's own public behaviour and contains no path, credential, or internal detail that is not already in the published source. |
| T-06-08-SC | Tampering | package-manager installs | accept | This plan installs nothing and touches no manifest. `pnpm-lock.yaml`, `pnpm-workspace.yaml` and every `package.json` are unmodified; `pnpm install --frozen-lockfile` must continue to succeed unchanged. |
</threat_model>

<verification>
- `pnpm lint && pnpm verify:spec && pnpm verify:pack && pnpm test && pnpm build` all exit 0.
- The disk↔§4 cross-check reports zero missing test files, and its output is recorded in the summary.
- `git diff --stat` touches only `spec/traceability.md`, `spec/invariants.md`, `spec/roadmap.md`, `packages/vitest/README.md` and `README.md`.
</verification>

<success_criteria>
- Every test file this phase added has a §4 row, verified against disk rather than against memory.
- ADR-EC-019, ADR-EC-004 and ADR-EC-017 trace to real modules; INV-EC-001's Source is no longer labelled planned.
- The three Status sections describe a shipped runner and an accurate list of what is still only specified.
</success_criteria>

<output>
Create `.planning/phases/06-plan-scenario-effect-runner-emission-and-drift-detection/06-08-SUMMARY.md` when done.
</output>
