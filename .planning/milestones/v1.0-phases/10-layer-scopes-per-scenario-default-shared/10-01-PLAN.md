---
phase: 10-layer-scopes-per-scenario-default-shared
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/vitest/src/describeFeature.ts
  - packages/vitest/test/SharedLayerConstraint.types.ts
autonomous: true
requirements: [RUN-03]

must_haves:
  truths:
    - "A `{ shared, perScenario }` call whose `shared` Layer has a non-`never` error channel does not compile."
    - "A `{ shared, perScenario }` call whose `shared` Layer has a `never` error channel still compiles."
    - "A `{ shared, perScenario }` call whose `perScenario` Layer has a non-`never` error channel still compiles — the constraint is asymmetric on purpose."
    - "`pnpm verify:tsgo-gate` still exits 0 — narrowing the object-form overload did not change which overload a plain-Layer call resolves against."
  artifacts:
    - path: "packages/vitest/test/SharedLayerConstraint.types.ts"
      provides: "D-04's compile-time proof: one @ts-expect-error negative, one positive control, one perScenario-unconstrained asymmetry control"
      contains: "@ts-expect-error"
      min_lines: 60
    - path: "packages/vitest/src/describeFeature.ts"
      provides: "The narrowed object-form overload on both describeFeature and collectFeature"
      contains: "readonly shared: Layer.Layer<RShared, never, never>"
  key_links:
    - from: "packages/vitest/test/SharedLayerConstraint.types.ts"
      to: "packages/vitest/src/describeFeature.ts"
      via: "direct relative import of describeFeature and collectFeature"
      pattern: "from \"\\.\\./src/describeFeature\\.ts\""
    - from: "packages/vitest/tsconfig.test.json"
      to: "packages/vitest/test/SharedLayerConstraint.types.ts"
      via: "include: [\"src\", \"test\"] compiled by pnpm typecheck:test"
      pattern: "\"include\": \\[\"src\", \"test\"\\]"
---

<objective>
Close roadmap Success Criterion 4 and Pitfall 27: a `shared` Layer whose error channel is not
`never` must be a COMPILE error, not an unrecoverable runtime defect reported out of `beforeAll`
and detached from any Scenario.

This is D-04 from `10-CONTEXT.md`, and it is deliberately a type-level constraint on `shared`
ALONE — `perScenario` stays as permissive as it is today.

Purpose: the type constraint lands BEFORE the runtime path that would otherwise be able to
produce the defect (the Phase 5 precedent: the negative type test is written before the runtime
code it guards). Plan 10-02 is what makes `layer(...)`'s `Effect.orDie` reachable at all.
Output: a narrowed object-form overload on both public entry points, plus a `.types.ts` file
carrying the claim in both directions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-layer-scopes-per-scenario-default-shared/10-CONTEXT.md
@.planning/phases/10-layer-scopes-per-scenario-default-shared/10-PATTERNS.md
</context>

<interfaces>
<!-- The two overload pairs this plan narrows. Extracted from packages/vitest/src/describeFeature.ts
     (lines 767-786 for collectFeature, 830-856 for describeFeature). The executor should not need
     to explore to find them. -->

Current object-form overload signature, identical in shape on BOTH `describeFeature` (line 830)
and `collectFeature` (line 767):

  type parameters: `<RShared, RScenario, E1, E2>`
  `layer` parameter: an object with
    `readonly shared: Layer.Layer<RShared, E1, never>`
    `readonly perScenario: Layer.Layer<RScenario, E2, never>`
  `define` parameter: `(dsl: FeatureDsl<RShared | RScenario>) => void`
  `describeFeature` additionally has a trailing `options?: DescribeFeatureOptions`;
  `collectFeature` deliberately does NOT (its doc comment says why — do not add one).

Current plain-Layer overload, declared LAST on both, and it must STAY last (note (a)):
  type parameters: `<ROut, E>`
  `layer` parameter: `Layer.Layer<ROut, E, never>`

Implementation signature (never resolved against by TypeScript, not part of the contract):
  `layer: LayerArgument`, where `LayerArgument` is declared at line 134.

Verified against the installed `@effect/vitest@4.0.0-rc.112`'s `dist/internal/internal.js`,
line 147 — this is the defect the constraint exists to prevent:
  `Layer.buildWithMemoMap(withTestEnv, memoMap, scope).pipe(Effect.orDie, Effect.cached, Effect.runSync)`
`Effect.orDie` converts a typed `Layer` failure into a defect, raised from a `beforeAll`/`beforeEach`
hook rather than from any Scenario's own test node.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Narrow the object-form overload's `shared` to a `never` error channel</name>
  <files>packages/vitest/src/describeFeature.ts</files>
  <read_first>
    - packages/vitest/src/describeFeature.ts — read the module doc comment (lines 1-94) IN FULL
      before touching anything. Note (a) is load-bearing for this edit: the plain-Layer overload
      is declared LAST on purpose, and this task must not reorder it.
    - .planning/research/PITFALLS.md, Pitfall 27 (lines ~768-776) — the finding, and the two
      candidate fixes. The first (a type-level constraint) is the one D-04 chose.
    - .planning/phases/10-layer-scopes-per-scenario-default-shared/10-CONTEXT.md, the
      "Error-channel constraint scope" section — the full argument for why `perScenario` is NOT
      constrained.
  </read_first>
  <action>
    On BOTH `describeFeature` (the first, object-form overload declared around line 830) and
    `collectFeature` (its object-form overload around line 767), change the `shared` field's type
    from `Layer.Layer<RShared, E1, never>` to `Layer.Layer<RShared, never, never>` and delete the
    now-unused `E1` type parameter from each overload's parameter list. The type parameter lists
    become `<RShared, RScenario, E2>` on both.

    Leave `perScenario` EXACTLY as it is: `Layer.Layer<RScenario, E2, never>`. The asymmetry is
    D-04 itself, not an oversight — a `perScenario` Layer meant to fail its own Scenario is a
    legitimate use case, and it surfaces safely through `it.effect`'s `unknown` error channel as
    that Scenario's own failure.

    Do NOT touch the plain-Layer overloads (`<ROut, E>`, `Layer.Layer<ROut, E, never>`) on either
    function, and do NOT change their position. They stay LAST for note (a)'s reason.

    Do NOT narrow `LayerArgument` (line 134). It is the implementation signature's union, TypeScript
    never resolves a call against it, and narrowing it would only make the implementation body
    disagree with itself for no observable gain. Add one sentence to its existing doc comment saying
    the public constraint lives on the overloads and deliberately not here.

    Add a SIXTH note to the module doc comment, note (f), placed after note (e), stating: the
    `shared` field's error channel is pinned to `never` because `@effect/vitest`'s `layer()`
    internally pipes `Layer.buildWithMemoMap(...)` through `Effect.orDie` (cite the verified
    location: the installed `@effect/vitest@4.0.0-rc.112`'s `dist/internal/internal.js` line 147),
    which converts a typed shared-Layer failure into an unrecoverable defect raised out of a
    `beforeAll`/`beforeEach` hook, detached from any Scenario — so the failure names no Scenario,
    no step, and no `.feature` file. The constraint forces the caller to make the choice visible
    with `Layer.catchAll`/`Layer.orDie` where the types can see it. State explicitly that
    `perScenario` is NOT constrained and why, and name
    `packages/vitest/test/SharedLayerConstraint.types.ts` as the file that carries the claim.

    Update the `@param layer` JSDoc line on `describeFeature` (around line 826) and the
    "Two accepted layer forms" paragraph in its doc comment (around lines 796-800) to state the
    `shared: Layer<R, never, never>` requirement in prose.

    Then RECORD, in the plan summary, the ACTUAL diagnostic text the compiler now produces for a
    failable `shared` Layer. Expect it to be reported against the LAST (plain-Layer) overload —
    note (a)'s own reporting rule — which means the message may name a missing-properties mismatch
    rather than the error channel. That is acceptable (the call IS rejected, which is what D-04
    requires) but it must be written down rather than assumed, because it is the diagnostic a
    consumer will actually read.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm typecheck:test &amp;&amp; pnpm verify:tsgo-gate &amp;&amp; pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'readonly shared: Layer.Layer&lt;RShared, never, never&gt;' packages/vitest/src/describeFeature.ts` returns exactly 2 (one per public entry point).
    - `grep -c 'readonly perScenario: Layer.Layer&lt;RScenario, E2, never&gt;' packages/vitest/src/describeFeature.ts` returns exactly 2 — `perScenario` is unchanged on both.
    - `grep -c 'E1' packages/vitest/src/describeFeature.ts` returns 0 — the type parameter is gone, not merely unused.
    - The plain-Layer overload is still the LAST overload of each function: in `packages/vitest/src/describeFeature.ts`, the line number of the last `layer: Layer.Layer&lt;ROut, E, never&gt;` occurrence is greater than the line number of the last `readonly shared:` occurrence.
    - `pnpm build` exits 0.
    - `pnpm typecheck:test` exits 0.
    - `pnpm verify:tsgo-gate` exits 0 — proving `effect(missingLayerContext)` still fires from the plain-Layer overload and the narrowing did not change overload resolution for a plain-Layer call.
    - `pnpm lint` exits 0 (oxlint + `dprint check`).
    - The module doc comment contains a note (f) whose text names both `Effect.orDie` and `SharedLayerConstraint.types.ts`.
  </acceptance_criteria>
  <done>
    Both public entry points reject a `shared` Layer with a non-`never` error channel at compile
    time, `perScenario` is untouched, the plain-Layer overload is still declared last, and every
    existing gate is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `SharedLayerConstraint.types.ts` — the claim in all three directions</name>
  <files>packages/vitest/test/SharedLayerConstraint.types.ts</files>
  <behavior>
    - Negative: `describeFeature(feature, { shared: &lt;Layer with a non-never E&gt;, perScenario: Layer.empty }, define)` does NOT compile (guarded by `@ts-expect-error`).
    - Positive control: `describeFeature(feature, { shared: &lt;Layer with E = never&gt;, perScenario: Layer.empty }, define)` DOES compile, with no directive above it.
    - Asymmetry control: `describeFeature(feature, { shared: &lt;Layer with E = never&gt;, perScenario: &lt;Layer with a non-never E&gt; }, define)` DOES compile, with no directive above it.
    - All three repeated for `collectFeature`, whose object-form overload carries the identical constraint and whose arity deliberately differs (no `options` parameter).
  </behavior>
  <read_first>
    - packages/vitest/test/GherkinTags.types.ts — the IN-PACKAGE `.types.ts` precedent. Copy its
      header's statement of why the `.types.ts` suffix is load-bearing and which config compiles it.
    - packages/gherkin/test/StepArgs.types.ts (lines 1-45 and the `@ts-expect-error` section around
      lines 132-160) — the repo's original precedent, and the source of the non-vacuity argument
      that BOTH directions are required.
    - packages/vitest/tsconfig.test.json — confirms `include: ["src", "test"]` and that this file
      is compiled by `pnpm typecheck:test`, a required step in `.github/workflows/check.yml`'s
      `types` job.
    - packages/vitest/src/describeFeature.ts — the signatures being asserted against, as left by
      Task 1.
    - packages/vitest/test/describeFeature.test.ts (lines 90-130) — the existing `Marker`
      service/`Layer.succeed` fixture idiom and the `parseFeature` call shape for a minimal
      `ParsedFeature`.
  </read_first>
  <action>
    Create `packages/vitest/test/SharedLayerConstraint.types.ts`.

    Header doc comment must state, in this repo's established voice: (1) that the `.types.ts`
    suffix keeps the file outside vitest's default include glob so `pnpm test` never collects it,
    while `packages/vitest/tsconfig.test.json`'s `include: ["src", "test"]` makes
    `pnpm typecheck:test` compile it on every push; (2) that renaming it to `.test.ts` breaks
    `pnpm test` with "No test suite found"; (3) the non-vacuity argument — the `@ts-expect-error`
    catches "the constraint stopped being enforced", and the positive control catches "the
    constraint became so strict it rejects the legitimate case too", and neither alone is
    sufficient; (4) that nothing in this file may be widened with a type assertion, because one
    escape hatch makes every surrounding assertion prove nothing.

    Declare the fixtures with `declare const`, so no runtime value is constructed and the file
    stays compile-time only:
    - a `ParsedFeature`-typed `declare const` (import the type from `@effect-cucumber/gherkin`),
    - `declare const okSharedLayer: Layer.Layer&lt;SomeService, never, never&gt;`,
    - `declare const failingLayer: Layer.Layer&lt;SomeService, SomeError, never&gt;` where `SomeError`
      is a locally-declared class or interface — a plain `class SharedLayerBuildError extends
      Error {}`-shaped declaration is enough; it never needs to be constructed.
    Use a `Context.Service` subclass for `SomeService`, matching `describeFeature.test.ts`'s
    `Marker` idiom.

    Write SIX cases, each as a named `export const` or a statement preceded by a doc comment naming
    which direction it carries, three for `describeFeature` and three for `collectFeature`:
    - the `@ts-expect-error`-guarded failable-`shared` call (the negative),
    - the `never`-channel `shared` call with `perScenario: Layer.empty` (the positive control),
    - the `never`-channel `shared` plus a FAILABLE `perScenario` (the asymmetry control, with NO
      directive — this is the case that proves D-04's asymmetry is real and not an oversight).
    The `define` callback in every case may be an empty arrow `() => {}`; the claim is entirely
    about the `layer` argument position.

    Import `describeFeature` and `collectFeature` by DIRECT RELATIVE PATH from
    `../src/describeFeature.ts`, never through `../src/index.ts` — `effect/no-import-from-barrel-package`
    runs with `checkRelativeIndexImports: true` and `collectFeature` is not in the barrel anyway.
    All `effect` imports must be submodule namespace imports (`import * as Layer from "effect/Layer"`).
    No semicolons, double quotes, 120 columns, no trailing commas — run `pnpm lint-fix` rather than
    hand-formatting.

    Then MUTATION-TEST the file and record all three proofs in the summary:
    (m1) revert Task 1's narrowing on `describeFeature` alone — the `@ts-expect-error` on the
    `describeFeature` negative must go red with "Unused '@ts-expect-error' directive";
    (m2) narrow `perScenario` to `Layer.Layer&lt;RScenario, never, never&gt;` as well — the asymmetry
    control must fail to compile, proving it is not decorative;
    (m3) delete the positive control and instead narrow `shared` to something that rejects every
    Layer — confirm the remaining `@ts-expect-error` still passes, which is exactly why the positive
    control cannot be dropped. Revert each mutation after recording it.
  </action>
  <verify>
    <automated>pnpm typecheck:test &amp;&amp; pnpm test &amp;&amp; pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `packages/vitest/test/SharedLayerConstraint.types.ts` exists.
    - `grep -c '@ts-expect-error' packages/vitest/test/SharedLayerConstraint.types.ts` returns exactly 2 (one per public entry point) — the two positive controls and the two asymmetry controls carry no directive.
    - `grep -vc '^\s*\*' packages/vitest/test/SharedLayerConstraint.types.ts | grep -q .` and the file contains zero occurrences of ` as ` used as a type assertion: `grep -c ' as [A-Z]' packages/vitest/test/SharedLayerConstraint.types.ts` returns 0.
    - `grep -c 'from "../src/index.ts"' packages/vitest/test/SharedLayerConstraint.types.ts` returns 0.
    - `pnpm typecheck:test` exits 0 — every `@ts-expect-error` is USED (an unused one is itself a compile error) and every control compiles.
    - `pnpm test` reports the SAME test count as before this plan — the `.types.ts` suffix means vitest collects nothing new. Record the before/after counts in the summary.
    - `pnpm lint` exits 0.
    - The summary records mutation proofs m1, m2 and m3, each with the exact failing output observed and confirmation it was reverted.
  </acceptance_criteria>
  <done>
    D-04's constraint is asserted in all three directions for both entry points, the assertions are
    mutation-proven non-vacuous, and `pnpm test`'s collected count is unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test author's `describeFeature` call → `@effect/vitest`'s `layer()` | a caller-supplied `Layer` crosses into a framework internal that applies `Effect.orDie`; this plan is the only thing standing at that boundary |
| `.types.ts` assertions → `pnpm typecheck:test` in CI | a claim that exists only at compile time; if the file stops being compiled, the claim silently stops being made |

This phase touches no network, no authentication, no persistence and no user-supplied runtime
input. The realistic risk class is TEST-ISOLATION CORRECTNESS — a test suite that reports a
false pass or attributes a failure to nothing — not a traditional confidentiality or integrity
vulnerability. The register below reflects that honestly.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Repudiation | `@effect/vitest`'s `layer()` → `Effect.orDie` | mitigate | Constrain `shared` to `Layer<R, never, never>` on both object-form overloads so a failable shared Layer cannot reach `orDie`; a defect raised out of `beforeAll` names no Scenario, no step and no `.feature` file, which is a failure nothing can be attributed to |
| T-10-01-02 | Tampering | `packages/vitest/test/SharedLayerConstraint.types.ts` | mitigate | Both directions asserted (negative `@ts-expect-error` plus positive control plus asymmetry control) and all three mutation-proven; a one-directional file passes green while the constraint has silently become vacuous or has silently become total |
| T-10-01-03 | Tampering | the `.types.ts` suffix / `tsconfig.test.json` include glob | mitigate | Header doc comment states the suffix is load-bearing and names the config that compiles it; a rename to `.test.ts` fails `pnpm test` loudly with "No test suite found" rather than silently dropping the claim |
| T-10-01-04 | Elevation of Privilege | n/a | accept | No privilege boundary exists in a compile-time constraint on a test-authoring API |
| T-10-01-05 | Information Disclosure | overload diagnostic text | accept | The diagnostic reported for a rejected failable `shared` Layer may name a missing-properties mismatch rather than the error channel (note (a)'s last-overload reporting rule). The call is still rejected, which is the requirement; the actual text is RECORDED in the summary rather than asserted on, because upstream compiler prose has never been a contract in this repo |
| T-10-01-SC | Tampering | npm/pip/cargo installs | n/a — accept | This plan installs no package and adds no dependency in any manifest. `pnpm-lock.yaml` is untouched, so `pnpm install --frozen-lockfile` in CI is unaffected. No package-legitimacy checkpoint applies |
</threat_model>

<verification>
- `pnpm build` exits 0 (the solution build still emits declarations for both packages).
- `pnpm typecheck:test` exits 0 (the new `.types.ts` compiles and every `@ts-expect-error` is used).
- `pnpm test` exits 0 with an UNCHANGED collected test count.
- `pnpm verify:tsgo-gate` exits 0 (the overload order note (a) protects is intact).
- `pnpm lint` exits 0.
- Mutation proofs m1, m2, m3 performed, recorded, reverted.
</verification>

<success_criteria>
Roadmap Phase 10 Success Criterion 4 is met at the type level: a `shared` Layer whose error
channel is not `never` is a compile error on both `describeFeature` and `collectFeature`, a
`never`-channel `shared` Layer still compiles, and `perScenario` is provably still unconstrained.
</success_criteria>

<output>
Create `.planning/phases/10-layer-scopes-per-scenario-default-shared/10-01-SUMMARY.md` when done.
It must record: the exact compiler diagnostic observed for a rejected failable `shared` Layer, the
three mutation proofs with their observed output, and the before/after `pnpm test` counts.
</output>
