---
phase: 10-layer-scopes-per-scenario-default-shared
plan: 06
type: execute
wave: 6
depends_on: ["10-05"]
files_modified:
  - packages/vitest/src/index.ts
  - packages/vitest/README.md
  - spec/invariants.md
  - spec/behaviors/02-shared-layers-and-tags.md
  - spec/behaviors/03-rules-outlines-and-testclock.md
  - spec/overview.md
  - spec/decisions/018-shared-layer-testclock-isolation.md
  - spec/roadmap.md
  - spec/traceability.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RUN-03, RUN-04]

must_haves:
  truths:
    - "No document in the repo still describes the build-once `shared` Layer or its per-Scenario `TestClock` isolation as unbuilt."
    - "`packages/vitest/README.md` carries one runnable worked example of the `{ shared, perScenario }` call form whose fixture shape mirrors the acceptance test's."
    - "INV-EC-002's `shared` clause no longer says it is planned, and names the mechanism and the assertions that enforce it."
    - "`spec/traceability.md` names the real source modules and the real test files for ADR-EC-018 and for BEH-EC-007, and lists no module that does not exist."
    - "RUN-03 and RUN-04 are marked Complete in `.planning/REQUIREMENTS.md`."
  artifacts:
    - path: "packages/vitest/README.md"
      provides: "the flipped status paragraph and D-05's worked example"
      contains: "perScenario"
    - path: "spec/traceability.md"
      provides: "§1, §3 and §4 rows reconciled against what Phase 10 actually built"
      contains: "SharedLayerConstraint.types.ts"
  key_links:
    - from: "spec/traceability.md §4"
      to: "packages/vitest/test/SharedLayerConstraint.types.ts"
      via: "a deliberate non-suite row, matching the two that already exist"
      pattern: "SharedLayerConstraint\\.types\\.ts"
    - from: "packages/vitest/README.md's worked example"
      to: "packages/vitest/test/emission.test.ts's shared build-count fixture"
      via: "the same counter-based expensive-resource shape (D-05)"
      pattern: "shared:"
---

<objective>
Close `10-CONTEXT.md`'s D-05 and reconcile every document that currently says the build-once
`shared` Layer is not built yet. Six documents, one package README, one package doc comment and
one requirements file all carry that claim today, and AGENTS.md §4's "say only what is true" makes
each one a defect the moment plan 10-02 shipped.

D-05 additionally asks for ONE small runnable worked example in the package README — a fake
counter-based "expensive resource" Layer used with the `{ shared, perScenario }` call form, whose
fixture shape MIRRORS the acceptance test's own so the example stays truthful against something
that is actually run, rather than becoming a second, drifting description of the same behaviour.

Purpose: RUN-03 and RUN-04 marked Complete, with nothing left claiming otherwise.
Output: reconciled docs and spec, and the two requirement IDs marked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-layer-scopes-per-scenario-default-shared/10-CONTEXT.md
@.planning/phases/10-layer-scopes-per-scenario-default-shared/10-02-SUMMARY.md
@.planning/phases/10-layer-scopes-per-scenario-default-shared/10-03-SUMMARY.md
@.planning/phases/10-layer-scopes-per-scenario-default-shared/10-04-SUMMARY.md
@.planning/phases/10-layer-scopes-per-scenario-default-shared/10-05-SUMMARY.md
</context>

<interfaces>
<!-- The exact locations carrying a now-false claim. Located by grep during planning; re-grep
     before editing, because earlier plans in this phase may already have moved some of them. -->

`packages/vitest/src/index.ts` lines ~59-67 — the "What is NOT built yet" paragraph, whose first
sentence names "the opt-in `shared` Layer built once per Feature, together with the per-Scenario
`TestClock` isolation that has to accompany it ... Phase 10 (RUN-03, RUN-04)". The "Current state"
paragraph it sits below (lines ~24-38) is where the true version belongs, in that paragraph's own
present-tense declarative voice.

`packages/vitest/README.md` lines ~104-107 — the "**What is not built yet:**" paragraph, same claim.
Lines ~90-97 hold the `gherkinTags` fenced-`ts`-block example, which is the STYLE the new worked
example follows. Lines ~47-54 hold the narrative Rule/Scenario paragraph, which is the style the
new PROSE around it follows.

`spec/invariants.md` line ~8 (the preamble sentence "INV-EC-002 holds in full for the per-Scenario
scope ... the `shared` clause of its own wording waits on Phase 10") and the paragraph inside
INV-EC-002 beginning "The `shared` half of the invariant's own wording ... is still **planned**"
(around line 56).

`spec/behaviors/02-shared-layers-and-tags.md` — BEH-EC-007 at line 87, "A shared Layer is opt-in
and built once". Its REQUIREMENT block includes a RELEASE clause: resources "MUST be released once,
after every Scenario in the Feature has run". That clause needs VERIFYING against the actual
implementation, not assuming — see Task 2.

`spec/behaviors/03-rules-outlines-and-testclock.md` line ~96 — the worked example's
`// Pre-implementation reference — not yet compiled against a real API.` caveat, immediately above
BEH-EC-012's `TestClock` example. The pattern map identifies this as the most plausible referent of
D-05's "unstated-exception hedge"; `spec/overview.md`'s own `TestClock` mention (line 9) is already
unconditional.

`spec/overview.md` — line 9's `TestClock`/`TestConsole` claim, and the Packages table whose
`@effect-cucumber/vitest` Status cell reads "Not yet implemented", plus the "Public API surface"
section beginning "Not yet implemented — there is no package to export from."

`spec/roadmap.md` lines ~62-65 and lines ~129-130 — two separate statements that the build-once
`shared` Layer is what remains.

`spec/decisions/018-shared-layer-testclock-isolation.md` — Accepted, dated 2026-08-28, with no
implementation note. Every other ADR this project has implemented carries one; ADR-EC-007's third
correction blockquote is the format precedent (55 additions, 0 deletions — the superseded sentence
is marked in place, never rewritten).

`spec/traceability.md`:
  §1's behaviors/02 row names a `SharedLayer.ts` module in its source list and says "only
  `SharedLayer` remains planned (Phase 10)". No such module exists or will — the shared path lives
  in `describeFeature.ts`. The preamble's own "no `Rule.ts` and no `ScenarioOutline.ts`" argument
  applies again and should be cited.
  §3's ADR-EC-018 row (line 123) has "—" in both its Invariant and Test columns.
  §4 is enumerated FROM DISK, one row per `packages/gherkin/test/*.test.ts` and
  `packages/vitest/test/*.test.ts`, plus TWO deliberate non-suite rows
  (`packages/gherkin/test/StepArgs.types.ts` and `packages/vitest/test/GherkinTags.types.ts`). Plan
  10-01 added a THIRD such file, `packages/vitest/test/SharedLayerConstraint.types.ts`, so the
  preamble's count and its justification sentence both change.

`.planning/REQUIREMENTS.md` lines 39-40 (the two checkbox entries) and lines 102-103 (the status
table rows) — RUN-03 and RUN-04, both currently Pending.

Formatting constraints that apply to every file in this plan:
  `spec/**/*.md` and both READMEs are dprint-formatted, INCLUDING fenced `ts` blocks. Run
  `pnpm format` after editing and confirm `pnpm lint` (which runs `dprint check`) exits 0. A `ts`
  fence must be syntactically valid TypeScript or dprint cannot format it.
  `pnpm verify:spec` (`spec/scripts/verify-traceability.sh`) check 5 rejects a broken relative
  markdown link, and a link target that exists but is gitignored counts as broken.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The package's own two documents, and D-05's worked example</name>
  <files>packages/vitest/src/index.ts, packages/vitest/README.md</files>
  <read_first>
    - packages/vitest/src/index.ts — the whole module doc comment, especially the "Current state"
      paragraph (lines ~24-38) and the "What is NOT built yet" paragraph (lines ~59-67).
    - packages/vitest/README.md — lines 30-124, covering the narrative Rule/Scenario paragraph, the
      tags paragraphs, the `gherkinTags` fenced example, and the "What is not built yet" paragraph.
    - packages/vitest/test/emission.test.ts, the shared build-count block plan 10-03 appended —
      the worked example's fixture shape must MIRROR this block's, per D-05, so the example stays
      truthful against something that is actually run.
    - .planning/phases/10-layer-scopes-per-scenario-default-shared/10-03-SUMMARY.md and
      10-04-SUMMARY.md — the observed build counts and clock readings the prose may state as fact.
    - spec/decisions/018-shared-layer-testclock-isolation.md — the decision the prose describes.
  </read_first>
  <action>
    `packages/vitest/src/index.ts`: DELETE the "What is NOT built yet" paragraph's first sentence
    (the one naming the `shared` Layer and the `TestClock` isolation as Phase 10) and FOLD its now-true
    content into the "Current state" paragraph above it, in that paragraph's present-tense declarative
    voice. The new sentences must state: that `describeFeature`'s second argument accepts either a
    plain Layer (the per-Scenario default, built fresh for every Scenario) or `{ shared, perScenario }`;
    that a `shared` Layer is built exactly once for the whole Feature via the test framework's own
    `layer(...)` helper; that every Scenario still receives its own fresh simulated clock and console
    regardless of which form was used; and that a `shared` Layer's error channel must be `never`,
    because the framework converts a typed shared-Layer failure into an unrecoverable defect
    reported out of a setup hook. Also state Pitfall 29's consequence in one sentence: on the shared
    path a Scenario cannot opt out of the simulated clock, because the `it` the framework hands the
    shared block has no live-clock member. Keep whatever remains of the "What is NOT built yet"
    paragraph accurate — if nothing is left, delete the paragraph entirely rather than leaving an
    empty marker, and check whether the sentence about `spec/roadmap.md` being the single authority
    on build status still has a subject.

    `packages/vitest/README.md`: replace the "**What is not built yet:**" paragraph. If nothing on
    this package remains unbuilt after Phase 10, say what is true instead — the remaining milestone
    work is Phase 11's dogfooded acceptance suite, which is not a gap in this package's behaviour —
    and keep the pointer to `spec/roadmap.md`. Do not delete the pointer.

    Add a NEW bolded paragraph to the README's feature narrative, placed next to the existing
    Rule/Scenario Layer paragraph (lines ~47-54) because that is the paragraph a reader arrives from,
    with a heading sentence in the same `**...**` style as its neighbours. It must state: the default
    per-Scenario scope and its freshness guarantee; the opt-in `shared` scope and its build-once
    guarantee; that `perScenario` is a REQUIRED key even for a Feature with no per-Scenario state
    (write `Layer.empty`); that where both name the same service `perScenario` wins; that every
    Scenario keeps its own simulated clock and console on BOTH scopes; and that a `shared` Layer's
    error channel must be `never`, with `Layer.catchAll`/`Layer.orDie` named as how a caller handles
    a failable resource where the types can see the choice.

    Then add ONE fenced `ts` worked example below that paragraph, in the same fenced-code-block style
    as the `gherkinTags` example. It must be a FAKE counter-based "expensive resource" — a
    `Layer.effect` whose build body increments a counter and returns a service carrying it — used
    with the `{ shared, perScenario }` call form, with two Scenarios whose step bodies read the
    counter and show it is the same build. Mirror the fixture shape of the block plan 10-03 appended
    to `emission.test.ts` rather than inventing a second arrangement. Every import in the fence must
    be a submodule namespace import from `effect/*` or a named import from this package's own
    barrel, and the fence must be syntactically valid TypeScript so `dprint` can format it. Keep it
    SMALL — D-05 says one small example, and a large one drifts faster than it teaches.
  </action>
  <verify>
    <automated>pnpm format &amp;&amp; pnpm lint &amp;&amp; pnpm build &amp;&amp; pnpm verify:pack</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'What is NOT built yet' packages/vitest/src/index.ts` returns 0, OR returns 1 with the remaining paragraph naming something that is genuinely still unbuilt — state which in the summary.
    - `grep -ci 'not built yet' packages/vitest/README.md` returns 0.
    - `grep -c 'both halves are built per Scenario' packages/vitest/README.md` returns 0 and `grep -c 'both halves are built per Scenario' packages/vitest/src/index.ts` returns 0.
    - `grep -c 'perScenario' packages/vitest/README.md` returns at least 4 (the new paragraph plus the worked example).
    - The README contains exactly one NEW fenced ` ```ts ` block relative to the pre-task file — record the before/after fence counts in the summary.
    - `pnpm lint` exits 0, which includes `dprint check` over `packages/vitest/README.md` and its fenced `ts` block.
    - `pnpm build` exits 0 — `index.ts` still compiles after the doc-comment edit.
    - `pnpm verify:pack` exits 0 — the README is still shipped in the tarball.
    - `pnpm test` exits 0 with an unchanged count.
  </acceptance_criteria>
  <done>
    Neither the package barrel's doc comment nor the package README claims the `shared` Layer is
    unbuilt, and the README carries one small worked example whose shape mirrors the fixture that is
    actually run.
  </done>
</task>

<task type="auto">
  <name>Task 2: Invariants, behaviors, overview, and ADR-EC-018's implementation note</name>
  <files>spec/invariants.md, spec/behaviors/02-shared-layers-and-tags.md, spec/behaviors/03-rules-outlines-and-testclock.md, spec/overview.md, spec/decisions/018-shared-layer-testclock-isolation.md</files>
  <read_first>
    - spec/invariants.md lines 1-70 — the preamble's four-are-enforced sentence and INV-EC-002's
      full entry including its "still **planned**" paragraph.
    - spec/behaviors/02-shared-layers-and-tags.md lines 85-100 — BEH-EC-007 and its REQUIREMENT
      block, including the RELEASE clause.
    - spec/behaviors/03-rules-outlines-and-testclock.md lines 76-135 — BEH-EC-012's REQUIREMENT
      block and the worked example beneath it with its pre-implementation caveat.
    - spec/overview.md IN FULL (73 lines) — the `TestClock` claim at line 9, the Packages table's
      Status cells, and the "Public API surface" section.
    - spec/decisions/007-cucumber-expressions-for-step-matching.md — read ONE of its correction
      blockquotes to copy the format: a dated blockquote appended in place, with the superseded
      sentence marked rather than rewritten (ADR-EC-014's precedent, recorded in STATE.md's 03-06
      entry as "55 additions and 0 deletions").
    - packages/vitest/src/describeFeature.ts as left by plan 10-02, and the installed
      `@effect/vitest`'s `dist/internal/internal.js` `layer` implementation — needed to VERIFY
      BEH-EC-007's release clause rather than assume it.
  </read_first>
  <action>
    `spec/invariants.md`: rewrite the preamble sentence about INV-EC-002 waiting on Phase 10, and
    replace INV-EC-002's "still **planned**" paragraph with the true statement plus its mechanism and
    its assertions. The mechanism is: on the `shared` path the shared tier is provided by
    `@effect/vitest`'s `layer(...)` at the block level and is NOT re-provided inside any Scenario
    Effect, while the per-Scenario tier is still supplied once around each Scenario Effect and still
    rebuilt on every execution. Name the assertions by file and by what they assert — the
    `[1, 1, 1]` shared-ordinals and `[1, 2, 3]` per-Scenario-ordinals pair in
    `packages/vitest/test/emission.test.ts`, and `scripts/verify-shared-layer-once.sh` for the
    whole-versus-filtered half. State explicitly that "fresh every Scenario" remains true of the
    per-Scenario tier on BOTH scopes and is deliberately false of the shared tier, which is the
    entire point of asking for one.

    `spec/behaviors/02-shared-layers-and-tags.md`, BEH-EC-007: VERIFY the release clause before
    touching it. Read `@effect/vitest`'s `layer` implementation and determine when `closeScope` is
    actually registered on the one-argument call form this library uses — including the branch taken
    when the callback's own emissions land inside a deferred `describe` factory. If the resource is
    released after every Scenario in the Feature has run, say the clause holds and name the
    mechanism. If it is released later than that (for example at the enclosing file suite's
    teardown), say THAT, in a dated correction blockquote beneath the REQUIREMENT block, in
    ADR-EC-007's format. Do not rewrite the REQUIREMENT text to match whatever was found; mark it in
    place. A spec that quietly narrows its own requirement to fit the implementation is worse than
    one that records the divergence.

    `spec/behaviors/03-rules-outlines-and-testclock.md`: remove the
    `// Pre-implementation reference — not yet compiled against a real API.` caveat above BEH-EC-012's
    worked example ONLY IF the example is now genuinely compilable against the real API. Read it
    line by line first: it calls `await loadFeature(...)` against ADR-EC-024's Promise-returning
    `loadFeature`, which may or may not exist in `@effect-cucumber/vitest` today. If any part of the
    example does not correspond to a real export, LEAVE the caveat and instead narrow it to name
    exactly which part is still pre-implementation, so the `TestClock` claim it sits above is no
    longer hedged by association. Record the finding either way. BEH-EC-012's REQUIREMENT block
    itself already states the target unconditionally and needs no edit.

    `spec/overview.md`: the `TestClock`/`TestConsole` claim at line 9 is already unconditional and is
    now true on both scopes — leave it, and record in the summary that D-05's "unstated-exception
    hedge" was located in `spec/behaviors/03`'s worked example rather than here, matching the pattern
    map's finding. Fix the two statements in this file that ARE now false: the Packages table's
    `@effect-cucumber/vitest` Status cell reading "Not yet implemented", and the "Public API surface"
    section's "Not yet implemented — there is no package to export from." Replace both with what is
    true, pointing at `spec/roadmap.md` as the single authority on build status exactly as the
    `@effect-cucumber/gherkin` row already does. Do not attempt to enumerate the whole public export
    table in this task — say the surface exists, name where it is documented, and leave the
    `check-api-surface` note as the still-planned thing it is.

    `spec/decisions/018-shared-layer-testclock-isolation.md`: append a dated implementation note in
    the correction-blockquote format, recording four things the ADR could not know when it was
    written: that `TestEnv` is NOT exported by `@effect/vitest` and had to be reconstructed from the
    public `effect/testing/TestConsole` and `effect/testing/TestClock` modules as
    `Layer.mergeAll(TestConsole.layer, TestClock.layer())`; that the ONE-ARGUMENT form of `layer(...)`
    is used rather than the two-argument form in the ADR's own sketch, because the two-argument form
    opens its own block and would nest a second Feature-named block around the runner's own; that the
    per-Scenario `TestEnv` provide is applied at the emission boundary in `describeFeature.ts` rather
    than inside `ScenarioEffect.ts`, keeping the Scenario builder free of any knowledge that two paths
    exist; and that Pitfall 29's consequence is accepted — the shared block's `it` has no live-clock
    member, so the two paths do not have identical capability surfaces. Name the tests and the gate
    script that enforce the decision. Additions only; change no existing sentence.
  </action>
  <verify>
    <automated>pnpm format &amp;&amp; pnpm lint &amp;&amp; pnpm verify:spec</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'waits on Phase 10' spec/invariants.md` returns 0 and `grep -c 'is still \*\*planned\*\*' spec/invariants.md` returns 0 in the INV-EC-002 entry — confirm by reading, not only by grep, that no other invariant's legitimate "planned" label was removed.
    - `grep -c 'verify-shared-layer-once' spec/invariants.md` returns at least 1.
    - `spec/decisions/018-shared-layer-testclock-isolation.md` grows by ADDITIONS ONLY: `git diff --numstat spec/decisions/018-shared-layer-testclock-isolation.md` shows a deletions count of 0.
    - `grep -c 'Not yet implemented' spec/overview.md` returns 0.
    - `pnpm verify:spec` exits 0 — no broken relative markdown link was introduced by any new cross-reference.
    - `pnpm lint` exits 0, which includes `dprint check` over every edited `spec/**/*.md`.
    - The summary records: the BEH-EC-007 release-clause finding with the exact `layer` implementation branch that produced it; and the BEH-EC-012 worked-example finding with the specific reason the caveat was removed, narrowed, or kept.
  </acceptance_criteria>
  <done>
    Every invariant, behavior and decision document states what is true about both Layer scopes, and
    ADR-EC-018 carries an implementation note recording the four things its own sketch could not.
  </done>
</task>

<task type="auto">
  <name>Task 3: Roadmap, traceability, requirements, and the full gate run</name>
  <files>spec/roadmap.md, spec/traceability.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - spec/roadmap.md lines 40-75 and lines 110-140 — the two separate statements that the
      build-once `shared` Layer is what remains, and the "Planned" section beneath them.
    - spec/traceability.md — the §1 preamble and the behaviors/02 row (which names a
      `SharedLayer.ts` that does not exist), the §3 ADR-EC-018 row at line 123, and §4's preamble
      paragraph naming exactly TWO deliberate non-suite entries.
    - .planning/REQUIREMENTS.md lines 35-45 and 98-108 — the RUN-03/RUN-04 checkbox entries and
      their status-table rows.
    - .planning/phases/10-layer-scopes-per-scenario-default-shared/10-01-SUMMARY.md through
      10-05-SUMMARY.md — every claim written into the spec in this task must be traceable to one of
      them. Do not write a claim no summary supports.
    - .planning/STATE.md, the "New since 03-06" block — "`spec/traceability.md` §4 is enumerated
      from disk. A plan that adds a test file adds its row in the same commit. The reusable guard is
      03-06's `node -e` cross-check."
  </read_first>
  <action>
    `spec/roadmap.md`: rewrite both statements that name the build-once `shared` Layer as what
    remains — the one around lines 62-65 and the one around lines 129-130 — into the present tense,
    naming the mechanism (`excludeTestServices: true` plus a per-Scenario `TestEnv` provided at the
    emission boundary) and the type constraint (`shared` must be `Layer<R, never, never>`). State
    what genuinely remains for this package after Phase 10: Phase 11's dogfooded acceptance suite and
    the doc-examples compile check, both of which the file already lists elsewhere. Do not invent new
    remaining work.

    `spec/traceability.md` §1: correct the behaviors/02 row. Remove `SharedLayer` from its source
    module list and remove the "only `SharedLayer` remains planned (Phase 10)" clause. Replace it
    with the truth — the shared-Layer path is delivered by `describeFeature.ts` (the composition
    root that calls the framework's `layer(...)` and constructs the second `TestApi`), with
    `Runner.ts` and `ScenarioEffect.ts` unchanged in behaviour on that path — and cite the preamble's
    own "no `Rule.ts` and no `ScenarioOutline.ts`" argument for why no module bearing that name was
    created. Add `TestApi.ts` if it is not already listed for that row; it is.

    `spec/traceability.md` §3: fill the ADR-EC-018 row's two "—" cells. Its Invariant column is
    INV-EC-002 (link it exactly as the ADR-EC-006 row links the same invariant). Its Test column
    names `packages/vitest/test/emission.test.ts` (the clock-isolation block and the shared
    build-count block) and `scripts/verify-shared-layer-once.sh`. Check the ADR-EC-006 row's Test
    cell too — it is also "—" today and this phase is what gives it one.

    `spec/traceability.md` §4: add a row for `packages/vitest/test/SharedLayerConstraint.types.ts`
    and update the preamble paragraph, which currently says "TWO deliberate non-suite entries" and
    names both. It becomes THREE, and the new one's justification sentence must say what the other
    two's say for themselves: this file is the ONLY place its claim is asserted — that a `shared`
    Layer with a non-`never` error channel does not compile — which no runtime assertion can check,
    because the defect it prevents is a compile-time one. Then RUN 03-06's `node -e` cross-check (or
    reproduce it) over `packages/gherkin/test/*.test.ts` and `packages/vitest/test/*.test.ts` and
    confirm every file on disk has a row and every row names a file on disk. Record its output.

    `.planning/REQUIREMENTS.md`: mark RUN-03 and RUN-04 Complete in BOTH places — the checkbox
    entries at lines 39-40 and the status-table rows at lines 102-103 — following the exact format
    the already-Complete requirements use. Mark them TOGETHER: RUN-04 is not separable from RUN-03
    here, since the `excludeTestServices` fix and the build-once path are one code path, and every
    preceding phase's precedent is that the plan making a requirement true end to end is the one that
    marks it.

    Finally, run the FULL gate set and record every exit code in the summary:
    `pnpm build`, `pnpm typecheck:test`, `pnpm lint`, `pnpm test`, `pnpm circular`,
    `pnpm verify:pack`, `pnpm verify:spec`, `pnpm verify:tsgo-gate`, `pnpm verify:oxlint-plugin`,
    `pnpm verify:no-runner-dep`, `pnpm verify:testapi-seam`, `pnpm verify:tags-filter`,
    `pnpm verify:shared-layer-once`. All thirteen must exit 0. This is the phase's closing check and
    the last chance to catch a document that claims something the code no longer does.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm typecheck:test &amp;&amp; pnpm lint &amp;&amp; pnpm test &amp;&amp; pnpm circular &amp;&amp; pnpm verify:pack &amp;&amp; pnpm verify:spec &amp;&amp; pnpm verify:tsgo-gate &amp;&amp; pnpm verify:oxlint-plugin &amp;&amp; pnpm verify:no-runner-dep &amp;&amp; pnpm verify:testapi-seam &amp;&amp; pnpm verify:tags-filter &amp;&amp; pnpm verify:shared-layer-once</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SharedLayer' spec/traceability.md` returns 1 — the only surviving occurrence is the new `SharedLayerConstraint.types.ts` §4 row; the phantom `SharedLayer.ts` module is gone from §1.
    - `grep -c 'SharedLayerConstraint.types.ts' spec/traceability.md` returns at least 2 (the §4 preamble's justification sentence and the §4 row).
    - `grep -c 'TWO deliberate non-suite entries' spec/traceability.md` returns 0 — the count was updated, not left stale.
    - The §3 ADR-EC-018 row contains no `—` in either its Invariant or its Test column; assert by reading that row and record it verbatim in the summary.
    - `grep -c 'build-once' spec/roadmap.md` returns 0, or every remaining occurrence is in the past tense describing what Phase 10 delivered — read each one and state which in the summary.
    - `.planning/REQUIREMENTS.md` shows RUN-03 and RUN-04 as Complete in BOTH the checkbox list and the status table: `grep -c 'RUN-03.*Complete' .planning/REQUIREMENTS.md` returns at least 1 and the same for RUN-04, and `grep -c 'RUN-03.*Pending' .planning/REQUIREMENTS.md` returns 0 and the same for RUN-04.
    - The §4 disk cross-check was RUN and its output recorded: every `packages/*/test/*.test.ts` file on disk has a row, and every row names a file that exists.
    - All THIRTEEN gate commands exit 0, each recorded by name with its exit code in the summary.
  </acceptance_criteria>
  <done>
    The roadmap, the traceability chain and the requirements register all state what Phase 10
    actually built, no document names a module that does not exist, and the full thirteen-gate set
    is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| what the code does → what the documents claim | the only boundary this plan operates on; AGENTS.md §4 makes a divergence a defect rather than a nit |
| `spec/traceability.md` rows → files on disk | two registries with no compiler between them; `spec/scripts/verify-traceability.sh` and the §4 disk cross-check are the enforcement |
| a README fenced `ts` example → the real public API | an example that no longer compiles is a consumer-facing defect that nothing in this repo currently catches automatically |

No network, no authentication, no persistence, no runtime user input. The risk class is
DOCUMENTATION DRIFT — a document that claims a guarantee the code does not deliver, or claims a
gap the code has already closed. Both mislead a reader with nothing red.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Repudiation | docs claiming a guarantee the code does not deliver | mitigate | Every claim written into `spec/` in this plan must be traceable to one of the five phase summaries, and Task 2 explicitly REQUIRES verifying BEH-EC-007's release clause against the framework's own `layer` implementation before restating it. Where the finding contradicts the spec, the spec records the divergence in a dated correction blockquote rather than being narrowed to fit |
| T-10-06-02 | Tampering | `spec/traceability.md` naming a module that does not exist | mitigate | §1's phantom `SharedLayer.ts` is removed and a grep criterion pins the only surviving `SharedLayer` occurrence to the new `.types.ts` row; §4's disk cross-check is RUN in both directions and its output recorded |
| T-10-06-03 | Tampering | a stale count in §4's preamble | mitigate | The "TWO deliberate non-suite entries" sentence is asserted to be gone by grep, so adding a row without updating the sentence that counts them cannot pass |
| T-10-06-04 | Information Disclosure | the README's fenced `ts` example | accept | The example uses a fake in-memory counter and names no credential, endpoint or path. The doc-examples compile check that would keep it compiling is Phase 11's, and `spec/roadmap.md` already lists it as planned — this plan does not claim otherwise |
| T-10-06-05 | Repudiation | marking RUN-03/RUN-04 Complete prematurely | mitigate | The marking is the LAST action of the LAST plan of the phase and is gated behind all thirteen verification commands exiting 0, including the two gates written in this phase. Every preceding phase set the same precedent — the plan that makes a requirement true end to end is the one that marks it |
| T-10-06-06 | Denial of Service | a broken relative link in spec | mitigate | `pnpm verify:spec` check 5 rejects a broken relative markdown link and treats a gitignored-but-present target as broken; it is in this plan's verify command and in the CI `package` job |
| T-10-06-SC | Tampering | npm/pip/cargo installs | n/a — accept | This plan installs no package and changes no manifest dependency; `pnpm-lock.yaml` is untouched |
</threat_model>

<verification>
- `pnpm format` then `pnpm lint` exits 0, covering `dprint check` over every edited markdown file
  including fenced `ts` blocks.
- `pnpm verify:spec` exits 0.
- `pnpm verify:pack` exits 0 — the edited README still ships in the tarball.
- The §4 disk cross-check runs clean in both directions, output recorded.
- All thirteen gate commands exit 0: `build`, `typecheck:test`, `lint`, `test`, `circular`,
  `verify:pack`, `verify:spec`, `verify:tsgo-gate`, `verify:oxlint-plugin`, `verify:no-runner-dep`,
  `verify:testapi-seam`, `verify:tags-filter`, `verify:shared-layer-once`.
</verification>

<success_criteria>
No document in this repository still describes the build-once `shared` Layer or its per-Scenario
`TestClock`/`TestConsole` isolation as unbuilt. `packages/vitest/README.md` carries one small
worked example of the `{ shared, perScenario }` call form mirroring the fixture that is actually
run. ADR-EC-018 carries a dated implementation note. `spec/traceability.md` names real modules and
real tests for BEH-EC-007 and ADR-EC-018. RUN-03 and RUN-04 are marked Complete, and every gate in
the repository is green.
</success_criteria>

<output>
Create `.planning/phases/10-layer-scopes-per-scenario-default-shared/10-06-SUMMARY.md` when done.
It must record: the BEH-EC-007 release-clause finding and the exact `layer` implementation branch
behind it; the BEH-EC-012 worked-example decision (caveat removed, narrowed, or kept, with the
reason); the §4 disk cross-check output; the §3 ADR-EC-018 row verbatim; the before/after fenced
`ts` block counts in the README; and all thirteen gate commands with their exit codes.
</output>
