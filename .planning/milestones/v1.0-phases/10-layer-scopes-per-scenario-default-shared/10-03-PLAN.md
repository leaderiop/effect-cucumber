---
phase: 10-layer-scopes-per-scenario-default-shared
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - packages/vitest/test/emission.test.ts
autonomous: true
requirements: [RUN-03]

must_haves:
  truths:
    - "A Feature with three Scenarios and a plain (per-Scenario) Layer builds that Layer three times — one build per Scenario (D-01)."
    - "State written into a per-Scenario service in one Scenario is not observable in the next."
    - "The same Feature declared with `{ shared, perScenario }` builds the `shared` Layer exactly ONCE for all three Scenarios, while its `perScenario` half still builds three times (D-01)."
    - "A service named by BOTH `shared` and `perScenario` resolves to `perScenario`'s implementation inside a running step (D-04, now delivered by provision order rather than merge order)."
  artifacts:
    - path: "packages/vitest/test/emission.test.ts"
      provides: "two new appended blocks: the default-path build-count proof and the shared-path build-count proof"
      contains: "sharedBuildOrdinals"
  key_links:
    - from: "packages/vitest/test/emission.test.ts"
      to: "packages/vitest/src/describeFeature.ts"
      via: "a real describeFeature call with the { shared, perScenario } argument form"
      pattern: "describeFeature\\([a-zA-Z]+, \\{ shared:"
    - from: "packages/vitest/test/emission.test.ts"
      to: "@effect-cucumber/gherkin's parseFeature"
      via: "an inline template-literal Feature source, never a hand-built ParsedFeature"
      pattern: "parseFeature\\("
---

<objective>
Prove roadmap Phase 10 Success Criteria 1 and 2 with real `describeFeature` runs and real build
counters: the default per-Scenario scope builds once per Scenario (N Scenarios, N builds) and the
opt-in `shared` scope builds exactly once for the whole Feature (N Scenarios, 1 build).

This is D-01's in-process half. It runs in the ordinary `pnpm test` suite and gives fast feedback.
Plan 10-05's CLI gate is the other half and does not replace it — `10-CONTEXT.md` D-02 says
plainly that both checks are wanted.

Purpose: RUN-03. Without a build COUNT, "shared" and "per-Scenario" are indistinguishable — every
step resolves either way and every existing assertion in this repo stays green under a shared Layer
that is silently rebuilt per Scenario (ARCHITECTURE.md Anti-Pattern 3).
Output: two new `describeFeature` blocks and their reader blocks, appended at the end of
`emission.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-layer-scopes-per-scenario-default-shared/10-CONTEXT.md
@.planning/phases/10-layer-scopes-per-scenario-default-shared/10-PATTERNS.md
@.planning/phases/10-layer-scopes-per-scenario-default-shared/10-02-SUMMARY.md
@spec/decisions/006-two-layer-scopes-only.md
</context>

<interfaces>
<!-- Fixture idioms already established in packages/vitest/test/emission.test.ts. Reuse them
     verbatim rather than inventing a second convention. -->

Module-scope helpers already declared in that file and available to any appended block:
  `const currentTestName = (): string => expect.getState().currentTestName ?? ""`   (line 195)
  `const nameSeparator = " > "`                                                      (line 203)

The Rule-composition block (line 728 onward) is the structural precedent for every appended block:
  a `Context.Service` subclass per tier; a `Layer.succeed` or `Layer.effect` per tier; a module-scope
  `Ref.makeUnsafe` or plain array for what the Scenarios record; an inline template-literal Feature
  parsed by `parseFeature(source, uri).pipe(Effect.provide(ParameterTypeStore.Default))` run through
  `Effect.runSync`; ONE real `describeFeature` call; and a trailing `describe`/`it` reader block that
  asserts what only a completed run can see.

Imports already present at the top of the file (line 141 onward) — extend, do not replace:
  `ParameterTypeStore`, `parseFeature` from `@effect-cucumber/gherkin`
  `assert`, `beforeAll`, `describe`, `expect`, `it` from `@effect/vitest`
  `* as Context`, `* as Effect`, `* as Layer`, `* as Ref` from their `effect/*` submodules
  `collectFeature`, `describeFeature` from `../src/describeFeature.ts`  (never `../src/index.ts`)

`describeFeature`'s object argument form, as left by plan 10-01:
  `describeFeature(feature, { shared: Layer.Layer<R1, never, never>, perScenario: Layer.Layer<R2, E, never> }, define)`
  `perScenario` is REQUIRED; a Feature with no per-Scenario tier writes `perScenario: Layer.empty`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Default path — three Scenarios, three Layer builds, no state carried across</name>
  <files>packages/vitest/test/emission.test.ts</files>
  <behavior>
    - A Feature with three Scenarios and a plain `Layer.effect` ambient Layer records build ordinals `[1, 2, 3]` — one build per Scenario, in Scenario order.
    - A `Ref` created INSIDE that Layer and written by Scenario one reads back EMPTY in Scenario two — per-Scenario freshness at the state level, not just the build-count level.
    - All three Scenarios' step bodies ran, asserted by name through `currentTestName()`, so a Feature that emitted nothing cannot pass this block vacuously.
  </behavior>
  <read_first>
    - packages/vitest/test/emission.test.ts lines 1-200 — the file's module doc comment IN FULL.
      Three rules there govern this task: every Feature is parsed from an inline template literal
      by the real `parseFeature`; every new block is APPENDED after every existing block because
      readers depend on vitest's declaration-order suite execution; and every new assertion needs
      its own recorded mutation proof in the header's own list format.
    - packages/vitest/test/emission.test.ts lines 728-925 — the Rule-composition block, which is
      the closest structural analog: services, layers, fixture Feature, one real `describeFeature`
      call, and a trailing reader block.
    - packages/vitest/test/emission.test.ts lines 380-400 — the `completedScenarios` reader, the
      precedent for "count the emissions from the inside so a Feature that emitted nothing fails".
    - spec/invariants.md, INV-EC-002 — the per-Scenario freshness invariant this block proves.
  </read_first>
  <action>
    Append a new block at the END of `packages/vitest/test/emission.test.ts`, after every existing
    block including Phase 9's tag blocks. Open it with a doc comment stating why it is appended last
    (the file's own declaration-order rule) and what only a real run can see here.

    Declare a `Context.Service` subclass named `PerScenarioProbe` carrying two readonly fields: a
    `buildOrdinal: number` and an `entries: Ref.Ref<ReadonlyArray<string>>`. Declare a module-scope
    `let perScenarioBuilds = 0`. Build the Layer with `Layer.effect`, whose body increments
    `perScenarioBuilds`, creates a FRESH `Ref` with `Ref.make([])`, and returns the service carrying
    the post-increment counter value as `buildOrdinal`. The `Ref` must be created INSIDE the Layer's
    build effect — that is what makes the second assertion about per-Scenario STATE rather than about
    the counter again. `Layer.effect` and not `Layer.succeed`: only the effectful constructor has a
    build-time body to count in.

    Declare a module-scope `const perScenarioBuildOrdinals: Array<number> = []` and a
    `const perScenarioScenarioNames: Array<string> = []`.

    Parse a three-Scenario Feature from an inline template literal, uri `"test/per-scenario-build-count.feature"`,
    with these EXACT Scenario titles — plan 10-05's CLI gate asserts on them by exact suffix match,
    so a rename must be made in both places:
      `the first per-scenario scenario records its own build`
      `the second per-scenario scenario sees a fresh build`
      `the third per-scenario scenario sees a third build`
    Give each Scenario a `When` and a `Then` step so both a body and a recorder exist, following the
    existing blocks' two-step shape.

    Register the steps through ONE real `describeFeature(feature, perScenarioLayer, define)` call —
    the PLAIN Layer form, which is the default per-Scenario scope. Each Scenario's `When` body reads
    `PerScenarioProbe`, pushes its `buildOrdinal` into `perScenarioBuildOrdinals`, and appends a
    marker string to its own `entries` `Ref`. The SECOND Scenario's `When` body must additionally
    assert its `entries` `Ref` was EMPTY before it wrote — that is SC#1's "service state set in one
    Scenario is unobservable in the next", and it is a different claim from the build count. Each
    `Then` body pushes `currentTestName()` into `perScenarioScenarioNames`.

    Add a trailing `describe`/`it` reader block asserting `perScenarioBuildOrdinals` equals
    `[1, 2, 3]` exactly, and `perScenarioScenarioNames` equals the three full names joined with
    `nameSeparator` under the Feature's own name. The ordinals assertion is what carries N-builds:
    a memoized or hoisted Layer produces `[1, 1, 1]`, and every step still resolves, so nothing else
    in this repo goes red.

    Use `assert` (never `expect`) inside every step body — oxlint's `vitest/no-standalone-expect`
    does not recognise an Effect-bodied test as a test block. `expect` is fine inside the
    synchronous reader `it`.

    MUTATION-TEST and record in the file's header mutation list plus the plan summary: replace the
    `Layer.effect` per-Scenario Layer with a `Layer.succeed` built once at module scope. The
    ordinals assertion must go red with `[1, 1, 1]`; confirm the per-Scenario `Ref` emptiness
    assertion goes red too, and confirm NOTHING outside this block fails. Revert.
  </action>
  <verify>
    <automated>pnpm test &amp;&amp; pnpm typecheck:test &amp;&amp; pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'per-scenario-build-count.feature' packages/vitest/test/emission.test.ts` returns exactly 1.
    - `grep -c 'Scenario: the first per-scenario scenario records its own build' packages/vitest/test/emission.test.ts` returns exactly 1, and the same for the other two titles.
    - `grep -c 'perScenarioBuildOrdinals' packages/vitest/test/emission.test.ts` returns at least 3 (declaration, three pushes collapse into one shared step body or three, plus the reader assertion).
    - `grep -c 'Layer.effect(' packages/vitest/test/emission.test.ts` increased by exactly 1 relative to the pre-task file; record both counts in the summary.
    - `pnpm test` exits 0 and its reported test count increased by exactly 7 (three Scenarios plus the two reader `it` blocks plus any warning nodes actually emitted — record the exact before/after numbers and reconcile any difference explicitly rather than adjusting this criterion).
    - `pnpm lint` exits 0 — no `expect` appears inside any Effect-bodied step body in the new block: `grep -n 'expect(' packages/vitest/test/emission.test.ts` shows no new occurrence inside a `function*()` body.
    - The summary records the `Layer.succeed` mutation with the exact observed `[1, 1, 1]` failure and confirmation that no test outside this block failed.
  </acceptance_criteria>
  <done>
    The default per-Scenario scope's N-builds property and its cross-Scenario state isolation are
    both asserted by a real run, and the assertion is mutation-proven non-vacuous.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared path — three Scenarios, ONE shared build, three per-Scenario builds</name>
  <files>packages/vitest/test/emission.test.ts</files>
  <behavior>
    - A Feature declared with `{ shared, perScenario }` and three Scenarios records shared build ordinals `[1, 1, 1]` — every Scenario reached the SAME single build.
    - The same Feature's `perScenario` half still records `[1, 2, 3]` — the shared fix did not memoize the per-Scenario tier along with it.
    - Each Scenario's own body asserts the shared build count is exactly 1 AT THE MOMENT IT RUNS, so the claim is observable from outside the process as that test's pass/fail status (plan 10-05 depends on this).
    - A service named by BOTH `shared` and `perScenario` resolves to `perScenario`'s implementation inside a running step (D-04).
  </behavior>
  <read_first>
    - packages/vitest/test/emission.test.ts lines 1-200 and the block Task 1 just appended — the
      new block goes after it, same idioms, its own counters.
    - packages/vitest/src/describeFeature.ts as left by plan 10-02 — the `sharedLayerTestApi`
      factory and the `layer(..., { excludeTestServices: true })` routing branch this block is the
      first real exercise of.
    - packages/vitest/test/describeFeature.test.ts lines 90-130 and 305-340 — the `Marker` service
      idiom and the D-04 collision fixture (`sharedMarker` / `perScenarioMarker`), whose RUNTIME
      verdict plan 10-02 deferred to this block by name.
    - spec/decisions/006-two-layer-scopes-only.md — the two-scope design and the collision rule.
  </read_first>
  <action>
    Append a second new block at the END of the file, after Task 1's.

    Declare two `Context.Service` subclasses: `SharedProbe` carrying `readonly buildOrdinal: number`,
    and `ScopedProbe` carrying `readonly buildOrdinal: number`. Declare a third,
    `CollisionMarker`, carrying `readonly who: string` — this one is provided by BOTH tiers with
    different values and is what proves D-04 at run time.

    Declare module-scope `let sharedBuilds = 0` and `let scopedBuilds = 0`, plus module-scope arrays
    `sharedBuildOrdinals`, `scopedBuildOrdinals`, `collisionWinners` and `sharedScenarioNames`.

    Build the shared Layer as `Layer.mergeAll` of a `Layer.effect` for `SharedProbe` (incrementing
    `sharedBuilds`) and a `Layer.succeed` for `CollisionMarker` with `who: "shared"`. Build the
    per-Scenario Layer as `Layer.mergeAll` of a `Layer.effect` for `ScopedProbe` (incrementing
    `scopedBuilds`) and a `Layer.succeed` for `CollisionMarker` with `who: "perScenario"`. Both
    Layers must have a `never` error channel — plan 10-01's constraint makes a failable `shared`
    Layer a compile error, and this block must not be the thing that discovers that.

    Parse a three-Scenario Feature from an inline template literal, uri
    `"test/shared-build-count.feature"`, with these EXACT Scenario titles — plan 10-05's CLI gate
    asserts on them by exact suffix match:
      `the first shared scenario observes the single shared build`
      `the second shared scenario observes the same shared build`
      `the third shared scenario observes the same shared build`

    Register through ONE real `describeFeature(feature, { shared: sharedLayer, perScenario: scopedLayer }, define)`
    call. Each Scenario's `When` body must, IN THIS ORDER: assert `sharedBuilds` is exactly 1 (the
    module-scope counter, read at the moment this Scenario runs — this is the assertion plan 10-05
    observes from outside the process, so it must be the FIRST thing that can fail); read
    `SharedProbe` and push its `buildOrdinal`; read `ScopedProbe` and push its `buildOrdinal`; read
    `CollisionMarker` and push its `who`. Each `Then` body pushes `currentTestName()` into
    `sharedScenarioNames`.

    Add a trailing reader block asserting FOUR things: `sharedBuildOrdinals` equals `[1, 1, 1]`;
    `scopedBuildOrdinals` equals `[1, 2, 3]`; `collisionWinners` equals
    `["perScenario", "perScenario", "perScenario"]`; and `sharedScenarioNames` equals the three full
    names under the Feature's own name. The second of those is not decoration — a fix that memoized
    BOTH tiers would satisfy the first assertion and break INV-EC-002 for every Feature that asked
    for a shared scope, and only the `[1, 2, 3]` assertion sees it.

    The `collisionWinners` assertion is D-04's runtime home. Add a comment saying so explicitly, and
    saying that the mechanism changed in plan 10-02 from `Layer.merge`'s argument order to provision
    order — the shared tier is ambient on the test node and the per-Scenario tier is provided inside
    the Scenario's own Effect, so the inner provide wins — while the RULE it delivers is unchanged.

    MUTATION-TEST and record three proofs, each performed, run, then reverted:
    (i) `describeFeature.ts`'s shared branch made to pass `vitestTestApi(...)` instead of
        `sharedLayerTestApi(...)` — i.e. the module-level `it`, ARCHITECTURE.md Anti-Pattern 3
        exactly. Record which assertions go red and confirm the shared Layer is then built three
        times.
    (ii) `splitLayerArgument` reverted to the pre-10-02 `Layer.merge(shared, perScenario)` collapse
        — record the failures, which should include both the shared ordinals and the per-Scenario
        ordinals.
    (iii) the two `Layer.succeed(CollisionMarker, ...)` arguments swapped between the tiers — only
        the `collisionWinners` assertion may go red, which is what proves it is a real collision
        test rather than a tautology.
  </action>
  <verify>
    <automated>pnpm test &amp;&amp; pnpm typecheck:test &amp;&amp; pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'shared-build-count.feature' packages/vitest/test/emission.test.ts` returns exactly 1.
    - `grep -c 'Scenario: the first shared scenario observes the single shared build' packages/vitest/test/emission.test.ts` returns exactly 1, and the same for the other two titles.
    - `grep -c 'describeFeature(.*{ shared:' packages/vitest/test/emission.test.ts` returns exactly 1 — this is the file's FIRST object-form `describeFeature` call.
    - `grep -c 'sharedBuildOrdinals' packages/vitest/test/emission.test.ts` returns at least 2 and `grep -c 'scopedBuildOrdinals' packages/vitest/test/emission.test.ts` returns at least 2.
    - `grep -c 'collisionWinners' packages/vitest/test/emission.test.ts` returns at least 2.
    - `pnpm test` exits 0; record the exact before/after test counts and reconcile the delta against the number of emitted nodes.
    - `pnpm typecheck:test` exits 0 — in particular the `{ shared, perScenario }` call compiles under plan 10-01's narrowed overload, which is a live check that the constraint did not become total.
    - `pnpm lint` exits 0.
    - The summary records all three mutation proofs (i), (ii) and (iii) with the exact set of tests that went red for each, and confirms each was reverted.
  </acceptance_criteria>
  <done>
    The `shared` Layer is proven built exactly once across three Scenarios by a real run, the
    per-Scenario tier is proven still fresh per Scenario in the same run, D-04's collision rule has
    a runtime proof again, and the Anti-Pattern 3 mutation is demonstrated to turn the block red.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a `shared` Layer's single build → three Scenarios | the boundary this plan measures; anything mutable crossing it that was not asked for is a leak |
| the assertions in this file → the claim they are supposed to make | a test that passes for the wrong reason is the failure mode this repo mutation-tests against |

No network, no authentication, no persistence, no runtime user input. The risk is entirely
test-isolation correctness and assertion vacuity.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering | the shared-path build counter | mitigate | Build ordinals are recorded from inside the SERVICE VALUE (`buildOrdinal` captured at build time) as well as from a module-scope counter; a counter alone can be satisfied by a Layer that builds once and is then handed to nobody, whereas an ordinal read out of the resolved service proves each Scenario actually reached that build |
| T-10-03-02 | Tampering | the `[1, 1, 1]` shared assertion in isolation | mitigate | Paired with a `[1, 2, 3]` assertion on the per-Scenario tier in the SAME Feature. A fix that memoized both tiers passes the first and silently breaks INV-EC-002; only the pair distinguishes them |
| T-10-03-03 | Repudiation | a Feature that emits nothing | mitigate | Both blocks carry a `currentTestName()` recorder asserted against an exact array, which is `emission.test.ts`'s own mutation C lesson: a suite cannot notice a test that was never registered by looking at the tests that were |
| T-10-03-04 | Tampering | the D-04 collision assertion | mitigate | Mutation (iii) swaps the two tiers' `CollisionMarker` values and must turn ONLY that assertion red, proving it is not a tautology satisfied by either arrangement |
| T-10-03-05 | Denial of Service | module-scope counters shared between blocks | mitigate | Each block brings its OWN counters and its OWN service classes, the rule the existing tag blocks already follow; a shared counter would make one block's assertion depend on another block's arrangement |
| T-10-03-06 | Information Disclosure | n/a | accept | Nothing in these blocks reads or writes anything outside the test process |
| T-10-03-SC | Tampering | npm/pip/cargo installs | n/a — accept | No package is installed and no manifest changes; `pnpm-lock.yaml` is untouched |
</threat_model>

<verification>
- `pnpm test` exits 0 with an increased, reconciled test count.
- `pnpm typecheck:test` exits 0.
- `pnpm lint` exits 0.
- `pnpm verify:tags-filter` exits 0 — the new blocks emit no tags, so the Phase 9 gate that runs
  this same file must be unaffected. If it is not, the new blocks introduced a tag; remove it.
- All five mutation proofs across the two tasks performed, recorded and reverted.
</verification>

<success_criteria>
Roadmap Phase 10 Success Criterion 1 is proven (default path: N Scenarios, N builds, and state not
carried across) and Success Criterion 2 is proven in process (shared path: N Scenarios, 1 build).
The Scenario titles this plan fixes are the ones plan 10-05's CLI gate asserts on by name.
</success_criteria>

<output>
Create `.planning/phases/10-layer-scopes-per-scenario-default-shared/10-03-SUMMARY.md` when done.
It must record: the five mutation proofs with their exact red output, the before/after `pnpm test`
counts at each task boundary with the delta reconciled, and the six Scenario titles verbatim so
plan 10-05 can copy them without re-deriving them.
</output>
