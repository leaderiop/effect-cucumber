---
phase: 02-loadfeature-parse-compile-correlate
plan: 06
type: execute
wave: 5
depends_on: ["02-05"]
files_modified:
  - packages/gherkin/test/Correlate.test.ts
autonomous: true
requirements: [PARSE-02]

must_haves:
  truths:
    - "An Outline with two Examples blocks yields one scenario per row, with per-block tags landing only on that block's rows"
    - "Two Features loaded in one process share no node ids"
    - "An Outline whose interpolated row names all differ still exposes the single un-interpolated AST name"
    - "An Outline whose rows share a name exposes distinct per-row locations, so Phase 6 can build unique titles"
    - "A step carrying both a DocString and a DataTable survives correlation with both arguments intact"
  artifacts:
    - path: "packages/gherkin/test/Correlate.test.ts"
      provides: "Group D correlation assertions F23, F24, F25, F26, F27"
      min_lines: 200
  key_links:
    - from: "packages/gherkin/test/Correlate.test.ts"
      to: "packages/gherkin/test/fixtures/outline-two-examples-blocks.feature"
      via: "the shared fixture-to-correlation helper from plan 02-05"
      pattern: "outline-two-examples-blocks"
---

<objective>
Close out PARSE-02 by extending `Correlate.test.ts` with the remaining Group D correctness
fixtures. Each pins a specific property that Phase 5, 6, or 8 will rely on, and that is expensive
to retrofit once a downstream phase has consumed the contract.

Purpose: success criterion 4 (un-interpolated name matching) and the raw material Phase 6 needs
for unique, `-t`-filterable test titles both live in this plan.
Output: `test/Correlate.test.ts` extended; no source changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loadfeature-parse-compile-correlate/02-RESEARCH.md
@.planning/phases/02-loadfeature-parse-compile-correlate/02-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Outline shape assertions — F24 two Examples blocks, F26 distinct row names, F27 identical row names</name>
  <files>packages/gherkin/test/Correlate.test.ts</files>
  <read_first>
    - packages/gherkin/test/Correlate.test.ts (the file being modified — reuse its existing fixture-to-correlation helper, do not duplicate it)
    - packages/gherkin/src/Correlate.ts (the CorrelationResult shape)
    - packages/gherkin/test/fixtures/outline-two-examples-blocks.feature
    - packages/gherkin/test/fixtures/outline-distinct-row-names.feature
    - packages/gherkin/test/fixtures/outline-identical-row-names.feature
    - .planning/phases/02-loadfeature-parse-compile-correlate/02-RESEARCH.md (fixture table Group D rows F24, F26, F27; the "Scope Boundary" note that this phase must expose `pickle.location` but must not decide the title format)
  </read_first>
  <action>
    Extend `packages/gherkin/test/Correlate.test.ts` with three new `describe` blocks. Reuse the
    existing fixture-to-correlation helper; do not add a second copy.

    F24, `outline-two-examples-blocks.feature`:
    - `allScenarios.length` is exactly 3 (two rows from block one, one from block two)
    - all three share the same `astId` — verified, every row of one Outline shares
      `astNodeIds[0]`, which is why `byScenarioId` is a one-to-many map
    - the two scenarios originating from the first block have `@blockone` in `tags` and do NOT
      have `@blocktwo`; the third has `@blocktwo` and not `@blockone`. Per-Examples-block tags
      land only on that block's rows.
    - each scenario's `tags` array preserves `compile()`'s flattening order, with the
      examples-block tag last

    F26, `outline-distinct-row-names.feature` — this is roadmap success criterion 4:
    - `allScenarios.length` is 2
    - the two `name` values are `outline a` and `outline b` and they differ from each other
    - BOTH scenarios have the identical `astName` `outline <name>` — the un-interpolated AST
      name. Assert `astName` with `toBe` on the exact literal, including the angle brackets.
    - add an inline comment recording WHY this matters: ARCHITECTURE.md Open Question 4 and this
      phase's success criterion 4 both settle on matching a Scenario to its registered definition
      by the un-interpolated name, which only works if that name survives correlation. Phase 6
      consumes this.

    F27, `outline-identical-row-names.feature`:
    - `allScenarios.length` is 3
    - all three `name` values are IDENTICAL (the Outline title references no column)
    - all three `location.line` values are DISTINCT — assert by collecting them into a `Set` and
      checking its size is 3
    - add an inline comment recording that this phase deliberately does NOT invent a test title:
      exposing `location` is this phase's job, deciding the title format is Phase 6's
      (Pitfalls 21/23, Gap 4).

    Every new `it` title must be unique across the WHOLE file, including the titles added in plan
    02-05 — `vitest/no-identical-title` is `"error"`. Prefix each title with its fixture row id
    (for example "F24: ...") to guarantee uniqueness.
  </action>
  <verify>
    <automated>pnpm vitest run packages/gherkin/test/Correlate.test.ts</automated>
    <automated>pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run packages/gherkin/test/Correlate.test.ts` exits 0 with strictly more passing
      tests than plan 02-05 left it with, and at least 18 in total.
    - `grep -c 'outline-two-examples-blocks\|outline-distinct-row-names\|outline-identical-row-names' packages/gherkin/test/Correlate.test.ts` is at least 3.
    - The exact un-interpolated literal is asserted:
      `grep -c 'outline <name>' packages/gherkin/test/Correlate.test.ts` is at least 1.
    - The distinct-lines assertion uses a `Set`:
      `grep -c 'new Set' packages/gherkin/test/Correlate.test.ts` is at least 1.
    - `grep -c '@blockone' packages/gherkin/test/Correlate.test.ts` is at least 1 and
      `grep -c '@blocktwo' packages/gherkin/test/Correlate.test.ts` is at least 1.
    - `pnpm lint` exits 0 (proves no duplicate `it` title across the file).
  </acceptance_criteria>
  <done>
    F24, F26, and F27 are asserted; both scenario names survive correlation and per-row locations
    are distinct.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-file id independence (F23) and dual step arguments (F25)</name>
  <files>packages/gherkin/test/Correlate.test.ts</files>
  <read_first>
    - packages/gherkin/test/Correlate.test.ts (the file being modified, after Task 1)
    - packages/gherkin/test/fixtures/id-collision-a.feature
    - packages/gherkin/test/fixtures/id-collision-b.feature
    - packages/gherkin/test/fixtures/docstring-and-datatable.feature
    - .planning/phases/02-loadfeature-parse-compile-correlate/02-RESEARCH.md (Decision D3 and its "Consequence for a future memo" paragraph; fixture table rows F23 and F25; the "State of the Art" row on v42 permitting both arguments; the "Scope Boundary" row placing the calling convention in Phase 4)
  </read_first>
  <action>
    Extend `packages/gherkin/test/Correlate.test.ts` with two more `describe` blocks.

    F23, cross-file id independence — the regression pin for Decision D3:
    - correlate `id-collision-a.feature` and `id-collision-b.feature` in the same test, each with
      its OWN fresh `IdGenerator.uuid()`
    - collect every node id reachable from both results: each scenario's `id` and `astId`, and
      every step's `id`
    - assert the collected array has no duplicates (compare its length to the size of a `Set`
      built from it)
    - add an inline comment recording the verified failure this pins: with
      `IdGenerator.incrementing()`, two different Features both give their first Scenario the id
      `"1"` and their first Pickle the id `"3"`, corrupting any cross-file map keyed on a node id.
      Also record D3's consequence — the ids differ between two `loadFeature` calls on identical
      source, so node ids must never be persisted or compared across calls.

    F25, a step carrying BOTH a DocString and a DataTable:
    - correlate `docstring-and-datatable.feature`
    - assert the single step's `argument` is defined and carries BOTH a `docString` and a
      `dataTable` — this is a real `@cucumber/gherkin@42` capability (`argumentIndex` records
      source order) that every pre-v42 `if (docString) ... else if (dataTable)` shape silently
      drops
    - assert the argument is the RAW `PickleStepArgument`, i.e. it has no `hashes`, `raw`, or
      `rowsHash` method. This is the executable guard for the Phase 4 scope boundary: the
      `DataTable` wrapper and the DocString+DataTable calling convention are PARSE-04's
      deliverable, and this phase must pass the argument through unwrapped.
    - add an inline comment naming Phase 4 as the owner of the calling convention, so a future
      reader does not "helpfully" add a wrapper here.

    Every new `it` title unique across the whole file; prefix with the fixture row id.
  </action>
  <verify>
    <automated>pnpm vitest run packages/gherkin/test/Correlate.test.ts</automated>
    <automated>pnpm test && pnpm build && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run packages/gherkin/test/Correlate.test.ts` exits 0 with at least 22 passing
      tests.
    - `pnpm test` (the whole suite) exits 0, and `pnpm build` and `pnpm lint` exit 0 — this is the
      per-wave gate from `02-VALIDATION.md`.
    - `grep -c 'id-collision-a\|id-collision-b' packages/gherkin/test/Correlate.test.ts` is at
      least 2.
    - `grep -c 'incrementing' packages/gherkin/test/Correlate.test.ts` is at least 1 (the comment
      recording the pinned failure mode).
    - The unwrapped-argument guard exists:
      `grep -cE 'hashes|rowsHash' packages/gherkin/test/Correlate.test.ts` is at least 1, and it
      appears only in a negative assertion (the argument does NOT have those methods).
    - `grep -c 'docString' packages/gherkin/test/Correlate.test.ts` is at least 1 and
      `grep -c 'dataTable' packages/gherkin/test/Correlate.test.ts` is at least 1.
  </acceptance_criteria>
  <done>
    Two Features correlated in one process share no node ids, and a step carrying both a DocString
    and a DataTable survives correlation with both arguments raw and unwrapped.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture `.feature` content → correlation pipeline | Test-only; the same boundary as plan 02-05, exercised with additional shapes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Spoofing | node-id collision across `loadFeature` calls | mitigate | F23 asserts that two Features correlated in one process share no node id, pinning Decision D3's `IdGenerator.uuid()` requirement. A regression to `incrementing()` fails this test by name rather than corrupting a downstream cross-file map silently. |
| T-02-15 | Tampering | scope creep of `PickleStepArgument` into a Phase 4 wrapper | mitigate | F25 asserts the argument carries no `hashes`/`raw`/`rowsHash` method, making a premature wrapper a test failure rather than a merge conflict discovered in Phase 4. |
| T-02-01 | Denial of Service | ReDoS | mitigate | No `RegExp` is constructed in this plan; assertions are literal string and `Set` comparisons. |
</threat_model>

<verification>
- `pnpm vitest run packages/gherkin/test/Correlate.test.ts` exits 0
- `pnpm test && pnpm build && pnpm lint` all exit 0 (wave gate)
</verification>

<success_criteria>
Every Group D correlation fixture (F21, F23, F24, F25, F26, F27) has an explicit passing
assertion, closing PARSE-02 and roadmap success criteria 2 and 4.
</success_criteria>

<output>
Create `.planning/phases/02-loadfeature-parse-compile-correlate/02-06-SUMMARY.md` when done.
</output>
