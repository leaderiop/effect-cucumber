---
phase: 02-loadfeature-parse-compile-correlate
plan: 11
type: execute
wave: 8
depends_on: ["02-09"]
files_modified:
  - spec/behaviors/04-loadfeature-parse-and-validation.md
  - spec/behaviors/index.yaml
  - spec/traceability.md
  - spec/decisions/014-loadfeature-consumes-gherkindocument-and-pickles.md
  - spec/roadmap.md
  - packages/gherkin/README.md
autonomous: true
requirements: [PARSE-01, PARSE-02, PARSE-03]

must_haves:
  truths:
    - "`loadFeature`'s failure path has a normative behavior entry with a permanent, contiguous BEH-EC id"
    - "The traceability matrix names the real source module for loadFeature (packages/gherkin, not packages/vitest)"
    - "Traceability section 4 is a real test-file map instead of the sentence 'no test files exist yet'"
    - "ADR-EC-014's correction describes the column-aware check that was actually built, not the naive bare-token scan"
    - "spec/roadmap.md's Current state table says only what is true after Phase 2"
    - "`pnpm verify:spec` still exits 0"
  artifacts:
    - path: "spec/behaviors/04-loadfeature-parse-and-validation.md"
      provides: "BEH-EC-014 — loadFeature fails loudly on every silent-failure mode of compile()"
      contains: "BEH-EC-014"
    - path: "spec/behaviors/index.yaml"
      provides: "registry entry for the new behavior doc"
    - path: "spec/traceability.md"
      provides: "section 1 correction plus a real section 4 test file map"
  key_links:
    - from: "spec/behaviors/index.yaml"
      to: "spec/behaviors/04-loadfeature-parse-and-validation.md"
      via: "registry entry; verify-traceability.sh check 1 fails in both directions"
      pattern: "04-loadfeature-parse-and-validation.md"
---

<objective>
`spec/` is normative and code follows the spec (AGENTS.md §1): a change to public behavior updates
the behavior doc, the invariant, and the traceability matrix IN THE SAME CHANGE. Phase 2 shipped
`loadFeature`, an entire named-error surface that no behavior doc describes, and a validation
mechanism that ADR-EC-014's correction blockquote describes INACCURATELY — it prescribes checking
"every Pickle step's text for a leftover `<...>` token", which is the verified-wrong naive form
with real false positives, and which also misses DataTable cells and DocString content entirely.

Phase 2 is also the first phase to falsify `spec/traceability.md` section 4's literal sentence
"Empty — no test files exist yet."

Purpose: close AGENTS.md §1 and §4 for this phase.
Output: one new behavior doc, a registry entry, two traceability corrections, an ADR correction
amendment, and two status updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loadfeature-parse-compile-correlate/02-RESEARCH.md
@.planning/phases/02-loadfeature-parse-compile-correlate/02-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write BEH-EC-014 and register it</name>
  <files>spec/behaviors/04-loadfeature-parse-and-validation.md, spec/behaviors/index.yaml</files>
  <read_first>
    - spec/behaviors/01-steps-and-world.md (the behavior-entry pattern: an `##` heading with the ID, a `> **See:**` blockquote linking the ADR, then a PLAIN fenced block with no language containing an all-caps `REQUIREMENT:` paragraph; also note BEH-EC-001's existing signature listing for `loadFeature`)
    - spec/behaviors/index.yaml (the registry shape: id, file, title, id_range — the highest allocated id today is BEH-EC-013)
    - spec/process/requirement-id-scheme.md (AGENTS.md §6: ids are permanent, contiguous, never renumbered, never reused)
    - packages/gherkin/src/Errors.ts (the ten error reasons and four warning reasons this behavior must describe)
    - packages/gherkin/src/loadFeature.ts (the real signatures of loadFeature and parseFeature)
    - spec/scripts/verify-traceability.sh (check 1 fails in BOTH directions — a declared file missing from disk, and a .md on disk missing from the registry; check 5 fails on a broken or gitignored relative link)
    - .planning/phases/02-loadfeature-parse-compile-correlate/02-PATTERNS.md (section "spec/ (behavior doc, index, traceability)")
  </read_first>
  <action>
    Create `spec/behaviors/04-loadfeature-parse-and-validation.md` describing `loadFeature`'s
    failure path. Allocate `BEH-EC-014` — the next free id. The highest allocated today is
    BEH-EC-013; ids are contiguous, never renumbered, never reused.

    Follow the existing behavior-entry pattern exactly: an `##` heading carrying the ID and a
    short title, a `> **See:**` blockquote linking `../decisions/014-...md` and
    `../decisions/019-...md` with working RELATIVE paths, then a PLAIN fenced block (no language
    tag) containing an all-caps `REQUIREMENT:` paragraph.

    The requirement text must state that `loadFeature` MUST reject, with a distinct named error
    identifying the file and line, every case where `@cucumber/gherkin`'s `compile()` produces a
    silently zero or silently wrong result — and it must ENUMERATE the ten reason tags:
    `MissingFile`, `ParseFailed`, `UnknownDialect`, `NoFeature`, `OutlineWithoutExamples`,
    `EmptyExamples`, `ZeroStepScenario`, `UninterpolatedPlaceholder`,
    `ScenarioKeywordWithExamples`, `DuplicateScenarioName`. State that consumers discriminate on
    the reason tag, never on message text.

    Add a second `REQUIREMENT:` block (still under BEH-EC-014) stating that heuristically-detected
    findings MUST be carried as non-throwing warnings on `ParsedFeature.warnings` with their own
    reason tags — `UnknownPlaceholder`, `DuplicateExamplesColumn`, `EmptyRule`,
    `SuspectedSwallowedStep` — and MUST NOT reject the feature file.

    Add prose (not inside a fence) recording the two decisions a reader will otherwise ask about:
    - duplicate un-interpolated Scenario names are rejected PER SCOPE (feature level, or within
      one Rule), so two different Rules may each contain a `Scenario: happy path`;
    - error messages reproduce DataTable and DocString content in FULL and are never truncated.

    Include one worked example. AGENTS.md §2 governs the fence language: use a
    a `typescript`-tagged fence (a runnable example that will be compiled against the real API by the
    planned doc-examples check) rather than a `ts`-tagged one, because Phase 2 IS the moment an API exists
    to compile against. The example must import what it uses from `@effect-cucumber/gherkin` and
    show catching a `LoadFeatureError` and switching on `err.reason`. Keep a signature listing for
    `parseFeature` in a `ts`-tagged fence if you add one — that form is reference-only.

    Do NOT restate build status in this document. AGENTS.md §4: `spec/roadmap.md`'s Current state
    table is the single place that says what is actually built; cite it rather than duplicating it.

    Register the new file in `spec/behaviors/index.yaml` with `id: "BEH-EC-014"`,
    `file: "04-loadfeature-parse-and-validation.md"`, a title, and `id_range: "014"`. Both
    directions of `verify-traceability.sh` check 1 must pass: the declared file must exist, and no
    `.md` on disk may be unregistered.

    Run `pnpm format` afterwards — `spec/**/*.md` is dprint-formatted INCLUDING its fenced code
    blocks, so hand-formatted examples will fail `dprint check` otherwise.
  </action>
  <verify>
    <automated>pnpm verify:spec</automated>
    <automated>pnpm lint</automated>
    <automated>grep -c 'BEH-EC-014' spec/behaviors/04-loadfeature-parse-and-validation.md spec/behaviors/index.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `spec/behaviors/04-loadfeature-parse-and-validation.md` exists and contains `BEH-EC-014`.
    - `spec/behaviors/index.yaml` contains an entry whose `file` is
      `"04-loadfeature-parse-and-validation.md"`.
    - `pnpm verify:spec` exits 0 with no FAIL rows (both directions of check 1, and check 5's
      relative-link integrity, are satisfied).
    - All ten error reason tags appear in the doc:
      `grep -o 'MissingFile\|ParseFailed\|UnknownDialect\|NoFeature\|OutlineWithoutExamples\|EmptyExamples\|ZeroStepScenario\|UninterpolatedPlaceholder\|ScenarioKeywordWithExamples\|DuplicateScenarioName' spec/behaviors/04-loadfeature-parse-and-validation.md | sort -u | wc -l` is 10.
    - All four warning reason tags appear:
      `grep -o 'UnknownPlaceholder\|DuplicateExamplesColumn\|EmptyRule\|SuspectedSwallowedStep' spec/behaviors/04-loadfeature-parse-and-validation.md | sort -u | wc -l` is 4.
    - The worked example uses a `typescript`-tagged fence, not a `ts`-tagged one:
      counting fence openers tagged `typescript` in
      `spec/behaviors/04-loadfeature-parse-and-validation.md` yields at least 1.
    - No id other than 014 is introduced: `grep -o 'BEH-EC-0[0-9][0-9]' spec/behaviors/04-loadfeature-parse-and-validation.md | sort -u` yields only ids that already exist plus `BEH-EC-014`.
    - `pnpm lint` exits 0 (`dprint check` covers `spec/**/*.md`).
  </acceptance_criteria>
  <done>
    `loadFeature`'s failure path is normative spec, with a permanent contiguous id, a registered
    entry, working relative links, and a compilable worked example.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct traceability section 1 and turn section 4 into a real test-file map</name>
  <files>spec/traceability.md</files>
  <read_first>
    - spec/traceability.md (the file being modified — note the pipe-table column order is a contract, that section 1's first row wrongly names `packages/vitest/src/loadFeature.ts`, and that section 4 is currently the literal sentence "Empty — no test files exist yet")
    - spec/behaviors/04-loadfeature-parse-and-validation.md (created in Task 1 — the row to add)
    - spec/scripts/verify-traceability.sh (which sections it parses, and what each check actually asserts)
    - packages/gherkin/test (the real test files created across plans 02-02 through 02-09)
    - .planning/phases/02-loadfeature-parse-compile-correlate/02-PATTERNS.md (section "spec/ (behavior doc, index, traceability)" including the two corrections Phase 2 owns)
  </read_first>
  <action>
    Edit `spec/traceability.md`.

    Section 1 (Behavior to source), two changes:
    - CORRECT the existing `01 — Steps and World` row: its "Source module (planned)" column names
      `packages/vitest/src/loadFeature.ts`. The real home is
      `packages/gherkin/src/loadFeature.ts`, because ADR-EC-015 forbids `@effect-cucumber/gherkin`
      from depending on Effect and `loadFeature` lives in that package. Leave the rest of that
      row's module list intact — `describeFeature`, `Step`, `World`, and `Plan` genuinely do belong
      to `packages/vitest`.
    - ADD a row for the new behavior file: link text `04 — loadFeature parse and validation`
      pointing at `behaviors/04-loadfeature-parse-and-validation.md`, range `BEH-EC-014`, and
      source modules
      `packages/gherkin/src/{loadFeature,Source,Parser,Pickles,Correlate,Validate,Errors,Model}.ts`.
    Preserve the exact pipe-table column order; it is a contract.

    Section 4 (Test file map): replace the placeholder sentence entirely with a real table. Its
    columns are the test file, the BEH-EC / INV-EC ids it covers, and a one-line description.
    Rows, one per test file that actually exists on disk:
    - `packages/gherkin/test/Contracts.test.ts` — BEH-EC-014 — error and warning shape, including
      the no-truncation policy
    - `packages/gherkin/test/upstream-pin.test.ts` — BEH-EC-014 — pins `@cucumber/gherkin@42`'s
      verified behavior per fixture so an upstream bump fails loudly
    - `packages/gherkin/test/Parser.test.ts` — BEH-EC-014 — parse-time throws wrapped as
      `MissingFile` / `ParseFailed` / `UnknownDialect` / `NoFeature`
    - `packages/gherkin/test/Correlate.test.ts` — BEH-EC-001 — substitution, Background stacking,
      tag inheritance, origin, keyword, and both scenario names
    - `packages/gherkin/test/Validate.test.ts` — BEH-EC-014 — one test per reason tag plus the
      Group C warnings and the placeholder false-positive guards
    - `packages/gherkin/test/loadFeature.test.ts` — BEH-EC-001 — synchronous, contributes zero
      tests, path and `?raw` parity
    - `packages/gherkin/test/dialect.test.ts` — BEH-EC-001 — a non-English feature parses with no
      special handling
    Enumerate the files by listing `packages/gherkin/test/*.test.ts` first, and include every one
    that exists — do not hand-guess the set.

    Add a sentence above the table noting that `packages/*/test/` now exists, superseding the
    preamble's "since `packages/*` doesn't exist yet" caveat for these rows. Do not delete that
    preamble wholesale — the Source module column is still `(planned)` for the vitest package.

    Do NOT add any `REQ-EC-NNN` row to section 5 — no `.feature` file in this repo carries a
    `@REQ-EC-NNN` tag, and the acceptance suite is Phase 11's deliverable. Section 5 must keep
    SKIPping cleanly.

    Run `pnpm format` afterwards, then `pnpm verify:spec`.
  </action>
  <verify>
    <automated>pnpm verify:spec</automated>
    <automated>pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm verify:spec` exits 0 with no FAIL rows.
    - `grep -c 'packages/vitest/src/loadFeature.ts' spec/traceability.md` is 0 — the section 1
      correction landed.
    - `grep -c 'packages/gherkin/src/loadFeature' spec/traceability.md` is at least 1.
    - `grep -c '04-loadfeature-parse-and-validation.md' spec/traceability.md` is at least 1.
    - `grep -c 'Empty — no test files exist yet' spec/traceability.md` is 0.
    - Every existing test file is listed: for each `f` in `packages/gherkin/test/*.test.ts`,
      `grep -q "$(basename $f)" spec/traceability.md` succeeds.
    - Section 5 still reads as empty/pending and no `REQ-EC-` row was added.
    - `pnpm lint` exits 0.
  </acceptance_criteria>
  <done>
    The traceability matrix names the real source module for `loadFeature`, carries a row for
    BEH-EC-014, and section 4 is a real test-file map covering every test file on disk.
  </done>
</task>

<task type="auto">
  <name>Task 3: Amend ADR-EC-014's correction and update the two status documents</name>
  <files>spec/decisions/014-loadfeature-consumes-gherkindocument-and-pickles.md, packages/gherkin/README.md, spec/roadmap.md</files>
  <read_first>
    - spec/decisions/014-loadfeature-consumes-gherkindocument-and-pickles.md (the file being modified — read the "Correction (2026-08-28, GSD Pitfalls research...)" blockquote at the end in full; its prescribed error-message sentence must be preserved verbatim)
    - packages/gherkin/src/Validate.ts (the check that was actually built — column-aware alpha error plus heuristic beta warning, scanning text, DocString content, and DataTable cell values)
    - .planning/phases/02-loadfeature-parse-compile-correlate/02-RESEARCH.md (Decision D4, including the three verified false positives, and fixture rows F7, F8, F9)
    - spec/roadmap.md (the file being modified — the "Current state" table is the single place that says what is actually built)
    - packages/gherkin/README.md (the file being modified — its Status section)
    - AGENTS.md (sections 1 and 4)
  </read_first>
  <action>
    Amend the correction blockquote at the end of
    `spec/decisions/014-loadfeature-consumes-gherkindocument-and-pickles.md`.

    The existing text says `loadFeature` "checks every Pickle step's text for a leftover `<...>`
    token". That describes the naive form, which is verified WRONG in two directions and must not
    stay in a normative document:
    - It has real false positives on legitimate, correctly-parsed step text. Name all three
      verified examples: `the assertion 2 < 3 holds`, `the html is <div>hello</div>`, and
      `an email <a@b.com>`. All three survive `compile()` unchanged and are valid Gherkin.
    - It misses two carriers entirely. Placeholders also survive un-interpolated inside a
      Background step's DataTable cell values and DocString content under an Outline; checking
      only `text` leaves those silent.

    Replace that sentence with the two-check description that was actually implemented, keeping
    the blockquote's existing framing and date attribution and adding a second dated correction
    note rather than rewriting history:
    - the EXACT check: within a pickle correlated to a Scenario Outline, a `<name>` whose name IS
      one of THAT Outline's own `Examples:` header columns is an error with reason
      `UninterpolatedPlaceholder`. It scans step text, DocString content, and every DataTable cell
      value. It has zero false positives, because the column name is proof of intent. It never
      runs on a plain-Scenario pickle, which is what removes the false-positive class above.
    - the HEURISTIC check: a `<name>` that is NOT one of that Outline's columns is a WARNING with
      reason `UnknownPlaceholder`, naming the columns that do exist. It catches a silently dropped
      Examples column (upstream cucumber/gherkin#22, still open) and a typo'd placeholder name.

    PRESERVE the prescribed error-message sentence verbatim — "this is a known `@cucumber/gherkin`
    limitation for Backgrounds nested under a Scenario Outline, not a bug in your Background text"
    — because `Validate.ts` reproduces it exactly and `Validate.test.ts` asserts it.

    Update `packages/gherkin/README.md`'s Status section. It currently says "Nothing is published
    to npm yet, and no library code has shipped. This package is scaffolding." The second half is
    no longer true (AGENTS.md §4). State what IS true: `loadFeature` / `parseFeature` and the
    `ParsedFeature` contract ship; step matching, parameter types, and the `DataTable` wrapper do
    not yet; nothing is published to npm. Keep the existing link to `spec/roadmap.md` as the
    single source of build status. Do NOT change the Install or Requirements sections, and do NOT
    add an `effect@rc` install line — this package declares no `effect` dependency (ADR-EC-015),
    which is exactly why its README names neither `effect` nor `@effect/vitest`.

    Update `spec/roadmap.md`'s "Current state" section. Its opening sentence
    ("Spec + GSD planning done; no library source code exists yet") and three table rows are now
    false. Correct only what is false:
    - the "Packages exist" row's "but contain no source files"
    - the "`tsc -b`" row's "nothing to build yet"
    - the "Unit tests" row's "None yet"
    - the "GSD project planning" row's "roadmap not yet created"
    Say what is true instead, citing `.planning/ROADMAP.md` for phase status. Leave the
    "Acceptance suite" and "Doc-examples compile check" rows as they are — both remain unbuilt.
    Do NOT rewrite the "Blocking first release" or "Planned" sections; they are still accurate.

    Run `pnpm format` afterwards, then `pnpm verify:spec`.
  </action>
  <verify>
    <automated>pnpm verify:spec && pnpm lint</automated>
    <automated>grep -c 'limitation for Backgrounds nested under a Scenario Outline' spec/decisions/014-loadfeature-consumes-gherkindocument-and-pickles.md</automated>
    <automated>pnpm test && pnpm build && pnpm circular && pnpm verify:pack && pnpm verify:tsgo-gate && pnpm verify:oxlint-plugin</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm verify:spec` exits 0 with no FAIL rows and `pnpm lint` exits 0.
    - The prescribed message sentence survives verbatim in ADR-EC-014 (the second verify command
      returns at least 1).
    - All three verified false-positive examples appear in the amended ADR:
      `grep -c '2 < 3' spec/decisions/014-loadfeature-consumes-gherkindocument-and-pickles.md`,
      `grep -c '<div>hello</div>' ...`, and `grep -c '<a@b.com>' ...` are each at least 1.
    - The ADR names both `UninterpolatedPlaceholder` and `UnknownPlaceholder`, and names
      DocString and DataTable as scan targets.
    - `grep -c 'no library code has shipped' packages/gherkin/README.md` is 0.
    - `grep -c 'contain \*\*no source files\*\*\|no library source code exists yet' spec/roadmap.md`
      is 0.
    - `spec/roadmap.md` still contains the "Acceptance suite" and "Doc-examples compile check"
      rows unchanged.
    - The full phase gate in the third verify command runs green.
  </acceptance_criteria>
  <done>
    ADR-EC-014's correction describes the column-aware check that actually shipped, and both status
    documents say only what is true after Phase 2.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `spec/` → implementation | `spec/` is normative; a spec document describing a check that was not built misdirects every future implementer |
| `spec/` → `pnpm verify:spec` | A required CI check parses the registry, the traceability matrix, and every relative link |
| README / roadmap → external readers | Status claims reach npm and GitHub readers who have no other source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-26 | Repudiation | ADR-EC-014's correction prescribing the verified-wrong naive placeholder check | mitigate | The blockquote is amended to the two-check column-aware form actually implemented, naming all three verified false positives and both extra scan carriers, while preserving the prescribed error-message sentence verbatim so `Validate.ts` and its test stay spec-conformant. |
| T-02-27 | Repudiation | status documents asserting untrue build state | mitigate | AGENTS.md §4 ("say only what is true"). `spec/roadmap.md`'s Current state table and the package README Status section are corrected; acceptance criteria grep for the now-false sentences being gone. |
| T-02-28 | Tampering | traceability drift | mitigate | `pnpm verify:spec` is a required `check.yml` step and fails in both directions on registry/disk mismatch and on broken or gitignored relative links. It is run as a verify command on all three tasks. |
| T-02-29 | Tampering | REQ-EC namespace collision | mitigate | No `REQ-EC-` row is added to section 5 and no fixture carries a `@REQ-EC-NNN` tag, so check 4 keeps SKIPping cleanly rather than failing. |
| T-02-SC | Tampering | supply chain | mitigate | Documentation only; no dependency, script, or manifest field is changed by this plan. |
</threat_model>

<verification>
- `pnpm verify:spec` exits 0 with no FAIL rows
- `pnpm lint` exits 0 (dprint covers `spec/**/*.md` including fenced blocks)
- The full phase gate runs green: `pnpm test`, `pnpm build`, `pnpm circular`, `pnpm verify:pack`, `pnpm verify:tsgo-gate`, `pnpm verify:oxlint-plugin`
</verification>

<success_criteria>
`spec/` describes what Phase 2 actually built — a registered BEH-EC-014 behavior, a corrected
traceability matrix with a real test-file map, an accurate ADR-EC-014 correction, and two status
documents that say only what is true.
</success_criteria>

<output>
Create `.planning/phases/02-loadfeature-parse-compile-correlate/02-11-SUMMARY.md` when done.
</output>
