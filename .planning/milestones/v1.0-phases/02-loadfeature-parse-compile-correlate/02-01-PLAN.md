---
phase: 02-loadfeature-parse-compile-correlate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/gherkin/package.json
  - packages/gherkin/tsconfig.json
  - pnpm-lock.yaml
autonomous: false
requirements: [PARSE-01, PARSE-02, PARSE-03]
user_setup: []

must_haves:
  truths:
    - "`pnpm install --frozen-lockfile` succeeds from a clean checkout with the committed lockfile"
    - "A source file under `packages/gherkin/src/` that imports `node:fs` type-checks under `pnpm build`"
    - "A test file under `packages/gherkin/test/` can `import { describe, expect, it } from \"vitest\"` with types"
  artifacts:
    - path: "packages/gherkin/package.json"
      provides: "devDependencies block with catalog: references for vitest and @types/node"
      contains: "devDependencies"
    - path: "packages/gherkin/tsconfig.json"
      provides: "opt-in to ambient node types"
      contains: "\"types\": [\"node\"]"
    - path: "pnpm-lock.yaml"
      provides: "regenerated lockfile committed in the same commit as the manifest change"
  key_links:
    - from: "packages/gherkin/package.json"
      to: "pnpm-workspace.yaml catalog"
      via: "catalog: protocol references"
      pattern: "\"(vitest|@types/node)\": \"catalog:\""
---

<objective>
Close the two blocking Wave 0 gaps identified in `02-VALIDATION.md` and `02-RESEARCH.md`
Pitfall P2: `packages/gherkin` has no `devDependencies` block at all, and `@types/node` is
installed nowhere in the repo. Until both are fixed, `Source.ts` cannot import `node:fs` and no
test file in this package can import `vitest` with types.

Purpose: every subsequent plan in Phase 2 writes either a `node:fs` consumer or a vitest test.
This plan is the precondition for both.
Output: a `devDependencies` block using `catalog:` references, `"types": ["node"]` in the
package tsconfig, and a regenerated `pnpm-lock.yaml` committed in the same commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loadfeature-parse-compile-correlate/02-RESEARCH.md
@.planning/phases/02-loadfeature-parse-compile-correlate/02-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for the two new devDependencies</name>
  <files>packages/gherkin/package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/02-loadfeature-parse-compile-correlate/02-RESEARCH.md (section "Package Legitimacy Audit")
    - pnpm-workspace.yaml
    - pnpm-lock.yaml (confirm `vitest` is already present as a root devDependency entry)
  </read_first>
  <what-built>
    Nothing yet — this checkpoint gates the install in Task 2. Two packages enter
    `packages/gherkin`'s `devDependencies`:

    - `vitest` — slopcheck verdict `[SUS]`. The sole complaint is "Suspiciously close to 'vite'.
      Could be a typosquat." `02-RESEARCH.md` adjudicates this a false positive: `vitest` is
      already a root devDependency of this repo, already pinned in `pnpm-workspace.yaml`'s
      default catalog at `^4.1.0`, already present in `pnpm-lock.yaml` at 4.1.11, and is a
      declared peer of `@effect/vitest@4.0.0-rc.112`. Adding it to a workspace package adds
      zero new registry surface.
    - `@types/node` — types-only, DefinitelyTyped. Already listed in `pnpm-workspace.yaml`'s
      catalog at `^26.4.0`, but installed in no package, so `pnpm install` will fetch it for
      the first time.

    Verified this research session: none of the packages in this phase's dependency closure
    declares a `postinstall` script. This phase adds zero new runtime dependencies.
  </what-built>
  <action>
    Do NOT run `pnpm install` and do NOT edit any manifest in this task. Present the two packages
    named in the block above, together with the slopcheck verdicts recorded in `02-RESEARCH.md`'s
    "Package Legitimacy Audit" table (`vitest` = `[SUS]`, adjudicated a false positive;
    `@types/node` = types-only, approved; all three `@cucumber/*` packages = `[OK]` and already
    installed), and wait for an explicit developer response before Task 2 runs.

    This is a blocking human gate. It is never auto-approvable, regardless of `mode: yolo` or
    `workflow.auto_advance` — a package-legitimacy decision belongs to the developer.
  </action>
  <how-to-verify>
    1. Open <https://www.npmjs.com/package/vitest> — confirm the publisher is the
       `vitest-dev` org and the repository is `github.com/vitest-dev/vitest`.
    2. Open <https://www.npmjs.com/package/@types/node> — confirm it is published by the
       `types` org and the repository is `github.com/DefinitelyTyped/DefinitelyTyped`.
    3. Confirm you accept the `[SUS]` adjudication for `vitest` recorded above.
  </how-to-verify>
  <acceptance_criteria>
    - Developer has explicitly responded; this checkpoint is never auto-approved regardless of
      `workflow.auto_advance` or `mode: yolo`.
    - If the developer rejects either package, STOP and do not run Task 2.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the install, or describe the objection.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add vitest and @types/node as catalog devDependencies, opt into node ambient types</name>
  <files>packages/gherkin/package.json, packages/gherkin/tsconfig.json, pnpm-lock.yaml</files>
  <read_first>
    - packages/gherkin/package.json (the file being modified — it currently has NO devDependencies block)
    - packages/gherkin/tsconfig.json (the file being modified — currently only `extends` + `include`)
    - packages/vitest/package.json (the repo's only manifest with a devDependencies block; copy its `catalog:` style)
    - packages/vitest/tsconfig.json (package tsconfig shape — no path duplication)
    - tsconfig.base.json (line with `"types": []`, which is what suppresses @types/node workspace-wide)
    - pnpm-workspace.yaml (confirms `vitest: ^4.1.0` and `"@types/node": ^26.4.0` are already in the default catalog — no catalog edit is needed)
  </read_first>
  <action>
    Edit `packages/gherkin/package.json`:

    - Add a `devDependencies` block containing exactly two entries, both using the `catalog:`
      protocol verbatim (never a literal version — STATE.md 01-04: version bumps happen only in
      `pnpm-workspace.yaml`): `"vitest": "catalog:"` and `"@types/node": "catalog:"`.
      Use the default `catalog:` (dev pins), NOT `catalog:peer` — the two are not
      interchangeable and `catalog:peer` holds ranges intended for `peerDependencies` only.
    - Add `"test": "vitest run"` to the existing `scripts` block alongside `"build": "tsc -b"`,
      matching `packages/vitest/package.json`.
    - Do NOT touch `exports`, `publishConfig`, `files`, `dependencies`, or `engines`. In
      particular do not add a subpath export — the single-barrel shape is deliberate
      (STATE.md 01-04: a subpath must be added to both `exports` and `publishConfig.exports`
      or it 404s for consumers).
    - `files: ["src/**/*.ts", "dist"]` already excludes `test/`, so the `.feature` fixtures
      added in plan 02-03 stay out of the tarball with no manifest change. Leave it alone.

    Edit `packages/gherkin/tsconfig.json`: add a `compilerOptions` object containing
    `"types": ["node"]`. `tsconfig.base.json` sets `"types": []` workspace-wide, which
    suppresses `@types/node` even once it is installed (verified: `node:fs` fails `TS2591`
    without `@types/node`, and `TS2688` with `types:["node"]` but no `@types/node`). Keep
    `"extends": "../../tsconfig.base.json"` and `"include": ["src"]` unchanged — do NOT widen
    `include` to cover `test`, because `rootDir` is `${configDir}/src` and files outside it
    break the build. Test-file type-checking gets its own config in plan 02-10.

    Run `pnpm install` and commit the regenerated `pnpm-lock.yaml` in the SAME commit as the
    manifest edits — `--frozen-lockfile` runs in all four `check.yml` jobs, so a manifest change
    landing without its lockfile fails CI.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile && pnpm build && node -e "const {createRequire}=require('node:module');createRequire(require('node:path').resolve('packages/gherkin/package.json')).resolve('@types/node/package.json');createRequire(require('node:path').resolve('packages/gherkin/package.json')).resolve('vitest/package.json');console.log('resolved')"</automated>
    <automated>printf 'import * as fs from "node:fs"\nexport const readsFiles = (p: string): string => fs.readFileSync(p, "utf8")\n' > packages/gherkin/src/__node_probe.ts; pnpm build; rc=$?; rm -f packages/gherkin/src/__node_probe.ts packages/gherkin/dist/__node_probe.*; pnpm build; exit $rc</automated>
    <automated>pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install --frozen-lockfile` exits 0 (proves the lockfile was regenerated and committed).
    - `@types/node/package.json` and `vitest/package.json` both resolve from
      `packages/gherkin/package.json` (the node one-liner above exits 0 and prints `resolved`).
    - The disposable `node:fs` probe compiles: the probe verify command exits 0. If it exits
      non-zero with `TS2591` or `TS2688`, `types: ["node"]` or the devDependency is missing.
    - `packages/gherkin/src/__node_probe.ts` does NOT exist after the task, and `pnpm build`
      exits 0 on a clean tree.
    - `grep -n 'catalog:' packages/gherkin/package.json` shows exactly two matches, both in
      `devDependencies`, and neither is `catalog:peer`.
    - No literal semver string for `vitest` or `@types/node` appears anywhere in
      `packages/gherkin/package.json`.
    - `pnpm lint` exits 0 (`dprint check` covers `package.json` and `tsconfig.json`).
  </acceptance_criteria>
  <done>
    `packages/gherkin` declares `vitest` and `@types/node` as `catalog:` devDependencies, its
    tsconfig opts into `types: ["node"]`, `pnpm-lock.yaml` is regenerated and staged with the
    manifest change, and a `node:fs` import in `packages/gherkin/src/` type-checks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local `node_modules` | New package tarballs enter the build environment at `pnpm install` time |
| `pnpm-workspace.yaml` catalog → package manifest | The single bump point for every pinned version; a manifest that bypasses it re-introduces drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | `pnpm install` of `vitest` + `@types/node` | mitigate | Blocking human-verify checkpoint (Task 1) before any install, per the Package Legitimacy Gate. `vitest` is slopcheck `[SUS]` (adjudicated a false positive in RESEARCH.md); `@types/node` is a first-time fetch. Neither declares a `postinstall`. Never auto-approvable. |
| T-02-05 | Tampering | `pnpm-lock.yaml` | mitigate | Lockfile regenerated and committed in the same commit; `--frozen-lockfile` in all four `check.yml` jobs rejects any later drift between manifest and lock. |
| T-02-06 | Elevation of Privilege | version pinning bypass | mitigate | Both entries use `catalog:` — a literal semver in a package manifest would silently escape the single bump point. Acceptance criteria assert no literal version appears. |
</threat_model>

<verification>
- `pnpm install --frozen-lockfile` exits 0
- `pnpm build` exits 0
- `pnpm lint` exits 0
- `node:fs` compiles inside `packages/gherkin/src` (disposable probe)
</verification>

<success_criteria>
`packages/gherkin` can host both a `node:fs` consumer and a vitest test file, and the change is
lockfile-consistent so `--frozen-lockfile` CI passes.
</success_criteria>

<output>
Create `.planning/phases/02-loadfeature-parse-compile-correlate/02-01-SUMMARY.md` when done.
</output>
