---
phase: 02-loadfeature-parse-compile-correlate
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/gherkin/test/fixtures/README.md
  - packages/gherkin/test/upstream-pin.test.ts
autonomous: true
requirements: [PARSE-01, PARSE-02, PARSE-03]

must_haves:
  truths:
    - "Every row of 02-RESEARCH.md's fixture table (F1-F27) has a corresponding artifact on disk or a documented reason it needs none"
    - "The verified behavior of @cucumber/gherkin@42.0.1 on each fixture is pinned by an executable assertion, so an upstream bump that changes it fails loudly"
    - "The F9 fixture omits the trailing pipe from BOTH the header and the body row, so it exercises the silent-column-drop path and not the loud inconsistent-cell-count path"
  artifacts:
    - path: "packages/gherkin/test/fixtures"
      provides: "one .feature file per fixture-table row, named for the reason it triggers"
    - path: "packages/gherkin/test/upstream-pin.test.ts"
      provides: "table-driven assertions on raw @cucumber/gherkin behavior for every fixture"
      min_lines: 80
    - path: "packages/gherkin/test/fixtures/README.md"
      provides: "the byte-exactness constraint and the fixture-to-row map"
  key_links:
    - from: "packages/gherkin/test/upstream-pin.test.ts"
      to: "packages/gherkin/test/fixtures/*.feature"
      via: "readFileSync over the fixture directory"
      pattern: "fixtures"
---

<objective>
Write the fixture corpus. `02-RESEARCH.md` is explicit that this is the phase's real
specification: "Write the fixtures first — they are the phase's actual specification." Every row
of the fixture table becomes one `.feature` file named for the reason it triggers, so a failing
test names the defect.

Then pin the verified UPSTREAM behavior of each fixture with a table-driven test that talks to
`@cucumber/gherkin` directly — no `@effect-cucumber/gherkin` source involved. This closes
`02-RESEARCH.md` Open Question 2: nothing upstream will fix any of these, so treat every row as
permanent library behavior and make a change in it fail loudly rather than silently altering
this library's semantics.

Purpose: gives every later implementation plan a real corpus to verify against, and gives the
fixture-writing itself an automated verification.
Output: ~28 `.feature` files, a fixtures README, and `test/upstream-pin.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loadfeature-parse-compile-correlate/02-RESEARCH.md
@.planning/phases/02-loadfeature-parse-compile-correlate/02-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Group A and Group B fixtures — silent-wrong output and parse-time throws</name>
  <files>packages/gherkin/test/fixtures/empty-examples-no-header.feature, packages/gherkin/test/fixtures/empty-examples-header-only.feature, packages/gherkin/test/fixtures/outline-without-examples.feature, packages/gherkin/test/fixtures/scenario-keyword-with-examples.feature, packages/gherkin/test/fixtures/zero-step-scenario.feature, packages/gherkin/test/fixtures/zero-step-scenario-in-rule.feature, packages/gherkin/test/fixtures/uninterpolated-placeholder-background.feature, packages/gherkin/test/fixtures/uninterpolated-placeholder-in-argument.feature, packages/gherkin/test/fixtures/no-feature.feature, packages/gherkin/test/fixtures/duplicate-scenario-name.feature, packages/gherkin/test/fixtures/duplicate-scenario-name-across-rules.feature, packages/gherkin/test/fixtures/parse-failed-misplaced-tag.feature, packages/gherkin/test/fixtures/unknown-dialect.feature, packages/gherkin/test/fixtures/parse-failed-inconsistent-cells.feature, packages/gherkin/test/fixtures/parse-failed-typo-keyword-after-step.feature, packages/gherkin/test/fixtures/parse-failed-background-after-rule.feature</files>
  <read_first>
    - .planning/phases/02-loadfeature-parse-compile-correlate/02-RESEARCH.md (sections "The Fixture Table" Group A + Group B, "Pattern 3" truth table, and Pitfalls P3, P4, P6, P7)
    - spec/scripts/verify-traceability.sh (check 4 greps every `*.feature` in the repo for `@REQ-EC-NNN` tags and FAILS if one is not defined in traceability.md — fixture tags must never use that form)
    - dprint.json (confirm `.feature` is NOT in `includes`, so fixtures are hand-formatted and stay byte-exact)
  </read_first>
  <action>
    Create the directory `packages/gherkin/test/fixtures/` and write these files. Use plain
    English Gherkin (`Feature:`, `Scenario:`, ...) unless the row says otherwise. Every fixture
    should be the minimum file that reproduces its row — no decorative extra scenarios.

    CRITICAL constraint on tags: no fixture may carry a tag matching `@REQ-EC-NNN`.
    `spec/scripts/verify-traceability.sh` check 4 greps every `*.feature` in the repository for
    that pattern and fails the required `pnpm verify:spec` CI step if the tag is not defined in
    `spec/traceability.md`. Use tags like `@featuretag`, `@ruletag`, `@scenariotag`,
    `@exampletag`, `@blockone`, `@blocktwo`.

    Group A — `compile()` produces silently zero or silently wrong output:

    - `empty-examples-no-header.feature` (row F1) — a `Scenario Outline:` with one step
      `Given a <x>` and an `Examples:` keyword followed by NOTHING (no header row, no body rows).
      Verified upstream: 0 pickles, no error.
    - `empty-examples-header-only.feature` (row F2) — the same shape but `Examples:` has a header
      row `| x |` and no body rows. Verified: 0 pickles, no error.
    - `outline-without-examples.feature` (row F3) — a `Scenario Outline:` with one step
      `Given a <x>` and NO `Examples:` keyword anywhere in the file. Verified: 1 pickle whose
      step text is the literal, un-interpolated `a <x>`. This is a different upstream code path
      from F1/F2 and is not in PITFALLS.md.
    - `scenario-keyword-with-examples.feature` (row F4) — a plain `Scenario:` (not Outline) with
      one step `Given a <x>` followed by an `Examples:` block with header `| x |` and two body
      rows. Verified: N pickles, compiled as an Outline — `compile()` branches on
      `examples.length`, never on the keyword.
    - `zero-step-scenario.feature` (row F5) — a feature-level `Background:` with one step, a
      `Scenario: no steps` with NO steps, and a second `Scenario: has steps` with one step.
      Verified: the zero-step scenario's pickle has `steps: []` and its Background steps are
      dropped too.
    - `zero-step-scenario-in-rule.feature` (row F6) — a `Rule:` containing a `Background:` with
      one step and a `Scenario: no steps` with NO steps. Verified: same, the Rule Background is
      dropped as well.
    - `uninterpolated-placeholder-background.feature` (row F7) — a feature-level `Background:`
      whose step is `Given a <name>`, plus a `Scenario Outline:` whose step is
      `When I use <name>`, plus `Examples:` with header `| name |` and one body row `| alice |`.
      Verified: the Background step's text stays the literal `a <name>` in every row's pickle
      while the Scenario step interpolates. This is exactly ADR-EC-014's correction case and
      PARSE-03 itself.
    - `uninterpolated-placeholder-in-argument.feature` (row F8) — a feature-level `Background:`
      with one step carrying a DataTable whose single cell value is `<x>`, and a second step
      carrying a DocString whose content contains `<x>`; plus a `Scenario Outline:` with a step
      that ALSO carries a DataTable whose cell value is `<x>`; plus `Examples:` with header
      `| x |` and one body row `| 1 |`. Verified: the Background cell/DocString stay `<x>` while
      the Scenario step's own cell interpolates to `1`. This is why the leftover-placeholder scan
      must reach `argument.dataTable.rows[].cells[].value` and `argument.docString.content`, not
      just `step.text`.
    - `no-feature.feature` (row F12) — a file containing only comment lines. Verified:
      parses fine, `document.feature` is `undefined` (NOT `null`), 0 pickles.
    - `duplicate-scenario-name.feature` (row F22) — a Feature containing two scenarios both
      literally named `dup`, each with one step. Verified: legal Gherkin, 2 pickles with distinct
      `astNodeIds` and identical `name`.
    - `duplicate-scenario-name-across-rules.feature` (row F22, negative control) — a Feature with
      `Rule: one` containing `Scenario: happy path` (one step) and `Rule: two` containing
      `Scenario: happy path` (one step). This file must remain LEGAL: the locked developer
      decision scopes duplicate-name rejection PER SCOPE (feature level, or within one Rule),
      explicitly so two Rules may each contain a `Scenario: happy path`.

    Group B — parse-time throws that must be wrapped, not leaked:

    - `parse-failed-misplaced-tag.feature` (row F17) — a `Feature:` line, then a `@tag` line, then
      `Background:`. Verified: throws `CompositeParserException` with about 9 collected errors for
      one bad line, and `.location` on the composite is `undefined` — locations live only on
      `.errors[i].location`.
    - `unknown-dialect.feature` (row F18) — first line `# language: xx`, then a normal Feature
      with one Scenario. Verified: throws `CompositeParserException` wrapping ONE
      `NoSuchLanguageException` at line 1 column 1, message `Language not supported: xx`.
    - `parse-failed-inconsistent-cells.feature` (row F10) — a `Scenario Outline:` with step
      `Given <a> and <b>` and an `Examples:` block whose header row is `| a | b |` (WITH the
      trailing pipe) and whose single body row is `| 1 | 2` (WITHOUT the trailing pipe).
      Verified: throws `AstBuilderException: inconsistent cell count within the table`. This row
      is already loud; the fixture exists to pin that.
    - `parse-failed-typo-keyword-after-step.feature` (row F15) — a `Scenario:` whose first line
      is a valid `Given y` and whose second line is the typo `Ginve x`. Verified: throws
      `CompositeParserException` at the typo's line. Position-dependent — contrast with F14.
    - `parse-failed-background-after-rule.feature` (row F20) — a `Feature:`, then a `Rule:` with
      one Scenario, then a feature-level `Background:` indented at the same level as the `Rule:`.
      Verified: throws `UnexpectedTokenException` — Gherkin's grammar
      (`Feature := header Background? ScenarioDefinition* Rule*`) forbids it outright. This
      REFUTES PITFALLS.md Pitfall 30, whose premise was that the syntax permits it. Keep the
      fixture to pin the parse error; do NOT implement Pitfall 30's recommended walk-time check
      anywhere in this phase — it is dead work.

    Do not add a `.gitignore` entry; these files must be committed.
  </action>
  <verify>
    <automated>ls packages/gherkin/test/fixtures/*.feature | wc -l | awk '{ if ($1 >= 16) exit 0; else exit 1 }'</automated>
    <automated>! grep -rlE '@REQ-EC-[0-9]{3}' packages/gherkin/test/fixtures</automated>
    <automated>pnpm verify:spec</automated>
  </verify>
  <acceptance_criteria>
    - All 16 named files exist under `packages/gherkin/test/fixtures/` with exactly those names.
    - `grep -rlE '@REQ-EC-[0-9]{3}' packages/gherkin/test/fixtures` finds nothing, and
      `pnpm verify:spec` exits 0.
    - In `parse-failed-inconsistent-cells.feature`, the Examples header line ends with `|` and
      the body row line does NOT: `grep -c '^ *| 1 | 2$' packages/gherkin/test/fixtures/parse-failed-inconsistent-cells.feature` is 1.
    - `no-feature.feature` contains only lines whose first non-whitespace character is `#`, or
      blank lines.
    - `duplicate-scenario-name.feature` contains exactly two occurrences of `Scenario: dup`.
    - `duplicate-scenario-name-across-rules.feature` contains exactly two occurrences of
      `Scenario: happy path` and exactly two `Rule:` lines.
    - `uninterpolated-placeholder-in-argument.feature` contains at least one DocString delimiter
      (`"""`) and at least two table rows containing `<x>`.
  </acceptance_criteria>
  <done>
    Sixteen Group A and Group B fixtures exist on disk, none carries a `@REQ-EC-NNN` tag, and
    `pnpm verify:spec` still passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Group C and Group D fixtures — warnings and correctness, plus the fixtures README</name>
  <files>packages/gherkin/test/fixtures/warning-dropped-examples-column.feature, packages/gherkin/test/fixtures/warning-duplicate-examples-column.feature, packages/gherkin/test/fixtures/warning-empty-rule.feature, packages/gherkin/test/fixtures/warning-swallowed-step.feature, packages/gherkin/test/fixtures/correlation-full.feature, packages/gherkin/test/fixtures/id-collision-a.feature, packages/gherkin/test/fixtures/id-collision-b.feature, packages/gherkin/test/fixtures/outline-two-examples-blocks.feature, packages/gherkin/test/fixtures/dialect-fr.feature, packages/gherkin/test/fixtures/outline-distinct-row-names.feature, packages/gherkin/test/fixtures/outline-identical-row-names.feature, packages/gherkin/test/fixtures/docstring-and-datatable.feature, packages/gherkin/test/fixtures/README.md</files>
  <read_first>
    - .planning/phases/02-loadfeature-parse-compile-correlate/02-RESEARCH.md (sections "The Fixture Table" Group C + Group D, Pitfall P3 on the F9 trap, Pitfall P7 on the swallowed step, and Pattern 2's tag-flattening order)
    - packages/gherkin/test/fixtures (the files created in Task 1 — match their naming and formatting style)
  </read_first>
  <action>
    Group C — silently wrong, but detection is heuristic, so these become WARNINGS (locked
    developer decision), never errors:

    - `warning-dropped-examples-column.feature` (row F9) — a `Scenario Outline:` with step
      `Given <a> and <b>` and an `Examples:` block whose header row is `| a | b` and whose single
      body row is `| 1 | 2`, with the trailing pipe omitted from BOTH lines.
      THIS IS THE TRAP (Pitfall P3): omitting the pipe on the body row only produces the LOUD
      `AstBuilderException` of row F10, so a fixture written that way tests the wrong thing and
      passes for the wrong reason. With both lines missing the pipe, upstream produces header
      cells `["a"]`, body cells `["1"]`, consistent counts, no error, and the step compiles to
      the string `1 and <b>` — the silent-column-drop this row exists to catch. This is upstream
      cucumber/gherkin#22, still open.
      Add a comment line at the top of the file stating that the missing trailing pipes are
      deliberate and must not be "fixed" by a formatter or an editor.
    - `warning-duplicate-examples-column.feature` (row F11) — a `Scenario Outline:` with step
      `Given <a> twice <a>` and an `Examples:` block with header `| a | a |` and one body row
      `| 1 | 2 |`. Verified: the first column wins for both occurrences, producing `1 twice 1`,
      with no error. Upstream cucumber/gherkin#28.
    - `warning-empty-rule.feature` (row F13) — a Feature containing one normal feature-level
      `Scenario:` with one step, followed by a `Rule: empty rule` with NO scenarios under it.
      (Order matters: the Gherkin grammar puts `Rule*` after `ScenarioDefinition*`.)
    - `warning-swallowed-step.feature` (row F14) — a `Scenario: ok` whose first line is the typo
      `Ginve x` and whose second line is the valid `Given y`. Verified: `Ginve x` is silently
      swallowed into `scenario.description`; the AST has ONE step, the pickle has ONE step, and
      no error is raised at any layer. The author wrote two steps and one does not exist.
      Position-dependent — the same typo after a valid step is the loud F15.

    Group D — correctness fixtures, no error expected:

    - `correlation-full.feature` (row F21) — the PARSE-02 workhorse. A `@featuretag`-tagged
      `Feature:` with a feature-level `Background:` containing one step
      `Given a feature background step`; then a `@ruletag`-tagged `Rule:` containing its own
      `Background:` with one step `Given a rule background step`, and a `@scenariotag`-tagged
      `Scenario Outline: outline <name>` with steps `When I use <name>` and `Then it works`, and
      an `@exampletag`-tagged `Examples:` block with header `| name |` and one body row `| a |`.
      Verified tag flattening order on the resulting pickle is exactly
      `["@featuretag", "@ruletag", "@scenariotag", "@exampletag"]`, and step order is
      feature-background, rule-background, then the Scenario's own steps.
    - `id-collision-a.feature` and `id-collision-b.feature` (row F23) — two separate, minimal,
      one-Scenario Features with DIFFERENT feature and scenario names. These exist to pin
      Decision D3: parsed in one process they must yield node ids with no overlap.
    - `outline-two-examples-blocks.feature` (row F24) — a `Scenario Outline:` with a step
      referencing `<n>`, then a `@blockone`-tagged `Examples:` block with header `| n |` and two
      body rows, then a `@blocktwo`-tagged `Examples:` block with header `| n |` and one body
      row. Verified: 3 scenarios from 2 blocks, per-block tags land only on that block's rows,
      and all three share the same `astNodeIds[0]`.
    - `dialect-fr.feature` (row F19) — first line `# language: fr`, then `Fonctionnalité:`,
      a `Contexte:` with one `Etant donné que ...` step, and a `Scénario:` with one
      `Etant donné que ...` step. Verified: parses with zero special handling;
      `feature.language === "fr"`, keyword `Fonctionnalité`, step keyword `Etant donné que `
      (with a trailing space).
    - `outline-distinct-row-names.feature` (row F26) — a
      `Scenario Outline: outline <name>` with one step referencing `<name>` and an `Examples:`
      block with header `| name |` and two body rows `| a |` and `| b |`. The interpolated pickle
      names (`outline a`, `outline b`) must differ from the un-interpolated AST name
      (`outline <name>`). This is success criterion 4.
    - `outline-identical-row-names.feature` (row F27) — a `Scenario Outline: same title` whose
      title contains NO placeholder, with one step referencing `<n>` and an `Examples:` block
      with header `| n |` and three body rows on three consecutive lines. Verified: 3 scenarios
      with IDENTICAL `name` and DISTINCT `location.line`. This pins the raw material Phase 6
      needs for unique test titles — this phase must not invent the title format.
    - `docstring-and-datatable.feature` (row F25) — a `Scenario:` with ONE step carrying BOTH a
      DocString and a DataTable. This is a real `@cucumber/gherkin@42` capability
      (`argumentIndex` records source order) that the spec does not contemplate. Fixture only —
      the calling convention is Phase 4's decision, so nothing in Phase 2 may decide an argument
      order for it.

    Also create `packages/gherkin/test/fixtures/README.md` containing a table mapping every
    fixture filename to its fixture-table row id (F1-F27), the reason tag or warning tag it
    triggers, and the verified upstream behavior in one line. Add a prominent note that these
    files are deliberately NOT covered by `dprint` (its `includes` glob is
    `**/*.{ts,tsx,js,jsx,json,md}`), that they must stay byte-exact, and that
    `warning-dropped-examples-column.feature` and `parse-failed-inconsistent-cells.feature`
    depend on the exact presence or absence of trailing `|` characters. Note that row F16
    (missing file) intentionally has no fixture: it is exercised by passing a path that does not
    exist. Run `pnpm format` after writing the README, since `.md` IS dprint-covered.
  </action>
  <verify>
    <automated>ls packages/gherkin/test/fixtures/*.feature | wc -l | awk '{ if ($1 >= 28) exit 0; else exit 1 }'</automated>
    <automated>! grep -rlE '@REQ-EC-[0-9]{3}' packages/gherkin/test/fixtures</automated>
    <automated>pnpm lint && pnpm verify:spec</automated>
  </verify>
  <acceptance_criteria>
    - All 12 additional fixture files plus `README.md` exist; the fixtures directory holds at
      least 28 `.feature` files in total.
    - In `warning-dropped-examples-column.feature`, NEITHER the Examples header line NOR the body
      row ends with `|`: `grep -cE '^\s*\|[^|]*\|[^|]*$' packages/gherkin/test/fixtures/warning-dropped-examples-column.feature` is 2.
    - `correlation-full.feature` contains all four of `@featuretag`, `@ruletag`, `@scenariotag`,
      `@exampletag`, exactly one `Rule:` line, and exactly two `Background:` lines.
    - `outline-two-examples-blocks.feature` contains exactly two `Examples:` lines.
    - `outline-identical-row-names.feature` contains no `<` character on its
      `Scenario Outline:` line.
    - `dialect-fr.feature` line 1 is exactly `# language: fr`.
    - `docstring-and-datatable.feature` contains both a `"""` delimiter and a `|` table row under
      the same single step.
    - `README.md` names every fixture file present in the directory, and explicitly states that
      row F16 has no fixture file.
    - `pnpm lint` exits 0 (covers the README under `dprint check`) and `pnpm verify:spec` exits 0.
  </acceptance_criteria>
  <done>
    The full fixture corpus exists, the F9 trap is avoided by construction, and the README records
    the byte-exactness constraint and the fixture-to-row map.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create test/upstream-pin.test.ts — pin @cucumber/gherkin's verified behavior</name>
  <files>packages/gherkin/test/upstream-pin.test.ts</files>
  <read_first>
    - packages/gherkin/test/fixtures/README.md (created in Task 2 — the authoritative fixture-to-row map)
    - .planning/phases/02-loadfeature-parse-compile-correlate/02-RESEARCH.md (Pattern 1 for the parser/compile construction, Pattern 2 for the verified id contract, "The Fixture Table" for every expected outcome, Open Question 2 for why this test exists)
    - tools/oxlint/effect/test/no-js-extension-imports.test.ts (house test style: table-driven cases, assert count first then the discriminating value)
    - .planning/phases/02-loadfeature-parse-compile-correlate/02-PATTERNS.md (the `vitest/no-identical-title` and barrel-import constraints)
  </read_first>
  <action>
    Create `packages/gherkin/test/upstream-pin.test.ts`. This test imports NOTHING from
    `packages/gherkin/src` — it talks to `@cucumber/gherkin` and `@cucumber/messages` directly.
    Its job is to make an upstream behavior change fail loudly and unambiguously, so a later
    failure in `Validate.test.ts` can be attributed to our code rather than to a dependency bump.

    Build the parser exactly as Pattern 1 prescribes: one `IdGenerator.uuid()` per case, shared
    by `new AstBuilder(newId)` and by `compile(document, uri, newId)`, with
    `new GherkinClassicTokenMatcher()`. Read fixtures with
    `readFileSync(new URL("./fixtures/<name>", import.meta.url), "utf8")`.

    Drive the test from a declared array of cases. Each case names the fixture file, its
    fixture-table row id, and its expected outcome, which is one of:
    - `throws` with the expected constructor NAME from the `Errors` namespace exported by
      `@cucumber/gherkin` — discriminate with `instanceof Errors.X`, NEVER with `err.name`.
      Verified: every one of those classes reports `.name === "Error"`, so a `.name` switch
      silently matches nothing.
    - `pickles: N` — the exact pickle count, optionally with the exact text of the first pickle's
      first step.

    Cases and their verified expectations:
    - `empty-examples-no-header.feature` -> 0 pickles, no throw
    - `empty-examples-header-only.feature` -> 0 pickles, no throw
    - `outline-without-examples.feature` -> 1 pickle, first step text is the literal `a <x>`
    - `scenario-keyword-with-examples.feature` -> 2 pickles
    - `zero-step-scenario.feature` -> the pickle for the zero-step Scenario has `steps.length === 0`
    - `zero-step-scenario-in-rule.feature` -> same
    - `uninterpolated-placeholder-background.feature` -> the Background step's text is still
      `a <name>` while the Scenario step's text is interpolated
    - `uninterpolated-placeholder-in-argument.feature` -> a Background step's DataTable cell value
      is still `<x>` (and the DocString content still contains `<x>`) while the Scenario step's
      own cell value is `1`
    - `no-feature.feature` -> `document.feature === undefined` (assert `=== undefined`, never
      `=== null` and never `== null`; `eqeqeq` is a lint error)
    - `duplicate-scenario-name.feature` -> 2 pickles, identical `name`, distinct `astNodeIds[0]`
    - `duplicate-scenario-name-across-rules.feature` -> 2 pickles, identical `name`
    - `parse-failed-misplaced-tag.feature` -> throws `Errors.CompositeParserException`; assert
      `errors.length > 1` and that `location` on the composite is `undefined`
    - `unknown-dialect.feature` -> throws a composite whose `errors[0]` is an
      `Errors.NoSuchLanguageException`
    - `parse-failed-inconsistent-cells.feature` -> throws; assert the message contains
      `inconsistent cell count`
    - `parse-failed-typo-keyword-after-step.feature` -> throws
    - `parse-failed-background-after-rule.feature` -> throws an
      `Errors.UnexpectedTokenException` (possibly wrapped in a composite — normalise with the
      cucumber-js idiom `err.errors ?? [err]` and inspect the first entry)
    - `warning-dropped-examples-column.feature` -> 1 pickle whose first step text is exactly
      `1 and <b>` (the silent-drop signature; if this ever becomes a throw, upstream fixed #22)
    - `warning-duplicate-examples-column.feature` -> 1 pickle whose first step text is exactly
      `1 twice 1`
    - `warning-empty-rule.feature` -> 1 pickle (the feature-level Scenario only)
    - `warning-swallowed-step.feature` -> 1 pickle with exactly one step, and the AST scenario's
      `description` contains the string `Ginve x`
    - `correlation-full.feature` -> 1 pickle whose `tags` names are exactly
      `["@featuretag", "@ruletag", "@scenariotag", "@exampletag"]` in that order, and whose
      `steps` are three, in feature-background / rule-background / scenario order
    - `id-collision-a.feature` + `id-collision-b.feature` -> parsed in the same test with two
      SEPARATE `IdGenerator.uuid()` generators, the union of all node ids across both documents
      and both pickle sets contains no duplicate
    - `outline-two-examples-blocks.feature` -> 3 pickles; the two from block one carry
      `@blockone` and not `@blocktwo`; all three share the same `astNodeIds[0]`
    - `dialect-fr.feature` -> `document.feature.language === "fr"`, feature keyword
      `Fonctionnalité`, and the AST step keyword ends with a trailing space
    - `outline-distinct-row-names.feature` -> 2 pickles with names `outline a` and `outline b`,
      while the AST scenario's own name is `outline <name>`
    - `outline-identical-row-names.feature` -> 3 pickles with identical `name` and three distinct
      `location.line` values
    - `docstring-and-datatable.feature` -> the single pickle step's `argument` carries BOTH a
      `docString` and a `dataTable`

    Also add one assertion pinning Decision D3's negative case: two documents parsed with two
    fresh `IdGenerator.incrementing()` generators produce COLLIDING ids (both first scenarios get
    the same id). This is why `uuid()` is mandatory, and pinning it means the day upstream changes
    `incrementing()`'s behavior we find out here rather than through a corrupted cross-file map.

    Every `it` title must be unique (`vitest/no-identical-title` is `"error"`); if you generate
    titles from the case table, include the fixture filename in the title. No `it.only`.
  </action>
  <verify>
    <automated>pnpm vitest run packages/gherkin/test/upstream-pin.test.ts</automated>
    <automated>pnpm lint</automated>
    <automated>grep -c 'instanceof Errors\.' packages/gherkin/test/upstream-pin.test.ts | awk '{ if ($1 >= 3) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run packages/gherkin/test/upstream-pin.test.ts` exits 0 and reports at least
      25 passing tests.
    - Every `.feature` file in `packages/gherkin/test/fixtures/` is named at least once in the
      test file: for each `f` in the fixtures directory, `grep -q "$(basename $f)" packages/gherkin/test/upstream-pin.test.ts` succeeds.
    - `grep -c 'err\.name ===' packages/gherkin/test/upstream-pin.test.ts` is 0 — error classes
      are discriminated with `instanceof Errors.X` only.
    - `grep -c '== null' packages/gherkin/test/upstream-pin.test.ts` is 0.
    - `grep -c 'from "\.\./src/' packages/gherkin/test/upstream-pin.test.ts` is 0 — this test
      pins UPSTREAM behavior and must not depend on our source.
    - `grep -c 'incrementing' packages/gherkin/test/upstream-pin.test.ts` is at least 1 (the D3
      collision pin).
    - `pnpm lint` exits 0.
  </acceptance_criteria>
  <done>
    Every fixture's verified upstream behavior is asserted executably, using `instanceof` for
    error discrimination and `=== undefined` for the absent-feature check, with no dependency on
    `@effect-cucumber/gherkin` source.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.feature` fixture file → `@cucumber/gherkin` parser | Untrusted-ish input crosses into the parser; all lexing is delegated upstream, this phase adds no custom lexer |
| fixture corpus → `pnpm verify:spec` | A fixture tag can break a required CI check if it collides with the `@REQ-EC-NNN` traceability namespace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Denial of Service | fixture corpus feeding the parser | accept | Every fixture is a hand-written file of a few lines under the repo's own control; no untrusted third-party feature file is parsed at build time. Parser hard-stops past ~10 collected errors, bounding the cascading-error case. |
| T-02-09 | Tampering | `spec/scripts/verify-traceability.sh` check 4 | mitigate | Fixtures must not carry `@REQ-EC-NNN` tags. Enforced by an acceptance criterion grep and by running `pnpm verify:spec` as a task verify command in both fixture tasks. |
| T-02-10 | Repudiation | upstream behavior drift | mitigate | `upstream-pin.test.ts` asserts the exact verified behavior of `@cucumber/gherkin@42.0.1` per fixture, so a dependency bump that changes semantics fails by name instead of silently altering this library's error surface. |
| T-02-01 | Denial of Service | ReDoS via feature-derived `RegExp` | mitigate | No file in this plan constructs a `RegExp` from feature content; the pin test uses literal string comparisons only. |
</threat_model>

<verification>
- `pnpm vitest run packages/gherkin/test/upstream-pin.test.ts` exits 0
- `pnpm lint` exits 0
- `pnpm verify:spec` exits 0
- No `@REQ-EC-NNN` tag appears in any fixture
</verification>

<success_criteria>
Every row F1-F27 of `02-RESEARCH.md`'s fixture table is represented on disk (or documented as
needing no file, for F16), and every row's verified upstream behavior is asserted executably.
</success_criteria>

<output>
Create `.planning/phases/02-loadfeature-parse-compile-correlate/02-03-SUMMARY.md` when done.
</output>
