---
phase: 07-hooks
plan: 07
type: execute
wave: 5
depends_on: ["07-05", "07-06"]
files_modified:
  - packages/vitest/test/Runner.test.ts
  - packages/vitest/test/emission.test.ts
  - packages/vitest/src/index.ts
autonomous: true
requirements: [DSL-07, RUN-02]

must_haves:
  truths:
    - "One append-only Ref log asserts the full six-hook ordering across a two-Scenario Feature in a single comparison (D-01, D-04, D-09)"
    - "A real describeFeature call runs its hooks in a real vitest run, not only against a fake"
    - "A consumer can annotate a hook registrar by importing HookRegistrar from the package barrel"
    - "The package's own status prose no longer says hooks are unbuilt"
  artifacts:
    - path: "packages/vitest/test/Runner.test.ts"
      provides: "the phase's headline full-ordering assertion"
    - path: "packages/vitest/test/emission.test.ts"
      provides: "hooks proven through a real describeFeature call"
    - path: "packages/vitest/src/index.ts"
      provides: "HookRegistrar exported, status prose corrected"
      contains: "HookRegistrar"
  key_links:
    - from: "packages/vitest/src/index.ts"
      to: "packages/vitest/src/Dsl.ts"
      via: "HookRegistrar travels on the existing Dsl.ts type export line"
      pattern: "HookRegistrar.*from \"./Dsl.ts\""
---

<objective>
Prove the phase's headline claim in one assertion, prove it again through a real `describeFeature`
call, and publish the one type a consumer needs.

Purpose: roadmap success criterion 2 asks for exactly one thing — "an append-only `Ref` log asserts
the full ordering across a two-Scenario Feature: `BeforeAllScenarios` → (`Before` →
`BeforeStep`/`AfterStep` per step → `After`) per Scenario → `AfterAllScenarios`". Every prior plan in
this phase proved one segment of that sequence against its own module; nothing yet proves the whole
sequence composes.
Output: the full-ordering test, a real-run hook proof, and the barrel edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hooks/07-CONTEXT.md
@.planning/phases/07-hooks/07-PATTERNS.md
@packages/vitest/test/Runner.test.ts
@packages/vitest/test/emission.test.ts
@packages/vitest/src/index.ts
@.planning/phases/07-hooks/07-05-SUMMARY.md
@.planning/phases/07-hooks/07-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: the full six-hook ordering across a two-Scenario Feature</name>
  <files>packages/vitest/test/Runner.test.ts</files>
  <read_first>
    - packages/vitest/test/Runner.test.ts (read whole, as extended by plan 07-06 — `makeRecordingApi`, `thunkAt`, the shared-log `Recorder` fixture and the mutation header)
    - packages/vitest/test/ScenarioEffect.test.ts (the `:start`/`:end` bracketing convention and the `Effect.yieldNow` requirement)
    - .planning/ROADMAP.md "Phase 7: Hooks" success criterion 2 — the ordering this test must assert literally
    - .planning/phases/07-hooks/07-CONTEXT.md "Phase Boundary"
  </read_first>
  <action>
    Add ONE test that is the phase's headline assertion, in its own `describe` block with a comment
    saying it is roadmap success criterion 2 and that it is deliberately a single comparison rather
    than a set of narrower checks.

    Fixture: a hand-built `FeaturePlan` for a two-Scenario Feature, each Scenario carrying two resolved
    steps, plus a `HookSet` registering exactly one hook of each of the six kinds. Every hook body and
    every step body brackets `:start` / `Effect.yieldNow` / `:end` into the one shared `Recorder` log
    (the `Layer.succeed` fixture from plan 07-06, so all Layer builds share one log). Name each hook's
    entries after its kind and each step's after its position, so the expected array reads as the
    sequence itself.

    Emit through the recording fake, then run the recorded thunks in emitted order — Scenario 1,
    Scenario 2, then the `⚙ AfterAllScenarios` node — and compare the WHOLE log with one
    `assert.deepStrictEqual` against the literal expected array:

    `BeforeAllScenarios` bracketed once; then for Scenario 1: `Before`, then for each of its two steps
    `BeforeStep` / the step / `AfterStep`, then `After`; then the identical block for Scenario 2 with
    no second `BeforeAllScenarios`; then `AfterAllScenarios` bracketed once.

    Write the expected array out in full and grouped by line so a reviewer can read the sequence
    directly. Do not compute it, do not build it from a helper, and do not assert on a projection —
    a filtered or de-duplicated comparison passes against several of the arrangements this test exists
    to separate.

    Add to the file's mutation header: (O) the per-step unit's `BeforeStep` and `AfterStep` batches
    swapped → this test fails and, by construction, nothing else in this file does; (P)
    `BeforeAllScenarios` composed inside `buildScenarioEffect` instead of the once-cell → the sequence
    gains a second `BeforeAllScenarios` block. Perform both, record the failing output in the summary,
    revert.
  </action>
  <acceptance_criteria>
    - `pnpm vitest run packages/vitest/test/Runner.test.ts` passes and includes exactly one test whose body ends in a single `assert.deepStrictEqual` over an expected array of at least 26 entries
    - That expected array contains exactly one `BeforeAllScenarios:start` and exactly one `AfterAllScenarios:start`
    - It contains exactly two `Before:start` and two `After:start` entries, and four `BeforeStep:start` and four `AfterStep:start` entries
    - The last entry is `AfterAllScenarios:end` and the first is `BeforeAllScenarios:start`
    - Every hook and step body in the fixture contains `Effect.yieldNow` between its two log writes
    - The file's module doc header lists mutations O and P with this test named as the one that breaks
    - `pnpm test` and `pnpm lint` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/vitest/test/Runner.test.ts && pnpm test && pnpm lint</automated>
  </verify>
  <done>One `assert.deepStrictEqual` over one append-only log asserts the complete `BeforeAllScenarios → (Before → BeforeStep/step/AfterStep → After) ×2 → AfterAllScenarios` sequence, and two recorded mutations prove it discriminates.</done>
</task>

<task type="auto">
  <name>Task 2: hooks through a real describeFeature run</name>
  <files>packages/vitest/test/emission.test.ts</files>
  <read_first>
    - packages/vitest/test/emission.test.ts (read whole — the module header explaining why this is the only file that calls `describeFeature` for real, the deliberately happy-path Feature, the module-scope-stub-and-assert-later pattern, and mutations A/B/C)
    - packages/vitest/src/index.ts (the public surface a consumer reaches)
    - .planning/phases/07-hooks/07-CONTEXT.md decisions D-01, D-04, D-09
  </read_first>
  <action>
    Add hooks to this file's real `describeFeature` usage without breaking its founding constraint:
    everything it emits must PASS, because a failing emitted test here is a red build and not a legible
    assertion. So register only succeeding hooks, and assert on what they recorded.

    Add a second `describeFeature` call (or extend the existing Feature — whichever keeps the existing
    assertions untouched; prefer a second call against its own fixture so the current Feature's log
    assertions do not have to be rewritten) that registers, against a Layer carrying a `Ref` log:
    - two `Before` hooks and two `After` hooks, so registration order (D-01) is observable in a real
      run and not only against a fake;
    - one `BeforeStep` and one `AfterStep`;
    - one `BeforeAllScenarios` and one `AfterAllScenarios`.

    The Feature has two Scenarios. Each emitted Scenario test asserts, from inside its own body, the
    prefix of the log it can legitimately see at that point — Scenario 1 sees `BeforeAllScenarios` and
    its own `Before`; Scenario 2 sees no second `BeforeAllScenarios`. Assert on the whole accumulated
    log, not the last entry, following the file's existing reasoning about why the joined sequence is
    what separates three otherwise-identical arrangements.

    Add one final assertion block, in the same style as the existing "last block" that catches mutation
    C: a sync `it` running after the emitted tests, reading the module-scope log array and asserting
    that the complete real-run sequence contains `AfterAllScenarios` exactly once and last. That block
    is what proves the `⚙ AfterAllScenarios` node is a real emitted node that a real vitest run
    executes, which the recording fake in `Runner.test.ts` cannot show.

    Keep every existing assertion in this file passing unchanged. Extend the mutation header with:
    (D) the `AfterAllScenarios` node not emitted → the final block's "exactly once and last" assertion
    fails while every emitted test still passes; (E) `BeforeAllScenarios` composed per Scenario → the
    Scenario 2 body's prefix assertion fails. Perform both, record the failing output, revert.
  </action>
  <acceptance_criteria>
    - `pnpm vitest run packages/vitest/test/emission.test.ts` passes with strictly more tests reported than before this plan and zero failures
    - Every assertion that existed in this file before this plan is still present and unmodified
    - The final sync `it` asserts the real-run log's last entries are the `AfterAllScenarios` hook's, and that its `:start` appears exactly once
    - Scenario 2's emitted body asserts its log prefix contains exactly one `BeforeAllScenarios:start`
    - The file's module doc header lists the two new mutations with the assertion each one breaks
    - `pnpm test` and `pnpm lint` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/vitest/test/emission.test.ts && pnpm test && pnpm lint</automated>
  </verify>
  <done>A real `describeFeature` call with all six hooks runs green in this repo's own suite, and a post-run block proves the `AfterAllScenarios` node was emitted and executed exactly once, last.</done>
</task>

<task type="auto">
  <name>Task 3: the barrel export and the status prose</name>
  <files>packages/vitest/src/index.ts</files>
  <read_first>
    - packages/vitest/src/index.ts (read whole — the "Current state" and "What is NOT built yet" paragraphs at L24-42, the export-policy section, and the "Deliberately NOT exported" list at L50-66)
    - packages/vitest/src/Dsl.ts (as edited by plan 07-02 — `HookRegistrar` is the only new name that belongs in the barrel)
    - packages/vitest/src/Hook.ts and packages/vitest/src/HookRegistry.ts (their closing internal-stage paragraphs, which this file's "Deliberately NOT exported" list must now name)
  </read_first>
  <action>
    Add `HookRegistrar` to the EXISTING `Dsl.ts` type export line so it reads
    `export type { BackgroundDsl, FeatureDsl, HookRegistrar, ScenarioDsl, StepRegistrar } from "./Dsl.ts"`.
    Extend that line's doc comment to say a consumer needs it to annotate a hook body written as a named
    function rather than inline, exactly as `StepRegistrar` is there for a step.

    Add nothing else. 07-CONTEXT.md's Integration Points is explicit: hooks are reached through the
    `FeatureDsl` surface the barrel already publishes, and there are NO module-level `Before`/`After`
    functions. BEH-EC-006's published free-standing-function signatures predate the DSL-04 architecture
    and are corrected by plan 07-08; do not create exports to match stale spec text.

    Add `registerHook`/`groupHooks`/`runHookBatch` from `Hook.ts` and `createHookRegistry` (with its
    `HookKind`/`HookDefinition`/`HookRegistryShape` types) from `HookRegistry.ts` to the "Deliberately
    NOT exported" list, in the same sentence form the existing entries use, with the same reason: each
    is an internal stage of `describeFeature` with no standalone consumer contract. `HookSet` and
    `HookBody` go in the same list — `HookSet` appears in `FeatureCollection`, which is itself not
    exported.

    Rewrite the two status paragraphs. "Current state" must now say that all six hooks are registered
    through the Feature-level dsl and describe the guarantees in one or two sentences: `Before` gates
    the Scenario's steps and every `Before` in a batch runs with their failures combined; `After` and
    `AfterStep` run whether the thing they guard succeeded or failed and never mask its error;
    `BeforeAllScenarios` runs once per Feature and its failure reaches every Scenario;
    `AfterAllScenarios` runs as a trailing node regardless of what failed. "What is NOT built yet" must
    drop the hooks sentence entirely — including its `(DSL-07, RUN-02)` citation — while leaving the
    Phase 8, 9 and 10 sentences untouched. AGENTS.md §4 makes a stale status paragraph a defect, not a
    nit.
  </action>
  <acceptance_criteria>
    - `grep -c "HookRegistrar" packages/vitest/src/index.ts` is at least `1` and it appears on the single existing `from "./Dsl.ts"` type-export line
    - `grep -c "from \"./Dsl.ts\"" packages/vitest/src/index.ts` outputs `1` — no second export statement was added
    - `grep -cE "^export " packages/vitest/src/index.ts` is unchanged from before this plan
    - `grep -c "DSL-07" packages/vitest/src/index.ts` outputs `0`
    - `grep -c "createHookRegistry\|registerHook\|runHookBatch" packages/vitest/src/index.ts` is at least `1`, and every occurrence is inside the "Deliberately NOT exported" prose rather than an export statement
    - `node -e "const a=Object.keys(require('./packages/vitest/package.json').exports); const b=Object.keys(require('./packages/vitest/package.json').publishConfig.exports); if(JSON.stringify(a)!==JSON.stringify(b)) throw new Error('exports key sets diverged')"` exits 0 — still a single barrel, no subpath added
    - `pnpm build`, `pnpm lint`, `pnpm test` and `pnpm verify:pack` all exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm build && pnpm lint && pnpm test && pnpm verify:pack</automated>
  </verify>
  <done>`HookRegistrar` is the one new public name, it rides the existing `Dsl.ts` export line, the internal hook modules are named in the do-not-export list, and the barrel's status prose describes hooks as built.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package barrel → consumer | The published surface. Adding a name here is a contract this project then has to keep. |
| test author's hook code → this library | In-process, fully trusted; no network, no untrusted input, no credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Tampering | an internal stage published by accident through the barrel | mitigate | Only `HookRegistrar` is added, on the existing `Dsl.ts` line; the acceptance criteria pin the export-statement count and require the internal hook modules to appear only in the do-not-export prose. |
| T-07-07-02 | Repudiation | a subpath export that resolves locally and 404s for consumers | mitigate | `verify:pack` runs, and the `exports` / `publishConfig.exports` key-set equality check is an acceptance criterion. The barrel stays single. |
| T-07-07-03 | Repudiation | published status prose claiming hooks are unbuilt after they ship | mitigate | AGENTS.md §4; the `grep -c "DSL-07"` criterion fails if the stale sentence survives. |
| T-07-07-04 | Information disclosure | the real-run test emitting a failing node that prints hook error text into CI | accept | The emission fixture is deliberately all-succeeding, so nothing is printed; the general no-truncation acceptance is unchanged from earlier plans. |
| T-07-07-SC | Tampering | npm/pip/cargo installs | n/a | This plan adds no dependency. `pnpm-lock.yaml` and both manifests are unchanged; no package-legitimacy checkpoint applies. |
</threat_model>

<verification>
- `pnpm test` passes, with the headline ordering test and the real-run block both green.
- `pnpm build`, `pnpm lint`, `pnpm typecheck:test` exit 0.
- `pnpm verify:pack` exits 0 and the two `exports` key sets are byte-identical.
- `pnpm verify:tsgo-gate` still exits 0 with eleven assertions.
- All four mutations across tasks 1 and 2 are reverted and recorded in the summary.
</verification>

<success_criteria>
- One `assert.deepStrictEqual` asserts the complete six-hook sequence across a two-Scenario Feature.
- A real `describeFeature` call exercises all six hooks in this repo's own vitest run and passes.
- A post-run block proves the `AfterAllScenarios` node was really emitted and really executed, once, last.
- `HookRegistrar` is the phase's only new public export and the barrel is still a single entry point.
- `index.ts`'s status prose describes hooks as built, with no `DSL-07` / `RUN-02` "not built yet" citation left.
</success_criteria>

<output>
Create `.planning/phases/07-hooks/07-07-SUMMARY.md` when done.
</output>
