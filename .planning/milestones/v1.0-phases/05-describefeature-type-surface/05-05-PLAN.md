---
phase: 05-describefeature-type-surface
plan: 05
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - packages/vitest/test/tsgo-gate/src/world-undeclared-field.ts
  - packages/vitest/test/tsgo-gate/tsconfig.world-field.json
  - packages/vitest/test/tsgo-gate/src/layer-missing-rin.ts
  - packages/vitest/test/tsgo-gate/tsconfig.layer-rin.json
  - packages/vitest/test/tsgo-gate/src/step-expect-error.ts
  - packages/vitest/test/tsgo-gate/tsconfig.step-expect-error.json
  - scripts/verify-tsgo-gate.sh
autonomous: true
requirements: [DSL-01, DSL-03]

must_haves:
  truths:
    - "Reading a field absent from World's declared type is a compile error, reported as plain TS2339 with no Effect diagnostic"
    - "Passing describeFeature a Layer whose own RIn is unsatisfied is rejected by name as effect(missingLayerContext)"
    - "The roadmap's literal @ts-expect-error negative form is honored by a supplementary fixture that compiles clean only because both the TypeScript error and the Effect diagnostic are suppressed"
    - "All three new cases are asserted in the same script run as the DSL-01 pair"
  artifacts:
    - path: "packages/vitest/test/tsgo-gate/src/world-undeclared-field.ts"
      provides: "DSL-03's negative case"
    - path: "packages/vitest/test/tsgo-gate/src/layer-missing-rin.ts"
      provides: "The Layer-argument negative case that exercises overload order"
    - path: "packages/vitest/test/tsgo-gate/src/step-expect-error.ts"
      provides: "The supplementary stacked-directive fixture (RESEARCH Open Question 1)"
    - path: "scripts/verify-tsgo-gate.sh"
      provides: "Assertions 7, 8 and 9"
      contains: "TS2339"
  key_links:
    - from: "scripts/verify-tsgo-gate.sh"
      to: "packages/vitest/test/tsgo-gate/tsconfig.world-field.json"
      via: "exit != 0 plus a TS2339 grep, deliberately NOT an effect( grep"
      pattern: "TS2339"
    - from: "scripts/verify-tsgo-gate.sh"
      to: "packages/vitest/test/tsgo-gate/tsconfig.layer-rin.json"
      via: "exit != 0 plus an effect(missingLayerContext) grep"
      pattern: "tsconfig.layer-rin.json"
---

<objective>
Close DSL-03's negative half and the Layer-argument half of DSL-01, and honor the roadmap's
literal `@ts-expect-error` wording with a supplementary fixture — all three asserted in the same
gate script run as plan 05-04's pair.

Three distinct failure modes, three distinct assertion shapes, and getting them confused is the
failure this plan is written to prevent (RESEARCH.md Finding 1):

| Fixture | Failure mode | What to assert |
|---|---|---|
| `world-undeclared-field.ts` | plain TypeScript | exit != 0 and `TS2339` — **never** grep `effect(` |
| `layer-missing-rin.ts` | Layer argument's own `RIn` unsatisfied | exit != 0 and `effect(missingLayerContext)` |
| `step-expect-error.ts` | both errors deliberately suppressed | exit **0** |

`layer-missing-rin.ts` is also the behavioral proof of `describeFeature`'s overload order
(Finding 6): declared the natural way round, this fixture is still rejected but the message
says "missing the properties shared, perScenario" and `effect(missingLayerContext)` never fires.

Purpose: after this plan every roadmap success criterion for Phase 5 has an automated assertion.
Output: three fixtures, three isolated configs, three gate assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-describefeature-type-surface/05-RESEARCH.md
@.planning/phases/05-describefeature-type-surface/05-PATTERNS.md
@.planning/phases/05-describefeature-type-surface/05-CONTEXT.md
@.planning/phases/05-describefeature-type-surface/05-04-SUMMARY.md
@AGENTS.md
</context>

<interfaces>
<!-- Same fixture template as 05-04. One file, one case, one tsconfig. -->

`packages/vitest/test/tsgo-gate/tsconfig.ok.json` shape: extends `./tsconfig.json`,
`"include": []`, `"files": ["src/<one>.ts"]`.

Stacked-directive form, verified exit 0 (RESEARCH.md Finding 3(A) and Code Examples §5). The
ORDER of the two comment lines is load-bearing: `@effect-diagnostics-next-line
missingEffectContext:off` must be the line IMMEDIATELY above the code; the `@ts-expect-error`
line goes above it. TypeScript skips intervening comment lines when resolving "next line"; the
plugin does not. Reversed, the compiler emits `warning TS377000: @effect-diagnostics directive
has no effect` PLUS the unsuppressed `TS377004`.

A file-level `@effect-diagnostics <rule>:off` also works but disables the rule for the whole
file — strictly worse for a fixture whose job is to be precise. Do not use it.

Reproduced diagnostics:
- World field absent from the declared type → `TS2339` only, no `TS377xxx`.
- `Layer<World, never, Db>` passed where `Layer<World, never, never>` is expected →
  `TS2769` plus `TS377034 effect(missingLayerContext)` naming `Db`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The two remaining negative fixtures — World field and Layer RIn</name>
  <files>packages/vitest/test/tsgo-gate/src/world-undeclared-field.ts, packages/vitest/test/tsgo-gate/tsconfig.world-field.json, packages/vitest/test/tsgo-gate/src/layer-missing-rin.ts, packages/vitest/test/tsgo-gate/tsconfig.layer-rin.json</files>
  <read_first>
    - .planning/phases/05-describefeature-type-surface/05-RESEARCH.md — Findings 1, 6, 10, 12 and Code Examples §3
    - packages/vitest/test/tsgo-gate/src/step-missing-service.ts (from 05-04 — the negative fixture shape and its World/Db declarations to duplicate)
    - packages/vitest/test/tsgo-gate/src/missing-layer-context.ts (the original negative template)
    - packages/vitest/src/describeFeature.ts (the overload pair whose order `layer-missing-rin.ts` exercises)
    - spec/decisions/002-world-is-a-typed-context-service.md
    - spec/behaviors/01-steps-and-world.md — BEH-EC-004 (DSL-03's requirement text)
  </read_first>
  <action>
    Create `packages/vitest/test/tsgo-gate/src/world-undeclared-field.ts`. Header states the
    asserted outcome: MUST NOT COMPILE, asserted as exit != 0 AND output containing `TS2339`,
    and — stated explicitly in the header — the assertion must NOT grep for `effect(`, because
    this is a plain TypeScript error, not an Effect diagnostic (RESEARCH.md Finding 10).

    Declare `World` inline as a `Context.Service` with an explicit
    `static readonly layer: Layer.Layer<World>` annotation, duplicated from the 05-04 fixtures
    rather than shared. Call `describeFeature(feature, World.layer, ({ Given }) => ...)` with a
    step body that yields `World` and then reads a field absent from World's declared shape.
    Comment above the offending line: this is the untyped-context-bag failure ADR-EC-002 exists
    to eliminate — a field that "doesn't exist yet" must be unreachable, not silently
    `undefined`.

    Create `packages/vitest/test/tsgo-gate/src/layer-missing-rin.ts`. Header states: MUST NOT
    COMPILE, asserted as exit != 0 AND output containing `effect(missingLayerContext)`. This
    fixture's defect is in the LAYER ARGUMENT, not in a step: construct a Layer whose own `RIn`
    is unsatisfied — a `Layer<World, never, Db>`, built by making `World`'s layer depend on `Db`
    and never providing `Db` — and pass it to `describeFeature`. Every step in the file may be
    trivially satisfiable; the Layer argument alone is the error.

    Add a comment on that call stating what this fixture actually guards: `describeFeature`'s
    overloads are declared object-form first and plain-Layer LAST precisely so TypeScript's
    "the LAST overload gave the following error" reporting lands on the plain-Layer form. With
    the overloads the natural way round, this call is still rejected — but the message reads
    "Type 'Layer<World, never, Db>' is missing the following properties from type
    '{ shared; perScenario }'", naming the wrong problem, and `effect(missingLayerContext)`
    never fires. Name `packages/vitest/src/describeFeature.ts`'s overload-order note as the
    other half of the pair.

    Both fixtures: no `any`, no `@ts-expect-error`, and never begin a comment line with `@ts-`
    (Finding 12). Export top-level bindings so nothing is unused-and-elided.

    Create `tsconfig.world-field.json` and `tsconfig.layer-rin.json`, each with a single `files`
    entry naming its fixture and no other keys. Run `pnpm format`.
  </action>
  <verify>
    <automated>node node_modules/typescript/bin/tsc -p packages/vitest/test/tsgo-gate/tsconfig.world-field.json 2>&1 | grep -q "TS2339" && node node_modules/typescript/bin/tsc -p packages/vitest/test/tsgo-gate/tsconfig.layer-rin.json 2>&1 | grep -q "effect(missingLayerContext)"</automated>
  </verify>
  <acceptance_criteria>
    - `tsc -p tsconfig.world-field.json` exits non-zero; its output contains `TS2339` and names the undeclared field.
    - That same output contains NO `TS377` code and no `effect(` substring — confirm with `grep -c 'effect(' <output>` returning 0. If an Effect diagnostic does appear, the fixture has a second defect and must be narrowed until `TS2339` is its only error.
    - `tsc -p tsconfig.layer-rin.json` exits non-zero; its output contains `effect(missingLayerContext)`, `TS377034`, and names the unprovided dependency.
    - That output does NOT contain `missing the following properties from type` — if it does, the overloads are in the wrong order in `describeFeature.ts` and that file must be fixed, not this fixture.
    - `grep -cE '^\s*//\s*@ts-' <each new fixture>` returns 0.
    - `grep -cE ':\s*any\b|<any>|as any' <each new fixture>` returns 0.
    - `bash scripts/verify-tsgo-gate.sh` still reports its six existing ✓ lines and `tsgo gate: ENFORCED` (the new fixtures are not yet wired in — that is task 3).
    - `pnpm lint` exits 0.
  </acceptance_criteria>
  <done>DSL-03's negative case fails as a plain `TS2339`, and an unsatisfied Layer argument fails by name as `effect(missingLayerContext)` — proving the overload order is right the way round.</done>
</task>

<task type="auto">
  <name>Task 2: The supplementary @ts-expect-error fixture (stacked directives)</name>
  <files>packages/vitest/test/tsgo-gate/src/step-expect-error.ts, packages/vitest/test/tsgo-gate/tsconfig.step-expect-error.json</files>
  <read_first>
    - .planning/phases/05-describefeature-type-surface/05-RESEARCH.md — Finding 3 (both resolutions), Finding 12, Code Examples §5, Open Question 1
    - packages/vitest/test/tsgo-gate/src/step-missing-service.ts (the exit-code fixture this one supplements — the code under the directives should be the same defect)
    - packages/gherkin/test/StepArgs.types.ts (the repo's existing bare-`@ts-expect-error` negatives block, and why that idiom is insufficient here)
  </read_first>
  <action>
    Create `packages/vitest/test/tsgo-gate/src/step-expect-error.ts`, the supplementary fixture
    that honors the roadmap's literal "`@ts-expect-error`-based negative type-test file" wording.

    Header must state, plainly: this file MUST COMPILE CLEAN (exit 0), and it compiles clean
    ONLY because both errors on the marked line are suppressed. If the DSL type is ever loosened
    so no error occurs there, `TS2578` ("Unused '@ts-expect-error' directive") fires and this
    fixture starts failing. The header must ALSO state that this fixture is strictly weaker than
    `step-missing-service.ts`: it proves an error occurred, not WHICH error. The exit-code
    fixture remains the primary DSL-01 proof; a regression that downgraded `TS377004` to a mere
    `TS2345` would pass this file unnoticed. Ship both, per RESEARCH.md Open Question 1's
    recommendation.

    Same `World`/`Db` declarations and the same defective step as `step-missing-service.ts`,
    duplicated per the fixture rule. Above the offending line, stack exactly two comment lines
    in this order:
    1. `@ts-expect-error` with a plain-English reason (the step requires `Db`, which the ambient
       Layer does not provide)
    2. `@effect-diagnostics-next-line missingEffectContext:off`

    The `@effect-diagnostics-next-line` comment MUST be the line immediately above the code.
    TypeScript skips intervening comment lines when resolving "next line"; the `@effect/tsgo`
    plugin does not. Reversed, the compiler emits `warning TS377000: @effect-diagnostics
    directive has no effect` plus the unsuppressed `TS377004`, and this fixture fails. Write
    that consequence as a comment beside the directives so nobody reorders them for readability.

    Do NOT use a file-level `@effect-diagnostics missingEffectContext:off` — it disables the
    rule for the whole file and would let a second, unrelated defect hide.

    Beyond the two directive lines themselves, never begin any comment line with `@ts-`
    (Finding 12) — including the header prose, which must write "a `@ts-expect-error` fixture"
    backticked and not line-initial or reword entirely. A prose line starting with `@ts-` becomes
    a live directive and produces a spurious `TS2578` pointing at the prose.

    Create `tsconfig.step-expect-error.json` with the single `files` entry. Run `pnpm format`,
    and re-check afterwards that dprint has not reflowed the two directive comments onto
    different lines or reordered them — if it does, adjust and re-verify exit 0.
  </action>
  <verify>
    <automated>node node_modules/typescript/bin/tsc -p packages/vitest/test/tsgo-gate/tsconfig.step-expect-error.json</automated>
  </verify>
  <acceptance_criteria>
    - `tsc -p tsconfig.step-expect-error.json` exits 0 and prints no diagnostics — no `TS2578`, no `TS377000`, no `TS377004`.
    - Directive order: in the file, the `@effect-diagnostics-next-line` line number is exactly one less than the offending code line, and the `@ts-expect-error` line is exactly one less than that. Verify with `grep -n`.
    - Reversal mutation (perform, then revert): swap the two directive lines, re-run the compile, and confirm it now emits `TS377000` and exits non-zero. Restore and confirm exit 0. Record the observed message in `05-05-SUMMARY.md`.
    - Loosening mutation (perform, then revert): temporarily add `Db.layer` to the ambient Layer so no error occurs on that line; confirm `TS2578` fires. Restore.
    - `grep -c 'effect-diagnostics missingEffectContext' packages/vitest/test/tsgo-gate/src/step-expect-error.ts` returns 0 — no file-level suppression.
    - `grep -cE '^\s*//\s*@ts-' packages/vitest/test/tsgo-gate/src/step-expect-error.ts` returns exactly 1 — the one real directive, and no prose line.
    - `pnpm lint` exits 0 and `pnpm format:check` passes with the directives on their required lines.
  </acceptance_criteria>
  <done>The roadmap's literal `@ts-expect-error` form exists, works, is documented as the weaker supplement, and both of its failure modes (reversed directives, loosened type) are demonstrated.</done>
</task>

<task type="auto">
  <name>Task 3: Gate assertions 7, 8 and 9</name>
  <files>scripts/verify-tsgo-gate.sh</files>
  <read_first>
    - scripts/verify-tsgo-gate.sh (as extended by 05-04 — assertions 5 and 6 are the immediate template)
    - .planning/phases/05-describefeature-type-surface/05-RESEARCH.md — Finding 1's diagnostic table, Finding 10
    - .planning/phases/05-describefeature-type-surface/05-PATTERNS.md — the gate-script section (failure-message style: what was observed, what it means, where to look)
    - .github/workflows/check.yml (no edit needed — `pnpm verify:tsgo-gate` is already a required step)
  </read_first>
  <action>
    Extend `scripts/verify-tsgo-gate.sh` with three more numbered assertion blocks and three
    config constants, spelled out in full:
    `WORLD_FIELD_CONFIG="packages/vitest/test/tsgo-gate/tsconfig.world-field.json"`,
    `LAYER_RIN_CONFIG="packages/vitest/test/tsgo-gate/tsconfig.layer-rin.json"`,
    `STEP_EXPECT_ERROR_CONFIG="packages/vitest/test/tsgo-gate/tsconfig.step-expect-error.json"`.
    Add all three to the existence loop.

    Assertion 7 — DSL-03. Exit != 0, then grep `TS2339`. Put a NOTE comment above the grep
    stating that this fixture produces a plain TypeScript error and that the assertion must NOT
    grep for `effect(` — an Effect diagnostic here would mean the fixture acquired a second
    defect (RESEARCH.md Finding 10). Failure message: a World field absent from the declared
    type was reachable, so ADR-EC-002's typed-context guarantee is decorative.

    Assertion 8 — the Layer argument. Exit != 0, then grep `effect(missingLayerContext)`.
    Failure message on the second check: the Layer argument was rejected but not by name — most
    likely `describeFeature`'s overloads were reordered so the plain-Layer form is no longer
    last, which makes TypeScript report against the object form and name the wrong problem
    (RESEARCH.md Finding 6). Note in a comment that assertion 4 greps the same diagnostic on a
    different fixture (a bare `Layer.merge` misuse), and that the two are not redundant: 4
    guards the compiler plugin, 8 guards this DSL's overload order.

    Assertion 9 — the supplementary stacked-directive fixture. Exit == 0 only. Failure message:
    the `@ts-expect-error` fixture stopped compiling clean — either the directive order was
    changed (the `@effect-diagnostics-next-line` comment must be immediately above the code) or
    the DSL type was loosened so no error occurs and `TS2578` fired. State in the banner comment
    that this assertion is deliberately weaker than assertion 6 and exists to honor the
    roadmap's literal `@ts-expect-error` wording, not to replace the exit-code proof.

    Keep the file's structure: banner comment per assertion, capture-with-exit-code idiom, echo
    the output before failing, ✓ line on success, `tsgo gate: ENFORCED` last.
  </action>
  <verify>
    <automated>bash scripts/verify-tsgo-gate.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-tsgo-gate.sh` exits 0, prints nine ✓ lines, and ends with `tsgo gate: ENFORCED`.
    - `grep -c 'TS2339' scripts/verify-tsgo-gate.sh` returns at least 1.
    - Assertion 7 does not grep `effect(` — confirm by reading the block; the NOTE comment explaining why is present.
    - All three new `*_CONFIG` constants appear in the existence loop.
    - Overload-order mutation (perform, then revert): in `packages/vitest/src/describeFeature.ts`, swap the two overload declarations so the plain-Layer form is first. Re-run the script. Assertion 8's SECOND check must fail while the call is still rejected — reproducing RESEARCH.md Finding 6. Restore and confirm green. Record the exact failure message in `05-05-SUMMARY.md`.
    - Vacuity mutation for assertion 7 (perform, then revert): change the fixture to read a field that IS declared. Assertion 7's exit-code check must fail. Restore.
    - Full suite green: `pnpm build && pnpm typecheck:test && pnpm verify:tsgo-gate && pnpm test && pnpm lint`.
  </acceptance_criteria>
  <done>Nine assertions run on every push; DSL-01, DSL-03 and the overload-order constraint each fail by name if broken, and both new mutation proofs are recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiler exit code → CI merge verdict | The only boundary. The fixtures never execute; they are compiled and discarded. |

No network, no I/O, no runtime input, no auth, no crypto, no persistence.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering / Repudiation | `scripts/verify-tsgo-gate.sh` assertions 7-9 | mitigate | Gate decay. Each assertion checks exit code plus a specific code or diagnostic name, never output text alone, and each carries a mutation proof (overload swap for 8, declared-field swap for 7, directive reversal for 9). |
| T-05-14 | Repudiation | assertion 7 grepping `effect(` | mitigate | Grepping for an Effect diagnostic on a fixture that produces only `TS2339` would make the assertion permanently fail or invite deleting it. A NOTE comment above the grep records Finding 10; the acceptance criteria require the fixture's output to contain no `effect(` substring. |
| T-05-15 | Tampering | `step-expect-error.ts` treated as the primary proof | mitigate | Assertion 9 only proves *an* error occurred. Its banner comment and the fixture header both state it is the weaker supplement and that assertion 6 is the DSL-01 proof, so a future editor cannot delete assertion 6 believing 9 covers it. |
| T-05-16 | Tampering | file-level `@effect-diagnostics` suppression | mitigate | A file-level `off` would hide a second, unrelated defect and make the fixture compile clean for the wrong reason. Forbidden by an acceptance grep. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; no manifest dependency field changes. RESEARCH.md's Package Legitimacy Audit records the phase as not applicable. |
</threat_model>

<verification>
- `bash scripts/verify-tsgo-gate.sh` — nine assertions, `tsgo gate: ENFORCED`
- `pnpm build && pnpm typecheck:test && pnpm test && pnpm lint`
- Overload-order and directive-reversal mutations recorded in the summary
</verification>

<success_criteria>
- `TS2339` on an undeclared World field, with no Effect diagnostic in that fixture's output
- `effect(missingLayerContext)` on an unsatisfied Layer argument, proving the overload order
- The stacked-directive fixture compiles clean, and both its failure modes were demonstrated
- Swapping `describeFeature`'s overloads fails assertion 8's named-diagnostic check
</success_criteria>

<output>
Create `.planning/phases/05-describefeature-type-surface/05-05-SUMMARY.md` when done
</output>
