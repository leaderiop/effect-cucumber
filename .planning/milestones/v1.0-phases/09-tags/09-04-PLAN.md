---
phase: 09-tags
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - packages/vitest/src/TestApi.ts
  - packages/vitest/src/Runner.ts
  - packages/vitest/src/describeFeature.ts
  - packages/vitest/test/Runner.test.ts
autonomous: true
requirements: [RUN-05]

must_haves:
  truths:
    - "Every tag on a Scenario reaches the emitted test node, in inheritance order, with its @ prefix intact (D-04)"
    - "A Scenario tagged @skip is emitted as a skipped test, never as a passing one (D-05)"
    - "A Scenario tagged @only is emitted with no only-marking of any kind (D-06)"
    - "A Scenario excluded by a tag filter produces no emission record at all — not a skipped one (D-03)"
    - "A tag filter never changes which step definitions are reported unused"
    - "A Feature whose Scenarios are all skipped or all filtered out emits no AfterAllScenarios node"
    - "No test framework is named anywhere in Runner.ts or TestApi.ts"
  artifacts:
    - path: "packages/vitest/src/TestApi.ts"
      provides: "EmitOptions — library-owned tag/skip data on the emission seam"
      exports: ["TestApi", "EmitOptions"]
    - path: "packages/vitest/src/Runner.ts"
      provides: "Registration filtering inside the emission walk plus per-Scenario emit options"
      contains: "shouldEmit"
  key_links:
    - from: "packages/vitest/src/Runner.ts"
      to: "packages/vitest/src/Tags.ts"
      via: "shouldEmit / isSkipped import"
      pattern: "from \"./Tags.ts\""
    - from: "packages/vitest/src/Runner.ts"
      to: "packages/vitest/src/TestApi.ts"
      via: "api.effect third argument"
      pattern: "skip:"
---

<objective>
Make tags real at the emission seam: `TestApi.effect` grows a third, library-owned options argument
carrying the Scenario's tags and its skip flag, and `emitFeature` computes both per Scenario, applies
the registration filter inside its own walk, and suppresses the `AfterAllScenarios` node when nothing
runnable was emitted.

Purpose: this is the load-bearing change of the phase. RESEARCH Findings 12 and 13 make the filter's
placement non-negotiable — earlier and it either trips `planFor`'s "unreachable by construction"
throw or silently corrupts the unused-step-definition warnings.
Output: one new interface, a filtered and option-carrying emission walk, and the unit assertions for
success criteria 1, 3 and 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tags/09-CONTEXT.md
@.planning/phases/09-tags/09-RESEARCH.md
@.planning/phases/09-tags/09-PATTERNS.md
@.planning/phases/09-tags/09-02-SUMMARY.md
@.planning/phases/09-tags/09-03-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- Contracts consumed (created by 09-02 and 09-03) -->

packages/vitest/src/Tags.ts:
  export const skipTag: "@skip"
  export const onlyTag: "@only"
  export interface TagFilter { readonly include: ReadonlyArray<string>; readonly exclude: ReadonlyArray<string> }
  export const noTagFilter: TagFilter
  export const shouldEmit: (filter: TagFilter, tags: ReadonlyArray<string>) => boolean
  export const isSkipped: (tags: ReadonlyArray<string>) => boolean

packages/vitest/src/Plan.ts:
  export type ScenarioPlan = {
    readonly scenarioId: string
    readonly name: string
    readonly astName: string
    readonly ruleId: Option.Option<string>
    readonly tags: ReadonlyArray<string>
    readonly steps: ReadonlyArray<PlannedStep>
  }

<!-- Contracts CREATED here. Plan 09-05 implements the real adapter against EmitOptions and reads
     EmitOutcome; do not reshape either without updating that plan. -->

packages/vitest/src/TestApi.ts:
  export interface EmitOptions {
    readonly tags: ReadonlyArray<string>
    readonly skip: boolean
  }
  export interface TestApi {
    readonly describe: (name: string, define: () => void) => void
    readonly effect: (
      name: string,
      self: () => Effect.Effect<void, unknown, Scope.Scope>,
      options: EmitOptions
    ) => void
  }

packages/vitest/src/Runner.ts:
  export interface EmitOutcome { readonly excludedScenarioCount: number }
  export const emitFeature: (args: { … existing seven fields … ; readonly tagFilter: TagFilter }) => EmitOutcome

<!-- Verified upstream shape (RESEARCH Findings 4, 10) — relevant only to plan 09-05's adapter:
     @effect/vitest's Tester takes TestOptions as its THIRD argument and forwards it verbatim;
     TestOptions.tags is `string[]` (mutable), TestOptions.skip is `boolean`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EmitOptions to TestApi.ts and rewrite the stale note (b)</name>
  <read_first>
    - packages/vitest/src/TestApi.ts (WHOLE FILE — note (a) at 13-25, note (b) at 27-33 which this task redeems, and the closing paragraph at 50-58 forbidding any runtime value here)
    - packages/vitest/src/Errors.ts (lines 190-199 — the library-owned plain-data interface pattern: `readonly` on every field, `ReadonlyArray`, one doc comment per field)
    - .planning/phases/09-tags/09-RESEARCH.md (Pattern 1, and the Alternatives Considered row weighing `TestOptions.skip` against a second `TestApi` member)
  </read_first>
  <action>
Add the `EmitOptions` interface from the `<interfaces>` block and make it `TestApi.effect`'s third,
REQUIRED parameter.

`EmitOptions` is the library's own type and names no framework type. Note (a) forbids even an
`import type` from the test framework in this file and in `Runner.ts`; plan 09-01's
`scripts/verify-testapi-seam.sh` now enforces that structurally. Modelling the framework's own
options type here would type-check, lint clean, pass every test, and quietly undo the seam.

Required, not optional, and there is no `EmitOptions | undefined`: `Runner.ts` computes a value for
every emission including the synthetic `⚙ AfterAllScenarios` and `⚠` warning nodes, and an optional
parameter would let a future call site forget the argument and silently emit an untagged test.
`describeFeature.ts`'s "All SEVEN fields" comment and `Runner.ts`'s required-map arguments are the
in-package precedent.

`tags` is `ReadonlyArray<string>` and `skip` is `boolean`. Document on the `tags` field that the
array arrives already flattened in feature → rule → scenario → examples order with literal `@`
prefixes retained, and that the single widening to a mutable array happens in `describeFeature.ts`'s
adapter and nowhere else. Document on `skip` that a skipped test's `self` thunk is NEVER invoked, so
`buildScenarioEffect` is never called and no hook Effect is ever constructed — that is what makes
"a `@skip` Scenario's `Before`/`After` hooks do not run" structural rather than arranged
(RESEARCH Finding 5), and it is also why Pitfall 15 holds: an unresolved step in a `@skip` Scenario
reports skipped rather than raising `StepMatchError`, because that error is only reached at `yield*`
time inside the Effect.

Rewrite note (b). It currently reserves `skip`/`only` for this phase; that reservation is now
redeemed and leaving the text standing violates AGENTS.md §4. The replacement must say what was
actually built and why: `skip` arrives as a FIELD on `EmitOptions` rather than as a second
`skipEffect` member on this interface, because the field keeps the interface at two members and makes
06-06's recording fake grow a recorded value rather than an unasserted method; and `only` is absent
entirely and permanently, because D-06 never routes `@only` to the framework's only-mode — an
`@only`-tagged Scenario is emitted as a plain tagged test, which is what makes a committed `@only`
unable to fail a CI run (RESEARCH Finding 6: the framework's `allowOnly` check is only reachable from
branches guarded by a task already being in only-mode, so never emitting one makes it unreachable).

This file still contains types only — no `const`, no function, no runtime value. Both imports stay
`import type`.
  </action>
  <verify>
    <automated>pnpm verify:testapi-seam</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export interface EmitOptions' packages/vitest/src/TestApi.ts` is 1.
    - `grep -vE '^[[:space:]]*(\*|//|/\*)' packages/vitest/src/TestApi.ts | grep -cE '^(const|export const|function|export function)' ` is 0 (types only).
    - `grep -cE '^import ' packages/vitest/src/TestApi.ts` is 2 and both are `import type`.
    - `bash scripts/verify-testapi-seam.sh` exits 0.
    - Note (b)'s text no longer contains the words "Phase 9" or "deliberately absent" applied to `skip`; it states what was built and why `only` is permanently absent.
    - `grep -c 'skipEffect' packages/vitest/src/TestApi.ts` is 0 and the interface still has exactly two members.
  </acceptance_criteria>
  <done>The emission seam carries library-owned tag and skip data, and its doc comment describes what exists rather than what was reserved.</done>
</task>

<task type="auto">
  <name>Task 2: Filter and option-carry inside emitFeature's walk; suppress AfterAllScenarios when nothing runnable emits</name>
  <read_first>
    - packages/vitest/src/Runner.ts (WHOLE FILE — especially note (a) at 18-30, note (c), note (e) at ~100-112 which this task amends, `warningTitle`/`afterAllScenariosTitle` at 184-197, the `emitFeature` signature at 275-286, the derived-once helpers at 288-331, the Feature-level Scenario loop at 342-355, the Rule-level loop at 379-398, the AfterAllScenarios emission at 404-411, and the warning-node loop at 415-417)
    - packages/vitest/src/Tags.ts (the surface created by 09-02)
    - packages/vitest/src/TestApi.ts (as amended in Task 1)
    - packages/vitest/src/describeFeature.ts (lines 215-223 for the `vitestTestApi` constant, and lines 693-712 for the `emitFeature` call and its "All SEVEN fields" comment)
    - .planning/phases/09-tags/09-RESEARCH.md (Findings 12 and 13, Pattern 3, and Pitfalls 3, 4, 6, 7)
    - .planning/phases/09-tags/09-CONTEXT.md (D-03, D-05, D-06, and the "AfterAllScenarios/BeforeAllScenarios asymmetry" bullet)
  </read_first>
  <action>
Change `emitFeature` in five ways, and make the one minimal `describeFeature.ts` edit that keeps the
build green.

1. **Signature.** Add `readonly tagFilter: TagFilter` as an eighth REQUIRED field of the args object,
   and change the return type from `void` to `EmitOutcome` (`{ readonly excludedScenarioCount: number }`).
   Declare and export `EmitOutcome` in this file. Required rather than optional, with `noTagFilter` as
   the explicit "no filter" value a caller passes — RESEARCH Finding 12 reaches this conclusion
   independently of `describeFeature.ts`'s own "All SEVEN fields" reasoning. Document the return value:
   `describeFeature.ts` needs the count to print D-10's single collection-time notice, and the count
   cannot be computed anywhere else without duplicating this walk. Do NOT print anything from this
   module — a `console.warn` here would spam `Runner.test.ts`, which calls `emitFeature` directly with
   a recording fake, and it would break the same rule `describeFeature.ts`'s terminal-channel comment
   states for `collectFeature`.

2. **Registration filter.** In BOTH Scenario loops, after `planFor(scenario)` and before anything is
   emitted, `continue` when `shouldEmit(tagFilter, scenarioPlan.tags)` is false, incrementing an
   exclusion counter. Apply it to both loops separately and do NOT factor them together —
   `Runner.ts`'s own comment at 357-359 says the duplication is deliberate because a shared helper
   hides which block a node lands in. Filtering here and nowhere else is mandatory: pre-filtering
   `plan.scenarios` in `describeFeature.ts` trips `planFor`'s "unreachable by construction" throw,
   because this walk iterates `plan.feature.scenarios` and looks the plan up (RESEARCH Finding 12);
   filtering at plan time silently makes every step definition used only by an excluded Scenario
   report as unused on all three warning channels (RESEARCH Finding 13). Write both failure modes into
   a new lettered note so a future reader cannot "simplify" the placement.

3. **Emit options.** Pass a third argument to every `api.effect` call:
   - Scenario nodes (both loops): `{ tags: scenarioPlan.tags, skip: isSkipped(scenarioPlan.tags) }`.
     Nothing branches on `onlyTag` — D-06.
   - The `⚙ AfterAllScenarios` node and every `⚠` warning node: `{ tags: [], skip: false }`. These are
     synthetic library nodes and carry no document text; giving them a Feature's tags would make a
     `--tagsFilter` invocation skip a Feature's teardown, and would put author-controlled strings
     into a second framework validation site for nothing.
   Do NOT emit tags on the `api.describe` calls. The framework already merges and de-duplicates
   parent-suite tags onto each test (RESEARCH Finding 8), so emitting on both is redundant, and suite
   tags are separately validated, which is one more place to throw.

4. **AfterAllScenarios suppression.** Count Scenarios that were both EMITTED (survived the filter) and
   NOT skipped. Change the emission condition to require that count to be greater than zero in
   addition to the existing `hooks.AfterAllScenarios.length > 0`. Amend note (e), which currently
   argues "runs always" as a virtue: the guarantee it describes is about a FAILED Scenario or a failed
   `BeforeAllScenarios` being unable to stop teardown, and that guarantee is unchanged. What changes is
   the vacuous case — when nothing runnable was emitted, `BeforeAllScenarios` (a once-cell reached only
   from inside a Scenario thunk) structurally cannot have run, so an `AfterAllScenarios` node would
   tear down resources that were never set up. State that a Feature with zero Scenarios falls in the
   same case for the same reason.

5. **What deliberately does NOT change.** The `⚠` warning nodes still emit even when every Scenario is
   filtered out — they describe REGISTRATION, not execution, and their absence would make a filtered
   run look like a Feature with no unused definitions. The `describe` blocks still emit even when empty
   for the same reason plus `Runner.ts` note (c)'s argument about reporter findability. Record both as
   decisions in the note, not as omissions.

Finally, in `describeFeature.ts` make ONLY the two minimal edits needed for the build: pass
`tagFilter: noTagFilter` in the `emitFeature` call (updating the "All SEVEN fields" comment's count to
EIGHT and adding a sentence for the new field), and give the module-scope `vitestTestApi` an `effect`
member that forwards the options — spreading `options.tags` into a fresh mutable array at that one
point, because the framework's options type wants `string[]` while everything upstream is
`ReadonlyArray` (RESEARCH Finding 10, Pitfall 8). Do not turn the adapter into a factory and do not
add the catch-and-degrade here; plan 09-05 owns both, along with the public option and the D-10
notice.
  </action>
  <verify>
    <automated>pnpm build && pnpm verify:testapi-seam && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` and `pnpm typecheck:test` exit 0.
    - `grep -c 'readonly tagFilter: TagFilter' packages/vitest/src/Runner.ts` is 1 and `grep -c 'export interface EmitOutcome' packages/vitest/src/Runner.ts` is 1.
    - `grep -c 'shouldEmit(' packages/vitest/src/Runner.ts` is 2 — one per Scenario loop, not factored together.
    - `grep -c 'isSkipped(' packages/vitest/src/Runner.ts` is 2.
    - `grep -cE 'console\.(warn|log|error)' packages/vitest/src/Runner.ts` is 0.
    - `bash scripts/verify-testapi-seam.sh` exits 0 (no framework import crept in).
    - `grep -vE '^[[:space:]]*(\*|//|/\*)' packages/vitest/src/Runner.ts | grep -c 'api.describe('` is 2 and neither call passes a third argument.
    - `grep -c 'All EIGHT fields' packages/vitest/src/describeFeature.ts` is 1 (the stale count was updated, not left at SEVEN).
    - `grep -c 'noTagFilter' packages/vitest/src/describeFeature.ts` is 1.
    - `pnpm test` exits 0 with no pre-existing assertion changed.
    - `Runner.ts` note (e) states the suppression condition and the vacuous-`BeforeAllScenarios` reason; a new lettered note states both forbidden filter placements and their exact failure modes.
  </acceptance_criteria>
  <done>The emission walk filters at registration time, carries tags and skip per Scenario, suppresses teardown when nothing runnable emitted, and still names no test framework.</done>
</task>

<task type="auto">
  <name>Task 3: Assert emission shape in Runner.test.ts — criteria 1, 3 and 4, plus the warning-invariance guard</name>
  <read_first>
    - packages/vitest/test/Runner.test.ts (WHOLE FILE — `EmissionRecord` at 143-160, `makeRecordingApi` at 177-198, `shapeOf` at 200-204, `thunkAt`, and every hand-built `ScenarioPlan` literal updated by plan 09-03)
    - packages/vitest/src/Runner.ts (as amended in Task 2)
    - packages/vitest/src/Tags.ts
    - .planning/phases/09-tags/09-RESEARCH.md (Finding 7 — the observable difference between an excluded Scenario and a CLI-filtered one; Pitfall 4 — the silent warning corruption this task guards)
  </read_first>
  <action>
Extend the recording fake so the third `api.effect` argument is captured, then assert the emission
contract.

Fake changes: add an options field to `EmissionRecord` — recorded as the `EmitOptions` value for an
`effect` record and as `null` for a `describe` record, exactly as `self` is `null` there today and for
the same reason lines 150-153 give. Extend `shapeOf`'s projection to include the tags and the skip
flag, or add a sibling projection; most existing assertions compare through `shapeOf`, so a value not
present in the projection is invisible to them.

Assertions to add (each with a name that states the criterion it carries):

- **SC1 — inheritance reaches the node.** A Feature whose Scenario carries four tags from four levels
  emits one record whose `options.tags` equals that four-element array in order, with `@` prefixes
  intact. Use the tag names plan 09-01 declared.
- **SC1 — no tags on the describe.** No `describe` record carries emit options.
- **SC2 — `@skip` routes to skip.** A Scenario tagged `@skip` emits a record with `options.skip` true
  and its tags still present; a Scenario without it emits `options.skip` false.
- **SC3 — `@only` is inert.** A Scenario tagged `@only` emits a record carrying `@only` in
  `options.tags` and NOTHING else different from an untagged Scenario — same record count, same
  `skip: false`. Assert structurally that no recorded emission carries any only-marking: the fake has
  no only channel to record one on, so state that in the test's comment and additionally assert
  `onlyTag` never appears outside `options.tags`.
- **SC4 — `excludeTags` removes the emission entirely.** With a filter excluding `@wip`, the record
  list contains NO record for the `@wip` Scenario. This is the distinction RESEARCH Finding 7 makes
  observable: an excluded Scenario is ABSENT, while a CLI-filtered one would appear as skipped. Assert
  absence by title, not merely a lower count.
- **SC4 — `includeTags` restricts emission.** With a filter including `@slow`, only `@slow` Scenarios
  emit, including across a Rule's nested loop, and an untagged Scenario does not.
- **SC4 — both filters together.** A tag named in both arrays excludes.
- **Pitfall 4 — the filter cannot change the warnings.** Call `emitFeature` twice over the SAME
  `FeaturePlan`, once with `noTagFilter` and once with a filter that excludes every Scenario, and
  assert the `⚠` warning nodes emitted are identical in both runs and that `plan.warnings` is
  unchanged. This is the only assertion that can see a regression to plan-time filtering; label it
  with that fact.
- **AfterAllScenarios suppression.** A Feature with an `AfterAllScenarios` hook emits the `⚙` node
  when at least one Scenario is emitted and not skipped, and does NOT emit it when every Scenario is
  `@skip`-tagged, when every Scenario is filtered out, and when the Feature has no Scenarios at all.
- **EmitOutcome.** `excludedScenarioCount` is the number of Scenarios the filter removed, across both
  the Feature-level and the Rule-level loops, and is 0 under `noTagFilter`.

Import from `../src/Tags.ts` and `../src/Runner.ts` by direct relative path, never through
`../src/index.ts`.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/vitest/test/Runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run packages/vitest/test/Runner.test.ts` exits 0 with at least 10 more tests than before this task.
    - `pnpm typecheck:test` exits 0.
    - `grep -c "from \"../src/index.ts\"" packages/vitest/test/Runner.test.ts` is 0.
    - Mutation proof 1 recorded in the summary: replacing the Feature-level loop's `continue` with a no-op leaves the include/exclude assertions failing by NAME (proves the filter is not asserted only through a count).
    - Mutation proof 2 recorded in the summary: moving the filter to a pre-filter of `plan.scenarios` in `describeFeature.ts` produces `planFor`'s "no ScenarioPlan for scenario id" throw (RESEARCH Finding 12 reproduced deliberately, then reverted).
    - Mutation proof 3 recorded in the summary: dropping the runnable-count conjunct from the `AfterAllScenarios` condition makes the fully-skipped-Feature assertion fail.
    - Mutation proof 4 recorded in the summary: making `isSkipped` always return `false` makes the `@skip` assertion fail while every other assertion still passes.
  </acceptance_criteria>
  <done>Success criteria 1, 3 and 4 hold at the emission seam under a recording fake, with the filter's placement and the teardown suppression each defended by a recorded mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.feature` author → framework tag validation | `ScenarioPlan.tags` crosses out of the library into the framework's collector via `EmitOptions`. |
| library seam → test framework | `TestApi` is the only crossing; a framework import here would dissolve it invisibly. |
| consumer filter config → emitted suite | `tagFilter` decides which Scenarios exist at all in a run's output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Spoofing | Tag strings interpolated into node titles | mitigate | No node title is built from a tag anywhere in this plan. Synthetic nodes keep their existing titles: `afterAllScenariosTitle` is a bare constant with nothing to forge with, and `warningTitle` already `JSON.stringify`s its pattern. Asserted by the `api.describe` third-argument criterion and by the unchanged title constants. |
| T-09-04-02 | Tampering | Silent-green from a stale `excludeTags` | mitigate (partial, completed in 09-05) | `emitFeature` returns `excludedScenarioCount` so plan 09-05 can print D-10's notice; asserted here for both loops. |
| T-09-04-03 | Tampering | Filter placed before planning, corrupting unused-definition warnings | mitigate | The filter lives inside the walk; the Pitfall 4 double-emission assertion is the only thing that can observe a regression and is labelled as such. |
| T-09-04-04 | Denial of service | Teardown running against resources never set up | mitigate | `AfterAllScenarios` is suppressed when no runnable Scenario was emitted, with a recorded mutation proving the conjunct is load-bearing. |
| T-09-04-05 | Elevation of privilege | An `only`-marked task failing or bypassing CI | mitigate | `EmitOptions` has no only channel and `Runner.ts` branches on nothing but `isSkipped`; the framework's `allowOnly` check is unreachable when no task is in only-mode (RESEARCH Finding 6). |
| T-09-04-06 | Tampering | A framework import dissolving the seam | mitigate | `pnpm verify:testapi-seam` (plan 09-01) is a verify command on two of this plan's three tasks. |
| T-09-04-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; RESEARCH's Package Legitimacy Audit records ONE new direct dependency for the phase overall (tinyglobby@0.2.17, added by plan 09-07 under CONTEXT.md's D-09 addendum), not zero. |
</threat_model>

<verification>
- `pnpm build`, `pnpm typecheck:test`, `pnpm lint`, `pnpm circular` — all exit 0.
- `pnpm test` — full suite green; no pre-existing assertion changed.
- `pnpm verify:testapi-seam` — exits 0.
- `pnpm verify:tsgo-gate` — unchanged; this plan does not touch `describeFeature`'s overloads.
</verification>

<success_criteria>
Roadmap criteria 1, 3 and 4 hold at the emission seam: every inherited tag reaches the emitted node
with its `@` prefix, `@skip` produces a skipped emission whose body is never invoked, `@only` produces
nothing but a plain tag, and a filtered-out Scenario produces no emission record at all — each
defended by a recorded mutation.
</success_criteria>

<output>
Create `.planning/phases/09-tags/09-04-SUMMARY.md` when done. Record: the final `emitFeature`
signature and `EmitOutcome` shape (plan 09-05 calls it), all four mutation proofs, and the exact
wording of the amended `Runner.ts` note (e) and the new filter-placement note.
</output>
