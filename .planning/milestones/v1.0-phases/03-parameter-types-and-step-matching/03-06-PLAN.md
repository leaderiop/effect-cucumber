---
phase: 03-parameter-types-and-step-matching
plan: 06
type: execute
wave: 5
depends_on: ["03-05"]
files_modified:
  - spec/behaviors/05-step-matching-and-parameter-types.md
  - spec/behaviors/index.yaml
  - spec/behaviors/04-loadfeature-parse-and-validation.md
  - spec/traceability.md
  - spec/decisions/007-cucumber-expressions-for-step-matching.md
  - spec/roadmap.md
  - packages/gherkin/README.md
autonomous: true
requirements: [MATCH-01, MATCH-02]

must_haves:
  truths:
    - "BEH-EC-015 exists as a normative behavior doc and is registered in spec/behaviors/index.yaml"
    - "ADR-EC-007's open implementation question — Layer-provided service versus ambient list — is closed in writing, citing ADR-EC-015 as the reason"
    - "BEH-EC-014's Signatures block matches loadFeature's real signature after the optional options argument was added"
    - "spec/traceability.md section 4 names every packages/gherkin test file that exists on disk"
    - "No status document claims step matching or parameter types are unbuilt"
  artifacts:
    - path: "spec/behaviors/05-step-matching-and-parameter-types.md"
      provides: "BEH-EC-015 — the normative contract for cucumber-expression coercion, custom parameter types as data, and match-every-pattern"
      contains: "BEH-EC-015"
      min_lines: 120
    - path: "spec/behaviors/index.yaml"
      provides: "the registry entry that makes verify-traceability check 1 pass in both directions"
      contains: "BEH-EC-015"
  key_links:
    - from: "spec/behaviors/05-step-matching-and-parameter-types.md"
      to: "spec/decisions/007-cucumber-expressions-for-step-matching.md"
      via: "a > **See:** blockquote linking the governing ADR by relative path"
      pattern: "007-cucumber-expressions-for-step-matching\\.md"
    - from: "spec/traceability.md"
      to: "packages/gherkin/test/"
      via: "section 4 rows enumerated from disk, never hand-guessed"
      pattern: "packages/gherkin/test/StepMatcher\\.test\\.ts"
---

<objective>
Close AGENTS.md §1 for Phase 3. `spec/` is normative and code follows it: a code change not reflected
in `spec/` in the same change is incomplete, not merely undocumented. Phase 3 shipped a real
parameter-type lifecycle, a real matcher, and a changed `loadFeature` signature, and none of it is in
`spec/` yet.

Four documents currently say something that is no longer true or was never decided:
`loadFeature`'s normative signature listing predates the optional options argument; ADR-EC-007's
second correction leaves the store-versus-`Layer` question explicitly open "for Phase 2 of the
roadmap"; `spec/traceability.md` §4 enumerates seven test files when there are now more; and both
status documents state that step matching and parameter types are not built.

Purpose: MATCH-01 and MATCH-02 become traceable spec, not just passing tests.
Output: BEH-EC-015 plus registry entry, a corrected BEH-EC-014 signature listing, an ADR-EC-007
implementation note, an updated traceability matrix, and two truthful status documents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@spec/decisions/007-cucumber-expressions-for-step-matching.md
@spec/decisions/019-fail-loudly-on-unmatched-or-ambiguous-steps.md
@spec/behaviors/04-loadfeature-parse-and-validation.md

<interfaces>
<!-- Established spec conventions, from plan 02-11's summary. Follow them exactly. -->
A behavior doc is: a `##` heading carrying the ID, then a `> **See:**` blockquote linking every
governing ADR by RELATIVE path, then prose, then a plain (untagged) fenced `REQUIREMENT:` block.
A `ts` fence is a signature listing (reference only). A `typescript` fence is a runnable example
that the planned doc-examples check will compile against the real API — it must import what it uses.
Every behavior file carries a `_Previous:_` and, where one exists, a `_Next:_` footer link.
A superseded normative rule is marked superseded IN PLACE and replaced by a dated correction note,
never rewritten out of history.
Status truth lives in exactly one place — `spec/roadmap.md`'s Current state table. Every other
document cites it rather than restating build status.
A normative sentence an implementation reproduces verbatim is kept on one unbroken markdown line so
it stays greppable against the source.

<!-- The next free behavior id is BEH-EC-015. The highest allocated is BEH-EC-014. -->
<!-- Ids are allocated contiguously, never renumbered, never reused (AGENTS.md section 6). -->

<!-- verify-traceability.sh (run as `pnpm verify:spec`) checks index.yaml against disk in BOTH
     directions, checks every invariant and decision is traced, and resolves every relative link.
     A new behavior file that is not registered fails check 1. A broken relative link fails check 5. -->

<!-- dprint formats spec/**/*.md including fenced ts blocks. Run `pnpm format` after editing, or
     `pnpm lint` (which runs `dprint check`) will fail. -->

<!-- The public surface Phase 3 shipped, for the signature listing: -->
export interface ParameterTypeDefinition<T> {
  readonly name: string
  readonly regexp: string | RegExp | ReadonlyArray<string | RegExp>
  readonly transform: (...match: Array<string>) => T
  readonly definedAt?: string
  readonly useForSnippets?: boolean
  readonly preferForRegexpMatch?: boolean
}
export const defineParameterType: <T>(definition: ParameterTypeDefinition<T>) => void
export const createParameterTypeStore: () => ParameterTypeStore
export const buildParameterTypeRegistry: () => ParameterTypeRegistry
export const builtInParameterTypeNames: ReadonlySet<string>
export const createStepMatcher: <D>(args: {
  registry: ParameterTypeRegistry
  entries: ReadonlyArray<StepPatternEntry<D>>
}) => StepMatcher<D>
export const compileExpression: (registry: ParameterTypeRegistry, pattern: string) => CucumberExpression
export type StepArgs<P extends string, Custom extends Record<string, unknown> = {}> = ...
export interface LoadFeatureOptions { readonly parameterTypes?: ParameterTypeStore }
export const loadFeature: (path: string, options?: LoadFeatureOptions) => ParsedFeature
export const parseFeature: (source: string, uri: string, options?: LoadFeatureOptions) => ParsedFeature
export interface ParsedFeature extends ParsedFeatureCore {
  readonly warnings: ReadonlyArray<LoadFeatureWarning>
  readonly parameterTypes: ParameterTypeRegistry
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write BEH-EC-015 and register it</name>
  <files>spec/behaviors/05-step-matching-and-parameter-types.md, spec/behaviors/index.yaml, spec/behaviors/04-loadfeature-parse-and-validation.md</files>
  <read_first>
    - spec/behaviors/04-loadfeature-parse-and-validation.md (the file this one is modelled on structurally, and the file whose footer must gain a `_Next:_` link — read its heading style, its `> **See:**` blockquote, its two `REQUIREMENT:` blocks, its `ts` Signatures section, and its `typescript` worked example)
    - spec/behaviors/index.yaml (the registry format: `id`, `file`, `title`, `id_range`)
    - spec/behaviors/01-steps-and-world.md (the `_Next:_` footer convention that file 04 currently lacks)
    - spec/decisions/007-cucumber-expressions-for-step-matching.md (both corrections — the second is the governing text for the lifecycle requirement)
    - spec/decisions/019-fail-loudly-on-unmatched-or-ambiguous-steps.md (BEH-EC-013's scope — the boundary this doc must not cross)
    - packages/gherkin/src/ParameterTypes.ts, packages/gherkin/src/StepMatcher.ts, packages/gherkin/src/StepArgs.ts (the shipped implementation this doc describes; the doc must say only what is true of it)
    - spec/glossary.md (the `cucumber-expression` and `Scenario Outline` entries — the doc should be consistent with both)
  </read_first>
  <action>
    Create `spec/behaviors/05-step-matching-and-parameter-types.md` holding one behavior,
    `BEH-EC-015` — the next free contiguous id, since BEH-EC-014 is the highest allocated.

    Open the file with a short preamble in the style of file 04's: name what this file specifies
    (how a step's text resolves to typed arguments, and how a custom parameter type gets into the
    registry it is resolved against), cross-link BEH-EC-013 as the place the VERDICT on zero or many
    matches lives, and link `spec/roadmap.md` for build status rather than restating it.

    The behavior heading is `## BEH-EC-015: Step text resolves to typed arguments against a
    per-call parameter type registry`. Under it, a `> **See:**` blockquote linking ADR-EC-007 and
    ADR-EC-019 by relative path.

    Write three plain (untagged) fenced `REQUIREMENT:` blocks:

    **Block 1 — coercion.** A step pattern is a cucumber-expression. Its arguments MUST arrive at
    the step body already coerced to the parameter type's TypeScript type, both at runtime and in the
    type system. Enumerate the eleven built-ins and their types exactly as verified: `int` number,
    `float` number, `word` string, `string` string with surrounding quotes stripped, `double` number,
    `bigdecimal` string, `byte` number, `short` number, `long` number, `biginteger` bigint, and the
    anonymous `{}` string. State that `{float}` also matches integer text while `{int}` does not
    match a decimal. State that an optional group `(s)`, an alternation `a/b`, and a parameter
    escaped by a preceding backslash all contribute NO argument. Add the consequence the glossary
    already relies on: a Scenario Outline's Examples values are typed for free by the pattern's own
    coercion, so no separate typed-example-row mechanism exists.

    **Block 2 — custom parameter types are data.** A custom parameter type MUST be declared as a
    plain `{ name, regexp, transform }` record appended to a store this library owns; declaring one
    MUST NOT touch a `ParameterTypeRegistry`. Every `loadFeature` / `parseFeature` call MUST
    construct a FRESH `ParameterTypeRegistry` and replay every recorded record into it, so a
    definition made once at module scope is present in every subsequent call and a
    duplicate-registration failure can never occur across calls. Declare the two rejections and
    require both at DECLARATION time rather than at replay or match time: a name colliding with one
    of the registry's pre-registered built-ins, and a name already recorded in the same store, the
    latter naming both definition sites. Require that a transform be synchronous, because the
    matched value is read back unwrapped and an async transform would hand a step body a `Promise`
    where its declared parameter type says otherwise.

    **Block 3 — the matcher returns every match.** `StepMatcher` MUST evaluate a step text against
    EVERY registered pattern and return every match, never the first registered. State the reason in
    the requirement itself: `@cucumber/cucumber-expressions` does not detect two step patterns both
    matching one text, so a first-match-wins matcher makes a step argument's TYPE depend on the order
    the step definitions were written in, and a refactor that reorders them silently changes what a
    test asserts. Require that zero matches and many matches are both returned as data rather than
    thrown, and cross-reference BEH-EC-013 / ADR-EC-019 as the place the resulting error is raised.
    Require that compilation is memoized per `(registry, pattern)` and NEVER per pattern alone,
    because a compiled expression permanently binds to and snapshots the parameter types of the
    registry it was constructed with.

    After the requirement blocks, add a `### Two decisions a reader will otherwise ask about`
    section, mirroring file 04's:

    - **Why a store rather than a `Layer`.** ADR-EC-007's correction floats exposing custom-type
      registration as a `Layer`-provided service. ADR-EC-015 forbids `@effect-cucumber/gherkin` from
      declaring `effect` in any manifest field and `pnpm verify:no-runner-dep` enforces it
      structurally, so that option is unavailable in this package. The store is a plain object with a
      process-wide default plus `createParameterTypeStore()` for callers who need isolation.
    - **Why a built-in name is rejected rather than shadowed.** Overriding a built-in would make the
      meaning of `{int}` depend on which modules happened to be imported. The rejection is raised by
      the declaration call itself so the error points at the caller's own code.

    Add a `### Signatures` section as a `ts` fence (reference material, not compiled) listing
    `defineParameterType`, `createParameterTypeStore`, `buildParameterTypeRegistry`,
    `builtInParameterTypeNames`, `createStepMatcher`, `compileExpression`, `StepArgs`,
    `ParameterTypeDefinition`, and the `ParsedFeature.parameterTypes` field.

    Add a `### Worked example` as a `typescript` fence. It MUST import only from
    `"@effect-cucumber/gherkin"` and MUST import everything it uses, because the planned
    doc-examples check will compile it against the real API. Show the whole path: `defineParameterType`
    at module scope with a synchronous transform; `loadFeature`; `createStepMatcher` built over
    `feature.parameterTypes`; a `match` call; and a `switch` on the returned array's length that
    names the zero-match and many-match cases as BEH-EC-013's territory rather than handling them
    here.

    Register the file in `spec/behaviors/index.yaml` with `id: "BEH-EC-015"`,
    `file: "05-step-matching-and-parameter-types.md"`, a title, and `id_range: "015"`.

    Add a `_Next: [05 — Step matching and parameter types](./05-step-matching-and-parameter-types.md)_`
    footer to `spec/behaviors/04-loadfeature-parse-and-validation.md`, matching the footer style of
    files 01 and 02, and give the new file a `_Previous:_` link back to 04. Do not change anything
    else in file 04 in this task.

    Run `pnpm format` before committing — dprint formats `spec/**/*.md` including fenced blocks.
  </action>
  <verify>
    <automated>pnpm verify:spec</automated>
    <automated>pnpm lint</automated>
    <automated>test "$(grep -c 'BEH-EC-015' spec/behaviors/index.yaml)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm verify:spec` exits 0 with FAIL 0 — the new file is registered in both directions and
      every relative link resolves.
    - `pnpm lint` exits 0 — the file survives `dprint check`.
    - `grep -c 'BEH-EC-015' spec/behaviors/05-step-matching-and-parameter-types.md` is at least 1,
      and `grep -c 'BEH-EC-016' spec/behaviors/` across the repo is 0 — no id was skipped.
    - Exactly three `REQUIREMENT:` blocks:
      `grep -c '^REQUIREMENT:' spec/behaviors/05-step-matching-and-parameter-types.md` is 3.
    - All eleven built-in names appear in the coercion requirement:
      `grep -o 'int\b\|float\|word\|string\|double\|bigdecimal\|byte\|short\|long\|biginteger' spec/behaviors/05-step-matching-and-parameter-types.md | sort -u | wc -l`
      is at least 9, and `grep -c 'bigint' spec/behaviors/05-step-matching-and-parameter-types.md`
      is at least 1.
    - Exactly one `typescript` fence and at least one `ts` fence:
      `grep -c '^```typescript' spec/behaviors/05-step-matching-and-parameter-types.md` is 1 and
      `grep -c '^```ts$' spec/behaviors/05-step-matching-and-parameter-types.md` is at least 1.
    - The worked example imports only from the package:
      `grep -c 'from "@effect-cucumber/gherkin"' spec/behaviors/05-step-matching-and-parameter-types.md`
      is at least 1, and
      `grep -c 'from "\.\./\|from "@cucumber/' spec/behaviors/05-step-matching-and-parameter-types.md`
      is 0.
    - Both governing ADRs are linked:
      `grep -c '007-cucumber-expressions-for-step-matching.md' spec/behaviors/05-step-matching-and-parameter-types.md`
      is at least 1 and
      `grep -c '019-fail-loudly-on-unmatched-or-ambiguous-steps.md' spec/behaviors/05-step-matching-and-parameter-types.md`
      is at least 1.
    - File 04 gained a forward link:
      `grep -c '_Next:' spec/behaviors/04-loadfeature-parse-and-validation.md` is 1.
    - The verdict on zero/many matches is deferred, not duplicated:
      `grep -c 'BEH-EC-013' spec/behaviors/05-step-matching-and-parameter-types.md` is at least 1.
  </acceptance_criteria>
  <done>
    BEH-EC-015 is a registered, link-resolving normative behavior covering coercion, the
    data-then-replay parameter-type lifecycle, and match-every-pattern, with the zero/many verdict
    deferred to BEH-EC-013 rather than restated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct BEH-EC-014's signature listing and update the traceability matrix</name>
  <files>spec/behaviors/04-loadfeature-parse-and-validation.md, spec/traceability.md</files>
  <read_first>
    - spec/behaviors/04-loadfeature-parse-and-validation.md (the `### Signatures` `ts` fence, which currently declares `loadFeature: (path: string) => ParsedFeature` — the signature this task corrects)
    - spec/traceability.md (all six sections — §1's Behavior-to-source table, §4's test file map added by plan 02-11, and the preamble/chain block wording that scopes "planned" to `packages/vitest`)
    - packages/gherkin/src/loadFeature.ts (after plan 03-05 — the real signature, including `LoadFeatureOptions`)
    - packages/gherkin/src/Model.ts (after plan 03-05 — `ParsedFeature.parameterTypes`)
  </read_first>
  <action>
    **`spec/behaviors/04-loadfeature-parse-and-validation.md`.**

    Correct the `### Signatures` `ts` fence so it declares the real shipped signatures:
    `loadFeature: (path: string, options?: LoadFeatureOptions) => ParsedFeature` and
    `parseFeature: (source: string, uri: string, options?: LoadFeatureOptions) => ParsedFeature`,
    plus the `LoadFeatureOptions` interface with its single optional `parameterTypes` member.

    Add one short paragraph beneath the fence stating three facts: the second argument is optional,
    so BEH-EC-001's `loadFeature("x.feature")` call form is unchanged; every call builds a fresh
    `ParameterTypeRegistry` from the resolved store and returns it on `ParsedFeature.parameterTypes`;
    and the option exists because the default store is append-only for the life of the process, so a
    caller that needs isolation supplies its own. Link BEH-EC-015 by relative path for the full
    contract.

    Do NOT touch either `REQUIREMENT:` block, the ten error reason tags, the four warning reason
    tags, or the "Two decisions a reader will otherwise ask about" section. `LoadFeatureError`'s
    closed ten-member reason set is deliberately unchanged — Phase 3's failures are `StepPatternError`
    precisely so that this block stays true.

    **`spec/traceability.md`.**

    In §1, add a row for the new behavior file: link
    `behaviors/05-step-matching-and-parameter-types.md`, range `BEH-EC-015`, source module column
    naming `packages/gherkin/src/{ParameterTypes,StepMatcher,StepArgs,Errors}.ts`. Also extend the
    existing BEH-EC-014 row's module list with `StepArgs`-free but accurate content if and only if
    the row's list is now wrong; if `loadFeature.ts` is already named there, leave it.

    In §4, replace the table with rows enumerated FROM DISK, not from memory. Run
    `ls packages/gherkin/test/*.test.ts` and produce exactly one row per file returned, keeping the
    seven existing rows' text unchanged and adding one row for each file this phase created:
    `expressions-pin.test.ts` (covers BEH-EC-015 — pins `@cucumber/cucumber-expressions@20.1.0`'s
    verified behavior, imports nothing from `../src`), `ParameterTypes.test.ts` (BEH-EC-015 —
    one test per definition-time rejection, repeated builds, store isolation), `StepMatcher.test.ts`
    (BEH-EC-015 — runtime coercion, match-every-pattern, memoization identity),
    `ParameterTypeLifecycle.test.ts` (BEH-EC-015 — a custom type resolving across two `loadFeature`
    calls in one process).

    Add one further row, clearly marked as a type-check rather than a suite, for
    `packages/gherkin/test/StepArgs.types.ts`: it is compiled by `pnpm typecheck:test` and is not
    collected by vitest, and it is where MATCH-01's type-level claim is asserted. Add a sentence
    above the table saying that the map includes this one non-suite file and why, so a future reader
    does not "fix" it by deleting the row or renaming the file.

    Update §4's preamble sentence only if it has become false; do not delete it.

    Change nothing in §2, §3, §5 or §6. In particular do not add any `REQ-EC-` row to §5 — the
    `.feature` files under `packages/gherkin/test/fixtures/` remain parser fixtures, not acceptance
    scenarios, and adding one would break the namespace that check 4's clean SKIP depends on.

    Run `pnpm format` before committing.
  </action>
  <verify>
    <automated>pnpm verify:spec &amp;&amp; pnpm lint</automated>
    <automated>node -e 'const fs=require("node:fs"); const files=fs.readdirSync("packages/gherkin/test").filter(f=>f.endsWith(".test.ts")); const doc=fs.readFileSync("spec/traceability.md","utf8"); const missing=files.filter(f=>!doc.includes(f)); if (missing.length) { console.error("test files missing from traceability section 4:", missing); process.exit(1) } console.log("ok", files.length, "test files mapped")'</automated>
    <automated>test "$(grep -c 'REQ-EC-[0-9]' spec/traceability.md)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm verify:spec` exits 0 with FAIL 0, and `pnpm lint` exits 0.
    - Every `packages/gherkin/test/*.test.ts` file on disk appears in `spec/traceability.md`
      (verify command 2 exits 0 and prints a count of at least 11).
    - `grep -c 'StepArgs.types.ts' spec/traceability.md` is at least 1.
    - `grep -c 'BEH-EC-015' spec/traceability.md` is at least 1 — the §1 row exists.
    - `spec/behaviors/04-loadfeature-parse-and-validation.md` declares the real signature:
      `grep -c 'options?: LoadFeatureOptions' spec/behaviors/04-loadfeature-parse-and-validation.md`
      is at least 2, and
      `grep -c 'loadFeature: (path: string) => ParsedFeature' spec/behaviors/04-loadfeature-parse-and-validation.md`
      is 0.
    - The ten error reason tags in file 04 are untouched:
      `grep -o 'MissingFile\|ParseFailed\|UnknownDialect\|NoFeature\|OutlineWithoutExamples\|EmptyExamples\|ZeroStepScenario\|UninterpolatedPlaceholder\|ScenarioKeywordWithExamples\|DuplicateScenarioName' spec/behaviors/04-loadfeature-parse-and-validation.md | sort -u | wc -l`
      is 10.
    - `spec/traceability.md` still has zero `REQ-EC-` rows (verify command 3 exits 0), and
      `pnpm verify:spec` still reports the §5 features-to-traceability check as SKIP.
    - `git diff spec/traceability.md` shows no change inside §2, §3 or §6.
  </acceptance_criteria>
  <done>
    BEH-EC-014's signature listing matches the shipped API, §1 traces BEH-EC-015 to its four source
    modules, and §4 names every test file on disk plus the one type-check-only file, with §5's clean
    SKIP preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close ADR-EC-007's open implementation question and make both status documents true</name>
  <files>spec/decisions/007-cucumber-expressions-for-step-matching.md, spec/roadmap.md, packages/gherkin/README.md</files>
  <read_first>
    - spec/decisions/007-cucumber-expressions-for-step-matching.md (the file being modified — both existing correction blockquotes, their dates and attributions, and the final sentence of the second one which explicitly leaves the store-versus-Layer question open "for Phase 2 of the roadmap")
    - spec/decisions/014-loadfeature-consumes-gherkindocument-and-pickles.md (the precedent for how a second dated correction note is appended without rewriting the first — mark superseded in place, never delete)
    - spec/decisions/015-effect-is-a-peer-dependency.md (the constraint that closes the question)
    - spec/roadmap.md (the Current state paragraph and the Current state table — the single authority on build status per AGENTS.md section 4)
    - packages/gherkin/README.md (the Status section, which currently states that step matching and parameter types are not built)
    - packages/gherkin/src/ParameterTypes.ts (after plan 03-03 — the doc comment that already records this reasoning in code, which this note must agree with)
  </read_first>
  <action>
    **`spec/decisions/007-cucumber-expressions-for-step-matching.md`.**

    Append a THIRD blockquote, dated and attributed to Phase 3 implementation, below the existing
    two. Do not edit, reword, reflow or delete either existing correction — both remain correct in
    substance and keep their dates. The precedent to follow is ADR-EC-014's second correction note.

    The new note closes the one question the second correction explicitly left open. State:

    - The second correction's closing paragraph floats exposing custom-type registration as a
      `Context.Service` + `Layer` rather than an ambient list, and defers the choice. That choice is
      now made and it was forced, not preferred: ADR-EC-015 forbids `@effect-cucumber/gherkin` from
      declaring `effect` in any manifest field, and `pnpm verify:no-runner-dep` enforces that
      structurally over both the source tree and the manifest. A `Layer`-provided registry is
      therefore unreachable from the package that owns `loadFeature`. Mark that sentence of the
      second correction as superseded in place rather than removing it.
    - What shipped instead: a `ParameterTypeStore` — a plain object holding an append-only array of
      `{ name, regexp, transform }` records — with a process-wide `defaultParameterTypeStore` and a
      `createParameterTypeStore()` factory for callers needing isolation. `loadFeature` and
      `parseFeature` accept an optional trailing options argument carrying a store override, and each
      call builds a fresh `ParameterTypeRegistry` and replays every record into it.
    - Two rejections the correction did not name, both raised at DECLARATION time: a name colliding
      with one of the registry's pre-registered built-ins, and a name already recorded in the same
      store. Both raise `StepPatternError`, a class deliberately separate from `LoadFeatureError` so
      BEH-EC-014's closed ten-member reason set stays true.
    - One further constraint the correction did not anticipate: a parameter transform must be
      synchronous. `Argument.getValue` returns the transform result unwrapped, so an async transform
      hands a step body a `Promise` where its declared parameter type says otherwise. The declared
      `transform` type omits `PromiseLike`, and `StepMatcher` guards the cast route at runtime.
    - A forward pointer: BEH-EC-015 is the normative behavior for all of the above.

    **`spec/roadmap.md`.** Update the Current state paragraph and table so they say only what is true
    after Phase 3. The opening paragraph currently lists step matching among the things that are
    "still an intended contract only" — that clause is now false and must go, while hooks, tags,
    `describeFeature` and Layer scoping stay listed. In the table, correct the "Packages exist" row's
    description of what `@effect-cucumber/gherkin` has, and correct the "Unit tests" row's file count
    by counting `packages/gherkin/test/*.test.ts` on disk rather than editing the number by hand.
    Leave the "Acceptance suite" and "Doc-examples compile check" rows exactly as they are — both are
    still unbuilt. Do not rewrite the "Blocking first release", "Planned", "Under consideration" or
    "Explicitly not planned" sections.

    **`packages/gherkin/README.md`.** Rewrite the Status section. It currently says "Step matching,
    parameter types, and the `DataTable` wrapper are specified but not built yet" — two thirds of
    that sentence is now false. The new text must state that the parse pipeline, the `ParsedFeature`
    contract, custom parameter types and step matching all ship; that the `DataTable` wrapper does
    NOT (it is Phase 4's deliverable); and that nothing is published to npm. Keep the existing
    citation of `spec/roadmap.md` as the authority on build status. Do not touch the Install or
    Requirements sections, and do not add an `effect` install line — this package declares no `effect`
    dependency (ADR-EC-015), and `pnpm verify:pack` asserts the README ships in the tarball.

    Run `pnpm format` before committing.
  </action>
  <verify>
    <automated>pnpm verify:spec &amp;&amp; pnpm lint &amp;&amp; pnpm verify:pack</automated>
    <automated>test "$(grep -c 'not built yet\|are specified but not built' packages/gherkin/README.md)" -eq 0</automated>
    <automated>node -e 'const fs=require("node:fs"); const n=fs.readdirSync("packages/gherkin/test").filter(f=>f.endsWith(".test.ts")).length; const md=fs.readFileSync("spec/roadmap.md","utf8"); if (md.includes("seven `test/*.test.ts`")) { console.error("roadmap still claims seven test files; there are", n); process.exit(1) } console.log("ok", n)'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm verify:spec` exits 0 with FAIL 0, `pnpm lint` exits 0, `pnpm verify:pack` exits 0.
    - ADR-EC-007 has exactly three correction blockquotes:
      `grep -c '^> \*\*Correction' spec/decisions/007-cucumber-expressions-for-step-matching.md` is 3.
    - The new note names the constraint and the shipped design:
      `grep -c 'ADR-EC-015' spec/decisions/007-cucumber-expressions-for-step-matching.md` is at
      least 1;
      `grep -c 'ParameterTypeStore\|createParameterTypeStore' spec/decisions/007-cucumber-expressions-for-step-matching.md`
      is at least 1;
      `grep -c 'StepPatternError' spec/decisions/007-cucumber-expressions-for-step-matching.md` is at
      least 1;
      `grep -c 'BEH-EC-015' spec/decisions/007-cucumber-expressions-for-step-matching.md` is at
      least 1.
    - The first two corrections are preserved verbatim: `git diff` on the file shows only additions
      plus the single in-place "superseded" marker, with no line deleted from either existing
      blockquote.
    - `packages/gherkin/README.md` no longer claims step matching is unbuilt (verify command 2 exits
      0), and `grep -c 'DataTable' packages/gherkin/README.md` is at least 1 — the one thing that
      genuinely is still unbuilt is still named.
    - `grep -c 'effect@rc\|pnpm add effect' packages/gherkin/README.md` is 0 — no `effect` install
      line was introduced.
    - `spec/roadmap.md`'s test-file count is no longer stale (verify command 3 exits 0), and both the
      "Acceptance suite" and "Doc-examples compile check" rows are still present:
      `grep -c 'Acceptance suite' spec/roadmap.md` is at least 1 and
      `grep -c 'Doc-examples compile check' spec/roadmap.md` is at least 1.
    - `git diff --stat spec/invariants.md spec/glossary.md spec/overview.md` is empty — no invariant
      or glossary change was needed or made.
  </acceptance_criteria>
  <done>
    ADR-EC-007's last open implementation question is closed in writing with the constraint that
    forced it, and no document in the repository still claims that step matching or parameter types
    are unbuilt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `spec/` → every future implementer | `spec/` is normative; a requirement stated wrongly here becomes wrong code in a later phase |
| status documents → a reader deciding what exists | AGENTS.md §4 makes an untrue status claim a defect, not a nit; `spec/roadmap.md`'s Current state table is the single authority |
| behavior registry (`index.yaml`) → `verify-traceability.sh` | An unregistered behavior file, or a registered file that does not exist, fails check 1 — this is the only automated guard on the spec's internal consistency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-29 | Spoofing | a normative document declaring a signature the code does not have — BEH-EC-014's `(path: string) => ParsedFeature` after plan 03-05 added an options argument | mitigate | Task 2 rewrites the `ts` fence and an acceptance criterion greps that the old form is absent and the new one appears in both signatures. |
| T-03-30 | Repudiation | ADR-EC-007's deferred store-versus-`Layer` question being silently resolved in code with nothing in `spec/` recording that the choice was FORCED by ADR-EC-015 | mitigate | Task 3 appends a dated third correction naming ADR-EC-015, the enforcement script, the shipped `ParameterTypeStore` design, and the two declaration-time rejections; the source module's doc comment already carries the same reasoning, so the two cannot drift silently. |
| T-03-31 | Tampering | rewriting a superseded correction out of history, destroying the record of what was believed and when | mitigate | Follows ADR-EC-014's precedent: mark superseded in place, append a dated note. An acceptance criterion requires `git diff` to show only additions plus the single in-place marker, with no line deleted from either existing blockquote. |
| T-03-32 | Tampering | adding a `REQ-EC-` row to traceability §5 for a parser fixture, breaking the namespace reserved for the acceptance suite | mitigate | Task 2 forbids touching §5, and a verify command asserts the `REQ-EC-[0-9]` count in `spec/traceability.md` stays 0 so check 4 keeps SKIPping cleanly. |
| T-03-33 | Repudiation | §4's test-file map silently going stale — `verify-traceability.sh` checks the registry and links, not §4 completeness | mitigate | A `node -e` assertion reads `packages/gherkin/test/*.test.ts` from disk and fails naming any file absent from the document, so the map cannot drift without failing a verify command. |
| T-03-34 | Information Disclosure | the worked example embedding a plausible-looking credential or token in a fixture pattern | mitigate | The example uses a `money` parameter type over digits and imports only from `@effect-cucumber/gherkin`; an acceptance criterion asserts it contains no relative or third-party import, so nothing outside the package surface is demonstrated. |
| T-03-SC | Tampering | supply chain | mitigate | No package-manager install task in this plan. `packages/gherkin/package.json` is not in `files_modified` and must not change; `pnpm verify:pack` runs as a verify command and re-checks the tarball shape, the Pitfall 20 peer-range guard, and the README's presence. |
</threat_model>

<verification>
- `pnpm verify:spec` exits 0 with PASS 7 / FAIL 0 / SKIP 1
- `pnpm lint` exits 0 (`oxlint -f unix && dprint check`)
- `pnpm verify:pack` exits 0
- `pnpm test`, `pnpm build`, `pnpm typecheck:test`, `pnpm verify:no-runner-dep` all still exit 0 —
  this plan changes documentation only and must not disturb any of them
- The disk-versus-document test-file cross-check exits 0
</verification>

<success_criteria>
`spec/` describes what Phase 3 actually built: BEH-EC-015 is a registered normative behavior,
ADR-EC-007's last open implementation question is closed with the constraint that forced it,
BEH-EC-014's signature listing matches the shipped API, the traceability matrix names every test file
on disk, and no document still claims that step matching or parameter types are unbuilt.
</success_criteria>

<output>
Create `.planning/phases/03-parameter-types-and-step-matching/03-06-SUMMARY.md` when done.
</output>
