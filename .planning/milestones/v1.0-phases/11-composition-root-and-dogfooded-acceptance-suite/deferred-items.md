# Deferred items — Phase 11

Out-of-scope discoveries logged rather than fixed, per the executor's scope boundary.

None open. One item (`spec/README.md`'s Behaviors table listing only 3 of 7
behavior documents, found during plan 11-09 Task 2) was fixed at v1.0
milestone close on 2026-08-30 — see commit `docs(spec): list all 7 behavior
docs in README table`.
